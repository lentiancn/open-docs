# Angular Manual de Usuario

> 🎯 Guía completa de Angular: conceptos básicos, uso y desarrollo
> 
> 📅 Actualizado: Febrero 2026
> 
> 📖 Versión: Angular 17+

---

## 1. Introducción

Angular es un framework de aplicaciones web de código abierto mantenido por Google, construido con TypeScript.

### Características Principales
- Componentes
- Binding de datos bidireccional
- Inyección de dependencias
- Enrutamiento
- Formularios
- HTTP client
- Testing

---

## 2. Estructura del Proyecto

```
mi-app/
├── src/app/
│   ├── app.component.ts
│   ├── app.config.ts
│   └── app.routes.ts
├── angular.json
├── package.json
└── tsconfig.json
```

---

## 3. Componentes

### Crear
```bash
ng generate component mi-componente
```

### Estructura básica
```typescript
@Component({
  selector: 'app-mi-componente',
  standalone: true,
  template: `<h1>{{ titulo }}</h1>`
})
export class MiComponenteComponent {
  titulo = 'Hola Angular';
}
```

---

## 4. Template Syntax

### Interpolación
```html
<p>Hola, {{ nombre }}!</p>
```

### Property Binding
```html
<img [src]="urlImagen">
<button [disabled]="deshabilitado">Botón</button>
```

### Event Binding
```html
<button (click)="onClick()">Clic</button>
```

### Two-way Binding
```html
<input [(ngModel)]="nombre">
```

---

## 5. Servicios

### Crear
```bash
ng generate service servicios/datos
```

### Uso
```typescript
@Injectable({ providedIn: 'root' })
export class DatosService {
  constructor(private http: HttpClient) {}
  
  getDatos(): Observable<any> {
    return this.http.get('/api/datos');
  }
}
```

---

## 6. Enrutamiento

### Configuración
```typescript
export const routes: Routes = [
  { path: '', component: HomeComponent },
  { path: 'about', component: AboutComponent },
  { path: '**', component: NotFoundComponent }
];
```

### Uso en template
```html
<nav>
  <a routerLink="/">Inicio</a>
  <a routerLink="/about">Acerca de</a>
</nav>
<router-outlet></router-outlet>
```

---

## 7. Formularios

### Reactivos
```typescript
form = this.fb.group({
  email: ['', [Validators.required, Validators.email]],
  password: ['', [Validators.required, Validators.minLength(6)]]
});
```

```html
<form [formGroup]="form" (ngSubmit)="onSubmit()">
  <input formControlName="email">
  <button type="submit" [disabled]="form.invalid">Enviar</button>
</form>
```

---

## 8. HTTP

### Configuración
```typescript
// app.config.ts
providers: [provideHttpClient()]
```

### GET
```typescript
this.http.get('/api/datos').subscribe(datos => console.log(datos));
```

### POST
```typescript
this.http.post('/api/datos', { nombre: 'Juan' }).subscribe();
```

---

## 9. Estado (Signal)

```typescript
contador = signal(0);

incrementar(): void {
  this.contador.update(c => c + 1);
}
```

```html
<p>Contador: {{ contador() }}</p>
<button (click)="incrementar()">+</button>
```

---

## 10. Build y Deploy

```bash
# Desarrollo
ng serve

# Producción
ng build --configuration production
```

---

## 📚 Recursos

- https://angular.io/docs
- https://angular.io/tutorial

---

*Generado por AI*
