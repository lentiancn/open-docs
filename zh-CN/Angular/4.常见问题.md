# Angular 常见问题

> 🎯 解答 Angular 开发中遇到的常见问题
> 
> 📅 更新时间：2026年2月
> 
> 📖 适用版本：Angular 17+

---

## 目录

1. [安装问题](#1-安装问题)
2. [项目创建问题](#2-项目创建问题)
3. [组件问题](#3-组件问题)
4. [路由问题](#4-路由问题)
5. [HTTP 和 API 问题](#5-http-和-api-问题)
6. [样式问题](#6-样式问题)
7. [构建和部署问题](#7-构建和部署问题)
8. [性能问题](#8-性能问题)
9. [测试问题](#9-测试问题)
10. [其他问题](#10-其他问题)

---

## 1. 安装问题

### Q1: `ng` 命令找不到怎么办？

**问题**：安装 Angular CLI 后，`ng` 命令提示找不到。

**解决方案**：

1. 确认 npm 全局 bin 目录在 PATH 中：
   ```bash
   # Windows
   npm config get prefix
   
   # 将返回的路径添加到系统 PATH
   ```

2. 或者直接使用 npx：
   ```bash
   npx @angular/cli version
   ```

3. 重新打开终端再试

---

### Q2: Node.js 版本不兼容

**问题**：安装时提示 Node.js 版本过低。

**解决方案**：

使用 nvm（Node Version Manager）切换版本：

```bash
# 安装 nvm (Linux/macOS)
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash

# 安装 Node.js 20
nvm install 20
nvm use 20
```

---

### Q3: macOS M1/M2 安装问题

**问题**：在 Apple Silicon 芯片上安装失败。

**解决方案**：

```bash
# 确保使用 arm64 版本
arch -arm64 node --version

# 如有问题，安装 x64 兼容版本
nvm install 20 --arm64
```

---

### Q4: 安装速度慢

**问题**：npm 安装很慢。

**解决方案**：

```bash
# 使用国内镜像
npm config set registry https://registry.npmmirror.com

# 安装 Angular CLI
npm install -g @angular/cli
```

---

## 2. 项目创建问题

### Q5: `ng new` 失败

**问题**：创建新项目失败。

**解决方案**：

```bash
# 清理 npm 缓存
npm cache clean --force

# 删除之前的安装残留
rm -rf node_modules package-lock.json

# 重新安装
npm install
```

---

### Q6: 项目依赖安装失败

**问题**：依赖安装失败，提示各种错误。

**解决方案**：

```bash
# 方法1：删除 node_modules 重新安装
rm -rf node_modules
npm install

# 方法2：使用 --legacy-peer-deps
npm install --legacy-peer-deps

# 方法3：使用 yarn
npm install -g yarn
yarn install
```

---

## 3. 组件问题

### Q7: 组件不显示

**问题**：创建的组件在页面上不显示。

**解决方案**：

1. 检查组件的 `selector` 是否正确
2. 确认组件已导入到当前模块或独立引入
3. 检查模板中是否正确使用选择器

```typescript
// 独立组件需要在 imports 中引入
@Component({
  standalone: true,
  imports: [CommonModule],
  ...
})
```

---

### Q8: 组件间通信失败

**问题**：父组件无法向子组件传值。

**解决方案**：

检查子组件是否正确使用 `@Input()`：

```typescript
// 子组件
@Input() data: string = '';
@Input() count: number = 0;
```

父组件模板：

```html
<app-child [data]="'hello'" [count]="5"></app-child>
```

---

### Q9: 双向绑定不工作

**问题**：`[(ngModel)]` 绑定不生效。

**解决方案**：

1. 确认已导入 `FormsModule`：
   ```typescript
   import { FormsModule } from '@angular/forms';
   
   @Component({
     imports: [FormsModule],
     ...
   })
   ```

2. 检查 `ngModel` 语法：
   ```html
   <input [(ngModel)]="name">
   ```

---

## 4. 路由问题

### Q10: 路由跳转无效

**问题**：点击链接页面不跳转。

**解决方案**：

1. 使用 `routerLink` 而非 `href`：
   ```html
   <!-- 错误 -->
   <a href="/about">关于</a>
   
   <!-- 正确 -->
   <a routerLink="/about">关于</a>
   ```

2. 确认有 `<router-outlet></router-outlet>`

---

### Q11: 路由参数获取不到

**问题**：无法获取路由参数。

**解决方案**：

```typescript
import { ActivatedRoute } from '@angular/router';

constructor(private route: ActivatedRoute) {}

ngOnInit() {
  this.route.paramMap.subscribe(params => {
    const id = params.get('id');
  });
}
```

---

### Q12: 路由守卫不生效

**问题**：路由守卫没有执行。

**解决方案**：

确保在路由配置中正确添加：

```typescript
export const routes: Routes = [
  {
    path: 'admin',
    component: AdminComponent,
    canActivate: [authGuard]
  }
];
```

---

## 5. HTTP 和 API 问题

### Q13: HTTP 请求失败 CORS

**问题**：跨域请求被阻止。

**解决方案**：

1. 后端添加 CORS 头
2. 使用代理：
   ```json
   // proxy.conf.json
   {
     "/api": {
       "target": "http://localhost:3000",
       "secure": false
     }
   }
   ```
   
   运行：
   ```bash
   ng serve --proxy-config proxy.conf.json
   ```

---

### Q14: HTTP 请求返回 401

**问题**：需要认证的请求返回未授权。

**解决方案**：

添加认证拦截器：

```typescript
import { HttpInterceptorFn } from '@angular/common/http';

export const authInterceptor: HttpInterceptorFn = (req, next) => {
  const token = localStorage.getItem('token');
  if (token) {
    const cloned = req.clone({
      setHeaders: { Authorization: `Bearer ${token}` }
    });
    return next(cloned);
  }
  return next(req);
};
```

在 `app.config.ts` 中注册：

```typescript
providers: [
  provideHttpClient(withInterceptors([authInterceptor]))
]
```

---

### Q15: RxJS 订阅不触发

**问题**：HTTP 请求的订阅回调不执行。

**解决方案**：

确保正确订阅：

```typescript
// 方法1：手动订阅
this.http.get('/api/data').subscribe({
  next: (data) => this.data = data,
  error: (err) => console.error(err),
  complete: () => console.log('完成')
});

// 方法2：使用 async 管道
// 在模板中
@if (data$ | async; as data) {
  {{ data.name }}
}
```

---

## 6. 样式问题

### Q16: 样式不生效

**问题**：添加的 CSS 样式没有效果。

**解决方案**：

1. 检查样式文件路径是否正确
2. 确保 `styleUrls` 或 `styles` 正确配置
3. 检查是否有其他样式覆盖

```typescript
@Component({
  styleUrls: ['./component.css']  // 注意是数组
})
```

---

### Q17: 全局样式不生效

**问题**：在 `styles.css` 中定义的样式没有全局效果。

**解决方案**：

1. 确认 `angular.json` 中已配置：
   ```json
   "styles": [
     "src/styles.css"
   ]
   ```

2. 检查文件路径是否正确

---

## 7. 构建和部署问题

### Q18: 构建失败内存不足

**问题**：生产构建时内存溢出。

**解决方案**：

```bash
# 增加 Node.js 内存限制
export NODE_OPTIONS="--max-old-space-size=4096"
ng build --configuration production
```

---

### Q19: 构建产物太大

**问题**：打包后的文件体积过大。

**解决方案**：

1. 启用生产优化：
   ```bash
   ng build --configuration production
   ```

2. 开启懒加载：
   ```typescript
   {
     path: 'admin',
     loadComponent: () => import('./admin/admin.component')
   }
   ```

3. 使用代码分割

---

### Q20: 部署后刷新 404

**问题**：刷新页面显示 404。

**解决方案**：

配置服务器处理 SPA 路由：

```nginx
# Nginx
location / {
  try_files $uri $uri/ /index.html;
}
```

---

## 8. 性能问题

### Q21: 变更检测太频繁

**问题**：页面响应慢，变更检测过于频繁。

**解决方案**：

使用 `OnPush` 策略：

```typescript
import { ChangeDetectionStrategy, Component } from '@angular/core';

@Component({
  changeDetection: ChangeDetectionStrategy.OnPush
})
export class MyComponent {}
```

---

### Q22: 列表渲染慢

**问题**：大量数据列表渲染很慢。

**解决方案**：

1. 使用 `trackBy`：
   ```html
   @for (item of items; track item.id) {
     {{ item.name }}
   }
   ```

2. 使用虚拟滚动（Angular CDK）

---

## 9. 测试问题

### Q23: 单元测试失败

**问题**：运行 `ng test` 时测试失败。

**解决方案**：

1. 确保 TestBed 配置正确：
   ```typescript
   await TestBed.configureTestingModule({
     declarations: [MyComponent],
     imports: [CommonModule]
   }).compileComponents();
   ```

2. 检查异步操作是否正确处理

---

### Q24: 测试覆盖率为 0

**问题**：代码覆盖率报告为空。

**解决方案**：

```bash
ng test --code-coverage --watch=false
```

---

## 10. 其他问题

### Q25: 如何升级 Angular 版本？

**问题**：如何将项目升级到新版本。

**解决方案**：

```bash
# 使用 Angular Update Guide
# https://update.angular.io/

# 或使用 ng update
ng update @angular/core@17 @angular/cli@17
```

---

### Q26: 独立组件 vs 模块

**问题**：应该使用独立组件还是 NgModule？

**解决方案**：

- **Angular 17+ 推荐**：使用独立组件
- **旧项目**：可以继续使用 NgModule
- **学习**：推荐学习独立组件

---

### Q27: Signal 怎么使用？

**问题**：Angular 16+ 的 Signal 如何使用。

**解决方案**：

```typescript
import { signal, computed } from '@angular/core';

// 创建 signal
count = signal(0);

// 创建计算属性
doubleCount = computed(() => this.count() * 2);

// 修改 signal
count.set(5);
count.update(c => c + 1);

// 在模板中使用
{{ count() }}
```

---

### Q28: 如何调试 Angular 应用？

**问题**：如何调试 Angular 应用。

**解决方案**：

1. 使用 Angular DevTools 浏览器扩展
2. 在组件中添加断点
3. 使用 `ng.probe`（开发模式）

---

## 📚 相关资源

- 官方文档：https://angular.io/docs
- 官方教程：https://angular.io/tutorial
- Stack Overflow：https://stackoverflow.com/questions/tagged/angular
- GitHub Issues：https://github.com/angular/angular/issues

---

*本文档由 AI 助手生成*
