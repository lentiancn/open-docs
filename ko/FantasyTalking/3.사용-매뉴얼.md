# FantasyTalking 사용자 매뉴얼

## 음성 합성

```javascript
const audio = await client.speak('합성할 텍스트');
```

## 스트리밍

```javascript
const stream = await client.speakStream('긴 텍스트');
stream.on('data', chunk => player.play(chunk));
```

## 모범 사례

- 연결 재사용
- 배치 처리 사용
- 결과 캐싱
