# 사용 매뉴얼

이 매뉴얼에서는 Angular의 핵심 기능과 사용 방법을 설명합니다. 컴포넌트, 서비스, 라우팅, 디렉티브, 파이프, 폼 등을 다룹니다.

## 컴포넌트 개발

### 컴포넌트 생성

```bash
ng generate component user-list
```

또는 수동으로:

```typescript
import { Component } from '@angular/core';

@Component({
  selector: 'app-user-list',
  templateUrl: './user-list.component.html',
  styleUrls: ['./user-list.component.scss']
})
export class UserListComponent {
  users = ['Alice', 'Bob', 'Charlie'];
}
```

### 템플릿 문법

**보간:**

```html
<h1>안녕하세요, {{ name }}</h1>
```

**속성 바인딩:**

```html
<img [src]="imageUrl">
<button [disabled]="isDisabled">클릭하세요</button>
```

**이벤트 바인딩:**

```html
<button (click)="onSave()">저장</button>
```

**양방향 바인딩:**

```html
<input [(ngModel)]="username">
```

### 라이프사이클 후크

| 후크 | 설명 |
|------|-------------|
| ngOnInit | 컴포넌트 초기화 |
| ngOnChanges | 입력 속성 변경 |
| ngDoCheck | 변경 감지 |
| ngAfterViewInit | 뷰 초기화 완료 |
| ngOnDestroy | 컴포넌트 파괴 |

## 서비스와 의존성 주입

### 서비스 생성

```bash
ng generate service user
```

서비스 코드:

```typescript
import { Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';

@Injectable({
  providedIn: 'root'
})
export class UserService {
  constructor(private http: HttpClient) {}

  getUsers(): Observable<User[]> {
    return this.http.get<User[]>('/api/users');
  }
}
```

### 서비스 사용

```typescript
import { Component, OnInit } from '@angular/core';
import { UserService } from './user.service';

@Component({
  selector: 'app-user-list',
  templateUrl: './user-list.component.html'
})
export class UserListComponent implements OnInit {
  users: User[] = [];

  constructor(private userService: UserService) {}

  ngOnInit() {
    this.userService.getUsers().subscribe(data => {
      this.users = data;
    });
  }
}
```

## 라우팅

### 라우트 설정

```typescript
import { Routes } from '@angular/router';

const routes: Routes = [
  { path: '', redirectTo: '/home', pathMatch: 'full' },
  { path: 'home', component: HomeComponent },
  { path: 'users', component: UserListComponent },
  { path: 'user/:id', component: UserDetailComponent },
  { path: '**', component: NotFoundComponent }
];
```

### 라우터 링크

```html
<nav>
  <a routerLink="/home">홈</a>
  <a routerLink="/users">사용자</a>
  <a [routerLink]="['/user', userId]">사용자 상세</a>
</nav>
```

### 라우트 파라미터

```typescript
import { ActivatedRoute } from '@angular/router';

constructor(private route: ActivatedRoute) {}

ngOnInit() {
  this.route.params.subscribe(params => {
    this.userId = params['id'];
  });
}
```

### 라우트 가드

```typescript
import { CanActivate } from '@angular/router';

@Injectable({
  providedIn: 'root'
})
export class AuthGuard implements CanActivate {
  canActivate(): boolean {
    return true; // true는 접근 허용, false는 거부
  }
}
```

## 디렉티브

### 구조적 디렉티브

**조건부 렌더링:**

```html
<div *ngIf="isLoggedIn">다시 오신 것을 환영합니다!</div>
```

**반복:**

```html
<ul>
  <li *ngFor="let user of users">{{ user.name }}</li>
</ul>
```

**스위칭:**

```html
<div [ngSwitch]="status">
  <p *ngSwitchCase="'success'">성공</p>
  <p *ngSwitchCase="'error'">오류</p>
  <p *ngSwitchDefault>기본</p>
</div>
```

### 속성 디렉티브

**CSS 클래스:**

```html
<div [ngClass]="{'active': isActive, 'disabled': isDisabled}">내용</div>
```

**스타일:**

```html
<div [ngStyle]="{'color': textColor, 'font-size': fontSize + 'px'}">내용</div>
```

## 파이프

### 내장 파이프

```html
<!-- 날짜 파이프 -->
<p>{{ today | date:'yyyy-MM-dd' }}</p>

<!-- 대소문자 파이프 -->
<p>{{ name | uppercase }}</p>
<p>{{ name | lowercase }}</p>

<!-- 통화 파이프 -->
<p>{{ price | currency:'USD' }}</p>

<!-- 숫자 파이프 -->
<p>{{ pi | number:'1.2-2' }}</p>

<!-- JSON 파이프 -->
<pre>{{ obj | json }}</pre>
```

### 커스텀 파이프

```bash
ng generate pipe capitalize
```

```typescript
import { Pipe, PipeTransform } from '@angular/core';

@Pipe({
  name: 'capitalize'
})
export class CapitalizePipe implements PipeTransform {
  transform(value: string): string {
    if (!value) return value;
    return value.charAt(0).toUpperCase() + value.slice(1);
  }
}
```

## 폼 처리

### 반응형 폼

```typescript
import { FormBuilder, FormGroup, Validators } from '@angular/forms';

export class LoginComponent implements OnInit {
  loginForm: FormGroup;

  constructor(private fb: FormBuilder) {}

  ngOnInit() {
    this.loginForm = this.fb.group({
      username: ['', Validators.required],
      password: ['', [Validators.required, Validators.minLength(6)]],
      remember: [false]
    });
  }

  onSubmit() {
    if (this.loginForm.valid) {
      console.log(this.loginForm.value);
    }
  }
}
```

템플릿:

```html
<form [formGroup]="loginForm" (ngSubmit)="onSubmit()">
  <input formControlName="username" placeholder="사용자명">
  <div *ngIf="loginForm.get('username').touched && loginForm.get('username').errors?.required">
    사용자名は必須です
  </div>
  
  <input type="password" formControlName="password" placeholder="비밀번호">
  
  <label>
    <input type="checkbox" formControlName="remember"> 로그인 유지
  </label>
  
  <button type="submit" [disabled]="loginForm.invalid">로그인</button>
</form>
```

### 템플릿 기반 폼

```html
<form #form="ngForm" (ngSubmit)="onSubmit(form)">
  <input name="username" [(ngModel)]="username" required #name="ngModel" placeholder="사용자명">
  <div *ngIf="name.touched && name.errors?.required">사용자명은 필수입니다</div>
  
  <button type="submit" [disabled]="form.invalid">제출</button>
</form>
```

## HTTP 클라이언트

### 설정

app.module.ts에서 가져오기:

```typescript
import { HttpClientModule } from '@angular/common/http';

@NgModule({
  imports: [HttpClientModule]
})
export class AppModule {}
```

### 요청 보내기

```typescript
import { HttpClient } from '@angular/common/http';

constructor(private http: HttpClient) {}

getData() {
  this.http.get('/api/data').subscribe(data => {
    console.log(data);
  });
}

postData() {
  this.http.post('/api/data', { name: 'test' }).subscribe();
}
```

### 인터셉터

```typescript
import { HttpInterceptorFn } from '@angular/common/http';

export const authInterceptor: HttpInterceptorFn = (req, next) => {
  const token = localStorage.getItem('token');
  const cloned = req.clone({
    setHeaders: { Authorization: `Bearer ${token}` }
  });
  return next(cloned);
};
```

## 상태 관리

### 서비스 기반 접근

애플리케이션 상태를 관리하기 위한 서비스 사용:

```typescript
@Injectable({
  providedIn: 'root'
})
export class StateService {
  private state = new BehaviorSubject<any>(null);
  state$ = this.state.asObservable();

  updateState(newState: any) {
    this.state.next(newState);
  }
}
```

## 성능 최적화

### 지연 로딩

라우트 지연 로딩:

```typescript
{
  path: 'admin',
  loadChildren: () => import('./admin/admin.module').then(m => m.AdminModule)
}
```

### 변경 감지

OnPush 전략:

```typescript
@Component({
  changeDetection: ChangeDetectionStrategy.OnPush
})
export class MyComponent {}
```

## 테스트

### 단위 테스트

```typescript
import { ComponentFixture, TestBed } from '@angular/core/testing';

describe('UserComponent', () => {
  let component: UserComponent;
  let fixture: ComponentFixture<UserComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [UserComponent]
    }).compileComponents();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
```

## 모범 사례

1. **TypeScript strict 모드 사용**: 엄격한 컴파일 옵션 활성화
2. **컴포넌트를 작고 집중하게 유지**: 각 컴포넌트는 하나의 작업만 수행
3. **OnPush 전략 사용**: 변경 감지 성능 향상
4. **파이프를 현명하게 사용**: 파이프에서 복잡한 계산 피하기
5. **지연 로딩 사용**: 초기 로드 시간 단축
6. **테스트 작성**: 코드 품질 보장
