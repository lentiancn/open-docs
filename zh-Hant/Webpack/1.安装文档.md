# Webpack 安裝指南

Webpack 是現代 JavaScript 應用程式的靜態模組打包器。本指南涵蓋在所有環境中的安裝方法。

## 版本資訊

| 版本 | 狀態 | 說明 |
|------|------|------|
| Webpack 5.x | ✅ 當前 | 最新穩定版本 |
| Webpack 4.x | ❌ 舊版 | 不建議新專案使用 |

> **重要提示**：本指南主要針對 **Webpack 5.x**。建議新專案使用最新版本。

## 前提條件

### 系統需求

| 需求 | 版本 |
|------|------|
| Node.js | 12.0.0+ |
| npm | 6.0.0+ |

### 瀏覽器支援

| 瀏覽器 | 版本 |
|--------|------|
| Chrome | 80+ |
| Firefox | 80+ |
| Safari | 13+ |
| Edge | 80+ |

## 安裝方法

### 方法一：本地安裝（推薦）

```bash
# 建立專案目錄
mkdir my-webpack-project
cd my-webpack-project

# 初始化 npm
npm init -y

# 安裝 webpack
npm install webpack --save-dev

# 安裝 webpack-cli
npm install webpack-cli --save-dev
```

### 方法二：全域安裝

```bash
npm install -g webpack webpack-cli
```

### 方法三：使用 npx

```bash
npx webpack --version
```

### 方法四：建立專案範本

```bash
# 使用 create-react-app（已內建 webpack）
npx create-react-app my-app

# 使用 Vue CLI
npm install -g @vue/cli
vue create my-app

# 使用 Vite
npm create vite@latest my-app -- --template vanilla
```

## 設定 webpack.config.js

### 基本設定

```javascript
const path = require('path');

module.exports = {
  mode: 'development', // 或 'production'
  
  // 入口檔案
  entry: './src/index.js',
  
  // 輸出設定
  output: {
    filename: 'bundle.js',
    path: path.resolve(__dirname, 'dist'),
    clean: true, // 清理 dist 目錄
  },
  
  // 載入器
  module: {
    rules: [
      {
        test: /\.css$/i,
        use: ['style-loader', 'css-loader'],
      },
      {
        test: /\.(png|svg|jpg|jpeg|gif)$/i,
        type: 'asset/resource',
      },
    ],
  },
};
```

### 生產環境設定

```javascript
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  mode: 'production',
  entry: './src/index.js',
  output: {
    filename: '[name].[contenthash].js',
    path: path.resolve(__dirname, 'dist'),
    clean: true,
  },
  module: {
    rules: [
      {
        test: /\.css$/i,
        use: ['style-loader', 'css-loader'],
      },
      {
        test: /\.js$/i,
        exclude: /node_modules/,
        use: {
          loader: 'babel-loader',
          options: {
            presets: ['@babel/preset-env'],
          },
        },
      },
    ],
  },
  plugins: [
    new HtmlWebpackPlugin({
      template: './src/index.html',
    }),
  ],
  optimization: {
    splitChunks: {
      chunks: 'all',
    },
  },
};
```

### 開發環境設定

```javascript
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  mode: 'development',
  entry: './src/index.js',
  output: {
    filename: 'bundle.js',
    path: path.resolve(__dirname, 'dist'),
  },
  devServer: {
    static: './dist',
    hot: true,
    port: 3000,
    open: true,
  },
  plugins: [
    new HtmlWebpackPlugin({
      template: './src/index.html',
    }),
  ],
};
```

## 載入器設定

### CSS 載入器

```bash
npm install --save-dev style-loader css-loader
```

```javascript
module: {
  rules: [
    {
      test: /\.css$/i,
      use: ['style-loader', 'css-loader'],
    },
  ],
}
```

### Sass 載入器

```bash
npm install --save-dev sass sass-loader style-loader css-loader
```

```javascript
{
  test: /\.scss$/i,
  use: ['style-loader', 'css-loader', 'sass-loader'],
}
```

### Babel 設定

```bash
npm install --save-dev @babel/core @babel/preset-env babel-loader
```

```javascript
{
  test: /\.js$/i,
  exclude: /node_modules/,
  use: {
    loader: 'babel-loader',
    options: {
      presets: ['@babel/preset-env'],
    },
  },
}
```

### TypeScript 設定

```bash
npm install --save-dev typescript ts-loader
```

```javascript
{
  test: /\.tsx?$/,
  use: 'ts-loader',
  exclude: /node_modules/,
}
```

### 圖片和字型

```javascript
{
  test: /\.(png|jpg|gif|jpeg)$/i,
  type: 'asset/resource',
},
{
  test: /\.(woff|woff2|eot|ttf|otf)$/i,
  type: 'asset/resource',
}
```

## 外掛設定

### HtmlWebpackPlugin

```bash
npm install --save-dev html-webpack-plugin
```

```javascript
const HtmlWebpackPlugin = require('html-webpack-plugin');

plugins: [
  new HtmlWebpackPlugin({
    title: 'My App',
    template: './src/index.html',
    filename: 'index.html',
  }),
];
```

### MiniCssExtractPlugin

```bash
npm install --save-dev mini-css-extract-plugin
```

```javascript
const MiniCssExtractPlugin = require('mini-css-extract-plugin');

module.exports = {
  plugins: [
    new MiniCssExtractPlugin({
      filename: '[name].[contenthash].css',
    }),
  ],
  module: {
    rules: [
      {
        test: /\.css$/i,
        use: [MiniCssExtractPlugin.loader, 'css-loader'],
      },
    ],
  },
};
```

### DefinePlugin

```javascript
const webpack = require('webpack');

plugins: [
  new webpack.DefinePlugin({
    'process.env.NODE_ENV': JSON.stringify('production'),
  }),
];
```

## 開發伺服器

### 安裝

```bash
npm install --save-dev webpack-dev-server
```

### 設定

```javascript
module.exports = {
  devServer: {
    static: {
      directory: path.join(__dirname, 'dist'),
    },
    compress: true,
    port: 3000,
    hot: true,
    open: true,
    historyApiFallback: true,
    proxy: {
      '/api': 'http://localhost:8080',
    },
  },
};
```

### 執行

```bash
# package.json
{
  "scripts": {
    "start": "webpack serve --mode development",
    "build": "webpack --mode production"
  }
}

npm start
```

## 程式碼分割

### 動態匯入

```javascript
// 延遲載入
button.addEventListener('click', () => {
  import('./module').then(module => {
    module.default();
  });
});
```

### SplitChunksPlugin

```javascript
optimization: {
  splitChunks: {
    chunks: 'all',
    cacheGroups: {
      vendor: {
        test: /[\\/]node_modules[\\/]/,
        name: 'vendors',
        chunks: 'all',
      },
    },
  },
}
```

## 效能優化

### 生產環境優化

```javascript
module.exports = {
  optimization: {
    usedExports: true,
    minimize: true,
    splitChunks: {
      chunks: 'all',
      cacheGroups: {
        defaultVendors: {
          test: /[\\/]node_modules[\\/]/,
          priority: -10,
          reuseExistingChunk: true,
        },
        default: {
          minChunks: 2,
          priority: -20,
          reuseExistingChunk: true,
        },
      },
    },
  },
  performance: {
    hints: 'warning',
    maxEntrypointSize: 512000,
    maxAssetSize: 512000,
  },
};
```

### Tree Shaking

```javascript
// package.json
{
  "sideEffects": [
    "*.css",
    "*.scss"
  ]
}
```

## 常見問題

### 建置失敗？

檢查錯誤訊息並確保所有相依套件已安裝：

```bash
npm install
npm audit fix
```

### 模組找不到？

確保正確設定了 resolve.extensions：

```javascript
resolve: {
  extensions: ['.js', '.jsx', '.ts', '.tsx'],
}
```

### 熱更新不運作？

確保 devServer 設定正確：

```javascript
devServer: {
  hot: true,
  liveReload: true,
}
```

## 相關連結

- [Webpack 官方文件](https://webpack.js.org/)
- [Webpack 中文文件](https://webpack.docschina.org/)
- [Loader API](https://webpack.js.org/loaders/)
- [Plugin API](https://webpack.js.org/plugins/)
