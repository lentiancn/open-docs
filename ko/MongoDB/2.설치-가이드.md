# MongoDB 설치 가이드

## 버전 선택 및 플랫폼 요구 사항

### 버전 선택

- **MongoDB 8.2**: 최신 안정 버전, 새 프로젝트에 권장
- **MongoDB 7.0**: 장기 지원 버전

### 플랫폼 요구 사항

MongoDB는 32비트 플랫폼을 지원하지 않습니다. 64비트 운영 체제를 사용해야 합니다.

## Ubuntu에 설치

### 1단계: 공개 키 가져오기

```bash
curl -fsSL https://www.mongodb.org/static/pgp/server-8.0.asc | sudo gpg -o /usr/share/keyrings/mongodb-server-8.0.gpg --dearmor
```

### 2단계: 저장소 파일 생성

```bash
echo "deb [ signed-by=/usr/share/keyrings/mongodb-server-8.0.gpg ] http://repo.mongodb.org/apt/debian focal/mongodb-org/8.0 main" | sudo tee /etc/apt/sources.list.d/mongodb-org-8.0.list
```

### 3단계: 업데이트 및 설치

```bash
sudo apt update
sudo apt install -y mongodb-org
```

### 4단계: MongoDB 시작

```bash
sudo systemctl start mongod
sudo systemctl enable mongod
```

## RHEL/CentOS에 설치

### 1단계: 저장소 파일 생성

```bash
[mongodb-org-8.0]
name=MongoDB Repository
baseurl=https://repo.mongodb.org/yum/redhat/$releasever/mongodb-org/8.0/x86_64/
gpgcheck=1
enabled=1
gpgkey=https://www.mongodb.org/static/pgp/server-8.0.asc
```

### 2단계: 설치

```bash
sudo dnf install -y mongodb-org
```

## macOS에 설치

```bash
brew update
brew install mongodb-community
brew services start mongodb-community
```

## Docker로 설치

```bash
docker pull mongodb/mongodb-community-server
docker run --name mongodb -d -p 27017:27017 mongodb/mongodb-community-server
```

## 설치 확인

### MongoDB에 연결

```bash
mongosh
```

### 버전 확인

```bash
db.version()
```

## 설치 후 설정

### 관리자 사용자 생성

```bash
use admin
db.createUser({
  user: "admin",
  pwd: "비밀번호",
  roles: [
    { role: "userAdminAnyDatabase", db: "admin" },
    { role: "readWriteAnyDatabase", db: "admin" }
  ]
})
```

### 인증 활성화

1. MongoDB 중지
2. `/etc/mongod.conf` 편집:

```yaml
security:
  authorization: enabled
```

3. 다시 시작:

```bash
sudo systemctl restart mongod
```

## 질문과 답변

### MongoDB의 기본 포트는?

기본 포트는 27017입니다.

### MongoDB는 어디에 데이터를 저장합니까?

기본:
- Linux/macOS: `/data/db`
- Windows: `C:\data\db`
