# JUnit 使用手冊

本手冊介紹 JUnit 4 和 JUnit 5 的使用方法，包括基本測試編寫、斷言、測試套件等核心功能。

## JUnit 5 基本用法

### 編寫測試類

```java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class CalculatorTest {
    
    @Test
    void testAddition() {
        int result = 2 + 3;
        assertEquals(5, result);
    }
}
```

### 常用斷言

```java
import static org.junit.jupiter.api.Assertions.*;

// 相等斷言
assertEquals(expected, actual);
assertEquals(expected, actual, "自訂錯誤訊息");

// 布林斷言
assertTrue(condition);
assertFalse(condition);

// 空值斷言
assertNull(object);
assertNotNull(object);

// 陣列斷言
assertArrayEquals(expectedArray, actualArray);

// 例外斷言
assertThrows(Exception.class, () -> {
    // 可能拋出例外的程式碼
});

// 組合斷言
assertAll("Person properties",
    () -> assertEquals("John", person.getFirstName()),
    () -> assertEquals("Doe", person.getLastName())
);
```

### 前置和後置操作

```java
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.AfterEach;

class MyTest {
    
    @BeforeEach
    void setUp() {
        // 每個測試方法執行前運行
    }
    
    @AfterEach
    void tearDown() {
        // 每個測試方法執行後運行
    }
}
```

### 巢狀測試

```java
import org.junit.jupiter.api.Nested;

@Nested
class NestedTests {
    
    @Test
    void testInNestedClass() {
        // 巢狀測試類中的測試
    }
}
```

### 參數化測試

```java
import org.junit.jupiter.params.ParameterizedTest;
import org.junit.jupiter.params.provider.ValueSource;

@ParameterizedTest
@ValueSource(ints = {1, 2, 3, 4, 5})
void testWithMultipleValues(int number) {
    assertTrue(number > 0);
}
```

### 動態測試

```java
import org.junit.jupiter.api.TestFactory;
import org.junit.jupiter.api.DynamicTest;
import java.util.Arrays;

@TestFactory
Collection<DynamicTest> dynamicTestsFromIterator() {
    return Arrays.asList(
        DynamicTest.dynamicTest("第一個動態測試", () -> assertTrue(true)),
        DynamicTest.dynamicTest("第二個動態測試", () -> assertEquals(4, 2 + 2))
    );
}
```

## JUnit 4 基本用法

### 編寫測試類

```java
import org.junit.Test;
import static org.junit.Assert.*;

public class CalculatorTest {
    
    @Test
    public void testAddition() {
        assertEquals(5, 2 + 3);
    }
}
```

### 常用斷言

```java
// 相等斷言
assertEquals(expected, actual);
assertEquals("訊息", expected, actual);

// 布林斷言
assertTrue(condition);
assertFalse(condition);

// 空值斷言
assertNull(object);
assertNotNull(object);

// 例外斷言
@Test(expected = NullPointerException.class)
public void testException() {
    Object obj = null;
    obj.toString();
}
```

### 前置和後置

```java
import org.junit.Before;
import org.junit.After;

public class MyTest {
    
    @Before
    public void setUp() {
        // 測試方法執行前運行
    }
    
    @After
    public void tearDown() {
        // 測試方法執行後運行
    }
    
    @BeforeClass
    public static void setUpClass() {
        // 整個測試類開始前運行一次
    }
    
    @AfterClass
    public static void tearDownClass() {
        // 整個測試類結束後運行一次
    }
}
```

### 測試套件

```java
import org.junit.runner.RunWith;
import org.junit.runners.Suite;
import org.junit.runners.Suite.SuiteClasses;

@RunWith(Suite.class)
@SuiteClasses({Test1.class, Test2.class, Test3.class})
public class AllTests {
    // 執行 Test1、Test2、Test3 中的所有測試
}
```

## JUnit 4 到 JUnit 5 遷移

### 注解變化

| JUnit 4 | JUnit 5 |
|---------|---------|
| @Test | @Test |
| @Before | @BeforeEach |
| @After | @AfterEach |
| @BeforeClass | @BeforeAll |
| @AfterClass | @AfterAll |
| @Ignore | @Disabled |
| @SuiteClasses | @SelectClasses |

### 斷言變化

```java
// JUnit 4
Assert.assertEquals(expected, actual);

// JUnit 5
Assertions.assertEquals(expected, actual);
```

### 例外測試變化

```java
// JUnit 4
@Test(expected = NullPointerException.class)
public void testException() {
    Object obj = null;
    obj.toString();
}

// JUnit 5
@Test
public void testException() {
    assertThrows(NullPointerException.class, () -> {
        Object obj = null;
        obj.toString();
    });
}
```

## 執行測試

### 命令列執行

```bash
# Maven
mvn test

# Gradle
gradle test

# 獨立控制台
java -jar junit-platform-console-standalone-5.14.3.jar -c MyTestClass
```

### IDE 執行

- IntelliJ IDEA：右鍵點擊測試類或方法，選擇 Run
- Eclipse：右鍵點擊測試類或方法，選擇 Run As → JUnit Test
- VS Code：使用 JUnit 擴展執行測試
