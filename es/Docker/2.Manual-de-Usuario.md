# Manual de Usuario de Docker

## Comandos Básicos

```bash
# Ejecutar contenedor
docker run -it ubuntu /bin/bash
docker run -d nginx
docker run -p 8080:80 nginx

# Gestionar contenedores
docker ps
docker stop <id>
docker rm <id>
```

## Dockerfile

```dockerfile
FROM node:18-alpine
WORKDIR /app
COPY . .
RUN npm install
CMD ["node", "server.js"]
```

---

*Actualizado: 2024*
