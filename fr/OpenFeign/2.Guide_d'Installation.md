# OpenFeign Guide d'Installation

## Configuration des dépendances du projet

### Dépendances Maven

```xml
<!-- Spring Cloud OpenFeign Starter -->
<dependency>
    <groupId>org.springframework.cloud</groupId>
    <artifactId>spring-cloud-starter-openfeign</artifactId>
</dependency>

<!-- Requis pour la prise en charge de l'équilibrage de charge -->
<dependency>
    <groupId>org.springframework.cloud</groupId>
    <artifactId>spring-cloud-starter-loadbalancer</artifactId>
</dependency>
```

### Dépendances Gradle

```gradle
implementation 'org.springframework.cloud:spring-cloud-starter-openfeign'
implementation 'org.springframework.cloud:spring-cloud-starter-loadbalancer'
```

### Gestion des versions

Utilisez Spring Cloud BOM pour gérer les versions dans `pom.xml` :

```xml
<dependencyManagement>
    <dependencies>
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-dependencies</artifactId>
            <version>2022.0.4</version>
            <type>pom</type>
            <scope>import</scope>
        </dependency>
    </dependencies>
</dependencyManagement>
```

## Configuration de base

### Activer les clients Feign

Ajoutez l'annotation `@EnableFeignClients` à votre classe d'application principale :

```java
@SpringBootApplication
@EnableFeignClients
public class Application {
    public static void main(String[] args) {
        SpringApplication.run(Application.class, args);
    }
}
```

### Spécifier les packages de scan des clients

Si vos interfaces de client Feign ne se trouvent pas dans le même package que votre classe d'application principale, spécifiez les packages de scan :

```java
@SpringBootApplication
@EnableFeignClients(basePackages = "com.example.clients")
public class Application {
    public static void main(String[] args) {
        SpringApplication.run(Application.class, args);
    }
}
```

Ou listez explicitement les clients :

```java
@SpringBootApplication
@EnableFeignClients(clients = {UserServiceClient.class, OrderServiceClient.class})
public class Application {
    public static void main(String[] args) {
        SpringApplication.run(Application.class, args);
    }
}
```

## Création de clients Feign

### Utilisation de base

```java
@FeignClient(name = "user-service")
public interface UserService {
    
    @GetMapping("/users/{id}")
    User getUser(@PathVariable("id") Long id);
    
    @GetMapping("/users")
    List<User> getAllUsers();
    
    @PostMapping("/users")
    User createUser(@RequestBody User user);
    
    @PutMapping("/users/{id}")
    User updateUser(@PathVariable("id") Long id, @RequestBody User user);
    
    @DeleteMapping("/users/{id}")
    void deleteUser(@PathVariable("id") Long id);
}
```

### Utilisation de l'attribut URL

Si vous n'avez pas besoin de découverte de services, spécifiez l'URL directement :

```java
@FeignClient(name = "external-api", url = "https://api.example.com")
public interface ExternalApiClient {
    @GetMapping("/data")
    ApiResponse getData();
}
```

### Annotations prises en charge

OpenFeign prend en charge les annotations Spring MVC suivantes :

- **Annotations de méthode HTTP**: `@GetMapping`, `@PostMapping`, `@PutMapping`, `@DeleteMapping`, `@PatchMapping`
- **Variables de chemin**: `@PathVariable`
- **Paramètres de requête**: `@RequestParam`
- **Corps de requête**: `@RequestBody`
- **En-têtes de requête**: `@RequestHeader`
- **Cookies**: `@CookieValue`

### Exemples de paramètres complexes

```java
@FeignClient(name = "order-service")
public interface OrderService {
    
    // Variable de chemin + paramètre de requête
    @GetMapping("/orders/{userId}")
    List<Order> getOrdersByUser(
        @PathVariable("userId") Long userId,
        @RequestParam("status") String status,
        @RequestParam(value = "page", defaultValue = "0") int page
    );
    
    // Corps de requête + en-tête de requête
    @PostMapping("/orders")
    @Headers("X-API-Version: 2.0")
    Order createOrder(
        @RequestBody CreateOrderRequest request,
        @RequestHeader("Authorization") String token
    );
    
    // Map de paramètres de requête
    @GetMapping("/orders/search")
    List<Order> searchOrders(@SpringQueryMap SearchCriteria criteria);
}
```

## Utilisation des clients Feign

### Injection dans le service

```java
@Service
public class OrderProcessingService {
    
    private final UserService userService;
    private final OrderService orderService;
    
    public OrderProcessingService(UserService userService, OrderService orderService) {
        this.userService = userService;
        this.orderService = orderService;
    }
    
    public Order processOrder(Long userId, CreateOrderRequest request) {
        // Valider l'utilisateur
        User user = userService.getUser(userId);
        if (user == null) {
            throw new UserNotFoundException("User not found: " + userId);
        }
        
        // Créer la commande
        return orderService.createOrder(request, "Bearer " + generateToken());
    }
    
    private String generateToken() {
        // Générer le jeton d'authentification
        return "token";
    }
}
```

### Utilisation dans le contrôleur

```java
@RestController
@RequestMapping("/api/v1")
public class OrderController {
    
    private final OrderService orderService;
    
    public OrderController(OrderService orderService) {
        this.orderService = orderService;
    }
    
    @GetMapping("/orders/{id}")
    public ResponseEntity<Order> getOrder(@PathVariable Long id) {
        Order order = orderService.getOrder(id);
        return ResponseEntity.ok(order);
    }
    
    @PostMapping("/orders")
    public ResponseEntity<Order> createOrder(@RequestBody CreateOrderRequest request) {
        Order order = orderService.createOrder(request);
        return ResponseEntity.status(HttpStatus.CREATED).body(order);
    }
}
```

## Propriétés de configuration

### Configuration de base

Configurez dans `application.yml` :

```yaml
spring:
  cloud:
    openfeign:
      client:
        config:
          default:
            connect-timeout: 5000
            read-timeout: 5000
            logger-level: basic
          user-service:
            connect-timeout: 3000
            read-timeout: 10000
            logger-level: full
```

### Configuration du niveau de journalisation

```yaml
logging:
  level:
    com.example.clients.UserService: DEBUG
```

Niveaux de journalisation pris en charge :
- **NONE**: Aucune journalisation (par défaut)
- **BASIC**: Journalise uniquement la méthode de requête, l'URL, le code de statut de réponse et le temps d'exécution
- **HEADERS**: Journalise les informations de base ainsi que les en-têtes de requête et de réponse
- **FULL**: Journalise les en-têtes, le corps et les métadonnées pour les requêtes et les réponses

## Gestion des erreurs

### Décodeur d'erreur personnalisé

```java
@Component
public class CustomErrorDecoder implements ErrorDecoder {
    
    private final ErrorDecoder defaultErrorDecoder = new Default();
    
    @Override
    public Exception decode(String methodKey, Response response) {
        if (response.status() >= 400 && response.status() < 500) {
            return new ClientException("Client error: " + response.status());
        } else if (response.status() >= 500) {
            return new ServerException("Server error: " + response.status());
        }
        return defaultErrorDecoder.decode(methodKey, response);
    }
}
```

### Enregistrer dans la configuration

```java
@Configuration
public class FeignConfig {
    
    @Bean
    public ErrorDecoder errorDecoder() {
        return new CustomErrorDecoder();
    }
}
```

## Test des clients Feign

### Tests unitaires

```java
@ExtendWith(MockitoExtension.class)
class OrderProcessingServiceTest {
    
    @Mock
    private UserService userService;
    
    @Mock
    private OrderService orderService;
    
    @InjectMocks
    private OrderProcessingService orderProcessingService;
    
    @Test
    void shouldProcessOrderSuccessfully() {
        // Given
        Long userId = 1L;
        User user = new User(userId, "John Doe");
        CreateOrderRequest request = new CreateOrderRequest();
        Order expectedOrder = new Order(1L, userId);
        
        when(userService.getUser(userId)).thenReturn(user);
        when(orderService.createOrder(any(), anyString())).thenReturn(expectedOrder);
        
        // When
        Order result = orderProcessingService.processOrder(userId, request);
        
        // Then
        assertThat(result).isEqualTo(expectedOrder);
        verify(userService).getUser(userId);
        verify(orderService).createOrder(request, anyString());
    }
}
```

### Tests d'intégration

```java
@SpringBootTest
@AutoConfigureTestDatabase
class FeignIntegrationTest {
    
    @Autowired
    private UserService userService;
    
    @Test
    void shouldGetUserFromRemoteService() {
        // When
        User user = userService.getUser(1L);
        
        // Then
        assertThat(user).isNotNull();
        assertThat(user.getId()).isEqualTo(1L);
    }
}
```

Avec ces étapes, vous pouvez rapidement commencer à utiliser OpenFeign pour simplifier la communication entre microservices. Les chapitres suivants présenteront des fonctionnalités plus avancées et des bonnes pratiques.