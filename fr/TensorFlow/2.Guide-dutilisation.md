# TensorFlow Guide d'Utilisation

Ce guide fournit des informations détaillées sur l'utilisation de TensorFlow.

## Concepts de Base

### Tenseurs

```python
import tensorflow as tf

# Scalaire
scalaire = tf.constant(5)

# Vecteur
vecteur = tf.constant([1, 2, 3])

# Matrice
matrice = tf.constant([[1, 2], [3, 4]])
```

### Opérations

```python
a = tf.constant([[1, 2], [3, 4]])
b = tf.constant([[5, 6], [7, 8]])

# Addition
c = tf.add(a, b)

# Multiplication matricielle
d = tf.matmul(a, b)
```

## Réseaux Neuronaux avec Keras

### Construire un Modèle

```python
from tensorflow import keras
from tensorflow.keras import layers

model = keras.Sequential([
    layers.Dense(128, activation='relu', input_shape=(784,)),
    layers.Dropout(0.2),
    layers.Dense(10, activation='softmax')
])
```

### Compiler le Modèle

```python
model.compile(
    optimizer='adam',
    loss='sparse_categorical_crossentropy',
    metrics=['accuracy']
)
```

### Entraîner le Modèle

```python
model.fit(x_train, y_train, epochs=5, validation_split=0.1)
```

### Évaluer

```python
test_loss, test_acc = model.evaluate(x_test, y_test)
print(f'Accuracy: {test_acc}')
```

### Prédictions

```python
predictions = model.predict(x_test)
```

## Callbacks

```python
# Early stopping
early_stop = keras.callbacks.EarlyStopping(
    monitor='val_loss',
    patience=5
)

# Model checkpoint
checkpoint = keras.callbacks.ModelCheckpoint(
    'best_model.keras',
    monitor='val_accuracy',
    save_best_only=True
)

model.fit(x_train, y_train, 
          epochs=10, 
          callbacks=[early_stop, checkpoint])
```

## Sauvegarder et Charger des Modèles

### Sauvegarder le Modèle Complet

```python
model.save('mon_modele.keras')
model = keras.models.load_model('mon_modele.keras')
```

### Sauvegarder les Poids

```python
model.save_weights('mes_poids.h5')
model.load_weights('mes_poids.h5')
```

## Meilleures Pratiques

1. Utiliser Keras pour la plupart des tâches deep learning
2. Activer le GPU quand disponible
3. Utiliser TensorBoard pour la visualisation
4. Sauvegarder les checkpoints pendant l'entraînement
5. Utiliser l'augmentation de données
