# Руководство по использованию Angular

В этом руководстве описано, как использовать Angular для создания веб-приложений.

## Быстрый старт

### Сервер разработки

```bash
# Запустить сервер разработки
ng serve

# Запустить на определённом порту
ng serve --port 4201

# Автоматически открыть браузер
ng serve --open

# Включить горячую перезагрузку
ng serve --hmr
```

### Сборка

```bash
# Сборка для разработки
ng build

# Сборка для продакшена
ng build --configuration production

# Сборка со статистикой
ng build --stats-json
```

## Структура проекта

```
my-app/
├── src/
│   ├── app/
│   │   ├── components/
│   │   ├── services/
│   │   ├── models/
│   │   ├── app.component.ts
│   │   ├── app.config.ts
│   │   └── app.routes.ts
│   ├── assets/
│   ├── styles.scss
│   └── index.html
├── angular.json
├── package.json
└── tsconfig.json
```

## Компоненты

### Создание компонента

```bash
# Создать компонент
ng generate component components/user-list

# Краткая форма
ng g c components/user-list
```

### Пример компонента

```typescript
import { Component } from '@angular/core';

@Component({
  selector: 'app-user-list',
  template: `
    <h1>Список пользователей</h1>
    <ul>
      <li *ngFor="let user of users">{{ user.name }}</li>
    </ul>
  `,
  standalone: true
})
export class UserListComponent {
  users = [
    { name: 'Иван' },
    { name: 'Мария' }
  ];
}
```

## Сервисы

### Создание сервиса

```bash
ng generate service services/user
# или
ng g s services/user
```

### Пример сервиса

```typescript
import { Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';

@Injectable({
  providedIn: 'root'
})
export class UserService {
  constructor(private http: HttpClient) {}

  getUsers(): Observable<User[]> {
    return this.http.get<User[]>('/api/users');
  }
}
```

## Маршрутизация

### Настройка маршрутов

```typescript
// app.routes.ts
import { Routes } from '@angular/router';

export const routes: Routes = [
  { path: '', redirectTo: '/home', pathMatch: 'full' },
  { path: 'home', component: HomeComponent },
  { path: 'users', component: UserListComponent },
  { path: 'users/:id', component: UserDetailComponent },
  { path: '**', component: NotFoundComponent }
];
```

### Навигация

```typescript
import { RouterLink, RouterOutlet } from '@angular/router';

@Component({
  imports: [RouterLink, RouterOutlet],
  template: `
    <nav>
      <a routerLink="/home">Главная</a>
      <a routerLink="/users">Пользователи</a>
    </nav>
    <router-outlet></router-outlet>
  `
})
export class AppComponent {}
```

## Формы

### Формы на основе шаблонов

```typescript
import { FormsModule } from '@angular/forms';

@Component({
  imports: [FormsModule],
  template: `
    <input [(ngModel)]="name" name="name">
    <button (click)="submit()">Отправить</button>
  `
})
export class LoginComponent {
  name = '';
}
```

### Реактивные формы

```typescript
import { ReactiveFormsModule, FormBuilder, FormGroup, Validators } from '@angular/forms';

@Component({
  imports: [ReactiveFormsModule],
  template: `
    <form [formGroup]="form" (ngSubmit)="submit()">
      <input formControlName="email">
      <input formControlName="password" type="password">
      <button type="submit">Отправить</button>
    </form>
  `
})
export class LoginComponent {
  form: FormGroup;

  constructor(private fb: FormBuilder) {
    this.form = this.fb.group({
      email: ['', [Validators.required, Validators.email]],
      password: ['', [Validators.required, Validators.minLength(6)]]
    });
  }
}
```

## HTTP-клиент

### Настройка

```typescript
// app.config.ts
import { provideHttpClient } from '@angular/common/http';

export const appConfig: ApplicationConfig = {
  providers: [
    provideHttpClient()
  ]
};
```

### Вызов API

```typescript
import { HttpClient } from '@angular/common/http';

@Component({})
export class UserComponent {
  constructor(private http: HttpClient) {}

  getUsers() {
    this.http.get('/api/users').subscribe(data => {
      console.log(data);
    });
  }
}
```

## Управление состоянием

### Signals (Angular 16+)

```typescript
import { signal, computed } from '@angular/core';

@Component({})
export class CounterComponent {
  count = signal(0);
  double = computed(() => this.count() * 2);

  increment() {
    this.count.update(c => c + 1);
  }
}
```

## Команды CLI

### Генерация

```bash
# Компонент
ng g c component-name

# Сервис
ng g s service-name

# Директива
ng g d directive-name

# Пайп
ng g p pipe-name

# Модуль
ng g m module-name

# Guard
ng g g guard-name
```

### Тестирование

```bash
# Запустить тесты
ng test

# Запустить тесты с покрытием
ng test --code-coverage

# Запустить e2e тесты
ng e2e
```

## Рекомендации

### 1. Используйте автономные компоненты

```typescript
@Component({
  standalone: true,
  imports: [CommonModule],
  ...
})
export class MyComponent {}
```

### 2. Используйте Signals для состояния

```typescript
// Предпочтительно
count = signal(0);

// Избегайте
count = 0;
```

### 3. Ленивая загрузка

```typescript
{
  path: 'admin',
  loadComponent: () => import('./admin/admin.component')
}
```

## Дополнительные ресурсы

- [Документация Angular](https://angular.io/docs)
- [Справка по Angular CLI](https://angular.io/cli)
- [Учебник по Angular](https://angular.io/tutorial)
