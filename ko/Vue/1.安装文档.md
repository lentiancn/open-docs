# Vue 설치 가이드

Vue는 사용자 인터페이스를 구축하기 위한渐进式 JavaScript 프레임워크입니다.

## 버전 개요

| 버전 |发布日期 | 상태 |
|------|----------|------|
| Vue 3 | 2020년 9월 | 권장 |
| Vue 2 | 2016년 10월 | 레거시 |

**권장:** 모든 새 프로젝트에 Vue 3을 사용하세요.

## 전제 조건

### 시스템 요구 사항

| 요구사항 | Vue 2 | Vue 3 |
|----------|-------|-------|
| Node.js | 8.9+ | 16+ (18+ 권장) |
| npm | 5.6+ | 8+ |

## 설치 방법

### 방법 1: npm 사용 (권장)

```bash
# 새 Vue 프로젝트 생성
npm create vue@latest
```

### 방법 2: Vite 사용 (Vue 3 권장)

```bash
# Vite로 Vue 3 프로젝트 생성
npm create vite@latest my-vue-app -- --template vue

cd my-vue-app
npm install
npm run dev
```

### 방법 3: CDN

```html
<script src="https://unpkg.com/vue@3/dist/vue.global.js"></script>
```

## 프로젝트 설정

### Vue 3 + Vite 프로젝트 구조

```
my-vue-app/
├── public/
├── src/
│   ├── components/
│   ├── App.vue
│   └── main.js
├── index.html
├── package.json
└── vite.config.js
```

## 종속성 설치

```bash
npm install
npm install vue-router@4
npm install pinia
```

## Vue Router 설정

```javascript
import { createRouter, createWebHistory } from 'vue-router'
import Home from './views/Home.vue'

const routes = [
  { path: '/', component: Home }
]

const router = createRouter({
  history: createWebHistory(),
  routes
})

export default router
```

## Pinia (상태 관리)

```javascript
import { defineStore } from 'pinia'

export const useCounterStore = defineStore('counter', {
  state: () => ({
    count: 0
  }),
  actions: {
    increment() {
      this.count++
    }
  }
})
```

## 개발 서버

```bash
# 개발 서버 시작
npm run dev

# 프로덕션용 빌드
npm run build
```

## 문제 해결

### npm install 실패

```bash
# npm 캐시 지우기
npm cache clean --force

# node_modules 삭제 후 재설치
rm -rf node_modules
npm install
```

## 모범 사례

1. **Vue 3 사용** - 새 프로젝트에는 Vue 3을
2. **Vite 사용** - 더 빠른 개발 및 빌드
3. **Composition API 사용** - 더 나은 TypeScript 지원

## 관련 링크

- [공식 웹사이트](https://vuejs.org)
- [Vue 3 문서](https://vuejs.org/guide/)
- [Vue Router](https://router.vuejs.org/)
- [Pinia](https://pinia.vuejs.org/)
