# Руководство пользователя Docker

## Основные команды

```bash
docker run -it ubuntu /bin/bash
docker run -d nginx
docker run -p 8080:80 nginx
docker ps
docker stop <id>
```

## Dockerfile

```dockerfile
FROM node:18-alpine
WORKDIR /app
COPY . .
RUN npm install
```

---

*Обновлено: 2024*
