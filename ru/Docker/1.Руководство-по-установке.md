# Docker Руководство по установке

> 🎯 Подробное руководство по установке Docker

## 1. Введение

### Что такое Docker?

Docker — платформа для разработки, развёртывания и запуска приложений.

### Версии

| Версия | Дата |
|--------|------|
| Docker 27.x | 2024 |
| Docker 26.x | 2024 |

---

## 2. Требования

### Windows
- Windows 10 64-bit Pro/Enterprise или Windows 11
- 4GB RAM

### macOS
- macOS 10.15+
- 4GB RAM

---

## 3. Установка Windows

### Docker Desktop (Рекомендуется)

1. Скачать: https://www.docker.com/products/docker-desktop
2. Установить
3. Проверить:
```powershell
docker --version
docker run hello-world
```

---

## 4. Установка macOS

### Docker Desktop

1. Скачать: https://www.docker.com/products/docker-desktop
2. Установить
3. Проверить:
```bash
docker --version
docker run hello-world
```

---

## 5. Установка Linux

### Ubuntu

```bash
# Зависимости
sudo apt install ca-certificates curl gnupg lsb-release

# GPG ключ
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg

# Репозиторий
echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list

# Установка
sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

# Запуск
sudo systemctl start docker
sudo systemctl enable docker
```

---

## 6. Проверка

```bash
docker --version
docker run hello-world
```

---

## 7. FAQ

### Docker Desktop не запускается?

Включите виртуализацию в BIOS.

### Нет прав?

```bash
sudo usermod -aG docker $USER
```

---

## 📚 Ресурсы

- Документация: https://docs.docker.com

*AI*
