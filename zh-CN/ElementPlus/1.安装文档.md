# Element Plus 安装指南

Element Plus 是为开发者、设计师和产品经理打造的 Vue 3 组件库。本指南涵盖所有环境的安装方式。

## 版本信息

| 版本 | 状态 | 说明 |
|---------|--------|-------------|
| Element Plus 2.x | ✅ 当前版本 | Vue 3 最新稳定版 |
| Element Plus 1.x | ⚠️ 维护中 | 仅提供安全更新 |
| Element UI | ❌ 旧版 | 仅支持 Vue 2，已停止维护 |

> **重要提示**：Element Plus 仅支持 Vue 3。如果您使用的是 Vue 2，请使用 Element UI。

## 环境要求

### 系统要求

| 要求 | 版本 |
|-------------|---------|
| Vue | 3.x |
| Node.js | 16.x+ |
| npm / yarn / pnpm | 最新版 |

### 浏览器支持

| 浏览器 | 版本 |
|---------|---------|
| Chrome | 80+ |
| Firefox | 80+ |
| Safari | 14+ |
| Edge | 80+ |

## 安装方式

### 方式一：npm（推荐）

```bash
# 使用 npm
npm install element-plus

# 使用 yarn
yarn add element-plus

# 使用 pnpm
pnpm add element-plus
```

### 方式二：CDN 引入

```html
<!-- 引入 Vue 3 -->
<script src="https://unpkg.com/vue@3/dist/vue.global.js"></script>

<!-- 引入 Element Plus -->
<link rel="stylesheet" href="https://unpkg.com/element-plus/dist/index.css" />
<script src="https://unpkg.com/element-plus/dist/index.full.js"></script>

<!-- 引入 Element Plus 图标（可选） -->
<script src="https://unpkg.com/@element-plus/icons-vue"></script>
```

#### 完整的 CDN 模板

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Element Plus CDN</title>
    <link rel="stylesheet" href="https://unpkg.com/element-plus/dist/index.css">
</head>
<body>
    <div id="app">
        <el-button>按钮</el-button>
        <el-date-picker></el-date-picker>
    </div>

    <script src="https://unpkg.com/vue@3/dist/vue.global.js"></script>
    <script src="https://unpkg.com/element-plus/dist/index.full.js"></script>
    <script src="https://unpkg.com/@element-plus/icons-vue"></script>
    
    <script>
        const { createApp, ref } = Vue
        const app = createApp({
            setup() {
                return {}
            }
        })
        
        for (const [key, component] of Object.entries(ElementPlus)) {
            app.component(key, component)
        }
        
        for (const [key, component] of Object.entries(ElementPlusIconsVue)) {
            app.component(key, component)
        }
        
        app.use(ElementPlus)
        app.mount('#app')
    </script>
</body>
</html>
```

### 方式三：使用 Vite

```bash
# 创建 Vite 项目
npm create vite@latest my-app -- --template vue

# 进入项目目录
cd my-app

# 安装 Element Plus
npm install element-plus

# 安装图标
npm install @element-plus/icons-vue
```

#### 在 main.js 中配置

```javascript
import { createApp } from 'vue'
import App from './App.vue'

// 引入 Element Plus 样式
import 'element-plus/dist/index.css'

// 引入 Element Plus
import ElementPlus from 'element-plus'

// 引入图标
import * as ElementPlusIconsVue from '@element-plus/icons-vue'

const app = createApp(App)

// 注册所有图标
for (const [key, component] of Object.entries(ElementPlusIconsVue)) {
    app.component(key, component)
}

app.use(ElementPlus)
app.mount('#app')
```

### 方式四：使用 Vue CLI

```bash
# 安装 Vue CLI
npm install -g @vue/cli

# 创建项目
vue create my-project

# 添加 Element Plus
cd my-project
npm install element-plus
npm install @element-plus/icons-vue
```

#### 在 main.js 中配置

```javascript
import { createApp } from 'vue'
import App from './App.vue'

import 'element-plus/dist/index.css'
import ElementPlus from 'element-plus'
import * as icons from '@element-plus/icons-vue'

const app = createApp(App)

// 全局注册图标
for (const [key, component] of Object.entries(icons)) {
    app.component(key, component)
}

app.use(ElementPlus)
app.mount('#app')
```

### 方式五：按需引入（推荐用于生产环境）

#### 使用 unplugin-vue-components

```bash
# 安装依赖
npm install element-plus unplugin-vue-components unplugin-auto-import
```

#### Vite 配置

```javascript
// vite.config.js
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import AutoImport from 'unplugin-auto-import/vite'
import Components from 'unplugin-vue-components/vite'
import { ElementPlusResolver } from 'unplugin-vue-components/resolvers'

export default defineConfig({
  plugins: [
    vue(),
    AutoImport({
      resolvers: [ElementPlusResolver()],
    }),
    Components({
      resolvers: [ElementPlusResolver()],
    }),
  ],
})
```

#### 使用方法

```vue
<template>
  <!-- 无需手动引入组件 -->
  <el-button type="primary">按钮</el-button>
  <el-input v-model="input" placeholder="请输入内容" />
</template>

<script setup>
// 直接使用即可
import { ref } from 'vue'
const input = ref('')
</script>
```

### 方式六：使用 Nuxt 3

```bash
# 创建 Nuxt 项目
npx nuxi@latest init my-app
cd my-app

# 安装 Element Plus
npm install element-plus @element-plus/nuxt
```

#### 配置 nuxt.config.ts

```typescript
export default defineNuxtConfig({
  modules: ['@element-plus/nuxt'],
  elementPlus: {
    /** 配置选项 */
  }
})
```

## 图标配置

### 全局注册

```javascript
import * as icons from '@element-plus/icons-vue'

const app = createApp(App)

for (const [key, icon] of Object.entries(icons)) {
    app.component(key, icon)
}
```

### 在模板中使用

```html
<el-icon><Plus /></el-icon>
<el-icon><Search /></el-icon>
<el-icon><Edit /></el-icon>
```

### 可用图标组件

- Plus、Minus、Close、Check
- Search、Edit、Delete、Copy
- Upload、Download、Link
- ArrowLeft、ArrowRight、ArrowUp、ArrowDown
- CirclePlus、CircleClose
- 以及更多...

## 配置

### 语言配置

```javascript
import ElementPlus from 'element-plus'
import zhCn from 'element-plus/es/locale/lang/zh-cn'

app.use(ElementPlus, {
  locale: zhCn,
})
```

### 支持的语言

- en（英语）
- zhCn（简体中文）
- zhTw（繁体中文）
- ja（日语）
- es（西班牙语）
- de（德语）
- fr（法语）
- ru（俄语）
- ko（韩语）

### 主题定制

#### 使用 CSS 变量

```css
:root {
    --el-color-primary: #409eff;
    --el-color-success: #67c23a;
    --el-color-warning: #e6a23c;
    --el-color-danger: #f56c6c;
    --el-font-size-base: 14px;
}
```

#### 使用 SCSS

```scss
$--color-primary: #409eff;

@import "element-plus/theme-chalk/src/common/var.scss";
@import "element-plus/packages/theme-chalk/src/index.scss";
```

## 项目结构示例

```
my-element-app/
├── src/
│   ├── main.js
│   ├── App.vue
│   └── components/
│       └── HelloWorld.vue
├── index.html
├── package.json
└── vite.config.js
```

## 下一步

安装完成后，请参阅 [使用指南](./2.使用指南.md) 了解如何使用 Element Plus 组件。

## 常见问题

### 图标不显示？

请确保已全局注册图标：

```javascript
import * as icons from '@element-plus/icons-vue'
for (const [key, icon] of Object.entries(icons)) {
    app.component(key, icon)
}
```

### 样式不生效？

请确保已引入 Element Plus 样式：

```javascript
import 'element-plus/dist/index.css'
```

### Vue 版本不匹配？

Element Plus 需要 Vue 3。请检查您的 Vue 版本：

```bash
npm list vue
```

## 相关链接

- [官方网站](https://element-plus.org/)
- [GitHub](https://github.com/element-plus/element-plus)
- [设计指南](https://element-plus.org/zh-CN/guide/design.html)
- [组件文档](https://element-plus.org/zh-CN/component/button.html)
