# Angular 常見問題

> 🎯 解答 Angular 開發中遇到的常見問題
> 
> 📅 更新時間：2026年2月
> 
> 📖 適用版本：Angular 17+

---

## 目錄

1. [安裝問題](#1-安裝問題)
2. [專案建立問題](#2-專案建立問題)
3. [元件問題](#3-元件問題)
4. [路由問題](#4-路由問題)
5. [HTTP 和 API 問題](#5-http-和-api-問題)
6. [樣式問題](#6-樣式問題)
7. [建置和部署問題](#7-建置和部署問題)
8. [效能問題](#8-效能問題)
9. [測試問題](#9-測試問題)
10. [其他問題](#10-其他問題)

---

## 1. 安裝問題

### Q1: `ng` 命令找不到怎麼辦？

**問題**：安裝 Angular CLI 後，`ng` 命令提示找不到。

**解決方案**：

1. 確認 npm 全域 bin 目錄在 PATH 中：
   ```bash
   # Windows
   npm config get prefix
   
   # 將回傳的路徑新增到系統 PATH
   ```

2. 或者直接使用 npx：
   ```bash
   npx @angular/cli version
   ```

3. 重新開啟終端機再試

---

### Q2: Node.js 版本不相容

**問題**：安裝時提示 Node.js 版本過低。

**解決方案**：

使用 nvm（Node Version Manager）切換版本：

```bash
# 安裝 nvm (Linux/macOS)
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash

# 安裝 Node.js 20
nvm install 20
nvm use 20
```

---

### Q3: macOS M1/M2 安裝問題

**問題**：在 Apple Silicon 晶片上安裝失敗。

**解決方案**：

```bash
# 確保使用 arm64 版本
arch -arm64 node --version

# 如有問題，安裝 x64 相容版本
nvm install 20 --arm64
```

---

### Q4: 安裝速度慢

**問題**：npm 安裝很慢。

**解決方案**：

```bash
# 使用國內鏡像
npm config set registry https://registry.npmmirror.com

# 安裝 Angular CLI
npm install -g @angular/cli
```

---

## 2. 專案建立問題

### Q5: `ng new` 失敗

**問題**：建立新專案失敗。

**解決方案**：

```bash
# 清理 npm 快取
npm cache clean --force

# 刪除之前的安裝殘留
rm -rf node_modules package-lock.json

# 重新安裝
npm install
```

---

### Q6: 專案依賴安裝失敗

**問題**：依賴安裝失敗，提示各種錯誤。

**解決方案**：

```bash
# 方法1：刪除 node_modules 重新安裝
rm -rf node_modules
npm install

# 方法2：使用 --legacy-peer-deps
npm install --legacy-peer-deps

# 方法3：使用 yarn
npm install -g yarn
yarn install
```

---

## 3. 元件問題

### Q7: 元件不顯示

**問題**：建立的元件在頁面上不顯示。

**解決方案**：

1. 檢查元件的 `selector` 是否正確
2. 確認元件已匯入到目前模組或獨立引入
3. 檢查範本中是否正確使用選擇器

```typescript
// 獨立元件需要在 imports 中引入
@Component({
  standalone: true,
  imports: [CommonModule],
  ...
})
```

---

### Q8: 元件間通訊失敗

**問題**：父元件無法向子元件傳值。

**解決方案**：

檢查子元件是否正確使用 `@Input()`：

```typescript
// 子元件
@Input() data: string = '';
@Input() count: number = 0;
```

父元件範本：

```html
<app-child [data]="'hello'" [count]="5"></app-child>
```

---

### Q9: 雙向綁定不工作

**問題**：`[(ngModel)]` 綁定不生效。

**解決方案**：

1. 確認已匯入 `FormsModule`：
   ```typescript
   import { FormsModule } from '@angular/forms';
   
   @Component({
     imports: [FormsModule],
     ...
   })
   ```

2. 檢查 `ngModel` 語法：
   ```html
   <input [(ngModel)]="name">
   ```

---

## 4. 路由問題

### Q10: 路由跳轉無效

**問題**：點擊連結頁面不跳轉。

**解決方案**：

1. 使用 `routerLink` 而非 `href`：
   ```html
   <!-- 錯誤 -->
   <a href="/about">關於</a>
   
   <!-- 正確 -->
   <a routerLink="/about">關於</a>
   ```

2. 確認有 `<router-outlet></router-outlet>`

---

### Q11: 路由參數擷取不到

**問題**：無法擷取路由參數。

**解決方案**：

```typescript
import { ActivatedRoute } from '@angular/router';

constructor(private route: ActivatedRoute) {}

ngOnInit() {
  this.route.paramMap.subscribe(params => {
    const id = params.get('id');
  });
}
```

---

### Q12: 路由守衛不生效

**問題**：路由守衛沒有執行。

**解決方案**：

確保在路由設定中正確新增：

```typescript
export const routes: Routes = [
  {
    path: 'admin',
    component: AdminComponent,
    canActivate: [authGuard]
  }
];
```

---

## 5. HTTP 和 API 問題

### Q13: HTTP 請求失敗 CORS

**問題**：跨域請求被阻止。

**解決方案**：

1. 後端新增 CORS 標頭
2. 使用代理：
   ```json
   // proxy.conf.json
   {
     "/api": {
       "target": "http://localhost:3000",
       "secure": false
     }
   }
   ```
   
   執行：
   ```bash
   ng serve --proxy-config proxy.conf.json
   ```

---

### Q14: HTTP 請求回傳 401

**問題**：需要認證的請求回傳未授權。

**解決方案**：

新增認證攔截器：

```typescript
import { HttpInterceptorFn } from '@angular/common/http';

export const authInterceptor: HttpInterceptorFn = (req, next) => {
  const token = localStorage.getItem('token');
  if (token) {
    const cloned = req.clone({
      setHeaders: { Authorization: `Bearer ${token}` }
    });
    return next(cloned);
  }
  return next(req);
};
```

在 `app.config.ts` 中註冊：

```typescript
providers: [
  provideHttpClient(withInterceptors([authInterceptor]))
]
```

---

### Q15: RxJS 訂閱不觸發

**問題**：HTTP 請求的訂閱回調不執行。

**解決方案**：

確保正確訂閱：

```typescript
// 方法1：手動訂閱
this.http.get('/api/data').subscribe({
  next: (data) => this.data = data,
  error: (err) => console.error(err),
  complete: () => console.log('完成')
});

// 方法2：使用 async 管道
// 在範本中
@if (data$ | async; as data) {
  {{ data.name }}
}
```

---

## 6. 樣式問題

### Q16: 樣式不生效

**問題**：新增的 CSS 樣式沒有效果。

**解決方案**：

1. 檢查樣式檔案路徑是否正確
2. 確保 `styleUrls` 或 `styles` 正確設定
3. 檢查是否有其他樣式覆蓋

```typescript
@Component({
  styleUrls: ['./component.css']  // 注意是陣列
})
```

---

### Q17: 全域樣式不生效

**問題**：在 `styles.css` 中定義的樣式沒有全域效果。

**解決方案**：

1. 確認 `angular.json` 中已設定：
   ```json
   "styles": [
     "src/styles.css"
   ]
   ```

2. 檢查檔案路徑是否正確

---

## 7. 建置和部署問題

### Q18: 建置失敗記憶體不足

**問題**：生產建置時記憶體溢位。

**解決方案**：

```bash
# 增加 Node.js 記憶體限制
export NODE_OPTIONS="--max-old-space-size=4096"
ng build --configuration production
```

---

### Q19: 建置產物太大

**問題**：打包後的檔案體積過大。

**解決方案**：

1. 啟用生產最佳化：
   ```bash
   ng build --configuration production
   ```

2. 開啟延遲載入：
   ```typescript
   {
     path: 'admin',
     loadComponent: () => import('./admin/admin.component')
   }
   ```

3. 使用程式碼分割

---

### Q20: 部署後重新整理 404

**問題**：重新整理頁面顯示 404。

**解決方案**：

設定伺服器處理 SPA 路由：

```nginx
# Nginx
location / {
  try_files $uri $uri/ /index.html;
}
```

---

## 8. 效能問題

### Q21: 變更偵測太頻繁

**問題**：頁面回應慢，變更偵測過於頻繁。

**解決方案**：

使用 `OnPush` 策略：

```typescript
import { ChangeDetectionStrategy, Component } from '@angular/core';

@Component({
  changeDetection: ChangeDetectionStrategy.OnPush
})
export class MyComponent {}
```

---

### Q22: 列表渲染慢

**問題**：大量資料列表渲染很慢。

**解決方案**：

1. 使用 `trackBy`：
   ```html
   @for (item of items; track item.id) {
     {{ item.name }}
   }
   ```

2. 使用虛擬滾動（Angular CDK）

---

## 9. 測試問題

### Q23: 單元測試失敗

**問題**：執行 `ng test` 時測試失敗。

**解決方案**：

1. 確保 TestBed 設定正確：
   ```typescript
   await TestBed.configureTestingModule({
     declarations: [MyComponent],
     imports: [CommonModule]
   }).compileComponents();
   ```

2. 檢查非同步操作是否正確處理

---

### Q24: 測試覆蓋率為 0

**問題**：程式碼覆蓋率報告為空。

**解決方案**：

```bash
ng test --code-coverage --watch=false
```

---

## 10. 其他問題

### Q25: 如何升級 Angular 版本？

**問題**：如何將專案升級到新版本。

**解決方案**：

```bash
# 使用 Angular Update Guide
# https://update.angular.io/

# 或使用 ng update
ng update @angular/core@17 @angular/cli@17
```

---

### Q26: 獨立元件 vs 模組

**問題**：應該使用獨立元件還是 NgModule？

**解決方案**：

- **Angular 17+ 推薦**：使用獨立元件
- **舊專案**：可以繼續使用 NgModule
- **學習**：推薦學習獨立元件

---

### Q27: Signal 怎麼使用？

**問題**：Angular 16+ 的 Signal 如何使用。

**解決方案**：

```typescript
import { signal, computed } from '@angular/core';

// 建立 signal
count = signal(0);

// 建立計算屬性
doubleCount = computed(() => this.count() * 2);

// 修改 signal
count.set(5);
count.update(c => c + 1);

// 在範本中使用
{{ count() }}
```

---

### Q28: 如何調試 Angular 應用？

**問題**：如何調試 Angular 應用。

**解決方案**：

1. 使用 Angular DevTools 瀏覽器擴充功能
2. 在元件中新增中斷點
3. 使用 `ng.probe`（開發模式）

---

## 📚 相關資源

- 官方文檔：https://angular.io/docs
- 官方教學：https://angular.io/tutorial
- Stack Overflow：https://stackoverflow.com/questions/tagged/angular
- GitHub Issues：https://github.com/angular/angular/issues

---

*本文檔由 AI 助手生成*
