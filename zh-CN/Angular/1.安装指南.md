# Angular 安装指南

> 🎯 本文档详细介绍 Angular 在各种操作系统环境下的安装方法
> 
> 📅 更新时间：2026年2月
> 
> 📖 适用版本：Angular 17+ (当前最新版本)

---

## 目录

1. [前置要求](#1-前置要求)
2. [在 Windows 上安装](#2-在-windows-上安装)
3. [在 macOS 上安装](#3-在-macos-上安装)
4. [在 Linux 上安装](#4-在-linux-上安装)
5. [验证安装](#5-验证安装)
6. [创建第一个项目](#6-创建第一个项目)
7. [卸载 Angular CLI](#7-卸载-angular-cli)
8. [常见问题](#8-常见问题)

---

## 1. 前置要求

### 1.1 Node.js 要求

Angular 需要 Node.js 环境。不同版本的 Angular 对 Node.js 版本有不同的要求：

| Angular 版本 | 最低 Node.js 版本 | 推荐 Node.js 版本 |
|-------------|------------------|------------------|
| Angular 17+ | Node.js 18.13.0+ | Node.js 20 LTS |
| Angular 16 | Node.js 16.14.0+ | Node.js 18 LTS |
| Angular 15 | Node.js 14.20.0+ | Node.js 16 LTS |
| Angular 14 | Node.js 14.20.0+ | Node.js 16 LTS |

> 💡 **提示**：建议使用 Node.js LTS（长期支持）版本以获得最佳稳定性。

### 1.2 npm 要求

Angular CLI 通过 npm（Node Package Manager）分发。npm 随 Node.js 一起安装。

- 最低要求：npm 6.14.0+
- 推荐：npm 8+（随 Node.js 20 一起提供）

### 1.3 可选：yarn 或 pnpm

如果你更喜欢使用 yarn 或 pnpm 作为包管理器，也可以使用：

```bash
# 安装 yarn（可选）
npm install -g yarn

# 安装 pnpm（可选）
npm install -g pnpm
```

---

## 2. 在 Windows 上安装

### 2.1 方法一：使用 npm（推荐）

这是最简单、最常用的安装方法。

#### 步骤 1：安装 Node.js

1. 访问 Node.js 官网：https://nodejs.org/
2. 下载 **LTS（长期支持）** 版本（推荐 Node.js 20 LTS）
3. 运行安装程序
4. 在安装向导中：
   - 勾选 "Add to PATH"（添加到环境变量）
   - 其他选项保持默认即可

#### 步骤 2：验证 Node.js 和 npm

打开 **命令提示符**（cmd）或 **PowerShell**，执行以下命令：

```cmd
node --version
npm --version
```

如果显示版本号，说明安装成功。

#### 步骤 3：安装 Angular CLI

在命令提示符或 PowerShell 中执行：

```cmd
npm install -g @angular/cli
```

> ⚠️ **注意**：`-g` 表示全局安装，这样你可以在任何目录下使用 `ng` 命令。

#### 步骤 4：验证 Angular CLI

```cmd
ng version
```

如果安装成功，会显示 Angular CLI 的版本信息。

---

### 2.2 方法二：使用管理员权限安装

如果你遇到权限错误，建议使用管理员权限：

1. 右键点击 "命令提示符" 或 "PowerShell"
2. 选择 "以管理员身份运行"
3. 然后执行安装命令：

```cmd
npm install -g @angular/cli
```

---

### 2.3 方法三：使用 Windows Package Manager (winget)

如果你使用 Windows 10/11，可以使用 winget：

```cmd
winget install OpenJS.NodeJS.LTS
winget install Angular.AngularCLI
```

---

### 2.4 方法四：使用 Chocolatey

如果你安装了 Chocolatey 包管理器：

```cmd
choco install nodejs-lts
choco install angular-cli
```

---

## 3. 在 macOS 上安装

### 3.1 方法一：使用 npm（推荐）

#### 步骤 1：安装 Node.js

**方式 A：通过 Homebrew（推荐）**

```bash
# 安装 Homebrew（如果没有）
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# 安装 Node.js LTS
brew install node@20
```

**方式 B：直接下载**

1. 访问 https://nodejs.org/
2. 下载 macOS Installer (.pkg)
3. 运行安装程序

#### 步骤 2：验证安装

```bash
node --version
npm --version
```

#### 步骤 3：安装 Angular CLI

```bash
sudo npm install -g @angular/cli
```

> 💡 **提示**：macOS 可能需要管理员权限（sudo）。

#### 步骤 4：验证安装

```bash
ng version
```

---

### 3.2 方法二：使用 nvm 管理多版本

如果你需要同时使用多个 Node.js 版本，推荐使用 nvm：

```bash
# 安装 nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash

# 使用 nvm 安装 Node.js
nvm install 20
nvm use 20

# 然后安装 Angular CLI
npm install -g @angular/cli
```

---

### 3.3 方法三：使用 yarn

```bash
# 安装 yarn
brew install yarn

# 使用 yarn 安装 Angular CLI
yarn global add @angular/cli
```

---

## 4. 在 Linux 上安装

### 4.1 Ubuntu / Debian

#### 步骤 1：安装 Node.js

```bash
# 更新软件源
sudo apt update

# 安装 Node.js 20 LTS
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs
```

#### 步骤 2：验证安装

```bash
node --version
npm --version
```

#### 步骤 3：安装 Angular CLI

```bash
sudo npm install -g @angular/cli
```

---

### 4.2 CentOS / RHEL / Fedora

#### 步骤 1：安装 Node.js

```bash
# CentOS/RHEL
curl -fsSL https://rpm.nodesource.com/setup_20.x | sudo bash -
sudo yum install nodejs

# Fedora
curl -fsSL https://rpm.nodesource.com/setup_20.x | sudo bash -
sudo dnf install nodejs
```

#### 步骤 2：安装 Angular CLI

```bash
sudo npm install -g @angular/cli
```

---

### 4.3 Arch Linux

```bash
sudo pacman -S nodejs npm
sudo npm install -g @angular/cli
```

---

### 4.4 openSUSE

```bash
sudo zypper install nodejs npm
sudo npm install -g @angular/cli
```

---

### 4.5 使用 nvm（适用于所有 Linux 发行版）

```bash
# 安装 nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash

# 重新加载 shell 配置
source ~/.bashrc

# 安装并使用 Node.js 20
nvm install 20
nvm use 20

# 安装 Angular CLI
npm install -g @angular/cli
```

---

## 5. 验证安装

无论使用哪种操作系统，安装完成后都可以通过以下命令验证：

### 5.1 查看 Angular CLI 版本

```bash
ng version
```

预期输出类似：

```
     _                      _                 ____ _     ___
    / \   _ __   __ _ _   _| | __ _ _ __     / ___| |   |_ _|
   / △ \ | '_ \ / _` | | | | |/ _` | '__|   | |   | |    | |
  / ___ \| | | | (_| | |_| | | (_| | |      | |___| |___ | |
 /_/   \_\_| |_|\__,_|\__,_|_|\__,_|_|       \____|_____|___|
 

Angular CLI: 17.3.0
Node: 20.11.0
Package Manager: npm 10.2.4
OS: win32 x64
```

### 5.2 查看可用命令

```bash
ng help
```

---

## 6. 创建第一个项目

安装完成后，你可以创建第一个 Angular 项目：

### 6.1 使用默认设置创建项目

```bash
ng new my-first-app
```

这会：
- 创建名为 `my-first-app` 的目录
- 安装项目依赖
- 配置 Angular 环境

### 6.2 跳过安装（仅创建项目文件）

```bash
ng new my-first-app --skip-install
```

### 6.3 使用特定选项创建项目

```bash
# 创建不使用 Git 的项目
ng new my-first-app --skip-git

# 创建不使用单元测试的项目
ng new my-first-app --skip-tests

# 使用 SCSS 替代 CSS
ng new my-first-app --style=scss

# 使用内联样式和模板（学习时有用）
ng new my-first-app --inline-template --inline-style
```

---

## 7. 卸载 Angular CLI

如果需要卸载 Angular CLI：

### 7.1 卸载 Angular CLI

```bash
npm uninstall -g @angular/cli
```

### 7.2 清理缓存

```bash
npm cache clean --force
```

### 7.3 验证卸载

```bash
ng version
```

应该会显示 "command not found" 或 "ng 不是内部或外部命令"。

---

## 8. 常见问题

### Q1: 安装时出现权限错误怎么办？

**Windows:**
- 以管理员身份运行命令提示符或 PowerShell
- 检查 Node.js 安装目录的权限

**macOS/Linux:**
- 使用 `sudo npm install -g @angular/cli`
- 或修改 npm 全局目录的权限

---

### Q2: 如何更新 Angular CLI 到最新版本？

```bash
npm install -g @angular/cli@latest
```

---

### Q3: 如何查看当前使用的 Angular 版本？

在项目目录下运行：

```bash
ng version
```

或在 `package.json` 中查看：

```json
{
  "dependencies": {
    "@angular/core": "^17.0.0"
  }
}
```

---

### Q4: 出现 "ng 不是内部或外部命令" 怎么办？

**Windows:**
1. 检查 Node.js 是否正确安装：`node --version`
2. 检查 npm 全局模块路径是否在 PATH 中
3. 手动添加路径到系统环境变量

**macOS/Linux:**
1. 检查 npm 全局模块路径
2. 可能需要将 npm 全局 bin 目录添加到 PATH：
   ```bash
   export PATH="$PATH:$(npm global bin)"
   ```

---

### Q5: 安装很慢怎么办？

可以使用国内镜像源：

```bash
# 设置 npm 镜像
npm config set registry https://registry.npmmirror.com

# 安装 Angular CLI
npm install -g @angular/cli
```

---

### Q6: 如何在不同项目中使用不同版本的 Angular CLI？

可以使用 nvm 管理多个 Node.js 版本，或在每个项目中本地安装 Angular CLI：

```bash
# 在项目目录中
npm install @angular/cli --save-dev
```

然后使用：

```bash
npx ng version
```

---

### Q7: Node.js 版本不兼容怎么办？

使用 nvm（Node Version Manager）切换 Node.js 版本：

```bash
# 查看可用版本
nvm list

# 安装特定版本
nvm install 18

# 使用特定版本
nvm use 18
```

---

### Q8: macOS M1/M2 芯片安装问题

如果使用 Apple Silicon (M1/M2/M3) 芯片：

```bash
# 确保使用 arm64 版本的 Node.js
arch -arm64 node --version

# 如果遇到问题，可能需要安装 x64 版本的 Node.js
nvm install 20 --arm64
```

---

### Q9: 如何查看 Angular CLI 安装路径？

```bash
npm list -g @angular/cli
```

---

### Q10: 企业网络需要代理怎么办？

```bash
# 设置代理
npm config set proxy http://proxy.company.com:8080
npm config set https-proxy http://proxy.company.com:8080

# 移除代理
npm config delete proxy
npm config delete https-proxy
```

---

## 📚 相关资源

- 官方文档：https://angular.io/docs
- Angular CLI 命令参考：https://angular.io/cli
- Angular 官方 GitHub：https://github.com/angular/angular
- Stack Overflow Angular 标签：https://stackoverflow.com/questions/tagged/angular

---

*本文档由 AI 助手生成，欢迎提交反馈和改进建议。*
