# JUnit 使用手册

本手册介绍 JUnit 4 和 JUnit 5 的使用方法，包括基本测试编写、断言、测试套件等核心功能。

## JUnit 5 基本用法

### 编写测试类

```java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class CalculatorTest {
    
    @Test
    void testAddition() {
        int result = 2 + 3;
        assertEquals(5, result);
    }
}
```

### 常用断言

```java
import static org.junit.jupiter.api.Assertions.*;

// 相等断言
assertEquals(expected, actual);
assertEquals(expected, actual, "自定义错误信息");

// 布尔断言
assertTrue(condition);
assertFalse(condition);

// 空值断言
assertNull(object);
assertNotNull(object);

// 数组断言
assertArrayEquals(expectedArray, actualArray);

// 异常断言
assertThrows(Exception.class, () -> {
    // 可能抛出异常的代码
});

// 组合断言
assertAll("Person properties",
    () -> assertEquals("John", person.getFirstName()),
    () -> assertEquals("Doe", person.getLastName())
);
```

### 前置和后置操作

```java
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.AfterEach;

class MyTest {
    
    @BeforeEach
    void setUp() {
        // 每个测试方法执行前运行
    }
    
    @AfterEach
    void tearDown() {
        // 每个测试方法执行后运行
    }
}
```

### 嵌套测试

```java
import org.junit.jupiter.api.Nested;

@Nested
class NestedTests {
    
    @Test
    void testInNestedClass() {
        // 嵌套测试类中的测试
    }
}
```

### 参数化测试

```java
import org.junit.jupiter.params.ParameterizedTest;
import org.junit.jupiter.params.provider.ValueSource;

@ParameterizedTest
@ValueSource(ints = {1, 2, 3, 4, 5})
void testWithMultipleValues(int number) {
    assertTrue(number > 0);
}
```

### 动态测试

```java
import org.junit.jupiter.api.TestFactory;
import org.junit.jupiter.api.DynamicTest;
import java.util.Arrays;

@TestFactory
Collection<DynamicTest> dynamicTestsFromIterator() {
    return Arrays.asList(
        DynamicTest.dynamicTest("第一个动态测试", () -> assertTrue(true)),
        DynamicTest.dynamicTest("第二个动态测试", () -> assertEquals(4, 2 + 2))
    );
}
```

## JUnit 4 基本用法

### 编写测试类

```java
import org.junit.Test;
import static org.junit.Assert.*;

public class CalculatorTest {
    
    @Test
    public void testAddition() {
        assertEquals(5, 2 + 3);
    }
}
```

### 常用断言

```java
// 相等断言
assertEquals(expected, actual);
assertEquals("消息", expected, actual);

// 布尔断言
assertTrue(condition);
assertFalse(condition);

// 空值断言
assertNull(object);
assertNotNull(object);

// 异常断言
@Test(expected = ArithmeticException.class)
public void testException() {
    int a = 1 / 0;
}
```

### 前置和后置

```java
import org.junit.Before;
import org.junit.After;

public class MyTest {
    
    @Before
    public void setUp() {
        // 测试方法执行前运行
    }
    
    @After
    public void tearDown() {
        // 测试方法执行后运行
    }
    
    @BeforeClass
    public static void setUpClass() {
        // 整个测试类开始前运行一次
    }
    
    @AfterClass
    public static void tearDownClass() {
        // 整个测试类结束后运行一次
    }
}
```

### 测试套件

```java
import org.junit.runner.RunWith;
import org.junit.runners.Suite;
import org.junit.runners.Suite.SuiteClasses;

@RunWith(Suite.class)
@SuiteClasses({Test1.class, Test2.class, Test3.class})
public class AllTests {
    // 运行 Test1、Test2、Test3 中的所有测试
}
```

## JUnit 4 到 JUnit 5 迁移

### 注解变化

| JUnit 4 | JUnit 5 |
|---------|---------|
| @Test | @Test |
| @Before | @BeforeEach |
| @After | @AfterEach |
| @BeforeClass | @BeforeAll |
| @AfterClass | @AfterAll |
| @Ignore | @Disabled |
| @SuiteClasses | @SelectClasses |

### 断言变化

```java
// JUnit 4
Assert.assertEquals(expected, actual);

// JUnit 5
Assertions.assertEquals(expected, actual);
```

### 异常测试变化

```java
// JUnit 4
@Test(expected = NullPointerException.class)
public void testException() {
    Object obj = null;
    obj.toString();
}

// JUnit 5
@Test
public void testException() {
    assertThrows(NullPointerException.class, () -> {
        Object obj = null;
        obj.toString();
    });
}
```

## 运行测试

### 命令行运行

```bash
# Maven
mvn test

# Gradle
gradle test

# 独立控制台
java -jar junit-platform-console-standalone-5.14.3.jar -c MyTestClass
```

### IDE 运行

- IntelliJ IDEA：右键点击测试类或方法，选择 Run
- Eclipse：右键点击测试类或方法，选择 Run As → JUnit Test
- VS Code：使用 JUnit 扩展运行测试
