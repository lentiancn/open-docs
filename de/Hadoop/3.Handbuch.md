# Hadoop Handbuch

## HDFS Befehle

```bash
hdfs dfs -ls /
hdfs dfs -mkdir -p /user/data
hdfs dfs -put local.txt /user/data/
hdfs dfs -get /user/data/output.txt
```

## MapReduce

WordCount Beispielcode für MapReduce Programmierung.

## YARN

```bash
hadoop jar wordcount.jar WordCount /input /output
yarn application -list
```

## Optimierung

1. Blockgröße anpassen
2. Datenkompression
3. Map/Reduce Anzahl optimieren
