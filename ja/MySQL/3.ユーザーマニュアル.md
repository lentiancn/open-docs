# MySQL ユーザーマニュアル

## データベースの基本操作

### MySQL への接続

ローカル接続：
```bash
mysql -u root -p
```

リモート接続：
```bash
mysql -h hostname -u username -p database_name
```

### データベースの作成

```sql
CREATE DATABASE database_name;
```

文字セットを指定して作成：
```sql
CREATE DATABASE database_name CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### データベースの表示

```sql
SHOW DATABASES;
```

### データベースの選択

```sql
USE database_name;
```

### データベースの削除

```sql
DROP DATABASE database_name;
```

## テーブル操作

### テーブルの作成

```sql
CREATE TABLE table_name (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    email VARCHAR(100) UNIQUE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

### テーブル構造の表示

```sql
DESCRIBE table_name;
-- または
SHOW COLUMNS FROM table_name;
```

### すべてのテーブルの表示

```sql
SHOW TABLES;
```

### テーブル構造の変更

列の追加：
```sql
ALTER TABLE table_name ADD COLUMN new_column VARCHAR(50);
```

列の変更：
```sql
ALTER TABLE table_name MODIFY column_name VARCHAR(200);
```

列の削除：
```sql
ALTER TABLE table_name DROP COLUMN column_name;
```

### テーブルの削除

```sql
DROP TABLE table_name;
```

## データ操作

### データの挿入

単一挿入：
```sql
INSERT INTO table_name (column1, column2) VALUES ('value1', 'value2');
```

複数挿入：
```sql
INSERT INTO table_name (column1, column2) VALUES 
('value1', 'value2'),
('value3', 'value4');
```

### データのクエリ

基本クエリ：
```sql
SELECT * FROM table_name;
```

条件付きクエリ：
```sql
SELECT column1, column2 FROM table_name WHERE condition;
```

並べ替え：
```sql
SELECT * FROM table_name ORDER BY column_name DESC;
```

ページネーション：
```sql
SELECT * FROM table_name LIMIT 10 OFFSET 20;
```

### データの更新

```sql
UPDATE table_name SET column1 = 'new_value' WHERE condition;
```

### データの削除

```sql
DELETE FROM table_name WHERE condition;
```

テーブルを空にする：
```sql
TRUNCATE TABLE table_name;
```

## インデックス操作

### インデックスの作成

通常インデックス：
```sql
CREATE INDEX index_name ON table_name (column_name);
```

一意インデックス：
```sql
CREATE UNIQUE INDEX index_name ON table_name (column_name);
```

複合インデックス：
```sql
CREATE INDEX index_name ON table_name (column1, column2);
```

### インデックスの表示

```sql
SHOW INDEX FROM table_name;
```

### インデックスの削除

```sql
DROP INDEX index_name ON table_name;
```

## ユーザーと権限管理

### ユーザーの作成

```sql
CREATE USER 'username'@'localhost' IDENTIFIED BY 'password';
```

### 権限の付与

```sql
GRANT SELECT, INSERT, UPDATE ON database_name.* TO 'username'@'localhost';
```

すべての権限を付与：
```sql
GRANT ALL PRIVILEGES ON database_name.* TO 'username'@'localhost';
```

### 権限のリロード

```sql
FLUSH PRIVILEGES;
```

### ユーザーの権限を表示

```sql
SHOW GRANTS FOR 'username'@'localhost';
```

### ユーザーの削除

```sql
DROP USER 'username'@'localhost';
```

## トランザクション制御

### トランザクションの開始

```sql
START TRANSACTION;
-- または
BEGIN;
```

### トランザクションのコミット

```sql
COMMIT;
```

### トランザクションのロールバック

```sql
ROLLBACK;
```

### セーブポイントの設定

```sql
SAVEPOINT savepoint_name;
ROLLBACK TO SAVEPOINT savepoint_name;
```

## 一般的な関数

### 文字列関数

```sql
SELECT CONCAT('Hello', ' ', 'World');
SELECT UPPER('hello');
SELECT LOWER('HELLO');
SELECT SUBSTRING('Hello World', 1, 5);
SELECT LENGTH('Hello');
SELECT TRIM('  Hello  ');
```

### 数値関数

```sql
SELECT ABS(-5);
SELECT CEIL(3.14);
SELECT FLOOR(3.14);
SELECT ROUND(3.14159, 2);
SELECT MOD(10, 3);
```

### 日付関数

```sql
SELECT NOW();
SELECT CURDATE();
SELECT CURTIME();
SELECT DATE('2024-01-01 10:30:00');
SELECT YEAR('2024-01-01');
SELECT MONTH('2024-01-01');
SELECT DAY('2024-01-01');
SELECT DATE_ADD('2024-01-01', INTERVAL 1 DAY);
SELECT DATEDIFF('2024-01-01', '2023-12-01');
```

### 集計関数

```sql
SELECT COUNT(*) FROM table_name;
SELECT SUM(column_name) FROM table_name;
SELECT AVG(column_name) FROM table_name;
SELECT MAX(column_name) FROM table_name;
SELECT MIN(column_name) FROM table_name;
```

## バックアップとリストア

### データベースのバックアップ

```bash
mysqldump -u root -p database_name > backup.sql
```

すべてのデータベースをバックアップ：
```bash
mysqldump -u root -p --all-databases > all_databases.sql
```

### データベースのリストア

```bash
mysql -u root -p database_name < backup.sql
```

### クエリ結果のエクスポート

```bash
mysql -u root -p -e "SELECT * FROM table_name" database_name > output.tsv
```

## パフォーマンス最適化

### EXPLAIN を使用したクエリ分析

```sql
EXPLAIN SELECT * FROM table_name WHERE condition;
```

### 最適化のヒント

1. 一般的なクエリ条件にインデックスを作成
2. SELECT * を避け、必要な列のみをクエリ
3. LIMIT を使用して結果セットを制限
4. インデックス列で関数を使用しない
5. JOIN を適切に使用し、デカルト積を避ける
6. テーブルを定期的に分析して最適化

### スロークエリログの表示

```sql
SHOW VARIABLES LIKE 'slow_query_log%';
```

## 一般的なエラー処理

### 接続エラー
MySQL サービスが起動しているかどうか、ポートが正しいかどうか、ユーザーの権限が正しく設定されているかどうかを確認します。

### 文字セットエラー
データベース、テーブル、接続がすべて同じ文字セットを使用んでいることを確認します（utf8mb4 を推奨）。

### 権限エラー
ユーザーが適切なデータベースとテーブルの操作権限を持っていることを確認します。
