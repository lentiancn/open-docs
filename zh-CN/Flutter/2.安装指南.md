# Flutter 安装指南

> 当前版本：Flutter 3.41.2 | 更新日期：2026-01-28
> 官方文档：https://docs.flutter.dev/get-started/install

---

## 系统要求

### Windows

| 要求 | 最低配置 | 推荐配置 |
|------|----------|----------|
| 操作系统 | Windows 10 (64-bit) | Windows 11 |
| 磁盘空间 | 2.8 GB | 10 GB+ |
| 内存 | 4 GB | 8 GB+ |
| GPU | 支持 DirectX 11 | 支持 DirectX 12 |

### macOS

| 要求 | 最低配置 | 推荐配置 |
|------|----------|----------|
| 操作系统 | macOS 10.14 (Mojave) | macOS 12+ |
| 磁盘空间 | 2.8 GB | 10 GB+ |
| 内存 | 4 GB | 8 GB+ |
| 芯片 | Intel 或 Apple Silicon | Apple Silicon |

### Linux

| 要求 | 最低配置 |
|------|----------|
| 操作系统 | Ubuntu 18.04+ 或其他主流 Linux |
| 磁盘空间 | 2.8 GB |
| 内存 | 4 GB |
| 开发工具 | GCC, CMake, Ninja |

---

## Windows 安装步骤

### 1. 下载 Flutter SDK

**方式一：直接下载（推荐）**

1. 访问：https://docs.flutter.dev/get-started/install/windows
2. 下载最新的 Flutter SDK（.zip 文件）
3. 解压到指定目录，例如：`C:\src\flutter`

**方式二：使用 Git**

```bash
# 克隆仓库
git clone https://github.com/flutter/flutter.git -b stable

# 添加到 PATH（需要手动添加到系统环境变量）
# 右键"此电脑" → 属性 → 高级系统设置 → 环境变量 → 编辑 PATH
```

### 2. 配置 PATH 环境变量

1. 打开「系统属性」→「高级」→「环境变量」
2. 在「用户变量」中找到 `Path`（或新建）
3. 添加 Flutter bin 目录路径，例如：`C:\src\flutter\bin`
4. **重启**命令行工具或电脑

### 3. 运行 Flutter Doctor

打开 PowerShell 或命令提示符，运行：

```bash
flutter doctor
```

这个命令会检查你的开发环境并报告问题。

### 4. 配置 Android 开发环境

Flutter 需要 Android SDK 才能构建 Android 应用。

**安装 Android Studio：**
1. 下载 Android Studio：https://developer.android.com/studio
2. 安装并启动
3. 首次启动会自动提示安装 Android SDK

**或者只安装命令行工具：**
1. 下载 command-line tools：https://developer.android.com/studio#command-line-tools
2. 解压并设置 `ANDROID_HOME` 环境变量

**接受许可证：**

```bash
flutter doctor --android-licenses
```

---

## macOS 安装步骤

### 1. 安装 Xcode

App Store 中搜索并安装 Xcode（免费），或下载：https://developer.apple.com/xcode/

```bash
# 验证安装
xcodebuild --version
```

安装后运行一次以接受许可协议。

### 2. 安装 Flutter SDK

**方式一：使用 Homebrew（推荐）**

```bash
# 更新 Homebrew
brew update

# 安装 Flutter
brew install flutter
```

**方式二：直接下载**

1. 下载：https://docs.flutter.dev/get-started/install/macos
2. 解压到指定目录，例如：`~/flutter`
3. 添加到 PATH：

```bash
# 在 ~/.bash_profile 或 ~/.zshrc 中添加
export PATH="$PATH:/Users/你的用户名/flutter/bin"

# 刷新配置
source ~/.bash_profile  # 或 source ~/.zshrc
```

### 3. 配置 iOS 开发

```bash
# 安装 iOS 模拟器工具
sudo xcodebuild -runFirstLaunch

# 验证
flutter doctor
```

---

## Linux 安装步骤

### 1. 安装依赖

```bash
# Ubuntu/Debian
sudo apt-get update
sudo apt-get install -y \
    curl \
    git \
    unzip \
    xz-utils \
    zip \
    libglu1-mesa \
    clang \
    cmake \
    ninja-build \
    pkg-config \
    libgtk-3-dev
```

### 2. 安装 Flutter SDK

```bash
# 下载并解压
cd /opt
sudo git clone https://github.com/flutter/flutter.git -b stable

# 添加到 PATH
echo 'export PATH="$PATH:/opt/flutter/bin"' >> ~/.bashrc
source ~/.bashrc
```

### 3. 运行 Flutter Doctor

```bash
flutter doctor
```

---

## 验证安装

### 基本命令

```bash
# 检查 Flutter 版本
flutter --version

# 查看 Flutter 详情
flutter doctor

# 检查 Flutter 通道（应显示 stable）
flutter channel
```

### 创建第一个项目

```bash
# 创建新项目
flutter create my_first_app

# 进入项目目录
cd my_first_app

# 运行应用（需要模拟器或真机）
flutter run

# 或者指定平台
flutter run -d chrome    # Web
flutter run -d windows   # Windows 桌面
flutter run -d macos     # macOS
```

---

## 升级 Flutter

### 升级到最新版本

```bash
# 升级 Flutter
flutter upgrade

# 升级特定版本
flutter upgrade v3.41.0
```

### 切换通道

```bash
# 查看可用通道
flutter channel

# 切换到 stable 通道
flutter channel stable

# 切换到 beta 通道
flutter channel beta
```

---

## 卸载 Flutter

### Windows

1. 删除 Flutter 安装目录（如 `C:\src\flutter`）
2. 从 PATH 中移除 Flutter bin 目录
3. 删除 `%APPDATA%\Local\Flutter` 目录（可选）

### macOS/Linux

```bash
# 删除 Flutter 目录
sudo rm -rf ~/flutter

# 从 shell 配置中移除 PATH 记录
# 编辑 ~/.bash_profile 或 ~/.zshrc
```

---

## 常见问题

### Q: flutter doctor 显示 Android SDK 缺失

**解决：**
1. 安装 Android Studio 并配置 SDK
2. 或者手动下载 Android SDK command-line tools
3. 设置 `ANDROID_HOME` 环境变量

### Q: 下载速度慢

**解决：**
1. 使用国内镜像源
2. 在 Windows PowerShell 中设置：
   ```powershell
   [Environment]::SetEnvironmentVariable("FLUTTER_STORAGE_BASE_URL", "https://storage.flutter-io.cn")
   [Environment]::SetEnvironmentVariable("PUB_CACHE", "$env:APPDATA\Pub\Cache")
   ```

### Q: macOS 权限错误

**解决：**
```bash
sudo xcodebuild -runFirstLaunch
```

---

## 下一步

- 📖 [使用手册](./3.使用手册.md) - 学习 Flutter 基础
- ❓ [常见问题](./4.常见问题.md) - 解答疑惑
- 🏠 [返回首页](./README.md)
