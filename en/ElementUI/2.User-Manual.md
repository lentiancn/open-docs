# Element UI User Manual

> Detailed usage instructions for Element UI component library
> 
> Updated: February 2026

---

## 1. Basic Components

### 1.1 Button Component

Buttons are one of the most commonly used components. Element UI provides multiple button types.

#### Basic Usage

```html
<template>
  <el-button>Default</el-button>
  <el-button type="primary">Primary</el-button>
  <el-button type="success">Success</el-button>
  <el-button type="info">Info</el-button>
  <el-button type="warning">Warning</el-button>
  <el-button type="danger">Danger</el-button>
</template>
```

#### Button Attributes

| Attribute | Description | Type | Optional Values | Default |
|-----------|-------------|------|-----------------|---------|
| type | Button type | string | primary / success / warning / danger / info / text | — |
| plain | Whether plain button | boolean | — | false |
| round | Whether round button | boolean | — | false |
| circle | Whether circle button | boolean | — | false |
| loading | Whether loading | boolean | — | false |
| disabled | Whether disabled | boolean | — | false |
| icon | Icon class name | string | Element UI icon names | — |
| size | Button size | string | medium / small / mini | — |

#### Button Group

```html
<el-button-group>
  <el-button type="primary" icon="el-icon-arrow-left">Previous</el-button>
  <el-button type="primary">Next<i class="el-icon-arrow-right el-icon--right"></i></el-button>
</el-button-group>
```

#### Button Events

```html
<el-button @click="handleClick" @dblclick="handleDblClick">
  Click Event
</el-button>
```

```javascript
export default {
  methods: {
    handleClick() {
      console.log('Single click')
    },
    handleDblClick() {
      console.log('Double click')
    }
  }
}
```

---

### 1.2 Icon Component

Element UI comes with a built-in set of commonly used icons.

#### Usage

```html
<i class="el-icon-edit"></i>
<i class="el-icon-search"></i>
<i class="el-icon-eleme"></i>

<!-- With text -->
<el-button icon="el-icon-search">Search</el-button>
```

#### Available Icons

| Icon Name | Description |
|-----------|-------------|
| el-icon-edit | Edit |
| el-icon-delete | Delete |
| el-icon-search | Search |
| el-icon-user | User |
| el-icon-phone | Phone |
| el-icon-message | Message |
| el-icon-setting | Settings |
| el-icon-star-on | Star (filled) |
| el-icon-star-off | Star (outline) |
| el-icon-plus | Plus |
| el-icon-minus | Minus |
| el-icon-check | Check |
| el-icon-close | Close |

---

## 2. Form Components

### 2.1 Input

#### Basic Input

```html
<el-input v-model="input" placeholder="Please enter content"></el-input>
```

```javascript
export default {
  data() {
    return {
      input: ''
    }
  }
}
```

#### Input Attributes

| Attribute | Description | Type | Optional Values | Default |
|-----------|-------------|------|-----------------|---------|
| type | Input type | string | text / textarea / password | text |
| v-model | Binding value | string / number | — | — |
| placeholder | Placeholder | string | — | — |
| disabled | Whether disabled | boolean | — | false |
| clearable | Whether clearable | boolean | — | false |
| show-password | Whether show password toggle | boolean | — | false |
| prefix-icon | Prefix icon | string | — | — |
| suffix-icon | Suffix icon | string | — | — |

#### Input with Icon

```html
<el-input placeholder="Enter username" prefix-icon="el-icon-user" v-model="username"></el-input>
<el-input placeholder="Enter password" type="password" suffix-icon="el-icon-view" v-model="password"></el-input>
```

#### Textarea

```html
<el-input type="textarea" :rows="3" placeholder="Please enter description"></el-input>
```

### 2.2 Select

#### Basic Select

```html
<el-select v-model="selected" placeholder="Please select">
  <el-option
    v-for="item in options"
    :key="item.value"
    :label="item.label"
    :value="item.value">
  </el-option>
</el-select>
```

```javascript
export default {
  data() {
    return {
      selected: '',
      options: [
        { value: 'option1', label: 'Option 1' },
        { value: 'option2', label: 'Option 2' },
        { value: 'option3', label: 'Option 3' }
      ]
    }
  }
}
```

#### Clearable Select

```html
<el-select v-model="selected" clearable placeholder="Please select">
  <el-option
    v-for="item in options"
    :key="item.value"
    :label="item.label"
    :value="item.value">
  </el-option>
</el-select>
```

#### Multiple Select

```html
<el-select v-model="selectedMultiple" multiple placeholder="Please select">
  <el-option
    v-for="item in options"
    :key="item.value"
    :label="item.label"
    :value="item.value">
  </el-option>
</el-select>
```

### 2.3 Radio and Checkbox

#### Radio

```html
<el-radio-group v-model="radio">
  <el-radio label="male">Male</el-radio>
  <el-radio label="female">Female</el-radio>
</el-radio-group>
```

#### Checkbox

```html
<el-checkbox-group v-model="checkList">
  <el-checkbox label="Checkbox A"></el-checkbox>
  <el-checkbox label="Checkbox B"></el-checkbox>
  <el-checkbox label="Checkbox C"></el-checkbox>
</el-checkbox-group>
```

### 2.4 Switch

```html
<el-switch v-model="switchValue" active-text="On" inactive-text="Off"></el-switch>
```

### 2.5 Slider

```html
<el-slider v-model="sliderValue"></el-slider>
```

### 2.6 Time Picker

```html
<el-time-select
  v-model="time"
  :picker-options="{
    start: '08:30',
    step: '00:15',
    end: '18:30'
  }"
  placeholder="Select time">
</el-time-select>
```

### 2.7 Date Picker

```html
<el-date-picker
  v-model="date"
  type="date"
  placeholder="Select date">
</el-date-picker>
```

### 2.8 Form Validation

Element UI provides powerful form validation capabilities.

#### Basic Validation

```html
<el-form :model="form" :rules="rules" ref="ruleForm">
  <el-form-item label="Username" prop="username">
    <el-input v-model="form.username"></el-input>
  </el-form-item>
  <el-form-item label="Password" prop="password">
    <el-input v-model="form.password" type="password"></el-input>
  </el-form-item>
  <el-form-item>
    <el-button type="primary" @click="submitForm('ruleForm')">Submit</el-button>
    <el-button @click="resetForm('ruleForm')">Reset</el-button>
  </el-form-item>
</el-form>
```

```javascript
export default {
  data() {
    return {
      form: {
        username: '',
        password: ''
      },
      rules: {
        username: [
          { required: true, message: 'Please enter username', trigger: 'blur' },
          { min: 3, max: 20, message: 'Length should be 3 to 20 characters', trigger: 'blur' }
        ],
        password: [
          { required: true, message: 'Please enter password', trigger: 'blur' },
          { min: 6, message: 'Password must be at least 6 characters', trigger: 'blur' }
        ]
      }
    }
  },
  methods: {
    submitForm(formName) {
      this.$refs[formName].validate(valid => {
        if (valid) {
          console.log('Submit success:', this.form)
        } else {
          this.$message.error('Form validation failed')
          return false
        }
      })
    },
    resetForm(formName) {
      this.$refs[formName].resetFields()
    }
  }
}
```

---

## 3. Table Component

### 3.1 Basic Table

```html
<el-table :data="tableData" stripe border>
  <el-table-column prop="date" label="Date" width="180"></el-table-column>
  <el-table-column prop="name" label="Name" width="180"></el-table-column>
  <el-table-column prop="address" label="Address"></el-table-column>
</el-table>
```

```javascript
export default {
  data() {
    return {
      tableData: [
        { date: '2024-01-01', name: 'John Doe', address: 'New York' },
        { date: '2024-01-02', name: 'Jane Smith', address: 'Los Angeles' },
        { date: '2024-01-03', name: 'Bob Johnson', address: 'Chicago' }
      ]
    }
  }
}
```

### 3.2 Table with Actions

```html
<el-table :data="tableData">
  <el-table-column prop="name" label="Name"></el-table-column>
  <el-table-column label="Actions" width="200">
    <template slot-scope="scope">
      <el-button size="mini" @click="handleEdit(scope.$index, scope.row)">Edit</el-button>
      <el-button size="mini" type="danger" @click="handleDelete(scope.$index, scope.row)">Delete</el-button>
    </template>
  </el-table-column>
</el-table>
```

### 3.3 Table Attributes

| Attribute | Description | Type | Optional Values | Default |
|-----------|-------------|------|-----------------|---------|
| data | Table data | array | — | — |
| stripe | Whether stripe | boolean | — | false |
| border | Whether border | boolean | — | false |
| height | Table height | string / number | — | — |
| v-loading | Loading state | boolean | — | false |

### 3.4 Table with Pagination

```html
<el-table :data="tableData" v-loading="loading">
  <el-table-column prop="title" label="Title"></el-table-column>
  <el-table-column prop="author" label="Author"></el-table-column>
  <el-table-column prop="date" label="Date"></el-table-column>
</el-table>

<el-pagination
  @size-change="handleSizeChange"
  @current-change="handleCurrentChange"
  :current-page="currentPage"
  :page-sizes="[10, 20, 50, 100]"
  :page-size="pageSize"
  layout="total, sizes, prev, pager, next, jumper"
  :total="total">
</el-pagination>
```

```javascript
export default {
  data() {
    return {
      tableData: [],
      loading: false,
      currentPage: 1,
      pageSize: 10,
      total: 0
    }
  },
  methods: {
    handleSizeChange(val) {
      this.pageSize = val
      this.loadData()
    },
    handleCurrentChange(val) {
      this.currentPage = val
      this.loadData()
    },
    loadData() {
      this.loading = true
      // Simulate API call
      setTimeout(() => {
        this.loading = false
      }, 500)
    }
  },
  mounted() {
    this.loadData()
  }
}
```

---

## 4. Dialogs and Notifications

### 4.1 Dialog

#### Basic Dialog

```html
<el-button type="text" @click="dialogVisible = true">Open Dialog</el-button>

<el-dialog
  title="Edit"
  :visible.sync="dialogVisible"
  width="30%"
  :before-close="handleClose">
  <span>This is a message</span>
  <span slot="footer">
    <el-button @click="dialogVisible = false">Cancel</el-button>
    <el-button type="primary" @click="dialogVisible = false">OK</el-button>
  </span>
</el-dialog>
```

```javascript
export default {
  data() {
    return {
      dialogVisible: false
    }
  },
  methods: {
    handleClose(done) {
      this.$confirm('Confirm close?')
        .then(_ => {
          done()
        })
        .catch(_ => {})
    }
  }
}
```

### 4.2 Message Notifications

#### Success Message

```javascript
this.$message.success('Operation successful')
```

#### Warning Message

```javascript
this.$message.warning('Warning message')
```

#### Error Message

```javascript
this.$message.error('Operation failed')
```

#### Info Message

```javascript
this.$message.info('Info message')
```

### 4.3 Notifications

```javascript
// Success notification
this.$notify({
  title: 'Success',
  message: 'Operation completed successfully',
  type: 'success'
})

// Error notification
this.$notify.error({
  title: 'Error',
  message: 'Operation failed'
})

// Warning notification
this.$notify.warning({
  title: 'Warning',
  message: 'Please note'
})
```

---

## 5. Navigation Components

### 5.1 Navigation Menu

#### Top Navigation

```html
<el-menu
  :default-active="activeIndex"
  class="el-menu-demo"
  mode="horizontal"
  @select="handleSelect">
  <el-menu-item index="1">Dashboard</el-menu-item>
  <el-submenu index="2">
    <template slot="title">My Workspace</template>
    <el-menu-item index="2-1">Option 1</el-menu-item>
    <el-menu-item index="2-2">Option 2</el-menu-item>
    <el-menu-item index="2-3">Option 3</el-menu-item>
  </el-submenu>
  <el-menu-item index="3">Messages</el-menu-item>
</el-menu>
```

#### Side Navigation

```html
<el-menu
  default-active="2"
  class="el-menu-vertical"
  background-color="#545c64"
  text-color="#fff"
  active-text-color="#ffd04b">
  <el-menu-item index="1">
    <i class="el-icon-menu"></i>
    <span slot="title">Navigation 1</span>
  </el-menu-item>
  <el-menu-item index="2">
    <i class="el-icon-menu"></i>
    <span slot="title">Navigation 2</span>
  </el-menu-item>
  <el-submenu index="3">
    <template slot="title">
      <i class="el-icon-setting"></i>
      <span>Navigation 3</span>
    </template>
    <el-menu-item index="3-1">Option 1</el-menu-item>
    <el-menu-item index="3-2">Option 2</el-menu-item>
  </el-submenu>
</el-menu>
```

### 5.2 Tabs

```html
<el-tabs v-model="activeName" @tab-click="handleTabClick">
  <el-tab-pane label="User Management" name="first">User Management</el-tab-pane>
  <el-tab-pane label="Config" name="second">Config</el-tab-pane>
  <el-tab-pane label="Role" name="third">Role</el-tab-pane>
  <el-tab-pane label="Task" name="fourth">Task</el-tab-pane>
</el-tabs>
```

### 5.3 Breadcrumb

```html
<el-breadcrumb separator="/">
  <el-breadcrumb-item :to="{ path: '/' }">Home</el-breadcrumb-item>
  <el-breadcrumb-item>Activity Management</el-breadcrumb-item>
  <el-breadcrumb-item>Activity List</el-breadcrumb-item>
  <el-breadcrumb-item>Activity Detail</el-breadcrumb-item>
</el-breadcrumb>
```

---

## 6. Layout Components

### 6.1 Layout Container

```html
<el-container>
  <el-header>Header</el-header>
  <el-container>
    <el-aside width="200px">Aside</el-aside>
    <el-main>Main</el-main>
  </el-container>
</el-container>
```

### 6.2 Grid System

Element UI uses a 24-column grid system.

```html
<el-row :gutter="20">
  <el-col :span="12"><div class="grid-content bg-purple"></div></el-col>
  <el-col :span="12"><div class="grid-content bg-purple-light"></div></el-col>
</el-row>

<el-row :gutter="20">
  <el-col :span="8"><div class="grid-content"></div></el-col>
  <el-col :span="8"><div class="grid-content"></div></el-col>
  <el-col :span="8"><div class="grid-content"></div></el-col>
</el-row>
```

---

## 7. Practical Tips

### 7.1 Common Utility Methods

```javascript
// Show message
this.$message.success('Success')
this.$message.error('Fail')
this.$message.warning('Warning')
this.$message.info('Info')

// Confirmation dialog
this.$confirm('Confirm this operation?', 'Notice', {
  confirmButtonText: 'OK',
  cancelButtonText: 'Cancel',
  type: 'warning'
}).then(() => {
  // Execute after confirm
}).catch(() => {
  // Execute after cancel
})

// Lightweight confirmation
this.$msgbox.confirm('This will permanently delete the file, continue?', 'Warning', {
  confirmButtonText: 'OK',
  cancelButtonText: 'Cancel',
  type: 'warning'
})
```

### 7.2 Table Selection Change Event

```html
<el-table
  :data="tableData"
  @selection-change="handleSelectionChange">
  <el-table-column type="selection" width="55"></el-table-column>
  <el-table-column prop="name" label="Name"></el-table-column>
</el-table>
```

```javascript
export default {
  methods: {
    handleSelectionChange(val) {
      console.log('Selected rows:', val)
      this.multipleSelection = val
    }
  }
}
```

### 7.3 Table Sorting

```html
<el-table :data="tableData" default-sort="{prop: 'date', order: 'descending'}">
  <el-table-column prop="date" label="Date" sortable></el-table-column>
  <el-table-column prop="name" label="Name" sortable></el-table-column>
  <el-table-column prop="address" label="Address"></el-table-column>
</el-table>
```

---

## 8. Related Resources

- Component Demo: https://element.eleme.io/#/component/layout
- Online Playground: Running in Vue CLI project
- GitHub: https://github.com/ElemeFE/element
