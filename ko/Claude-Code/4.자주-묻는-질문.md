# Claude Code 자주 묻는 질문

일반적인 문제와 해결 방법입니다.

## 설치 문제

### 명령을 찾을 수 없음

오류: `command not found: claude`

**해결:** PATH를 확인하세요.

macOS/Linux:
```bash
echo 'export PATH="$HOME/.local/bin:$PATH"' >> ~/.zshrc
source ~/.zshrc
```

Windows PowerShell:
```powershell
$currentPath = [Environment]::GetEnvironmentVariable('PATH', 'User')
[Environment]::SetEnvironmentVariable('PATH', "$currentPath;$env:USERPROFILE\.local\bin", 'User')
```

확인:
```bash
claude --version
```

### 스크립트가 HTML을 반환

**해결:** 대체 방법 사용:
- Homebrew: `brew install --cask claude-code`
- WinGet: `winget install Anthropic.ClaudeCode`

### TLS/SSL 오류

**해결:**
1. CA 인증서 업데이트
2. 프록시 설정 확인

## 네트워크 구성

설치 프로그램은 다음에서 다운로드:
```bash
curl -sI https://storage.googleapis.com
```

## 충돌 확인

```bash
which -a claude
```

## 진단

자세한 정보를 보려면 사용:
```bash
claude doctor
```

## 도움말

- Claude Code에서 `/help` 입력
- [Discord](https://www.anthropic.com/discord) 참여
