# OpenFeign 常見問題

## 安裝相關問題

### Q: 如何檢查 OpenFeign 是否正確安裝？
**A:** 在專案中新增相依性後，可以透過以下方式驗證：

```java
@SpringBootApplication
@EnableFeignClients
public class Application {
    public static void main(String[] args) {
        SpringApplication.run(Application.class, args);
    }
}
```

確保 Spring Boot 應用能夠正常啟動，並且 Feign 客戶端能夠被正確注入。

### Q: 相依性版本衝突怎麼辦？
**A:** 使用 Spring Cloud BOM 管理版本：

```xml
<dependencyManagement>
    <dependencies>
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-dependencies</artifactId>
            <version>2022.0.4</version>
            <type>pom</type>
            <scope>import</scope>
        </dependency>
    </dependencies>
</dependencyManagement>
```

這樣可以避免版本衝突問題。

### Q: 為什麼需要額外新增 spring-cloud-starter-loadbalancer？
**A:** 從 Spring Cloud 2020.0.0 開始，Ribbon 被移除，Spring Cloud LoadBalancer 成為預設的負載平衡器。`spring-cloud-starter-openfeign` 將其作為可選相依性，所以需要顯式新增：

```xml
<dependency>
    <groupId>org.springframework.cloud</groupId>
    <artifactId>spring-cloud-starter-loadbalancer</artifactId>
</dependency>
```

## 設定相關問題

### Q: 如何設定逾時時間？
**A:** 可以透過設定檔或 Java 設定兩種方式：

**設定檔方式：**
```yaml
spring:
  cloud:
    openfeign:
      client:
        config:
          default:
            connect-timeout: 5000
            read-timeout: 5000
          user-service:
            connect-timeout: 3000
            read-timeout: 10000
```

**Java 設定方式：**
```java
@Configuration
public class FeignConfig {
    
    @Bean
    public Request.Options requestOptions() {
        return new Request.Options(3000, 10000);
    }
}
```

### Q: 如何啟用 GZIP 壓縮？
**A:** 在設定檔中啟用壓縮：

```yaml
spring:
  cloud:
    openfeign:
      compression:
        request:
          enabled: true
          mime-types: text/xml,application/xml,application/json
          min-request-size: 2048
        response:
          enabled: true
```

注意：當使用 OkHttpClient 時，如果存在 `content-encoding` 或 `accept-encoding` 標頭，壓縮會被停用。

### Q: 如何設定日誌級別？
**A:** 兩種設定方式：

**設定檔：**
```yaml
spring:
  cloud:
    openfeign:
      client:
        config:
          user-service:
            logger-level: full
            
logging:
  level:
    com.example.clients.UserService: DEBUG
```

**Java 設定：**
```java
@Configuration
public class FeignLoggingConfig {
    
    @Bean
    Logger.Level feignLoggerLevel() {
        return Logger.Level.FULL;
    }
}
```

支援的日誌級別：
- **NONE**: 不記錄日誌（預設）
- **BASIC**: 只記錄請求方法、URL 和回應狀態碼、執行時間
- **HEADERS**: 記錄基本資訊以及請求和回應標頭
- **FULL**: 記錄標頭、主體和中繼資料

## 使用相關問題

### Q: FeignClient 介面無法注入怎麼辦？
**A:** 常見原因和解決方案：

**1. 掃描套件路徑問題：**
```java
// 確保 FeignClient 在掃描路徑下
@SpringBootApplication
@EnableFeignClients(basePackages = "com.example.clients")
public class Application {
    // ...
}
```

**2. 循環相依問題：**
如果 FeignClient 被多個 Service 引用，可能會出現循環相依。解決方案：
- 使用 `@Lazy` 註解延遲注入
- 重構程式碼結構，避免循環相依

**3. Bean 名稱衝突：**
如果有多个同名的 FeignClient，使用 `contextId` 區分：
```java
@FeignClient(contextId = "userClient1", name = "user-service")
public interface UserClient1 { }

@FeignClient(contextId = "userClient2", name = "user-service")  
public interface UserClient2 { }
```

### Q: 如何處理複雜的請求參數？
**A:** 對於複雜參數，有以下幾種方式：

**1. 使用 @SpringQueryMap：**
```java
public class SearchCriteria {
    private String name;
    private String status;
    private Integer page;
}

@FeignClient(name = "search-service")
public interface SearchService {
    @GetMapping("/search")
    SearchResult search(@SpringQueryMap SearchCriteria criteria);
}
```

**2. 使用 Map：**
```java
@FeignClient(name = "api-service")
public interface ApiService {
    @GetMapping("/data")
    Data getData(@RequestParam Map<String, Object> params);
}
```

**3. 自訂 QueryMapEncoder：**
```java
@Bean
public QueryMapEncoder queryMapEncoder() {
    return new CustomQueryMapEncoder();
}
```

### Q: 如何處理檔案上傳？
**A:** 檔案上傳需要特殊設定：

```java
@FeignClient(name = "file-service")
public interface FileService {
    
    @PostMapping(value = "/upload", 
                consumes = MediaType.MULTIPART_FORM_DATA_VALUE)
    UploadResponse uploadFile(@RequestPart("file") MultipartFile file,
                             @RequestPart("metadata") FileMetadata metadata);
}
```

同時需要設定編碼器：
```java
@Bean
public Encoder feignFormEncoder() {
    return new FeignFormEncoder(new SpringEncoder(messageConverters()));
}
```

## 錯誤處理問題

### Q: 如何自訂錯誤處理？
**A:** 實作 ErrorDecoder 介面：

```java
@Component
public class CustomErrorDecoder implements ErrorDecoder {
    
    @Override
    public Exception decode(String methodKey, Response response) {
        if (response.status() == 404) {
            return new ResourceNotFoundException("Resource not found");
        } else if (response.status() >= 500) {
            return new ServerException("Server error");
        }
        return new Default().decode(methodKey, response);
    }
}
```

### Q: 熔斷器不生效怎麼辦？
**A:** 檢查以下設定：

**1. 啟用熔斷器：**
```yaml
spring:
  cloud:
    openfeign:
      circuitbreaker:
        enabled: true
```

**2. 新增相依性：**
```xml
<dependency>
    <groupId>org.springframework.cloud</groupId>
    <artifactId>spring-cloud-starter-circuitbreaker-resilience4j</artifactId>
</dependency>
```

**3. 設定熔斷器規則：**
```yaml
resilience4j:
  circuitbreaker:
    instances:
      UserServicegetUser:
        failure-rate-threshold: 50
        minimum-number-of-calls: 5
        wait-duration-in-open-state: 5000
```

### Q: 回退（Fallback）不觸發怎麼辦？
**A:** 常見問題：

**1. 確保回退類別是 Spring Bean：**
```java
@Component  // 必須有這個註解
public class UserServiceFallback implements UserService {
    // ...
}
```

**2. 檢查例外類型：**
熔斷器只對特定例外觸發回退，確保拋出的是預期的例外類型。

**3. 設定正確的回退工廠：**
```java
@FeignClient(name = "service", fallbackFactory = MyFallbackFactory.class)
public interface MyService { }
```

## 效能相關問題

### Q: Feign 客戶端效能慢怎麼辦？
**A:** 效能最佳化建議：

**1. 合理設定逾時：**
```yaml
spring:
  cloud:
    openfeign:
      client:
        config:
          default:
            connect-timeout: 1000
            read-timeout: 5000
```

**2. 啟用連線池：**
```yaml
spring:
  cloud:
    openfeign:
      httpclient:
        hc5:
          enabled: true
          max-connections: 200
          max-connections-per-route: 20
```

**3. 啟用壓縮：**
```yaml
spring:
  cloud:
    openfeign:
      compression:
        request:
          enabled: true
        response:
          enabled: true
```

**4. 使用快取：**
```java
@FeignClient(name = "config-service")
public interface ConfigService {
    @GetMapping("/config/{key}")
    @Cacheable(cacheNames = "config-cache", key = "#key")
    ConfigValue getConfig(@PathVariable String key);
}
```

### Q: 記憶體洩漏問題如何解決？
**A:** 記憶體洩漏通常由以下原因引起：

**1. 連線未關閉：**
確保 HTTP 客戶端正確管理連線池。

**2. 快取未清理：**
定期清理快取或設定合理的過期時間。

**3. 監控記憶體使用：**
```java
@RestController
public class MetricsController {
    
    @GetMapping("/memory")
    public MemoryInfo getMemoryInfo() {
        Runtime runtime = Runtime.getRuntime();
        return new MemoryInfo(
            runtime.totalMemory(),
            runtime.freeMemory(),
            runtime.maxMemory()
        );
    }
}
```

## 安全相關問題

### Q: 如何在請求中新增認證令牌？
**A:** 使用 RequestInterceptor：

```java
@Configuration
public class FeignConfig {
    
    @Bean
    public RequestInterceptor authRequestInterceptor() {
        return requestTemplate -> {
            String token = getToken(); // 取得令牌的邏輯
            requestTemplate.header("Authorization", "Bearer " + token);
        };
    }
    
    private String getToken() {
        // 實作取得令牌的邏輯
        return "your-token";
    }
}
```

### Q: 如何處理 OAuth2 認證？
**A:** 啟用 OAuth2 支援：

```yaml
spring:
  cloud:
    openfeign:
      oauth2:
        enabled: true
        client-registration-id: my-client
        
  security:
    oauth2:
      client:
        registration:
          my-client:
            client-id: ${OAUTH2_CLIENT_ID}
            client-secret: ${OAUTH2_CLIENT_SECRET}
            authorization-grant-type: client_credentials
            scope: read,write
```

### Q: 如何過濾敏感資訊？
**A:** 自訂日誌過濾器：

```java
public class SecureLogger extends Logger {
    
    @Override
    protected void logRequest(String configKey, Level logLevel, Request request) {
        Map<String, Collection<String>> safeHeaders = new HashMap<>(request.headers());
        safeHeaders.remove("Authorization");
        safeHeaders.remove("X-API-Key");
        
        super.logRequest(configKey, logLevel, 
            Request.create(request.httpMethod(), request.url(), safeHeaders,
                request.body(), request.charset(), request.requestTemplate()));
    }
}
```

## 測試相關問題

### Q: 如何測試 Feign 客戶端？
**A:** 推薦使用 WireMock：

```java
@SpringBootTest
@AutoConfigureWireMock(port = 0)
class FeignClientTest {
    
    @Autowired
    private UserService userService;
    
    @Test
    void shouldGetUserSuccessfully() {
        // Given
        stubFor(get(urlEqualTo("/users/1"))
            .willReturn(aResponse()
                .withStatus(200)
                .withHeader("Content-Type", "application/json")
                .withBody("{\"id\":1,\"name\":\"John\"}")));
        
        // When
        User user = userService.getUser(1L);
        
        // Then
        assertThat(user.getName()).isEqualTo("John");
    }
}
```

### Q: 如何模擬錯誤場景？
**A:** 使用 WireMock 模擬各種錯誤：

```java
@Test
void shouldHandleServerError() {
    // Given
    stubFor(get(urlEqualTo("/users/999"))
        .willReturn(aResponse().withStatus(500)));
    
    // When & Then
    assertThatThrownBy(() -> userService.getUser(999L))
        .isInstanceOf(ServerException.class);
}
```

## 部署相關問題

### Q: 如何在生產環境設定？
**A:** 生產環境推薦設定：

```yaml
# application-prod.yml
spring:
  cloud:
    openfeign:
      client:
        config:
          default:
            connect-timeout: 1000
            read-timeout: 5000
            logger-level: basic
      compression:
        request:
          enabled: true
        response:
          enabled: true
      circuitbreaker:
        enabled: true
      cache:
        enabled: true
        
logging:
  level:
    com.example.clients: WARN
```

### Q: 如何監控 Feign 客戶端？
**A:** 整合 Micrometer：

```yaml
spring:
  cloud:
    openfeign:
      micrometer:
        enabled: true
```

然後透過 Actuator 端點檢視指標：
```bash
GET /actuator/metrics
GET /actuator/metrics/feign.client.requests
```

透過理解和應用這些解決方案，您將能夠更有效地開發、偵錯和維護基於 OpenFeign 的應用程式。