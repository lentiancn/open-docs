# Kubernetes 使用指南

Kubernetes 基本操作指南。

---

## 基础概念

| 概念 | 描述 |
|------|------|
| Pod | 最小部署单元 |
| Deployment | 管理 Pod |
| Service | 网络抽象 |
| Ingress | HTTP 路由 |
| ConfigMap | 配置 |
| Secret | 敏感配置 |

---

## 常用命令

```bash
# 查看集群信息
kubectl cluster-info

# 查看节点
kubectl get nodes

# 查看 Pod
kubectl get pods

# 查看所有命名空间
kubectl get pods -A

# 查看日志
kubectl logs <pod-name>

# 进入 Pod
kubectl exec -it <pod-name> -- sh

# 应用配置
kubectl apply -f deployment.yaml

# 删除资源
kubectl delete -f deployment.yaml
```

---

## 部署应用

### 创建 Deployment

```bash
kubectl create deployment nginx --image=nginx
```

### 暴露服务

```bash
kubectl expose deployment nginx --port=80 --type=LoadBalancer
```

### 扩缩容

```bash
kubectl scale deployment nginx --replicas=3
```

---

## 快速参考

| 命令 | 说明 |
|------|------|
| kubectl get | 列出资源 |
| kubectl describe | 显示详情 |
| kubectl apply | 创建/更新 |
| kubectl delete | 删除 |
| kubectl logs | 查看日志 |
| kubectl exec | 执行命令 |

---

## 下一步

- 探索 [Helm](https://helm.sh/)
- 学习 [Kubernetes 网络](https://kubernetes.io/docs/concepts/cluster-administration/networking/)
