# OpenFeign 사용자 매뉴얼

## 로드 밸런싱

### Spring Cloud LoadBalancer 통합

OpenFeign은 기본적으로 Spring Cloud LoadBalancer와 통합되어 클라이언트 측 로드 밸런싱 기능을 제공합니다.

```java
@FeignClient(name = "user-service")
public interface UserService {
    @GetMapping("/users/{id}")
    User getUser(@PathVariable("id") Long id);
}
```

`application.yml`에서 서비스 인스턴스 구성:

```yaml
spring:
  cloud:
    discovery:
      client:
        simple:
          instances:
            user-service:
              - uri: http://localhost:8081
              - uri: http://localhost:8082
              - uri: http://localhost:8083
```

### 사용자 정의 로드 밸런싱 전략

```java
@Configuration
public class LoadBalancerConfig {
    
    @Bean
    public ServiceInstanceListSupplier serviceInstanceListSupplier(
            ConfigurableApplicationContext context) {
        return ServiceInstanceListSupplier.builder()
                .withDiscoveryClient()
                .withCaching()
                .build(context);
    }
}
```

### 로드 밸런싱 HTTP 요청 변환

```java
@Bean
public LoadBalancerFeignRequestTransformer transformer() {
    return new LoadBalancerFeignRequestTransformer() {
        @Override
        public Request transformRequest(Request request, ServiceInstance instance) {
            Map<String, Collection<String>> headers = new HashMap<>(request.headers());
            headers.put("X-ServiceId", Collections.singletonList(instance.getServiceId()));
            headers.put("X-InstanceId", Collections.singletonList(instance.getInstanceId()));
            return Request.create(request.httpMethod(), request.url(), headers, 
                request.body(), request.charset(), request.requestTemplate());
        }
    };
}
```

## 서킷 브레이커 지원

### Spring Cloud CircuitBreaker 통합

서킷 브레이커 지원 활성화:

```yaml
spring:
  cloud:
    openfeign:
      circuitbreaker:
        enabled: true
```

### 서킷 브레이커 폴백

```java
@FeignClient(name = "user-service", fallback = UserServiceFallback.class)
public interface UserService {
    @GetMapping("/users/{id}")
    User getUser(@PathVariable("id") Long id);
}

@Component
public class UserServiceFallback implements UserService {
    @Override
    public User getUser(Long id) {
        // 기본 사용자 반환 또는 특정 예외 발생
        return new User(id, "Default User");
    }
}
```

### 예외 정보가 있는 폴백 팩토리

```java
@FeignClient(name = "user-service", fallbackFactory = UserServiceFallbackFactory.class)
public interface UserService {
    @GetMapping("/users/{id}")
    User getUser(@PathVariable("id") Long id);
}

@Component
public class UserServiceFallbackFactory implements FallbackFactory<UserService> {
    @Override
    public UserService create(Throwable cause) {
        return new UserService() {
            @Override
            public User getUser(Long id) {
                // 폴백을 트리거한 예외에 접근 가능
                log.error("UserService fallback triggered", cause);
                return new User(id, "Fallback User");
            }
        };
    }
}
```

### 사용자 정의 서킷 브레이커 이름

```java
@Configuration
public class CircuitBreakerConfig {
    @Bean
    public CircuitBreakerNameResolver circuitBreakerNameResolver() {
        return (String feignClientName, Target<?> target, Method method) -> 
            feignClientName + "_" + method.getName();
    }
}
```

## 로깅

### 로그 레벨 구성

```java
@Configuration
public class FeignLoggingConfig {
    
    @Bean
    Logger.Level feignLoggerLevel() {
        return Logger.Level.FULL;
    }
}
```

또는 구성 파일을 통해:

```yaml
logging:
  level:
    com.example.clients.UserService: DEBUG

spring:
  cloud:
    openfeign:
      client:
        config:
          user-service:
            logger-level: full
```

### 사용자 정의 로그 필터링

```java
public class CustomLogger extends Logger {
    
    @Override
    protected void logRequest(String configKey, Level logLevel, Request request) {
        // 민감한 정보 필터링
        if (!shouldLogRequestHeader(request)) {
            // 사용자 정의 로깅 로직
        }
    }
    
    @Override
    protected Response logAndRebufferResponse(String configKey, Level logLevel, 
            Response response, long elapsedTime) throws IOException {
        // 민감한 응답 정보 필터링
        if (!shouldLogResponseHeader(response)) {
            // 사용자 정의 로깅 로직
        }
        return super.logAndRebufferResponse(configKey, logLevel, response, elapsedTime);
    }
    
    private boolean shouldLogRequestHeader(Request request) {
        // Authorization 헤더 및 기타 민감한 정보 필터링
        return !request.headers().containsKey("Authorization");
    }
    
    private boolean shouldLogResponseHeader(Response response) {
        return true; // 필요에 따라 필터링
    }
}
```

## 요청/응답 압축

### 압축 활성화

```yaml
spring:
  cloud:
    openfeign:
      compression:
        request:
          enabled: true
          mime-types: text/xml,application/xml,application/json
          min-request-size: 2048
        response:
          enabled: true
```

### 주요 참고 사항

- OkHttpClient를 사용할 때 `content-encoding` 또는 `accept-encoding` 헤더가 있으면 압축이 비활성화됨
- 압축은 지정된 MIME 유형에만 적용됨
- 요청 크기가 `min-request-size`를 초과해야 압축됨

## OAuth2 지원

### OAuth2 활성화

```yaml
spring:
  cloud:
    openfeign:
      oauth2:
        enabled: true
        client-registration-id: my-client
```

### 의존성 구성

```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-oauth2-client</artifactId>
</dependency>
```

### OAuth2 클라이언트 구성

```yaml
spring:
  security:
    oauth2:
      client:
        registration:
          my-client:
            client-id: your-client-id
            client-secret: your-client-secret
            authorization-grant-type: client_credentials
            scope: read,write
        provider:
          my-provider:
            token-uri: https://auth-server.com/oauth/token
```

## 캐싱 지원

### 캐싱 활성화

```java
@SpringBootApplication
@EnableCaching
@EnableFeignClients
public class Application {
    // ...
}
```

### Feign 클라이언트에서 캐싱 사용

```java
@FeignClient(name = "data-service")
public interface DataService {
    
    @GetMapping("/data/{key}")
    @Cacheable(cacheNames = "data-cache", key = "#key")
    Data getData(@PathVariable String key);
    
    @PostMapping("/data")
    @CachePut(cacheNames = "data-cache", key = "#data.key")
    Data updateData(@RequestBody Data data);
    
    @DeleteMapping("/data/{key}")
    @CacheEvict(cacheNames = "data-cache", key = "#key")
    void deleteData(@PathVariable String key);
}
```

### 캐싱 비활성화

```yaml
spring:
  cloud:
    openfeign:
      cache:
        enabled: false
```

## 비동기 지원

### CompletableFuture 지원

```java
@FeignClient(name = "async-service")
public interface AsyncService {
    
    @GetMapping("/data/{id}")
    CompletableFuture<Data> getDataAsync(@PathVariable String id);
}
```

### 비동기 클라이언트 구성

```java
@Configuration
public class AsyncFeignConfig {
    
    @Bean
    public AsyncClient asyncClient() {
        return new AsyncClient.Default();
    }
}
```

### 사용 예제

```java
@Service
public class AsyncProcessingService {
    
    private final AsyncService asyncService;
    
    public AsyncProcessingService(AsyncService asyncService) {
        this.asyncService = asyncService;
    }
    
    public void processData() {
        CompletableFuture<Data> future1 = asyncService.getDataAsync("1");
        CompletableFuture<Data> future2 = asyncService.getDataAsync("2");
        
        // 병렬 처리
        CompletableFuture.allOf(future1, future2).thenRun(() -> {
            try {
                Data data1 = future1.get();
                Data data2 = future2.get();
                // 데이터 처리
            } catch (Exception e) {
                log.error("Error processing async data", e);
            }
        });
    }
}
```

## 상속 지원

### 기본 인터페이스 정의

```java
public interface BaseService<T, ID> {
    @GetMapping("/{id}")
    T findById(@PathVariable ID id);
    
    @GetMapping
    List<T> findAll();
    
    @PostMapping
    T save(@RequestBody T entity);
    
    @DeleteMapping("/{id}")
    void deleteById(@PathVariable ID id);
}
```

### 구체적인 서비스 구현

```java
@FeignClient(name = "user-service")
public interface UserService extends BaseService<User, Long> {
    @GetMapping("/users/by-email/{email}")
    User findByEmail(@PathVariable String email);
}

@FeignClient(name = "order-service")
public interface OrderService extends BaseService<Order, Long> {
    @GetMapping("/orders/by-user/{userId}")
    List<Order> findByUserId(@PathVariable Long userId);
}
```

## 쿼리 파라미터 매핑

### @SpringQueryMap 사용

```java
public class SearchCriteria {
    private String name;
    private String status;
    private Integer page;
    private Integer size;
    
    // getters and setters
}

@FeignClient(name = "search-service")
public interface SearchService {
    @GetMapping("/search")
    SearchResult search(@SpringQueryMap SearchCriteria criteria);
}
```

### 사용자 정의 QueryMapEncoder

```java
@Configuration
public class FeignConfig {
    
    @Bean
    public QueryMapEncoder queryMapEncoder() {
        return new BeanQueryMapEncoder(); // Java Bean 속성 사용
        // 또는 return new CustomQueryMapEncoder(); // 사용자 정의 구현
    }
}
```

## HATEOAS 지원

### HATEOAS 활성화

의존성 추가:

```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-hateoas</artifactId>
</dependency>
```

### HATEOAS 모델 사용

```java
@FeignClient(name = "hateoas-service")
public interface HateoasService {
    
    @GetMapping("/resources")
    CollectionModel<Resource> getResources();
    
    @GetMapping("/resources/{id}")
    EntityModel<Resource> getResource(@PathVariable String id);
}
```

## 새로 고침 범위 지원

### 새로 고침 지원 활성화

```yaml
spring:
  cloud:
    openfeign:
      client:
        refresh-enabled: true
```

### 구성 새로 고침

Actuator 엔드포인트를 통해 새로 고침:

```bash
POST /actuator/refresh
```

### 주요 참고 사항

- `@FeignClient` 인터페이스에 `@RefreshScope` 애노테이션을 사용하지 마세요
- 새로 고침 기능은 기본적으로 비활성화되어 있으며 명시적으로 활성화해야 합니다
- 연결 시간 초과, 읽기 시간 초과 및 URL 구성 새로 고침을 지원합니다

## AOT 및 네이티브 이미지 지원

### 구성 요구 사항

```yaml
spring:
  cloud:
    refresh:
      enabled: false
    openfeign:
      client:
        refresh-enabled: false
      lazy-attributes-resolution: false
```

### 네이티브 이미지 빌드

빌드 시 올바른 구성을 설정하고 실행 시 시스템 속성을 통해 URL을 재정의할 수 있습니다:

```bash
-Dspring.cloud.openfeign.client.config.[clientId].url=[url]
```

이러한 고급 기능들은 OpenFeign을 단순한 HTTP 클라이언트가 아니라 다양한 엔터프라이즈 애플리케이션 요구사항을 충족할 수 있는 포괄적인 마이크로서비스 통신 솔루션으로 만듭니다.