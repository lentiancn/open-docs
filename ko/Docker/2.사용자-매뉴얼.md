# Docker 사용자 매뉴얼

## 기본 명령어

```bash
docker run -it ubuntu /bin/bash
docker run -d nginx
docker run -p 8080:80 nginx
docker ps
docker stop <id>
```

## Dockerfile

```dockerfile
FROM node:18-alpine
WORKDIR /app
COPY . .
RUN npm install
```

---

*최종 업데이트: 2024*
