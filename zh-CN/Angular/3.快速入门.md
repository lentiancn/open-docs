# Angular 快速入门

> 🎯 本指南将帮助你在 5 分钟内创建并运行第一个 Angular 应用
> 
> 📅 更新时间：2026年2月
> 
> 📖 适用版本：Angular 17+

---

## 目录

1. [前置准备](#1-前置准备)
2. [创建项目](#2-创建项目)
3. [运行应用](#3-运行应用)
4. [理解项目结构](#4-理解项目结构)
5. [修改第一个组件](#5-修改第一个组件)
6. [添加交互功能](#6-添加交互功能)
7. [下一步](#7-下一步)

---

## 1. 前置准备

在开始之前，请确保已安装：

### 1.1 Node.js

```bash
node --version
# 期望输出: v18.x.x 或更高版本
```

### 1.2 npm

```bash
npm --version
# 期望输出: 8.x.x 或更高版本
```

### 1.3 Angular CLI

```bash
ng version
# 期望输出: Angular CLI 版本信息
```

如果未安装，请先阅读 [安装指南](./1.安装指南.md)

---

## 2. 创建项目

打开终端，执行以下命令创建新项目：

```bash
ng new hello-angular
```

### 2.1 安装选项选择

安装过程中会询问以下问题：

| 选项 | 推荐选择 | 说明 |
|------|----------|------|
| Would you like to add Angular routing? | Yes | 添加路由功能 |
| Which stylesheet format would you like to use? | CSS 或 SCSS | 推荐 SCSS |

等待安装完成（约 2-5 分钟）。

### 2.2 进入项目目录

```bash
cd hello-angular
```

---

## 3. 运行应用

### 3.1 启动开发服务器

```bash
ng serve
```

### 3.2 在浏览器中打开

打开浏览器，访问：http://localhost:4200

你应该能看到类似以下页面：

![Angular 默认页面](https://angular.io/generated/images/guide/setup-local/app-works.png)

---

## 4. 理解项目结构

### 4.1 目录结构

```
hello-angular/
├── src/
│   ├── app/
│   │   ├── app.component.ts       # 根组件
│   │   ├── app.component.html    # 根组件模板
│   │   ├── app.component.css    # 根组件样式
│   │   ├── app.config.ts        # 应用配置
│   │   └── app.routes.ts        # 路由配置
│   ├── assets/                   # 静态资源
│   ├── main.ts                  # 应用入口
│   └── index.html               # HTML 入口
├── angular.json                  # CLI 配置
├── package.json                  # 依赖配置
└── tsconfig.json                 # TS 配置
```

### 4.2 根组件文件

打开 `src/app/app.component.ts`：

```typescript
import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';
import { RouterOutlet, RouterLink } from '@angular/router';

@Component({
  selector: 'app-root',
  standalone: true,
  imports: [CommonModule, RouterOutlet, RouterLink],
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css']
})
export class AppComponent {
  title = 'hello-angular';
}
```

---

## 5. 修改第一个组件

### 5.1 修改模板

打开 `src/app/app.component.html`，替换为：

```html
<div class="container">
  <h1>欢迎来到 Angular！</h1>
  <p>🎉 你的第一个 Angular 应用正在运行。</p>
  
  <div class="card">
    <h2>{{ title }}</h2>
    <p>这是通过插值绑定显示的变量。</p>
  </div>
</div>
```

### 5.2 修改样式

打开 `src/app/app.component.css`，添加：

```css
.container {
  max-width: 800px;
  margin: 0 auto;
  padding: 2rem;
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
}

h1 {
  color: #dd0031;
}

.card {
  background: #f5f5f5;
  padding: 1.5rem;
  border-radius: 8px;
  margin-top: 1rem;
}
```

### 5.3 查看效果

保存文件后，浏览器会自动刷新，你应该能看到更新后的页面。

---

## 6. 添加交互功能

### 6.1 创建计数器功能

在 `app.component.ts` 中添加：

```typescript
import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
  selector: 'app-root',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css']
})
export class AppComponent {
  title = 'hello-angular';
  count = 0;
  
  increment(): void {
    this.count++;
  }
  
  decrement(): void {
    this.count--;
  }
}
```

### 6.2 在模板中使用

更新 `app.component.html`：

```html
<div class="container">
  <h1>欢迎来到 Angular！</h1>
  
  <div class="card">
    <h2>计数器示例</h2>
    <p class="count">{{ count }}</p>
    <div class="buttons">
      <button (click)="decrement()">-</button>
      <button (click)="increment()">+</button>
    </div>
  </div>
</div>
```

### 6.3 添加按钮样式

更新 `app.component.css`：

```css
.count {
  font-size: 3rem;
  font-weight: bold;
  text-align: center;
  margin: 1rem 0;
}

.buttons {
  display: flex;
  gap: 1rem;
  justify-content: center;
}

button {
  padding: 0.5rem 1.5rem;
  font-size: 1.2rem;
  border: none;
  border-radius: 4px;
  cursor: pointer;
  background: #dd0031;
  color: white;
  transition: background 0.2s;
}

button:hover {
  background: #b3002d;
}
```

### 6.4 查看效果

保存后，浏览器自动刷新，你可以点击按钮测试交互功能。

---

## 7. 下一步

### 7.1 学习更多

- 📖 阅读 [使用手册](./2.使用手册.md) 深入学习
- 🛠️ 查看 [安装指南](./1.安装指南.md) 了解环境配置

### 7.2 创建新组件

```bash
ng generate component my-component
```

### 7.3 创建新服务

```bash
ng generate service services/my-service
```

### 7.4 添加路由

```bash
ng generate component pages/home
ng generate component pages/about
```

然后在 `app.routes.ts` 中配置路由。

### 7.5 构建生产版本

```bash
ng build --configuration production
```

构建产物在 `dist/hello-angular` 目录下。

---

## 🎉 恭喜！

你已经完成了 Angular 快速入门。Angular 还有更多强大的功能等待你去探索：

- 📦 组件化开发
- 🌐 路由系统
- 📝 表单处理
- 🔌 HTTP 通信
- 📊 状态管理
- 🧪 测试工具

祝你学习愉快！

---

*本文档由 AI 助手生成*
