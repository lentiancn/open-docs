# Element UI 使用ガイド

Vue 2アプリケーションでElement UIコンポーネントを使用する方法を説明します。

## 基本的な使用

### Hello World

```vue
<template>
  <div>
    <el-button>デフォルトボタン</el-button>
    <el-button type="primaryプライマリボタン</el-button>
  </div>
</template>

<script>
export default {
  name: 'App'
}
</script>
```

## 一般的なコンポーネント

### ボタン

```vue
<template>
  <div>
    <!-- タイプ -->
    <el-button>デフォルト</el-button>
    <el-button type="primary">プライマリ</el-button>
    <el-button type="success">成功</el-button>
    <el-button type="warning">警告</el-button>
    <el-button type="danger">危険</el-button>
    
    <!-- プレーン -->
    <el-button plain>プレーン</el-button>
    
    <!-- ラウンド -->
    <el-button round>ラウンド</el-button>
    
    <!-- アイコン -->
    <el-button circle icon="el-icon-search"></el-button>
    
    <!-- 無効 -->
    <el-button disabled>無効</el-button>
    
    <!-- ローディング -->
    <el-button loading>ローディング</el-button>
  </div>
</template>
```

### 入力

```vue
<template>
  <div>
    <!-- -basic -->
    <el-input v-model="input" placeholder="テキストを入力"></el-input>
    
    <!-- アイコン付き -->
    <el-input
      v-model="input1"
      placeholder="検索"
      prefix-icon="el-icon-search">
    </el-input>
    
    <!-- パスワード -->
    <el-input
      v-model="input2"
      placeholder="パスワード"
      show-password>
    </el-input>
    
    <!-- テキストエリア -->
    <el-input
      v-model="textarea"
      type="textarea"
      :rows="3"
      placeholder="説明を入力">
    </el-input>
  </div>
</template>

<script>
export default {
  data() {
    return {
      input: '',
      input1: '',
      input2: '',
      textarea: ''
    }
  }
}
</script>
```

### テーブル

```vue
<template>
  <el-table :data="tableData" stripe border>
    <el-table-column prop="date" label="日付" width="180"></el-table-column>
    <el-table-column prop="name" label="名前" width="180"></el-table-column>
    <el-table-column prop="address" label="住所"></el-table-column>
    <el-table-column label="操作">
      <template slot-scope="scope">
        <el-button size="mini" @click="handleEdit(scope.$index, scope.row)">編集</el-button>
        <el-button size="mini" type="danger" @click="handleDelete(scope.$index, scope.row)">削除</el-button>
      </template>
    </el-table-column>
  </el-table>
</template>

<script>
export default {
  data() {
    return {
      tableData: [{
        date: '2024-01-01',
        name: '田中',
        address: '東京都渋谷区'
      }]
    }
  },
  methods: {
    handleEdit(index, row) {
      console.log(index, row);
    },
    handleDelete(index, row) {
      console.log(index, row);
    }
  }
}
</script>
```

### フォーム

```vue
<template>
  <el-form
    :model="ruleForm"
    :rules="rules"
    ref="ruleForm"
    label-width="120px"
  >
    <el-form-item label="名前" prop="name">
      <el-input v-model="ruleForm.name"></el-input>
    </el-form-item>
    
    <el-form-item label="メール" prop="email">
      <el-input v-model="ruleForm.email"></el-input>
    </el-form-item>
    
    <el-form-item>
      <el-button type="primary" @click="submitForm('ruleForm')">送信</el-button>
      <el-button @click="resetForm('ruleForm')">リセット</el-button>
    </el-form-item>
  </el-form>
</template>

<script>
export default {
  data() {
    return {
      ruleForm: {
        name: '',
        email: ''
      },
      rules: {
        name: [
          { required: true, message: '名前を入力してください', trigger: 'blur' }
        ],
        email: [
          { required: true, message:メールを入力してください', trigger: 'blur' },
          { type: 'email', message: 'メール形式が無効です', trigger: 'blur' }
        ]
      }
    };
  },
  methods: {
    submitForm(formName) {
      this.$refs[formName].validate((valid) => {
        if (valid) {
          alert('送信成功!');
        }
      });
    },
    resetForm(formName) {
      this.$refs[formName].resetFields();
    }
  }
}
</script>
```

### ダイアログ

```vue
<template>
  <div>
    <el-button type="text" @click="dialogVisible = true">ダイアログを開く</el-button>
    
    <el-dialog
      title="ダイアログタイトル"
      :visible.sync="dialogVisible"
      width="30%">
      <span>ダイアログコンテンツ</span>
      <span slot="footer" class="dialog-footer">
        <el-button @click="dialogVisible = false">キャンセル</el-button>
        <el-button type="primary" @click="dialogVisible = false">確定</el-button>
      </span>
    </el-dialog>
  </div>
</template>

<script>
export default {
  data() {
    return {
      dialogVisible: false
    }
  }
}
</script>
```

### メッセージ

```javascript
// 成功
this.$message.success('成功メッセージ')

// エラー
this.$message.error('エラーメッセージ')

// 警告
this.$message.warning('警告メッセージ')

// 情報
this.$message.info('情報メッセージ')
```

## スロット

### デフォルトスロット

```vue
<el-button>ボタンテキスト</el-button>
<el-card>カードコンテンツ</el-card>
```

### 名前付きスロット

```vue
<el-dialog>
  <span slot="title">カスタムタイトル</span>
  <span>ダイアログコンテンツ</span>
  <span slot="footer">
    <el-button>キャンセル</el-button>
    <el-button type="primary">確定</el-button>
  </span>
</el-dialog>
```

## v-model

```vue
<template>
  <el-input v-model="value"></el-input>
  <el-checkbox v-model="checked">ラベル</el-checkbox>
  <el-switch v-model="switchValue"></el-switch>
</template>

<script>
export default {
  data() {
    return {
      value: '',
      checked: true,
      switchValue: true
    }
  }
}
</script>
```

## 関連リンク

- [Element UI公式ドキュメント](https://element.eleme.io/)
- [コンポーネントリスト](https://element.eleme.io/#/component/button)
- [Vue 2ドキュメント](https://v2.vuejs.org/)
