# Angular ユーザーマニュアル

> 🎯 このドキュメントでは、Angularのコアコンセプト、基本的な使用方法、開発ガイドについて詳しく説明します
> 
> 📅 最終更新日：2026年2月
> 
> 📖 対応バージョン：Angular 17+

---

## 目次

1. [Angularの概要](#1-angularの概要)
2. [プロジェクト構造](#2-プロジェクト構造)
3. [コアコンセプト](#3-コアコンセプト)
4. [コンポーネント開発](#4-コンポーネント開発)
5. [サービスと依存性注入](#5-サービスと依存性注入)
6. [ルーティング](#6-ルーティング)
7. [フォーム処理](#7-フォーム処理)
8. [HTTP通信](#8-http通信)
9. [状態管理](#9-状態管理)
10. [スタイルとアニメーション](#10-スタイルとアニメーション)
11. [テスト](#11-テスト)
12. [ビルドとデプロイ](#12-ビルドとデプロイ)
13. [ベストプラクティス](#13-ベストプラクティス)

---

## 1. Angularの概要

### 1.1 Angularとは？

Angularは、GoogleがメンテナンスするオープンソースのWebアプリケーションフレームワークです。TypeScriptで構築されており、以下の特徴があります：

- **コンポーネントベース**：コンポーネントベースの架构
- **雙方向データバインディング**：テンプレートとデータを自動同期
- **依存性注入**：強力な依存性管理システム
- **ルーティング**：完全なクライアントサイドルーティングソリューション
- **フォーム**：強力なフォーム検証機能
- **HTTP**：シンプルなHTTPクライアント
- **テスト**：完全なテストツールチェーン

### 1.2 Angularバージョン履歴

| バージョン | 発売日 | 主な機能 |
|-----------|--------|----------|
| Angular 17 | 2023年11月 | スタンドアロンコンポーネント、Signal、if/forブロック構文 |
| Angular 16 | 2023年5月 | Signal、実験的SSR |
| Angular 15 | 2022年11月 | スタンドアロンコンポーネントAPI安定版 |
| Angular 14 | 2022年6月 | -strongly-typedフォーム |
| Angular 13 | 2021年11月 | View Engine削除 |
| Angular 2 | 2016年9月 | 新アーキテクチャ |

> 💡 **ヒント**：このマニュアルはAngular 17+に基づいて書かれており、**スタンドアロンコンポーネント**（Standalone Components）を推奨の написаとしてを使用しています。

---

## 2. プロジェクト構造

### 2.1 典型的なプロジェクト構造

```
my-app/
├── src/
│   ├── app/
│   │   ├── app.component.ts       # ルートコンポーネント
│   │   ├── app.component.html    # ルートコンポーネントテンプレート
│   │   ├── app.component.css     # ルートコンポーネントスタイル
│   │   ├── app.config.ts         # アプリケーション設定（Angular 17+）
│   │   ├── app.routes.ts         # ルーティング設定
│   │   ├── components/          # 共有コンポーネント
│   │   ├── services/              # サービス
│   │   ├── models/               # データモデル
│   │   └── pages/                 # ページコンポーネント
│   ├── assets/                   # 静的アセット
│   ├── environments/             # 環境設定
│   ├── styles.css                # グローバルスタイル
│   ├── index.html                # HTMLエントリ
│   └── main.ts                   # アプリケーションエントリ
├── angular.json                   # Angular CLI設定
├── package.json                   # プロジェクト依存関係
├── tsconfig.json                  # TypeScript設定
└── node_modules/                  # 依存関係ディレクトリ
```

### 2.2 主なファイルの説明

| ファイル | 用途 |
|---------|------|
| `angular.json` | Angular CLI設定ファイル |
| `package.json` | プロジェクト依存関係とスクリプト設定 |
| `tsconfig.json` | TypeScriptコンパイラ設定 |
| `src/main.ts` | アプリケーション起動エントリ |
| `src/index.html` | HTMLエントリファイル |

---

## 3. コアコンセプト

### 3.1 モジュール（Module）

モジュールは、Angularアプリケーションの基本的な構成単位で、コードの整理に使用されます。

#### モジュールの作成

```bash
ng generate module my-module
```

#### モジュールの基本構造

```typescript
import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';
import { MyComponentComponent } from './my-component/my-component.component';

@NgModule({
  declarations: [
    MyComponentComponent
  ],
  imports: [
    CommonModule  // *ngIf、*ngForなどのディレクティブを提供
  ],
  exports: [
    MyComponentComponent
  ]
})
export class MyModuleModule { }
```

> ⚠️ **注意**：Angular 17+では、**スタンドアロンコンポーネント**の使用が推奨され、NgModuleは不要です。

---

### 3.2 コンポーネント（Component）

コンポーネントは、AngularアプリケーションのコアUIユニットです。

#### コンポーネントの作成

```bash
# コンポーネントの作成
ng generate component my-component

# スタンドアロンコンポーネントの作成（Angular 17+推奨）
ng generate component my-component --standalone
```

#### コンポーネントの基本構造

```typescript
import { Component } from '@angular/core';

@Component({
  selector: 'app-my-component',
  standalone: true,  // スタンドアロンコンポーネント
  imports: [],
  templateUrl: './my-component.component.html',
  styleUrls: ['./my-component.component.css']
})
export class MyComponentComponent {
  // コンポーネントロジック
  title: string = 'Hello Angular';
  
  // メソッド
  greet(): void {
    console.log('Hello!');
  }
}
```

#### インラインテンプレートとスタイル

```typescript
@Component({
  selector: 'app-my-component',
  standalone: true,
  template: `
    <h1>{{ title }}</h1>
    <button (click)="greet()">クリック</button>
  `,
  styles: [`
    h1 { color: blue; }
  `]
})
export class MyComponentComponent {
  title: string = 'Hello Angular';
}
```

---

### 3.3 テンプレート構文

#### 補間バインディング

```html
<!-- 変数バインディング -->
<p>Hello, {{ name }}!</p>

<!-- 式の評価 -->
<p>{{ 1 + 1 }}</p>

<!-- メソッドの呼び出し -->
<p>{{ getName() }}</p>
```

#### プロパティバインディング

```html
<!-- HTML属性 -->
<img [src]="imageUrl">

<!-- クラスバインディング -->
<div [class.active]="isActive">コンテンツ</div>

<!-- スタイルバインディング -->
<div [style.color]="textColor">コンテンツ</div>

<!-- ボタンの無効化 -->
<button [disabled]="isDisabled">ボタン</button>
```

#### イベントバインディング

```html
<!-- クリックイベント -->
<button (click)="onClick()">クリック</button>

<!-- 入力イベント -->
<input (input)="onInput($event)">

<!-- キーボードイベント -->
<input (keyup.enter)="onEnter()">
```

#### 雙方向データバインディング

```typescript
import { FormsModule } from '@angular/forms';

// コンポーネント内
@Component({
  imports: [FormsModule],
  ...
})
export class MyComponent {
  name: string = '';
}
```

```html
<input [(ngModel)]="name">
<p>入力内容: {{ name }}</p>
```

---

### 3.4 ディレクティブ（Directive）

#### 組み込みディレクティブ

```html
<!-- 条件付きレンダリング -->
<div *ngIf="isVisible">コンテンツ</div>

<!-- 分岐 -->
<div *ngIf="isLoggedIn; else loginTemplate">
  お帰りなさい！
</div>
<ng-template #loginTemplate>
  ログインしてください
</ng-template>

<!-- ループレンダリング -->
<li *ngFor="let item of items; let i = index">
  {{ i + 1 }}. {{ item.name }}
</li>

<!-- クラス切り替え -->
<div [ngClass]="{'active': isActive, 'disabled': isDisabled}">

<!-- スタイル切り替え -->
<div [ngStyle]="{'color': textColor, 'font-size': fontSize + 'px'}">
```

#### Angular 17+ 控制フロー構文

```html
<!-- @if は *ngIf の代わり -->
@if (isVisible) {
  <p>コンテンツ</p>
} @else {
  <p>その他のコンテンツ</p>
}

<!-- @for は *ngFor の代わり -->
@for (item of items; track item.id) {
  <li>{{ item.name }}</li>
} @empty {
  <li>データなし</li>
}

<!-- @switch -->
@switch (type) {
  @case ('A') { <p>タイプA</p> }
  @case ('B') { <p>タイプB</p> }
  @default { <p>その他のタイプ</p> }
}
```

---

## 4. コンポーネント開発

### 4.1 コンポーネント通信

#### 親から子への値渡し（入力プロパティ）

```typescript
// 子コンポーネント
import { Component, Input } from '@angular/core';

@Component({
  selector: 'app-child',
  standalone: true,
  template: `<p>{{ message }}</p>`
})
export class ChildComponent {
  @Input() message: string = '';
  @Input() count: number = 0;
}
```

```html
<!-- 親コンポーネントテンプレート -->
<app-child message="こんにちは" [count]="5"></app-child>
```

#### 子から親への値渡し（出力プロパティ）

```typescript
// 子コンポーネント
import { Component, Output, EventEmitter } from '@angular/core';

@Component({
  selector: 'app-child',
  standalone: true,
  template: `<button (click)="sendEvent()">送信</button>`
})
export class ChildComponent {
  @Output() messageEvent = new EventEmitter<string>();
  
  sendEvent(): void {
    this.messageEvent.emit('子コンポーネントからのメッセージ');
  }
}
```

```typescript
// 親コンポーネント
@Component({
  standalone: true,
  imports: [ChildComponent],
  template: `<app-child (messageEvent)="onMessage($event)"></app-child>`
})
export class ParentComponent {
  onMessage(message: string): void {
    console.log(message);
  }
}
```

#### ViewChildを使用した子コンポーネントへのアクセス

```typescript
import { Component, ViewChild, AfterViewInit } from '@angular/core';
import { ChildComponent } from './child.component';

@Component({
  standalone: true,
  imports: [ChildComponent],
  template: `<app-child #childRef></app-child>`
})
export class ParentComponent implements AfterViewInit {
  @ViewChild('childRef') childComponent!: ChildComponent;
  
  ngAfterViewInit(): void {
    // 子コンポーネントのプロパティとメソッドにアクセス
    this.childComponent.someMethod();
  }
}
```

---

### 4.2 ライフサイクルフック

| フック | トリガータイミング |
|--------|-----------------|
| `ngOnInit` | コンポーネント初期化後 |
| `ngOnChanges` | 入力プロパティ変更時 |
| `ngDoCheck` | 変更検出時 |
| `ngAfterViewInit` | ビュー初期化後 |
| `ngAfterViewChecked` | ビューチェック後 |
| `ngOnDestroy` | コンポーネント破棄前 |

```typescript
import { Component, OnInit, OnDestroy } from '@angular/core';

@Component({
  selector: 'app-lifecycle',
  standalone: true,
  template: `<p>{{ message }}</p>`
})
export class LifecycleComponent implements OnInit, OnDestroy {
  message: string = '';
  
  ngOnInit(): void {
    console.log('コンポーネント初期化');
    this.message = 'Hello';
  }
  
  ngOnDestroy(): void {
    console.log('コンポーネント破棄');
    // クリーンアップ：サブスクリプション解除、タイマークリアなど
  }
}
```

---

## 5. サービスと依存性注入

### 5.1 サービスの作成

```bash
ng generate service services/data
```

### 5.2 サービスの基本構造

```typescript
import { Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';

@Injectable({
  providedIn: 'root'  // グローバルシングルトン
})
export class DataService {
  private apiUrl = 'https://api.example.com';
  
  constructor(private http: HttpClient) { }
  
  // GETリクエスト
  getData(): Observable<any> {
    return this.http.get(this.apiUrl);
  }
  
  // POSTリクエスト
  postData(data: any): Observable<any> {
    return this.http.post(this.apiUrl, data);
  }
}
```

### 5.3 コンポーネントでのサービスの使用

```typescript
import { Component, OnInit } from '@angular/core';
import { DataService } from './services/data.service';

@Component({
  selector: 'app-my',
  standalone: true,
  template: `<p>{{ data | json }}</p>`
})
export class MyComponent implements OnInit {
  data: any = null;
  
  constructor(private dataService: DataService) { }
  
  ngOnInit(): void {
    this.dataService.getData().subscribe({
      next: (result) => this.data = result,
      error: (err) => console.error(err)
    });
  }
}
```

### 5.4 依存性注入の詳細

#### コンポーネントレベル注入

```typescript
@Component({
  selector: 'app-my',
  standalone: true,
  providers: [MyService]  // コンポーネントレベルインスタンス
})
export class MyComponent {
  constructor(private myService: MyService) { }
}
```

---

## 6. ルーティング

### 6.1 ルート設定

```typescript
// app.routes.ts
import { Routes } from '@angular/router';
import { HomeComponent } from './pages/home/home.component';
import { AboutComponent } from './pages/about/about.component';
import { UserComponent } from './pages/user/user.component';

export const routes: Routes = [
  { path: '', redirectTo: '/home', pathMatch: 'full' },
  { path: 'home', component: HomeComponent },
  { path: 'about', component: AboutComponent },
  { 
    path: 'users', 
    children: [
      { path: '', component: UserComponent },
      { path: ':id', component: UserDetailComponent }
    ]
  },
  { path: '**', component: NotFoundComponent }  // 404
];
```

### 6.2 ルートアウトレット

```typescript
// app.component.ts
import { Component } from '@angular/core';
import { RouterOutlet, RouterLink, RouterLinkActive } from '@angular/router';

@Component({
  selector: 'app-root',
  standalone: true,
  imports: [RouterOutlet, RouterLink, RouterLinkActive],
  template: `
    <nav>
      <a routerLink="/home" routerLinkActive="active">ホーム</a>
      <a routerLink="/about" routerLinkActive="active">概要</a>
    </nav>
    <router-outlet></router-outlet>
  `,
  styles: [`
    .active { color: red; }
  `]
})
export class AppComponent { }
```

### 6.3 ルートパラメータ

```typescript
import { ActivatedRoute, ParamMap } from '@angular/router';
import { switchMap } from 'rxjs/operators';

@Component({
  standalone: true,
  template: `<p>ユーザーID: {{ userId }}</p>`
})
export class UserDetailComponent implements OnInit {
  userId: string = '';
  
  constructor(private route: ActivatedRoute) { }
  
  ngOnInit(): void {
    this.route.paramMap.subscribe(params => {
      this.userId = params.get('id') || '';
    });
  }
}
```

### 6.4 ルートガード

#### CanActivate - ルートガード

```typescript
import { CanActivateFn, Router } from '@angular/router';

export const authGuard: CanActivateFn = (route, state) => {
  const router = new Router();
  const isLoggedIn = /* あなたのログイン檢查ロジック */;
  
  if (isLoggedIn) {
    return true;
  } else {
    router.navigate(['/login']);
    return false;
  }
};
```

```typescript
// ルートで使用
export const routes: Routes = [
  { 
    path: 'admin', 
    component: AdminComponent,
    canActivate: [authGuard]
  }
];
```

---

## 7. フォーム処理

### 7.1 リアクティブフォーム

```typescript
import { Component } from '@angular/core';
import { FormBuilder, FormGroup, Validators, ReactiveFormsModule } from '@angular/forms';

@Component({
  selector: 'app-login',
  standalone: true,
  imports: [ReactiveFormsModule],
  template: `
    <form [formGroup]="loginForm" (ngSubmit)="onSubmit()">
      <div>
        <label>メールアドレス</label>
        <input type="email" formControlName="email">
        <span *ngIf="loginForm.get('email')?.invalid">
          有効なメールアドレスを入力してください
        </span>
      </div>
      <div>
        <label>パスワード</label>
        <input type="password" formControlName="password">
      </div>
      <button type="submit" [disabled]="loginForm.invalid">ログイン</button>
    </form>
  `
})
export class LoginComponent {
  loginForm: FormGroup;
  
  constructor(private fb: FormBuilder) {
    this.loginForm = this.fb.group({
      email: ['', [Validators.required, Validators.email]],
      password: ['', [Validators.required, Validators.minLength(6)]]
    });
  }
  
  onSubmit(): void {
    if (this.loginForm.valid) {
      console.log(this.loginForm.value);
    }
  }
}
```

### 7.2 フォームバリデーター

#### 組み込みバリデーター

```typescript
this.form = this.fb.group({
  name: ['', Validators.required],
  email: ['', [Validators.required, Validators.email]],
  age: ['', [Validators.required, Validators.min(18), Validators.max(100)]],
  url: ['', Validators.pattern(/^https?:\/\/.+/)]
});
```

#### カスタムバリデーター

```typescript
import { AbstractControl, ValidationErrors, ValidatorFn } from '@angular/forms';

export function forbiddenValidator(forbidden: RegExp): ValidatorFn {
  return (control: AbstractControl): ValidationErrors | null => {
    const forbiddenValue = forbidden.test(control.value);
    return forbiddenValue ? { forbidden: { value: control.value } } : null;
  };
}

// 使用
this.form = this.fb.group({
  username: ['', forbiddenValidator(/^admin/)]
});
```

---

## 8. HTTP通信

### 8.1 HttpClientの設定

```typescript
// app.config.ts (Angular 17+)
import { ApplicationConfig } from '@angular/core';
import { provideHttpClient } from '@angular/common/http';

export const appConfig: ApplicationConfig = {
  providers: [
    provideHttpClient()
  ]
};
```

### 8.2 GETリクエスト

```typescript
import { HttpClient } from '@angular/common/http';

@Component({ standalone: true })
export class MyComponent {
  constructor(private http: HttpClient) { }
  
  getData(): void {
    this.http.get('https://api.example.com/data').subscribe({
      next: (data) => console.log(data),
      error: (err) => console.error(err),
      complete: () => console.log('リクエスト完了')
    });
  }
}
```

### 8.3 POSTリクエスト

```typescript
this.http.post('https://api.example.com/data', { 
  name: '田中',
  age: 25 
}).subscribe(result => console.log(result));
```

### 8.4 RxJSオペレーターの使用

```typescript
import { catchError, of } from 'rxjs';

this.http.get('https://api.example.com/data').pipe(
  catchError(error => {
    console.error(error);
    return of({ error: true, message: 'リクエスト失敗' });
  })
).subscribe(data => console.log(data));
```

---

## 9. 状態管理

### 9.1 サービス + Signal（Angular 16+推奨）

```typescript
import { Injectable, signal, computed } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class CounterService {
  // Signal
  count = signal(0);
  
  // 計算プロパティ
  doubleCount = computed(() => this.count() * 2);
  
  // メソッド
  increment(): void {
    this.count.update(c => c + 1);
  }
  
  decrement(): void {
    this.count.update(c => c - 1);
  }
  
  reset(): void {
    this.count.set(0);
  }
}
```

```typescript
import { Component } from '@angular/core';
import { CounterService } from './services/counter.service';

@Component({
  selector: 'app-counter',
  standalone: true,
  template: `
    <p>カウント: {{ counterService.count() }}</p>
    <p>2倍: {{ counterService.doubleCount() }}</p>
    <button (click)="counterService.increment()">+</button>
    <button (click)="counterService.decrement()">-</button>
  `
})
export class CounterComponent {
  constructor(public counterService: CounterService) { }
}
```

### 9.2 NgRx（複雑な状態管理）

大規模アプリケーションには、NgRxを使用できます：

```bash
ng add @ngrx/store
```

---

## 10. スタイルとアニメーション

### 10.1 グローバルスタイル

```css
/* styles.css */
body {
  margin: 0;
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
}

* {
  box-sizing: border-box;
}
```

### 10.2 コンポーネントスタイル

```typescript
@Component({
  selector: 'app-styled',
  standalone: true,
  template: `<p class="highlight">スタイル例</p>`,
  styles: [`
    .highlight {
      background-color: yellow;
      padding: 10px;
      border-radius: 4px;
    }
  `]
})
export class StyledComponent { }
```

### 10.3 Angularアニメーション

```bash
npm install @angular/animations
```

```typescript
import { Component, trigger, state, style, transition, animate } from '@angular/animations';

@Component({
  selector: 'app-animated',
  standalone: true,
  animations: [
    trigger('expandCollapse', [
      state('collapsed', style({ height: '0px', opacity: 0 })),
      state('expanded', style({ height: '*', opacity: 1 })),
      transition('collapsed <=> expanded', [
        animate('300ms ease-in-out')
      ])
    ])
  ],
  template: `
    <button (click)="toggle()">切り替え</button>
    <div [@expandCollapse]="isExpanded ? 'expanded' : 'collapsed'">
      コンテンツ領域
    </div>
  `
})
export class AnimatedComponent {
  isExpanded = false;
  
  toggle(): void {
    this.isExpanded = !this.isExpanded;
  }
}
```

---

## 11. テスト

### 11.1 ユニットテスト

```bash
# テストの実行
ng test

# テストカバレッジレポートの生成
ng test --code-coverage
```

#### コンポーネントのテスト

```typescript
import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MyComponent } from './my.component';

describe('MyComponent', () => {
  let component: MyComponent;
  let fixture: ComponentFixture<MyComponent>;
  
  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [MyComponent]
    }).compileComponents();
    
    fixture = TestBed.createComponent(MyComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });
  
  it('コンポーネントが作成されるべき', () => {
    expect(component).toBeTruthy();
  });
  
  it('タイトルが表示されるべき', () => {
    const compiled = fixture.nativeElement as HTMLElement;
    expect(compiled.querySelector('h1')?.textContent).toContain('Hello');
  });
});
```

#### サービスのテスト

```typescript
import { TestBed } from '@angular/core/testing';
import { DataService } from './data.service';

describe('DataService', () => {
  let service: DataService;
  
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [DataService]
    });
    
    service = TestBed.inject(DataService);
  });
  
  it('サービスが作成されるべき', () => {
    expect(service).toBeTruthy();
  });
});
```

### 11.2 E2Eテスト

```bash
# Cypressのインストール
ng add @cypress/schematics

# E2Eテストの実行
ng e2e
```

---

## 12. ビルドとデプロイ

### 12.1 開発モード

```bash
# 開発サーバーの起動
ng serve

# 起動してブラウザを開く
ng serve --open

# ポート指定
ng serve --port 4201
```

### 12.2 本番ビルド

```bash
# 本番ビルド
ng build

# 本番ビルド（最適化）
ng build --configuration production

# 簡略化ビルド出力
ng build --optimization --source-map=false
```

### 12.3 ビルド成果物

ビルド完了後、成果物は`dist/`ディレクトリにあります：

```
dist/
└── my-app/
    ├── index.html
    ├── main.js
    ├── polyfills.js
    ├── runtime.js
    └── styles.css
```

### 12.4 静的サーバーへのデプロイ

#### Nginxへのデプロイ

```nginx
server {
    listen 80;
    server_name example.com;
    root /var/www/my-app;
    index index.html;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # 静的アセットのキャッシュ
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

#### Vercelへのデプロイ

```bash
# Vercel CLIのインストール
npm i -g vercel

# デプロイ
vercel
```

---

## 13. ベストプラクティス

### 13.1 プロジェクト構造のベストプラクティス

```
src/
├── app/
│   ├── core/                 # コアモジュール（シングルトンサービス、インターセプターなど）
│   │   ├── services/
│   │   ├── interceptors/
│   │   └── guards/
│   ├── shared/               # 共有モジュール
│   │   ├── components/
│   │   ├── directives/
│   │   ├── pipes/
│   │   └── models/
│   ├── features/             # 機能モジュール
│   │   ├── dashboard/
│   │   ├── settings/
│   │   └── users/
│   ├── app.component.ts
│   ├── app.config.ts
│   └── app.routes.ts
├── assets/
├── environments/
└── styles.scss
```

### 13.2 命名規則

```bash
# コンポーネント
ng g c features/users/user-list
ng g c shared/components/loading-spinner

# サービス
ng g s core/services/auth

# パイプ
ng g p shared/pipes/format-date
```

### 13.3 パフォーマンス最適化

1. **OnPush変更検出戦略の使用**
   ```typescript
   @Component({
     changeDetection: ChangeDetectionStrategy.OnPush
   })
   ```

2. **trackByを使用してリストパフォーマンスを向上**
   ```html
   @for (item of items; track item.id) {
     <li>{{ item.name }}</li>
   }
   ```

3. **遅延ロードモジュール**
   ```typescript
   export const routes: Routes = [
     { 
       path: 'admin', 
       loadComponent: () => import('./admin/admin.component')
     }
   ];
   ```

4. **Signalの使用**
   ```typescript
   // Zone.js変更検出の代わりにSignalを優先
   count = signal(0);
   ```

### 13.4 セキュリティのベストプラクティス

1. **XSS防止**
   - Angularの組み込み保護を使用
   - `innerHTML`の使用を避け、または`DomSanitizer`を使用

2. **CSRF防止**
   - HttpClientのCSRF保護を使用
   ```typescript
   provideHttpClient(withCsrf())
   ```

3. **URLインジェクション防止**
   - Angular Routerのナビゲーションガードを使用

---

## 📚 関連リソース

- 公式ドキュメント：https://angular.io/docs
- Angular CLI：https://angular.io/cli
- Angularチュートリアル：https://angular.io/tutorial
- Angular Material：https://material.angular.io/
- NgRx：https://ngrx.io/

---

*このドキュメントはAIアシスタントによって生成されました。フィードバック和改进の提案を歓迎します。*
