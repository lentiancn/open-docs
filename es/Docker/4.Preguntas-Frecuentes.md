# Docker Preguntas Frecuentes

> 🎯 Preguntas frecuentes sobre Docker

---

## Instalación

### P: ¿Docker Desktop no inicia en Windows?

**Soluciones**:
1. Habilitar virtualización en BIOS
2. Instalar/actualizar WSL 2
3. Reiniciar computadora

---

### P: Error "docker: permission denied"?

**Solución**:
```bash
sudo usermod -aG docker $USER
# Cerrar sesión y volver a iniciar
```

---

### P: ¿Cómo verificar la versión de Docker?

```bash
docker --version
docker-compose --version
```

---

## Imágenes

### P: ¿Cómo reducir el tamaño de la imagen?

**Mejores prácticas**:
1. Usar imágenes alpine
2. Usar construcciones multi-etapa
3. Minimizar capas
4. Limpiar en la misma capa

---

### P: ¿Cómo construir imagen sin caché?

```bash
docker build --no-cache -t my-image .
```

---

### P: ¿Cómo eliminar imágenes no utilizadas?

```bash
docker image prune -a
```

---

## Contenedores

### P: ¿Cómo mantener el contenedor en ejecución?

```bash
# Método 1: Seguimiento de log
docker run -d nginx tail -f /dev/null

# Método 2: Usar sleep
docker run -d nginx sleep infinity
```

---

### P: ¿El contenedor sale inmediatamente?

**Razones**:
1. Sin proceso en primer plano
2. Proceso completado

**Solución**:
```bash
docker run -it image_name /bin/bash
```

---

### P: ¿Cómo entrar en un contenedor en ejecución?

```bash
docker exec -it container_id /bin/bash
```

---

## Redes

### P: ¿Cómo acceder al contenedor desde el host?

Usar mapeo de puertos:
```bash
docker run -p 8080:80 nginx
```

---

## Volúmenes

### P: ¿Cómo persistir datos?

Usar volúmenes:
```bash
docker volume create my-data
docker run -v my-data:/data nginx
```

---

## Docker Compose

### P: ¿Cómo reconstruir después de cambios?

```bash
docker-compose up -d --build
```

---

## Rendimiento

### P: ¿Docker lento / consume mucho disco?

**Soluciones**:
```bash
docker system prune -a
docker system df
```

---

## Solución de Problemas

### P: ¿Cómo depurar contenedor fallido?

```bash
docker logs container_id
docker inspect container_id
```

---

## Seguridad

### P: ¿Cómo escanear vulnerabilidades?

```bash
docker scan my-image
```

---

## Docker vs VM

### P: ¿Cuál es la diferencia entre Docker y VM?

| Aspecto | Docker | VM |
|---------|--------|-----|
| SO | Comparte kernel del host | SO separado |
| Tamaño | MBs | GBs |
| Inicio | Segundos | Minutos |
| Aislamiento | Contenedores | Aislamiento total |

---

## Recursos

- Documentación Oficial: https://docs.docker.com
- Docker Hub: https://hub.docker.com

---

*Este documento fue generado por Asistente IA*
