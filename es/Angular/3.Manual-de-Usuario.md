# Angular Manual de Usuario

Funciones principales de Angular y consejos de uso.

## Desarrollo de componentes

### Crear componente

```bash
ng generate component components/user-list
```

### Básicos de componentes

```typescript
import { Component } from '@angular/core';

@Component({
  selector: 'app-user-list',
  template: `
    <h2>Lista de usuarios</h2>
    <ul>
      <li *ngFor="let user of users">
        {{ user.name }} - {{ user.email }}
      </li>
    </ul>
  `,
  styles: [`
    ul { list-style: none; padding: 0; }
    li { padding: 10px; border-bottom: 1px solid #eee; }
  `]
})
export class UserListComponent {
  users = [
    { name: 'Juan', email: 'juan@example.com' },
    { name: 'María', email: 'maria@example.com' }
  ];
}
```

### Comunicación entre componentes

**Padre a hijo (Input):**

```typescript
// Componente hijo
import { Component, Input } from '@angular/core';

@Component({
  selector: 'app-user-card',
  template: `
    <div class="card">
      <h3>{{ title }}</h3>
      <p>{{ user?.name }}</p>
    </div>
  `
})
export class UserCardComponent {
  @Input() title = '';
  @Input() user?: { name: string; email: string };
}
```

```typescript
// Componente padre
@Component({
  selector: 'app-parent',
  template: `
    <app-user-card 
      title="Info de usuario" 
      [user]="currentUser">
    </app-user-card>
  `
})
export class ParentComponent {
  currentUser = { name: 'Alice', email: 'alice@example.com' };
}
```

**Hijo a padre (Output):**

```typescript
// Componente hijo
import { Component, Output, EventEmitter } from '@angular/core';

@Component({
  selector: 'app-child',
  template: `<button (click)="onClick()">Clic</button>`
})
export class ChildComponent {
  @Output() buttonClick = new EventEmitter<string>();

  onClick() {
    this.buttonClick.emit('¡Hola desde hijo!');
  }
}
```

```typescript
// Componente padre
@Component({
  selector: 'app-parent',
  template: `
    <app-child (buttonClick)="handleClick($event)"></app-child>
  `
})
export class ParentComponent {
  handleClick(message: string) {
    console.log(message);
  }
}
```

## Servicios e Inyección de dependencias

### Crear servicio

```bash
ng generate service services/user
```

### Básicos de servicios

```typescript
import { Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';

export interface User {
  id: number;
  name: string;
  email: string;
}

@Injectable({
  providedIn: 'root'
})
export class UserService {
  private apiUrl = 'https://api.example.com/users';

  constructor(private http: HttpClient) {}

  getUsers(): Observable<User[]> {
    return this.http.get<User[]>(this.apiUrl);
  }

  getUser(id: number): Observable<User> {
    return this.http.get<User>(`${this.apiUrl}/${id}`);
  }

  createUser(user: Partial<User>): Observable<User> {
    return this.http.post<User>(this.apiUrl, user);
  }

  updateUser(id: number, user: Partial<User>): Observable<User> {
    return this.http.put<User>(`${this.apiUrl}/${id}`, user);
  }

  deleteUser(id: number): Observable<void> {
    return this.http.delete<void>(`${this.apiUrl}/${id}`);
  }
}
```

### Usar servicio

```typescript
import { Component, OnInit } from '@angular/core';
import { UserService, User } from './services/user.service';

@Component({
  selector: 'app-users',
  template: `
    <div *ngFor="let user of users">
      {{ user.name }}
    </div>
  `
})
export class UsersComponent implements OnInit {
  users: User[] = [];

  constructor(private userService: UserService) {}

  ngOnInit() {
    this.userService.getUsers().subscribe(data => {
      this.users = data;
    });
  }
}
```

## Enrutamiento

### Configurar rutas

```typescript
// app.routes.ts
import { Routes } from '@angular/router';

export const routes: Routes = [
  { path: '', redirectTo: '/home', pathMatch: 'full' },
  { 
    path: 'home', 
    loadComponent: () => import('./pages/home/home.component')
      .then(m => m.HomeComponent)
  },
  { 
    path: 'about', 
    loadComponent: () => import('./pages/about/about.component')
      .then(m => m.AboutComponent)
  },
  { 
    path: 'users', 
    loadChildren: () => import('./pages/users/users.routes')
      .then(m => m.USER_ROUTES)
  },
  { path: '**', component: NotFoundComponent }
];
```

### Navegación de rutas

```typescript
import { RouterLink, RouterLinkActive, RouterOutlet } from '@angular/router';

@Component({
  selector: 'app-nav',
  template: `
    <nav>
      <a routerLink="/home" routerLinkActive="active">Inicio</a>
      <a routerLink="/about" routerLinkActive="active">Acerca</a>
      <a routerLink="/users" routerLinkActive="active">Usuarios</a>
    </nav>
    <router-outlet></router-outlet>
  `,
  standalone: true,
  imports: [RouterLink, RouterLinkActive, RouterOutlet]
})
export class NavComponent {}
```

### Parámetros de ruta

```typescript
// Configuración de ruta
{ path: 'user/:id', component: UserDetailComponent }

// Navegar con parámetros
<a [routerLink]="['/user', user.id]">Ver</a>
<button (click)="goToDetail(123)">Ver</button>

// Obtener parámetros en componente
import { ActivatedRoute } from '@angular/router';

@Component({...})
export class UserDetailComponent implements OnInit {
  constructor(private route: ActivatedRoute) {}

  ngOnInit() {
    this.route.params.subscribe(params => {
      const id = params['id'];
      console.log('ID de usuario:', id);
    });
  }

  goToDetail(id: number) {
    this.router.navigate(['/user', id]);
  }
}
```

## Formularios

### Formularios reactivos

```typescript
import { Component } from '@angular/core';
import { FormBuilder, FormGroup, Validators, ReactiveFormsModule } from '@angular/forms';
import { CommonModule } from '@angular/common';

@Component({
  selector: 'app-login',
  template: `
    <form [formGroup]="form" (ngSubmit)="onSubmit()">
      <div>
        <label>Email</label>
        <input formControlName="email" type="email">
        <span *ngIf="form.get('email')?.errors?.['required']">Requerido</span>
        <span *ngIf="form.get('email')?.errors?.['email']">Email inválido</span>
      </div>
      <div>
        <label>Contraseña</label>
        <input formControlName="password" type="password">
      </div>
      <button type="submit" [disabled]="form.invalid">Iniciar sesión</button>
    </form>
  `,
  standalone: true,
  imports: [ReactiveFormsModule, CommonModule]
})
export class LoginComponent {
  form: FormGroup;

  constructor(private fb: FormBuilder) {
    this.form = this.fb.group({
      email: ['', [Validators.required, Validators.email]],
      password: ['', [Validators.required, Validators.minLength(6)]]
    });
  }

  onSubmit() {
    if (this.form.valid) {
      console.log('Valor del formulario:', this.form.value);
    }
  }
}
```

### Formularios basados en plantillas

```typescript
import { Component } from '@angular/core';
import { FormsModule } from '@angular/forms';

@Component({
  selector: 'app-contact',
  template: `
    <form #contactForm="ngForm" (ngSubmit)="onSubmit(contactForm)">
      <div>
        <label>Nombre</label>
        <input name="name" [(ngModel)]="name" required>
      </div>
      <div>
        <label>Mensaje</label>
        <textarea name="message" [(ngModel)]="message" required></textarea>
      </div>
      <button type="submit" [disabled]="!contactForm.form.valid">Enviar</button>
    </form>
  `,
  standalone: true,
  imports: [FormsModule]
})
export class ContactComponent {
  name = '';
  message = '';

  onSubmit(form: any) {
    console.log('Enviar:', { name: this.name, message: this.message });
  }
}
```

## Cliente HTTP

### Configurar HttpClient

```typescript
// app.config.ts (Standalone)
import { ApplicationConfig } from '@angular/core';
import { provideHttpClient } from '@angular/common/http';

export const appConfig: ApplicationConfig = {
  providers: [
    provideHttpClient()
  ]
};
```

### Usar HttpClient

```typescript
import { Component, inject } from '@angular/core';
import { HttpClient } from '@angular/common/http';

@Component({
  selector: 'app-data',
  template: '<button (click)="fetchData()">Obtener datos</button>'
})
export class DataComponent {
  private http = inject(HttpClient);

  fetchData() {
    this.http.get('https://api.example.com/data').subscribe({
      next: (data) => console.log('Datos:', data),
      error: (error) => console.error('Error:', error)
    });
  }
}
```

## Directivas

### Directivas estructurales

```html
<!-- *ngIf -->
<div *ngIf="isLoggedIn">¡Bienvenido de nuevo!</div>
<div *ngIf="users.length > 0; else noUsers">
  Lista de usuarios
</div>
<ng-template #noUsers>
  Sin usuarios
</ng-template>

<!-- *ngFor -->
<ul>
  <li *ngFor="let user of users; trackBy: trackById">
    {{ user.name }}
  </li>
</ul>

<!-- *ngSwitch -->
<div [ngSwitch]="status">
  <p *ngSwitchCase="'loading'">Cargando...</p>
  <p *ngSwitchCase="'success'">Éxito</p>
  <p *ngSwitchCase="'error'">Error</p>
  <p *ngSwitchDefault>Desconocido</p>
</div>
```

### Directivas de atributo

```typescript
import { Directive, ElementRef, Renderer2, OnInit } from '@angular/core';

@Directive({
  selector: '[appHighlight]'
})
export class HighlightDirective implements OnInit {
  constructor(
    private el: ElementRef,
    private renderer: Renderer2
  ) {}

  ngOnInit() {
    this.renderer.setStyle(
      this.el.nativeElement,
      'backgroundColor',
      'yellow'
    );
  }
}
```

Uso:

```html
<p appHighlight>Este texto será resaltado</p>
```

## Pipes

### Pipes incorporados

```html
<!-- Pipe de fecha -->
<p>{{ today | date:'yyyy-MM-dd' }}</p>
<!-- Salida: 2024-01-01 -->

<!-- Pipe de moneda -->
<p>{{ price | currency:'USD' }}</p>
<!-- Salida: $100.00 -->

<!-- Pipe de número -->
<p>{{ pi | number:'1.2-2' }}</p>
<!-- Salida: 3.14 -->

<!-- Pipe mayúsculas -->
<p>{{ 'Hola' | uppercase }}</p>
<!-- Salida: HOLA -->

<!-- Pipe JSON -->
<p>{{ obj | json }}</p>
```

### Pipe personalizado

```bash
ng generate pipe pipes/truncate
```

```typescript
import { Pipe, PipeTransform } from '@angular/core';

@Pipe({
  name: 'truncate'
})
export class TruncatePipe implements PipeTransform {
  transform(value: string, limit: number = 10, trail: string = '...'): string {
    if (value.length <= limit) return value;
    return value.substring(0, limit) + trail;
  }
}
```

Uso:

```html
<p>{{ 'Este es un texto largo' | truncate:5 }}</p>
<!-- Salida: Este e... -->
```

## Ganchos de ciclo de vida

```typescript
import { 
  Component, 
  OnInit, 
  OnChanges, 
  OnDestroy,
  AfterViewInit,
  AfterContentInit
} from '@angular/core';

@Component({...})
export class MyComponent implements 
  OnInit, 
  OnChanges, 
  OnDestroy,
  AfterViewInit,
  AfterContentInit {
  
  // Llamado después del constructor
  ngOnInit() {
    console.log('ngOnInit - Componente inicializado');
  }
  
  // Llamado cuando cambian las propiedades de entrada
  ngOnChanges(changes: SimpleChanges) {
    console.log('ngOnChanges - Propiedades de entrada cambiadas', changes);
  }
  
  // Llamado después de inicialización de la vista
  ngAfterViewInit() {
    console.log('ngAfterViewInit - Vista inicializada');
  }
  
  // Llamado después de inicialización de contenido proyectado
  ngAfterContentInit() {
    console.log('ngAfterContentInit - Contenido proyectado');
  }
  
  // Llamado antes de destrucción del componente
  ngOnDestroy() {
    console.log('ngOnDestroy - Componente a punto de ser destruido');
  }
}
```

## Mejores prácticas

### Organización del código

```typescript
// Usar Componentes Standalone
import { Component, signal, computed } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
  selector: 'app-counter',
  standalone: true,
  imports: [CommonModule],
  template: `
    <p>Contador: {{ count() }}</p>
    <p>Doble: {{ doubleCount() }}</p>
    <button (click)="increment()">+1</button>
  `
})
export class CounterComponent {
  // Usando Signals
  count = signal(0);
  
  // Valor calculado
  doubleCount = computed(() => this.count() * 2);
  
  increment() {
    this.count.update(c => c + 1);
  }
}
```

### Operaciones asíncronas

```typescript
import { Component, inject } from '@angular/core';
import { AsyncPipe } from '@angular/common';
import { Observable } from 'rxjs';

@Component({
  selector: 'app-users',
  template: `
    <ul *ngIf="users$ | async as users">
      <li *ngFor="let user of users">{{ user.name }}</li>
    </ul>
  `,
  standalone: true,
  imports: [AsyncPipe]
})
export class UsersComponent {
  private userService = inject(UserService);
  users$ = this.userService.getUsers();
}
```
