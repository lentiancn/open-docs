# Angular 사용자 매뉴얼

> 🎯 Angular 완벽 가이드
> 
> 📅 업데이트: 2026년 2월
> 
> 📖 버전: Angular 17+

---

## 1. 소개

Angular는 Google의 오픈소스 웹 프레임워크입니다.

### 주요 기능
- 컴포넌트
- 양방향 데이터 바인딩
- 의존성 주입
- 라우팅
- 폼
- HTTP 클라이언트
- 테스트

---

## 2. 프로젝트 구조

```
my-app/
├── src/app/
├── angular.json
└── package.json
```

---

## 3. 컴포넌트

```bash
ng generate component my-component
```

```typescript
@Component({
  selector: 'app-my-component',
  standalone: true,
  template: `<h1>{{ title }}</h1>`
})
export class MyComponentComponent {
  title = 'Hello Angular';
}
```

---

## 4. 템플릿

```html
<!-- 보간 -->
<p>Hello, {{ name }}!</p>

<!-- 프로퍼티 바인딩 -->
<img [src]="imageUrl">

<!-- 이벤트 바인딩 -->
<button (click)="click()">클릭</button>

<!-- 양방향 바인딩 -->
<input [(ngModel)]="name">
```

---

## 5. 서비스

```typescript
@Injectable({ providedIn: 'root' })
export class DataService {
  constructor(private http: HttpClient) {}
}
```

---

## 6. 라우팅

```typescript
export const routes: Routes = [
  { path: '', component: HomeComponent },
  { path: 'about', component: AboutComponent }
];
```

---

## 7. HTTP

```typescript
this.http.get('/api/data').subscribe(d => console.log(d));
```

---

## 8. Signal

```typescript
count = signal(0);

increment(): void {
  this.count.update(c => c + 1);
}
```

---

## 9. 빌드

```bash
ng build --configuration production
```

---

## 📚 자료

- https://angular.io/docs

---

*AI로 생성됨*
