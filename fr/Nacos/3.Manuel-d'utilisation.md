# Manuel Utilisateur Nacos

## Aperçu

Nacos fournit deux fonctionnalités principales : découverte de services et gestion de configuration. Ce manuel expliquera comment utiliser ces fonctionnalités en détail.

## Découverte de Services

### Enregistrement de Services

Les fournisseurs de services peuvent enregistrer des services sur Nacos de la manière suivante :

#### Enregistrer avec le SDK Natif

Enregistrer des services en utilisant le SDK Java Nacos :

```java
import com.alibaba.nacos.api.naming.NamingService;
import com.alibaba.nacos.api.naming.pojo.Instance;

NamingService namingService = NacosFactory.createNamingService("127.0.0.1:8848");

Instance instance = new Instance();
instance.setIp("192.168.1.100");
instance.setPort(8080);
instance.setEphemeral(true);

Map<String, String> metadata = new HashMap<>();
metadata.put("version", "1.0");
instance.setMetadata(metadata);

namingService.registerInstance("service-provider", instance);
```

#### Enregistrer avec OpenAPI

```bash
curl -X POST 'http://127.0.0.1:8848/nacos/v3/client/ns/instance' \
  -d 'serviceName=service-provider' \
  -d 'ip=192.168.1.100' \
  -d 'port=8080'
```

### Découverte de Services

Les consommateurs de services peuvent découvrir des services de la manière suivante :

#### Utiliser le SDK Natif

```java
NamingService namingService = NacosFactory.createNamingService("127.0.0.1:8848");

// Obtenir toutes les instances saines
List<Instance> instances = namingService.selectInstances("service-provider", true);

// Utiliser l'équilibrage de charge pour sélectionner une instance
Instance instance = namingService.selectOneHealthyInstance("service-provider");
```

#### Utiliser OpenAPI

```bash
# Obtenir la liste des services
curl -X GET 'http://127.0.0.1:8848/nacos/v3/client/ns/instance/list?serviceName=service-provider'
```

### Gestion des Métadonnées de Services

Nacos prend en charge l'ajout et la gestion de métadonnées pour les services :

```java
Map<String, String> metadata = new HashMap<>();
metadata.put("version", "1.0");
metadata.put("env", "production");

namingService.registerInstance("service-name", instance);
```

## Gestion de Configuration

### Publier une Configuration

#### Utiliser OpenAPI

```bash
# D'abord se connecter pour obtenir le jeton d'accès
curl -X POST 'http://127.0.0.1:8848/nacos/v3/auth/user/login' \
  -d 'username=nacos' \
  -d 'password=${your_password}'

# Utiliser le jeton d'accès pour créer une configuration
curl -X POST 'http://127.0.0.1:8848/nacos/v3/admin/cs/config?dataId=example.config&groupName=DEFAULT_GROUP&content=HelloWorld' \
  -H "accessToken:${your_access_token}"
```

#### Utiliser le SDK

```java
ConfigService configService = NacosFactory.createConfigService("127.0.0.1:8848");

boolean isPublished = configService.publishConfig(
    "example.config",
    "DEFAULT_GROUP",
    "Hello Nacos"
);
```

### Obtenir une Configuration

#### Utiliser OpenAPI

```bash
curl -X GET 'http://127.0.0.1:8848/nacos/v3/client/cs/config?dataId=example.config&groupName=DEFAULT_GROUP'
```

#### Utiliser le SDK

```java
String config = configService.getConfig(
    "example.config",
    "DEFAULT_GROUP",
    5000
);
```

### Écoute de Configuration

#### Utiliser le SDK

```java
configService.addListener(
    "example.config",
    "DEFAULT_GROUP",
    new Listener() {
        @Override
        public void receiveConfigInfo(String configInfo) {
            System.out.println("Configuration modifiée :" + configInfo);
        }

        @Override
        public Executor getExecutor() {
            return Executors.newSingleThreadExecutor();
        }
    }
);
```

## Intégration Nacos avec Spring Cloud

### Prérequis

Assurez-vous que Nacos Server est installé et démarré.

### Ajouter des Dépendances

Ajoutez les dépendances suivantes dans pom.xml de votre projet Spring Cloud :

```xml
<dependency>
    <groupId>com.alibaba.cloud</groupId>
    <artifactId>spring-cloud-starter-alibaba-nacos-config</artifactId>
    <version>2023.0.3.2</version>
</dependency>

<dependency>
    <groupId>com.alibaba.cloud</groupId>
    <artifactId>spring-cloud-starter-alibaba-nacos-discovery</artifactId>
    <version>2023.0.3.2</version>
</dependency>
```

### Gestion de Configuration

Configurez l'adresse du serveur Nacos dans application.properties :

```properties
spring.application.name=springclouddemo
spring.config.import[0]=nacos:springclouddemo.properties?group=DEFAULT_GROUP
spring.cloud.nacos.config.server-addr=127.0.0.1:8848
spring.cloud.nacos.discovery.server-addr=127.0.0.1:8848
```

### Utilisation des Annotations

#### Annotation @NacosConfig

Injecter les valeurs de configuration de Nacos directement dans les propriétés du Bean Spring :

```java
@RestController
public class ConfigController {
    @NacosConfig(dataId = "routeconfig", group = "config", key = "rate")
    String rate;

    @RequestMapping("/rate")
    public String rate() {
        return rate;
    }
}
```

#### Annotation @NacosConfigListener

Recevoir les événements de modification de configuration de Nacos :

```java
@Service
public class MyRateConfigService {
    @NacosConfigListener(dataId = "routeconfig", group = "config")
    public void rate(String rateConfig) {
        System.out.println("Reçu modification de configuration :" + rateConfig);
    }
}
```

### Enregistrement et Découverte de Services

Activer l'enregistrement et la découverte de services dans l'application Spring Boot :

```java
@SpringBootApplication
@EnableDiscoveryClient
public class NacosProviderApplication {
    public static void main(String[] args) {
        SpringApplication.run(NacosProviderApplication.class, args);
    }

    @RestController
    class EchoController {
        @RequestMapping(value = "/echo/{string}", method = RequestMethod.GET)
        public String echo(@PathVariable String string) {
            return "Hello Nacos Discovery " + string;
        }
    }
}
```

Configurer dans application.properties :

```properties
server.port=8070
spring.application.name=service-provider
spring.cloud.nacos.discovery.server-addr=127.0.0.1:8848
```

Configuration du consommateur de services :

```java
@SpringBootApplication
@EnableDiscoveryClient
public class NacosConsumerApplication {
    @LoadBalanced
    @Bean
    public RestTemplate restTemplate() {
        return new RestTemplate();
    }

    @RestController
    public class TestController {
        private final RestTemplate restTemplate;

        @Autowired
        public TestController(RestTemplate restTemplate) {
            this.restTemplate = restTemplate;
        }

        @RequestMapping(value = "/echo/{str}", method = RequestMethod.GET)
        public String echo(@PathVariable String str) {
            return restTemplate.getForObject("http://service-provider/echo/" + str, String.class);
        }
    }
}
```

## Gestion des Services MCP

Nacos 3.0 prend en charge l'enregistrement, la découverte et la gestion des services MCP (Model Context Protocol).

### Auto-Enregistrement du Serveur MCP

#### Utiliser le Framework Spring AI Alibaba Nacos MCP

1. Ajouter des dépendances :

```xml
<dependency>
    <groupId>com.alibaba.cloud.ai</groupId>
    <artifactId>spring-ai-alibaba-starter-mcp-registry</artifactId>
    <version>1.0.0.3+</version>
</dependency>
<dependency>
    <groupId>org.springframework.ai</groupId>
    <artifactId>spring-ai-starter-mcp-server-webmvc</artifactId>
    <version>1.0.0+</version>
</dependency>
```

2. Configurer l'auto-enregistrement :

```yaml
spring:
  application:
    name: mcp-nacos-registry-example
  ai:
    mcp:
      server:
        name: webmvc-mcp-server
        version: 1.0.0
        type: SYNC
        instructions: "This mcp server provides time information tools and resources"
  alibaba:
    mcp:
      nacos:
        server-addr: 127.0.0.1:8848
        namespace: public
        username: nacos
        password: nacos
        register:
          enabled: true
```

#### Utiliser Nacos MCP Wrapper Python

```python
pip install nacos-mcp-wrapper-python

from nacos_mcp_wrapper.server.nacos_mcp import NacosMCP
from nacos_mcp_wrapper.server.nacos_settings import NacosSettings

nacos_settings = NacosSettings()
nacos_settings.SERVER_ADDR = "127.0.0.1:8848"
nacos_settings.NAMESPACE = "public"
nacos_settings.USERNAME = "nacos"
nacos_settings.PASSWORD = "nacos"

mcp = NacosMCP("nacos-mcp-python", nacos_settings=nacos_settings, version="1.0.1", port=18001)

@mcp.tool()
def add(a: int, b: int) -> int:
    """Add two integers together"""
    return a + b

if __name__ == "__main__":
    mcp.run(transport="sse")
```

### Découverte et Invocation de Services MCP

#### Utiliser Spring AI Alibaba pour Découvrir le Serveur MCP

```java
@Autowired
private List<LoadbalancedMcpSyncClient> loadbalancedMcpSyncClients;
```

Ou :

```java
@Qualifier("loadbalancedMcpToolCallbacks")
ToolCallbackProvider tools
```

Configurer application.yml :

```yaml
spring:
  alibaba:
    mcp:
      nacos:
        namespace: public
        server-addr: 127.0.0.1:8848
        username: nacos
        password: nacos
        client:
          enabled: true
```

## Utilisation de la Console

Nacos fournit une console web pour la gestion visuelle :

- Adresse de la console : http://127.0.0.1:8080/index.html
- Nom d'utilisateur par défaut : nacos
- Mot de passe par défaut : nacos@demo (veuillez changer le mot de passe en production)

Fonctionnalités principales de la console :

- Gestion des Services : Voir, modifier et supprimer les services enregistrés
- Gestion de Configuration : Créer, modifier, supprimer et interroger les configurations
- Gestion des Espaces de Noms : Créer des espaces de noms isolés
- Gestion des Utilisateurs : Gérer les utilisateurs d'accès et les permissions
- Gestion du Cluster : Voir l'état des nœuds du cluster
