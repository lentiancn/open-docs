# JIRA 安裝指南

JIRA 是 Atlassian 公司開發的問題追蹤和專案管理工具。本指南涵蓋在各種環境中的安裝方法。

## 版本資訊

| 版本 | 狀態 | 說明 |
|------|------|------|
| JIRA 9.x | ✅ 目前 | 最新版本 |
| JIRA 8.x | ✅ 支援 | 穩定版本 |

## 系統需求

### 硬體需求

| 需求 | 最低配置 | 推薦配置 |
|------|----------|----------|
| CPU | 4 核心 | 8+ 核心 |
| 記憶體 | 4 GB | 16+ GB |
| 磁碟 | 10 GB | 50+ GB |

### 軟體需求

| 軟體 | 版本 |
|------|------|
| Java | JDK 11 或 17 |
| 作業系統 | Linux/Windows/macOS |
| 資料庫 | PostgreSQL/MySQL/Oracle |

## 安裝方法

### 方法一：Windows 安裝

#### 1. 下載安裝程式

1. 造訪 https://www.atlassian.com/software/jira/download
2. 下載 JIRA Software
3. 執行安裝程式

#### 2. 執行安裝

按照安裝精靈完成安裝：
- 選擇安裝類型
- 設定連接埠
- 設定資料庫
- 建立管理員帳戶

### 方法二：Linux 安裝

#### 1. 下載安裝包

```bash
# 下載 JIRA
wget https://product-downloads.atlassian.com/software/jira/downloads/atlassian-jira-software-X.X.X-x64.bin
```

#### 2. 安裝

```bash
# 新增執行權限
chmod +x atlassian-jira-software-X.X.X-x64.bin

# 執行安裝
sudo ./atlassian-jira-software-X.X.X-x64.bin
```

#### 3. 設定

```bash
# 啟動 JIRA
sudo /opt/atlassian/jira/bin/start-jira.sh

# 停止 JIRA
sudo /opt/atlassian/jira/bin/stop-jira.sh
```

### 方法三：Docker 安裝

```bash
# 拉取映像檔
docker pull atlassian/jira-software

# 執行容器
docker run -v jira-data:/var/atlassian/application-data/jira --name="jira" -d -p 8080:8080 atlassian/jira-software
```

### 方法四：使用 AWS

1. 在 AWS Marketplace 搜尋 JIRA
2. 選擇執行個體類型
3. 設定資料庫
4. 啟動執行個體

## 設定

### 資料庫設定

```properties
# dbconfig.xml
<?xml version="1.0" encoding="UTF-8"?>
<jira-database-config>
  <name>defaultDS</name>
  <connection-type>default</connection-type>
  <url>jdbc:postgresql://localhost:5432/jira</url>
  <username>jira</username>
  <password>password</password>
  <pool-size>20</pool-size>
</jira-database-config>
```

### 反向代理設定

#### Nginx

```nginx
server {
    listen 80;
    server_name jira.example.com;
    
    location / {
        proxy_pass http://localhost:8080;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

## 常見問題

### 啟動失敗？

檢查 Java 版本：

```bash
java -version
```

### 資料庫連線失敗？

檢查資料庫設定和連線資訊。

### 記憶體不足？

修改記憶體設定：

```bash
# setenv.sh
export JVM_MINIMUM_MEMORY="2048m"
export JVM_MAXIMUM_MEMORY="4096m"
```

## 相關連結

- [JIRA 官方網站](https://www.atlassian.com/software/jira)
- [JIRA 文件](https://docs.atlassian.com/jira)
