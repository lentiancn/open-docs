# Element Plus 安裝指南

Element Plus 是為開發者、設計師和產品經理打造的 Vue 3 組件庫。本指南涵蓋所有環境的安裝方式。

## 版本資訊

| 版本 | 狀態 | 說明 |
|---------|--------|-------------|
| Element Plus 2.x | ✅ 當前版本 | Vue 3 最新穩定版 |
| Element Plus 1.x | ⚠️ 維護中 | 僅提供安全更新 |
| Element UI | ❌ 舊版 | 僅支援 Vue 2，已停止維護 |

> **重要提示**：Element Plus 僅支援 Vue 3。如果您使用的是 Vue 2，請使用 Element UI。

## 環境要求

### 系統需求

| 要求 | 版本 |
|-------------|---------|
| Vue | 3.x |
| Node.js | 16.x+ |
| npm / yarn / pnpm | 最新版 |

### 瀏覽器支援

| 瀏覽器 | 版本 |
|---------|---------|
| Chrome | 80+ |
| Firefox | 80+ |
| Safari | 14+ |
| Edge | 80+ |

## 安裝方式

### 方式一：npm（推薦）

```bash
# 使用 npm
npm install element-plus

# 使用 yarn
yarn add element-plus

# 使用 pnpm
pnpm add element-plus
```

### 方式二：CDN 引入

```html
<!-- 引入 Vue 3 -->
<script src="https://unpkg.com/vue@3/dist/vue.global.js"></script>

<!-- 引入 Element Plus -->
<link rel="stylesheet" href="https://unpkg.com/element-plus/dist/index.css" />
<script src="https://unpkg.com/element-plus/dist/index.full.js"></script>

<!-- 引入 Element Plus 圖示（可選） -->
<script src="https://unpkg.com/@element-plus/icons-vue"></script>
```

#### 完整的 CDN 範本

```html
<!DOCTYPE html>
<html lang="zh-TW">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Element Plus CDN</title>
    <link rel="stylesheet" href="https://unpkg.com/element-plus/dist/index.css">
</head>
<body>
    <div id="app">
        <el-button>按鈕</el-button>
        <el-date-picker></el-date-picker>
    </div>

    <script src="https://unpkg.com/vue@3/dist/vue.global.js"></script>
    <script src="https://unpkg.com/element-plus/dist/index.full.js"></script>
    <script src="https://unpkg.com/@element-plus/icons-vue"></script>
    
    <script>
        const { createApp, ref } = Vue
        const app = createApp({
            setup() {
                return {}
            }
        })
        
        for (const [key, component] of Object.entries(ElementPlus)) {
            app.component(key, component)
        }
        
        for (const [key, component] of Object.entries(ElementPlusIconsVue)) {
            app.component(key, component)
        }
        
        app.use(ElementPlus)
        app.mount('#app')
    </script>
</body>
</html>
```

### 方式三：使用 Vite

```bash
# 建立 Vite 專案
npm create vite@latest my-app -- --template vue

# 進入專案目錄
cd my-app

# 安裝 Element Plus
npm install element-plus

# 安裝圖示
npm install @element-plus/icons-vue
```

#### 在 main.js 中設定

```javascript
import { createApp } from 'vue'
import App from './App.vue'

// 引入 Element Plus 樣式
import 'element-plus/dist/index.css'

// 引入 Element Plus
import ElementPlus from 'element-plus'

// 引入圖示
import * as ElementPlusIconsVue from '@element-plus/icons-vue'

const app = createApp(App)

// 註冊所有圖示
for (const [key, component] of Object.entries(ElementPlusIconsVue)) {
    app.component(key, component)
}

app.use(ElementPlus)
app.mount('#app')
```

### 方式四：使用 Vue CLI

```bash
# 安裝 Vue CLI
npm install -g @vue/cli

# 建立專案
vue create my-project

# 新增 Element Plus
cd my-project
npm install element-plus
npm install @element-plus/icons-vue
```

#### 在 main.js 中設定

```javascript
import { createApp } from 'vue'
import App from './App.vue'

import 'element-plus/dist/index.css'
import ElementPlus from 'element-plus'
import * as icons from '@element-plus/icons-vue'

const app = createApp(App)

// 全域註冊圖示
for (const [key, component] of Object.entries(icons)) {
    app.component(key, component)
}

app.use(ElementPlus)
app.mount('#app')
```

### 方式五：按需引入（推薦用於生產環境）

#### 使用 unplugin-vue-components

```bash
# 安裝依賴
npm install element-plus unplugin-vue-components unplugin-auto-import
```

#### Vite 設定

```javascript
// vite.config.js
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import AutoImport from 'unplugin-auto-import/vite'
import Components from 'unplugin-vue-components/vite'
import { ElementPlusResolver } from 'unplugin-vue-components/resolvers'

export default defineConfig({
  plugins: [
    vue(),
    AutoImport({
      resolvers: [ElementPlusResolver()],
    }),
    Components({
      resolvers: [ElementPlusResolver()],
    }),
  ],
})
```

#### 使用方法

```vue
<template>
  <!-- 無需手動引入組件 -->
  <el-button type="primary">按鈕</el-button>
  <el-input v-model="input" placeholder="請輸入內容" />
</template>

<script setup>
// 直接使用即可
import { ref } from 'vue'
const input = ref('')
</script>
```

### 方式六：使用 Nuxt 3

```bash
# 建立 Nuxt 專案
npx nuxi@latest init my-app
cd my-app

# 安裝 Element Plus
npm install element-plus @element-plus/nuxt
```

#### 設定 nuxt.config.ts

```typescript
export default defineNuxtConfig({
  modules: ['@element-plus/nuxt'],
  elementPlus: {
    /** 設定選項 */
  }
})
```

## 圖示設定

### 全域註冊

```javascript
import * as icons from '@element-plus/icons-vue'

const app = createApp(App)

for (const [key, icon] of Object.entries(icons)) {
    app.component(key, icon)
}
```

### 在範本中使用

```html
<el-icon><Plus /></el-icon>
<el-icon><Search /></el-icon>
<el-icon><Edit /></el-icon>
```

### 可用圖示組件

- Plus、Minus、Close、Check
- Search、Edit、Delete、Copy
- Upload、Download、Link
- ArrowLeft、ArrowRight、ArrowUp、ArrowDown
- CirclePlus、CircleClose
- 以及更多...

## 設定

### 語言設定

```javascript
import ElementPlus from 'element-plus'
import zhTw from 'element-plus/es/locale/lang/zh-tw'

app.use(ElementPlus, {
  locale: zhTw,
})
```

### 支援的語言

- en（英語）
- zhCn（簡體中文）
- zhTw（繁體中文）
- ja（日語）
- es（西班牙語）
- de（德語）
- fr（法語）
- ru（俄語）
- ko（韓語）

### 主題定制

#### 使用 CSS 變數

```css
:root {
    --el-color-primary: #409eff;
    --el-color-success: #67c23a;
    --el-color-warning: #e6a23c;
    --el-color-danger: #f56c6c;
    --el-font-size-base: 14px;
}
```

#### 使用 SCSS

```scss
$--color-primary: #409eff;

@import "element-plus/theme-chalk/src/common/var.scss";
@import "element-plus/packages/theme-chalk/src/index.scss";
```

## 專案結構範例

```
my-element-app/
├── src/
│   ├── main.js
│   ├── App.vue
│   └── components/
│       └── HelloWorld.vue
├── index.html
├── package.json
└── vite.config.js
```

## 下一步

安裝完成後，請參閱 [使用指南](./2.使用指南.md) 了解如何使用 Element Plus 組件。

## 常見問題

### 圖示不顯示？

請確保已全域註冊圖示：

```javascript
import * as icons from '@element-plus/icons-vue'
for (const [key, icon] of Object.entries(icons)) {
    app.component(key, icon)
}
```

### 樣式不生效？

請確保已引入 Element Plus 樣式：

```javascript
import 'element-plus/dist/index.css'
```

### Vue 版本不相符？

Element Plus 需要 Vue 3。請檢查您的 Vue 版本：

```bash
npm list vue
```

## 相關連結

- [官方網站](https://element-plus.org/)
- [GitHub](https://github.com/element-plus/element-plus)
- [設計指南](https://element-plus.org/zh-TW/guide/design.html)
- [組件文檔](https://element-plus.org/zh-TW/component/button.html)
