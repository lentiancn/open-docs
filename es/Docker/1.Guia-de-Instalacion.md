# Docker Guía de Instalación

> 🎯 Guía de instalación detallada de Docker
> 
> 📅 Actualizado: Febrero 2026
> 
> 📖 Versión: Docker 27.x

---

## 1. Introducción

### 1.1 ¿Qué es Docker?

Docker es una plataforma de código abierto para desarrollar, implementar y ejecutar aplicaciones.

### 1.2 Versiones

| Versión | Fecha |
|---------|-------|
| Docker 27.x | 2024 |
| Docker 26.x | 2024 |

---

## 2. Requisitos

### 2.1 Windows
- Windows 10 64 bits Pro/Enterprise o Windows 11
- 4GB RAM
- 64GB SSD

### 2.2 macOS
- macOS 10.15+ 
- 4GB RAM

---

## 3. Instalar en Windows

### Docker Desktop (Recomendado)

1. Descarga: https://www.docker.com/products/docker-desktop
2. Instala y ejecuta
3. Verifica:
```powershell
docker --version
docker run hello-world
```

---

## 4. Instalar en macOS

### Docker Desktop (Recomendado)

1. Descarga: https://www.docker.com/products/docker-desktop
2. Arrastra Docker a Applications
3. Verifica:
```bash
docker --version
docker run hello-world
```

---

## 5. Instalar en Linux

### Ubuntu

```bash
# Instalar dependencias
sudo apt install ca-certificates curl gnupg lsb-release

# Añadir clave GPG
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg

# Añadir repositorio
echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list

# Instalar Docker
sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

# Iniciar
sudo systemctl start docker
sudo systemctl enable docker
```

---

## 6. Verificar

```bash
docker --version
docker run hello-world
```

---

## 7. Configuración

### Mirrors (China)

```json
{
  "registry-mirrors": [
    "https://docker.mirrors.ustc.edu.cn"
  ]
}
```

---

## 8. Preguntas Frecuentes

### ¿Docker Desktop no inicia?

Asegúrate de tener virtualización habilitada en BIOS y WSL 2 instalado.

### ¿Descarga lenta?

Configura mirrors locales.

### ¿Sin permisos?

```bash
sudo usermod -aG docker $USER
```

---

## 📚 Recursos

- Docs: https://docs.docker.com

*AI*
