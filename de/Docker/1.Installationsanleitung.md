# Docker Installationsanleitung

> 🎯 Detaillierte Docker Installationsanleitung
> 
> 📅 Aktualisiert: Februar 2026
> 
> 📖 Version: Docker 27.x

---

## 1. Einführung

### Was ist Docker?

Docker ist eine Open-Source-Plattform für Entwicklung, Bereitstellung und Ausführung von Anwendungen.

### Versionen

| Version | Datum |
|---------|-------|
| Docker 27.x | 2024 |
| Docker 26.x | 2024 |

---

## 2. Anforderungen

### Windows
- Windows 10 64-bit Pro/Enterprise oder Windows 11
- 4GB RAM

### macOS
- macOS 10.15+
- 4GB RAM

---

## 3. Installation Windows

### Docker Desktop (Empfohlen)

1. Herunterladen: https://www.docker.com/products/docker-desktop
2. Installieren
3. Prüfen:
```powershell
docker --version
docker run hello-world
```

---

## 4. Installation macOS

### Docker Desktop (Empfohlen)

1. Herunterladen: https://www.docker.com/products/docker-desktop
2. Installieren
3. Prüfen:
```bash
docker --version
docker run hello-world
```

---

## 5. Installation Linux

### Ubuntu

```bash
# Abhängigkeiten
sudo apt install ca-certificates curl gnupg lsb-release

# Docker GPG-Schlüssel
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg

# Repository
echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list

# Installieren
sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

# Starten
sudo systemctl start docker
sudo systemctl enable docker
```

---

## 6. Verifizieren

```bash
docker --version
docker run hello-world
```

---

## 7. Konfiguration

### Mirrors (China)

```json
{
  "registry-mirrors": [
    "https://docker.mirrors.ustc.edu.cn"
  ]
}
```

---

## 8. FAQ

### Docker Desktop startet nicht?

Stelle sicher, dass Virtualisierung im BIOS aktiviert ist.

### Keine Rechte?

```bash
sudo usermod -aG docker $USER
```

---

## 📚 Ressourcen

- Docs: https://docs.docker.com

*AI*
