# Oracle Database 使用手册

## 数据库连接

### SQL*Plus 连接
```sql
-- 本地连接
sqlplus username/password@localhost:1521/SID

-- 远程连接
sqlplus username/password@hostname:1521/service_name

-- 系统管理员连接
sqlplus / as sysdba
```

### JDBC 连接字符串
```java
// Oracle Database 19c/26ai
String url = "jdbc:oracle:thin:@//hostname:1521/service_name";
String driver = "oracle.jdbc.OracleDriver";
```

### TNS 配置
```tns
ORCL =
  (DESCRIPTION =
    (ADDRESS = (PROTOCOL = TCP)(HOST = localhost)(PORT = 1521))
    (CONNECT_DATA =
      (SERVER = DEDICATED)
      (SERVICE_NAME = ORCL)
    )
  )
```

## SQL 基础操作

### 数据定义语言 (DDL)
```sql
-- 创建表
CREATE TABLE employees (
    employee_id NUMBER PRIMARY KEY,
    first_name VARCHAR2(50),
    last_name VARCHAR2(50) NOT NULL,
    email VARCHAR2(100) UNIQUE,
    hire_date DATE DEFAULT SYSDATE,
    salary NUMBER(8,2)
);

-- 修改表结构
ALTER TABLE employees ADD department_id NUMBER;
ALTER TABLE employees MODIFY salary NUMBER(10,2);
ALTER TABLE employees DROP COLUMN department_id;

-- 删除表
DROP TABLE employees;
```

### 数据操作语言 (DML)
```sql
-- 插入数据
INSERT INTO employees (employee_id, first_name, last_name, email, salary)
VALUES (101, 'John', 'Doe', 'john.doe@example.com', 5000.00);

-- 查询数据
SELECT employee_id, first_name, last_name, salary
FROM employees
WHERE salary > 4000
ORDER BY last_name;

-- 更新数据
UPDATE employees
SET salary = salary * 1.1
WHERE department_id = 10;

-- 删除数据
DELETE FROM employees
WHERE employee_id = 101;
```

## PL/SQL 编程

### 存储过程
```sql
CREATE OR REPLACE PROCEDURE update_employee_salary(
    p_employee_id IN NUMBER,
    p_new_salary IN NUMBER
)
IS
BEGIN
    UPDATE employees
    SET salary = p_new_salary
    WHERE employee_id = p_employee_id;
    
    IF SQL%ROWCOUNT = 0 THEN
        RAISE_APPLICATION_ERROR(-20001, 'Employee not found');
    END IF;
    
    COMMIT;
EXCEPTION
    WHEN OTHERS THEN
        ROLLBACK;
        RAISE;
END;
/
```

### 函数
```sql
CREATE OR REPLACE FUNCTION get_employee_name(
    p_employee_id IN NUMBER
) RETURN VARCHAR2
IS
    v_full_name VARCHAR2(101);
BEGIN
    SELECT first_name || ' ' || last_name
    INTO v_full_name
    FROM employees
    WHERE employee_id = p_employee_id;
    
    RETURN v_full_name;
EXCEPTION
    WHEN NO_DATA_FOUND THEN
        RETURN NULL;
END;
/
```

### 触发器
```sql
CREATE OR REPLACE TRIGGER employees_audit_trigger
    BEFORE INSERT OR UPDATE OR DELETE ON employees
    FOR EACH ROW
DECLARE
    v_action VARCHAR2(10);
BEGIN
    IF INSERTING THEN
        v_action := 'INSERT';
    ELSIF UPDATING THEN
        v_action := 'UPDATE';
    ELSE
        v_action := 'DELETE';
    END IF;
    
    INSERT INTO employees_audit (
        employee_id, action, action_date, user_name
    ) VALUES (
        NVL(:OLD.employee_id, :NEW.employee_id),
        v_action,
        SYSDATE,
        USER
    );
END;
/
```

## 性能优化

### 索引管理
```sql
-- 创建 B-tree 索引
CREATE INDEX idx_employees_last_name ON employees(last_name);

-- 创建位图索引（适用于低基数列）
CREATE BITMAP INDEX idx_employees_department ON employees(department_id);

-- 创建函数索引
CREATE INDEX idx_employees_upper_name ON employees(UPPER(last_name));
```

### 执行计划分析
```sql
-- 查看执行计划
EXPLAIN PLAN FOR
SELECT * FROM employees WHERE last_name = 'Smith';

SELECT * FROM TABLE(DBMS_XPLAN.DISPLAY);
```

### 统计信息收集
```sql
-- 收集表统计信息
EXEC DBMS_STATS.GATHER_TABLE_STATS('HR', 'EMPLOYEES');

-- 收集模式统计信息
EXEC DBMS_STATS.GATHER_SCHEMA_STATS('HR');
```

## 备份与恢复

### RMAN 备份
```bash
# 连接 RMAN
rman target /

# 完整数据库备份
BACKUP DATABASE;

# 增量备份
BACKUP INCREMENTAL LEVEL 1 DATABASE;

# 归档日志备份
BACKUP ARCHIVELOG ALL;
```

### 数据泵导出/导入
```bash
# 导出整个数据库
expdp system/password FULL=Y DIRECTORY=DATA_PUMP_DIR DUMPFILE=full_db.dmp

# 导出特定用户
expdp hr/hr SCHEMAS=HR DIRECTORY=DATA_PUMP_DIR DUMPFILE=hr_schema.dmp

# 导入数据
impdp system/password DIRECTORY=DATA_PUMP_DIR DUMPFILE=hr_schema.dmp
```

## 高可用性功能

### Real Application Clusters (RAC)
- 多节点集群架构
- 共享存储配置
- 服务负载均衡
- 故障自动切换

### Data Guard
- 主备数据库同步
- 物理备库和逻辑备库
- 自动故障转移
- 读写分离

## 监控与管理

### 动态性能视图
```sql
-- 查看会话信息
SELECT sid, serial#, username, status, machine, program
FROM v$session
WHERE username IS NOT NULL;

-- 查看锁信息
SELECT session_id, lock_type, mode_held, mode_requested
FROM dba_locks;

-- 查看表空间使用情况
SELECT tablespace_name, 
       ROUND(SUM(bytes)/1024/1024, 2) AS size_mb,
       ROUND(SUM(maxbytes)/1024/1024, 2) AS max_size_mb
FROM dba_data_files
GROUP BY tablespace_name;
```

### AWR 报告
```sql
-- 生成 AWR 报告
@?/rdbms/admin/awrrpt.sql
```

## Oracle Database 26ai 新特性

Oracle Database 26ai 引入了 AI 驱动的自动化功能：
- **自动 SQL 调优**：AI 分析查询模式并自动优化
- **预测性维护**：预测潜在问题并提前预警
- **智能资源管理**：动态调整内存和 CPU 分配
- **增强的安全监控**：AI 驱动的异常检测

详细的新特性文档请参考 Oracle 官方网站。

## 最佳实践

### 安全配置
- 使用强密码策略
- 启用透明数据加密 (TDE)
- 配置细粒度访问控制
- 定期审计数据库活动

### 性能优化
- 合理设计表结构和索引
- 使用绑定变量避免硬解析
- 定期收集统计信息
- 监控和优化慢查询

### 高可用性
- 实施适当的备份策略
- 配置 Data Guard 或 RAC
- 定期测试恢复流程
- 监控系统健康状态

## 参考文档

- Oracle Database 19c SQL 参考：https://docs.oracle.com/en/database/oracle/oracle-database/19/sqlrf/index.html
- Oracle Database 19c PL/SQL 参考：https://docs.oracle.com/en/database/oracle/oracle-database/19/lnpls/index.html
- Oracle Database 26ai 文档：https://docs.oracle.com/en/database/oracle/oracle-database/26/index.html