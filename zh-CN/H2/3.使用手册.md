# H2 数据库使用手册

## 基础操作

### 创建数据库

嵌入式模式（内存数据库）：
```java
String url = "jdbc:h2:mem:testdb";
Connection conn = DriverManager.getConnection(url, "sa", "");
```

文件数据库：
```java
String url = "jdbc:h2:file:./data/testdb";
Connection conn = DriverManager.getConnection(url, "sa", "password");
```

### 创建表

```sql
CREATE TABLE users (
    id INT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(100) NOT NULL,
    email VARCHAR(100) UNIQUE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 插入数据

```sql
INSERT INTO users (name, email) VALUES ('张三', 'zhangsan@example.com');
INSERT INTO users (name, email) VALUES ('李四', 'lisi@example.com');
```

### 查询数据

```sql
SELECT * FROM users;
SELECT * FROM users WHERE name = '张三';
SELECT COUNT(*) FROM users;
```

### 更新数据

```sql
UPDATE users SET email = 'newemail@example.com' WHERE id = 1;
```

### 删除数据

```sql
DELETE FROM users WHERE id = 2;
```

## Java 代码示例

### 完整的 JDBC 示例

```java
import java.sql.*;

public class H2Example {
    public static void main(String[] args) throws Exception {
        // 加载驱动
        Class.forName("org.h2.Driver");
        
        // 连接数据库
        String url = "jdbc:h2:mem:testdb";
        Connection conn = DriverManager.getConnection(url, "sa", "");
        
        // 创建表
        Statement stmt = conn.createStatement();
        stmt.execute("CREATE TABLE users (id INT PRIMARY KEY AUTO_INCREMENT, name VARCHAR(100))");
        
        // 插入数据
        stmt.execute("INSERT INTO users (name) VALUES ('张三')");
        stmt.execute("INSERT INTO users (name) VALUES ('李四')");
        
        // 查询数据
        ResultSet rs = stmt.executeQuery("SELECT * FROM users");
        while (rs.next()) {
            System.out.println(rs.getInt("id") + ": " + rs.getString("name"));
        }
        
        // 关闭连接
        rs.close();
        stmt.close();
        conn.close();
    }
}
```

### 使用 PreparedStatement

```java
PreparedStatement pstmt = conn.prepareStatement("INSERT INTO users (name, email) VALUES (?, ?)");
pstmt.setString(1, "王五");
pstmt.setString(2, "wangwu@example.com");
pstmt.executeUpdate();
pstmt.close();
```

### 使用事务

```java
conn.setAutoCommit(false);
try {
    Statement stmt = conn.createStatement();
    stmt.execute("INSERT INTO users (name) VALUES ('赵六')");
    stmt.execute("INSERT INTO users (name) VALUES ('孙七')");
    conn.commit();
} catch (Exception e) {
    conn.rollback();
} finally {
    conn.setAutoCommit(true);
}
```

## 高级特性

### 数据库模式切换

H2 可以模拟其他数据库的 SQL 语法：

```java
// 模拟 MySQL 模式
String url = "jdbc:h2:mem:testdb;MODE=MySQL";

// 模拟 PostgreSQL 模式
String url = "jdbc:h2:mem:testdb;MODE=PostgreSQL";

// 模拟 Oracle 模式
String url = "jdbc:h2:mem:testdb;MODE=Oracle";
```

### 数据库加密

```java
// 使用加密数据库
String url = "jdbc:h2:file:./data/encrypted;CIPHER=AES";
Connection conn = DriverManager.getConnection(url, "sa", "password");
```

### 跟踪功能

```java
// 启用 SQL 跟踪
String url = "jdbc:h2:mem:testdb;TRACE_LEVEL_FILE=3";
```

### 启动 TCP 服务器

```java
import org.h2.tools.Server;

Server server = Server.createTcpServer().start();
System.out.println("H2 TCP Server started: " + server.getURL());
```

### 使用 H2 控制台

启动 H2 Web 控制台：

```bash
java -jar h2.jar -web
```

然后在浏览器中打开：http://localhost:8082

## 常用 SQL 语句

### 数据类型

H2 支持多种数据类型，包括：
- 整数类型：INT、INTEGER、BIGINT、SMALLINT、TINYINT
- 浮点类型：FLOAT、REAL、DOUBLE、DECIMAL
- 字符串类型：VARCHAR、CHAR、CLOB
- 日期时间类型：DATE、TIME、TIMESTAMP
- 布尔类型：BOOLEAN
- 二进制类型：BLOB、BINARY

### 索引操作

创建索引：
```sql
CREATE INDEX idx_name ON users(name);
```

删除索引：
```sql
DROP INDEX idx_name;
```

### 约束操作

添加主键：
```sql
ALTER TABLE users ADD PRIMARY KEY (id);
```

添加外键：
```sql
ALTER TABLE orders ADD FOREIGN KEY (user_id) REFERENCES users(id);
```

### 查看表结构

```sql
DESCRIBE users;
-- 或
SHOW COLUMNS FROM users;
```

### 查看所有表

```sql
SHOW TABLES;
```

## 性能优化

### 批量操作

使用批量操作提高性能：

```java
conn.setAutoCommit(false);
PreparedStatement pstmt = conn.prepareStatement("INSERT INTO users (name) VALUES (?)");
for (int i = 0; i < 1000; i++) {
    pstmt.setString(1, "User" + i);
    pstmt.addBatch();
}
pstmt.executeBatch();
conn.commit();
```

### 连接池

在生产环境中，建议使用连接池，如 HikariCP：

```java
HikariConfig config = new HikariConfig();
config.setJdbcUrl("jdbc:h2:file:./data/testdb");
config.setUsername("sa");
config.setPassword("");
config.setMaximumPoolSize(10);

HikariDataSource ds = new HikariDataSource(config);
```

## 备份与恢复

### 备份数据库

```sql
SCRIPT TO 'backup.sql';
```

### 恢复数据库

```sql
RUNSCRIPT FROM 'backup.sql';
```

## 常见错误处理

### 数据库文件被锁定
确保没有其他连接在使用数据库文件。H2 支持并发读取，但写入需要独占访问。

### 内存数据库数据丢失
内存数据库在连接关闭后数据会丢失。如需持久化，请使用文件数据库模式。

### 类找不到
确保 org.h2.Driver 类在 classpath 中。
