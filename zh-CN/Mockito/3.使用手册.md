# Mockito 使用手册

## 创建 Mock 对象

创建 Mock 对象是使用 Mockito 进行测试的第一步。有多种方式可以创建 Mock 对象，开发者可以根据实际需求选择合适的方法。

### 使用 mock() 方法

最基本的方式是使用 Mockito 类的静态方法 mock() 来创建模拟对象。这个方法接受一个 Class 参数，返回该类的模拟实例。

```java
import static org.mockito.Mockito.*;

List<String> mockedList = mock(List.class);
Map<String, Integer> mockedMap = mock(Map.class);
```

从 Mockito 4.10.0 版本开始，可以省略 Class 参数，编译器会自动推断类型。

```java
List<String> mockedList = mock();
```

### 使用 @Mock 注解

@Mock 注解提供了一种更简洁的声明方式，特别适合需要创建多个 Mock 对象的测试类。使用注解需要先初始化 Mockito 框架。

```java
public class UserServiceTest {
    @Mock
    private UserRepository userRepository;
    
    @Mock
    private EmailService emailService;
    
    @Before
    public void setup() {
        MockitoAnnotations.initMocks(this);
    }
}
```

使用 MockitoJUnitRunner 可以自动完成初始化。

```java
@RunWith(MockitoJUnitRunner.class)
public class UserServiceTest {
    @Mock
    private UserRepository userRepository;
}
```

使用 JUnit 5 的 @ExtendWith 注解同样可以实现自动初始化。

```java
@ExtendWith(MockitoExtension.class)
public class UserServiceTest {
    @Mock
    private UserRepository userRepository;
}
```

## 设定方法行为

设定方法行为也称为 Stubbing（打桩），用于定义 Mock 对象在特定方法调用时返回什么值或抛出什么异常。

### 基本返回值设定

使用 when().thenReturn() 链式语法设定返回值。

```java
when(mockedList.size()).thenReturn(100);
when(mockedList.get(0)).thenReturn("first");
when(mockedList.get(1)).thenReturn("second");
```

可以连续设定多个返回值，下次调用时会依次返回不同的值。

```java
when(mockedList.get(anyInt()))
    .thenReturn("first")
    .thenReturn("second")
    .thenThrow(new RuntimeException("Error"));

System.out.println(mockedList.get(0)); // 输出: first
System.out.println(mockedList.get(0)); // 输出: second
System.out.println(mockedList.get(0)); // 抛出异常
```

### 设定异常抛出

使用 thenThrow() 可以设定方法调用时抛出异常。

```java
when(mockedList.get(0)).thenThrow(new IndexOutOfBoundsException("Index 0"));
```

对于返回类型为 void 的方法，使用 doThrow()。

```java
doThrow(new RuntimeException("Error")).when(mockedList).clear();
```

### 使用 Answer 自定义响应

如果预设的返回值无法满足需求，可以实现 Answer 接口来动态计算返回值。

```java
when(mockedList.get(anyInt())).thenAnswer(invocation -> {
    int index = invocation.getArgument(0);
    return "Value at index " + index;
});
```

## 验证交互

验证交互用于确认被测试代码正确地与 Mock 对象进行了预期的交互。

### 基本验证

使用 verify() 方法验证方法是否被调用。

```java
mockedList.add("one");
verify(mockedList).add("one");
```

### 验证调用次数

可以验证方法被调用的次数。

```java
mockedList.add("one");
mockedList.add("one");
mockedList.add("one");

verify(mockedList, times(3)).add("one");       // 恰好3次
verify(mockedList, atLeast(2)).add("one");     // 至少2次
verify(mockedList, atMost(3)).add("one");      // 最多3次
verify(mockedList, never()).add("two");        // 从未调用
verify(mockedList, atLeastOnce()).add("one");  // 至少1次
```

### 验证调用顺序

InOrder 验证器可以检查方法调用是否按照特定顺序执行。

```java
InOrder inOrder = inOrder(mockedList);
inOrder.verify(mockedList).add("first");
inOrder.verify(mockedList).add("second");
inOrder.verify(mockedList).add("third");
```

### 验证调用参数

可以使用参数匹配器来灵活验证调用参数。

```java
verify(mockedList).add(anyString());
verify(mockedList).get(anyInt());
verify(mockedList).contains(argThat(s -> s.length() > 5));
```

## 参数匹配器

Mockito 提供了丰富的参数匹配器，用于灵活地匹配方法参数。

### 内置匹配器

```java
anyInt()          // 任意整数
anyString()       // 任意字符串
any()             // 任意对象
anyList()         // 任意列表
anyMap()          // 任意映射
isNull()          // null值
isNotNull()       // 非null值
eq(value)         // 精确匹配
startsWith()      // 以指定字符串开头
endsWith()        // 以指定字符串结尾
contains()        // 包含指定字符串
```

### 自定义匹配器

通过 argThat() 方法可以创建自定义的参数匹配器。

```java
when(mockedList.add(argThat(s -> s != null && s.length() > 3)))
    .thenReturn(true);
```

## 使用 Spy

Spy 与 Mock 不同，它会保留真实对象的大部分行为，只对特定方法进行模拟。

### 创建 Spy

```java
List<String> realList = new ArrayList<>();
List<String> spyList = spy(realList);
```

或者使用 @Spy 注解。

```java
@Spy
private List<String> list = new ArrayList<>();
```

### 设定 Spy 的行为

与 Mock 不同，对 Spy 使用 when() 时要格外小心，因为这可能会影响真实方法的调用。建议使用 doReturn().when() 语法。

```java
doReturn(100).when(spyList).size();
```

Spy 会调用真实方法，除非明确设定了返回值。

```java
spyList.add("element");  // 真实方法会被调用
verify(spyList).add("element");  // 验证通过
```

## 使用 @InjectMocks

@InjectMocks 注解用于自动将被测试对象创建出来，并将带有 @Mock 或 @Spy 注解的字段注入其中。

```java
@RunWith(MockitoJUnitRunner.class)
public class UserServiceTest {
    @Mock
    private UserRepository userRepository;
    
    @Mock
    private EmailService emailService;
    
    @InjectMocks
    private UserService userService;
    
    @Test
    public void testCreateUser() {
        User user = new User("testUser");
        when(userRepository.save(any(User.class))).thenReturn(user);
        
        User result = userService.createUser("testUser");
        
        assertEquals("testUser", result.getUsername());
        verify(userRepository).save(any(User.class));
    }
}
```

## 使用 ArgumentCaptor

ArgumentCaptor 用于捕获方法调用时传递的参数，以便进行更详细的断言。

```java
@Mock
private List<String> mockedList;

@Test
public void testCaptureArgument() {
    mockedList.add("test");
    
    ArgumentCaptor<String> captor = ArgumentCaptor.forClass(String.class);
    verify(mockedList).add(captor.capture());
    
    assertEquals("test", captor.getValue());
}
```

当方法被多次调用时，可以捕获所有调用的参数。

```java
mockedList.add("first");
mockedList.add("second");
mockedList.add("third");

ArgumentCaptor<String> captor = ArgumentCaptor.forClass(String.class);
verify(mockedList, times(3)).add(captor.capture());

assertEquals(3, captor.getAllValues().size());
assertEquals("second", captor.getAllValues().get(1));
```

## BDD 风格测试

Mockito 支持行为驱动开发（BDD）风格的测试，使用 given().willReturn() 语法。

```java
import static org.mockito.BDDMockito.*;

@Test
public void testBddStyle() {
    // Given
    given(mockedList.get(0)).willReturn("first");
    
    // When
    String result = mockedList.get(0);
    
    // Then
    assertEquals("first", result);
}
```

## 常见使用模式

### 测试 void 方法

```java
@Test
public void testVoidMethod() {
    doNothing().when(mockedList).clear();
    mockedList.clear();
    verify(mockedList).clear();
}
```

### 模拟方法调用链

```java
@Test
public void testMethodChaining() {
    when(mockedList.iterator()).thenReturn(Collections.emptyIterator());
    
    Iterator<String> iterator = mockedList.iterator();
    assertFalse(iterator.hasNext());
}
```

### 模拟异常处理

```java
@Test(expected = RuntimeException.class)
public void testException() {
    when(mockedList.get(0)).thenThrow(new RuntimeException("Error"));
    mockedList.get(0);
}
```

## 最佳实践

使用 Mockito 时应遵循一些最佳实践以确保测试代码的质量和可维护性。不要模拟你拥有的类或值对象，这会破坏测试的真实性。只模拟必要的依赖，保持测试的真实性。每个测试方法应该只测试一个功能点。合理组织测试代码，使用 @Before 进行公共设置。验证交互要适度，关注重要的交互，不要验证每个方法调用。
