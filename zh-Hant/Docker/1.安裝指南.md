# Docker 安裝指南

> 🎯 本文詳細介紹 Docker 在各種操作系統環境下的安裝方法
> 
> 📅 更新時間：2026年2月
> 
> 📖 適用版本：Docker 27.x（當前最新版本）

---

## 目錄

1. [Docker 簡介](#1-docker-簡介)
2. [前置要求](#2-前置要求)
3. [在 Windows 上安裝](#3-在-windows-上安裝)
4. [在 macOS 上安裝](#4-在-macos-上安裝)
5. [在 Linux 上安裝](#5-在-linux-上安裝)
6. [驗證安裝](#6-驗證安裝)
7. [Docker Desktop 與 Docker Engine](#7-docker-desktop-與-docker-engine)
8. [配置 Docker](#8-配置-docker)
9. [卸載 Docker](#9-卸載-docker)
10. [常見問題](#10-常見問題)

---

## 1. Docker 簡介

### 1.1 什麼是 Docker？

Docker 是一個開源的容器化平台，用於開發、部署和運行應用程序。通過容器化技術，Docker 可以將應用程序及其依賴打包成輕量級、可移植的容器。

### 1.2 Docker 的核心概念

- **鏡像（Image）**：唯讀模板，用於創建容器
- **容器（Container）**：鏡像的運行實例
- **倉庫（Repository）**：存儲鏡像的地方
- **Dockerfile**：定義鏡像構建腳本

### 1.3 Docker 版本

| 版本 | 發布日期 | 說明 |
|------|----------|------|
| Docker 27.x | 2024年 | 最新穩定版 |
| Docker 26.x | 2024年 | 穩定版 |
| Docker 25.x | 2023年 | 穩定版 |
| Docker 24.x | 2023年 | 穩定版 |

---

## 2. 前置要求

### 2.1 Windows 要求

| 要求 | 最低版本 |
|------|----------|
| 操作系統 | Windows 10 64位 專業版/企業版 或 Windows 11 |
| 內存 | 4GB RAM |
| 磁盤 | 至少 64GB SSD |
| 虛擬化 | 必須啟用 BIOS 虛擬化 |

**注意**：Windows 家庭版不支持 Docker Desktop，需要使用 Docker Toolbox 或升級到專業版。

### 2.2 macOS 要求

| 要求 | 最低版本 |
|------|----------|
| 操作系統 | macOS 10.15 (Catalina) 或更高 |
| 內存 | 4GB RAM |
| 磁盤 | 至少 64GB SSD |
| 芯片 | Intel 或 Apple Silicon (M1/M2/M3) |

### 2.3 Linux 要求

| 組件 | 要求 |
|------|------|
| 內核 | 3.10 或更高 |
| 架構 | x86_64, ARM64, ARMv7 |
| 存儲驅動 | overlay2 |

---

## 3. 在 Windows 上安裝

### 3.1 方法一：Docker Desktop（推薦）

#### 步驟 1：下載 Docker Desktop

1. 訪問 Docker 官網：https://www.docker.com/products/docker-desktop
2. 點擊 "Download for Windows"
3. 運行下載的安裝程序 `Docker Desktop Installer.exe`

#### 步驟 2：安裝 Docker Desktop

1. 雙擊安裝程序
2. 勾選 "Use WSL 2 instead of Hyper-V"（推薦）
3. 點擊 "Install"
4. 安裝完成後，點擊 "Close"

#### 步驟 3：啟動 Docker Desktop

1. 在開始菜單找到 "Docker Desktop"
2. 首次啟動需要登錄 Docker Hub可選）
3. 等待 Docker 引擎啟動完成

#### 步驟 4：驗證安裝

打開 PowerShell 或命令提示符，執行：

```powershell
docker --version
docker-compose --version
docker run hello-world
```

### 3.2 方法二：使用 Winget

```powershell
winget install Docker.DockerDesktop
```

---

## 4. 在 macOS 上安裝

### 4.1 方法一：Docker Desktop（推薦）

#### 步驟 1：下載 Docker Desktop

1. 訪問 https://www.docker.com/products/docker-desktop
2. 點擊 "Download for Mac"
3. 選擇芯片類型：
   - Apple Silicon: "Apple Chip"
   - Intel: "Intel Chip"

#### 步驟 2：安裝 Docker Desktop

1. 雙擊 `Docker.dmg`
2. 將 Docker 圖標拖入 Applications 文件夾
3. 啟動 Docker Desktop

#### 步驟 3：驗證安裝

打開終端，執行：

```bash
docker --version
docker-compose --version
docker run hello-world
```

### 4.2 方法二：使用 Homebrew

```bash
# 安裝 Homebrew（如果沒有）
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# 安裝 Docker
brew install --cask docker
```

---

## 5. 在 Linux 上安裝

### 5.1 Ubuntu / Debian

#### 步驟 1：卸載舊版本

```bash
sudo apt remove docker docker-engine docker.io containerd runc
```

#### 步驟 2：安裝依賴

```bash
sudo apt update
sudo apt install ca-certificates curl gnupg lsb-release
```

#### 步驟 3：添加 Docker GPG 密鑰

```bash
sudo install -m 0755 -d /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
sudo chmod a+r /etc/apt/keyrings/docker.gpg
```

#### 步驟 4：添加 Docker 倉庫

```bash
echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
```

#### 步驟 5：安裝 Docker

```bash
sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

### 5.2 啟動 Docker

```bash
# 啟動 Docker 服務
sudo systemctl start docker

# 設置開機自啟
sudo systemctl enable docker

# 驗證安裝
sudo docker run hello-world
```

---

## 6. 驗證安裝

### 6.1 檢查 Docker 版本

```bash
docker --version
docker-compose --version
```

### 6.2 運行測試容器

```bash
docker run hello-world
```

成功輸出：

```
Hello from Docker!
This message shows that your installation appears to be working correctly.
```

---

## 7. 配置 Docker

### 7.1 配置 Docker 鏡像加速器

中國地區用戶建議配置國內鏡像加速：

```json
{
  "registry-mirrors": [
    "https://docker.mirrors.ustc.edu.cn",
    "https://hub-mirror.c.163.com"
  ]
}
```

#### Linux 配置

```bash
sudo mkdir -p /etc/docker
sudo tee /etc/docker/daemon.json <<EOF
{
  "registry-mirrors": [
    "https://docker.mirrors.ustc.edu.cn",
    "https://hub-mirror.c.163.com"
  ]
}
EOF
sudo systemctl daemon-reload
sudo systemctl restart docker
```

---

## 8. 常見問題

### Q1: Docker Desktop 在 Windows 上啟動失敗？

**解決方案**：
1. 確保 BIOS 中已啟用虛擬化
2. 確保 WSL 2 已正確安裝
3. 重啟電腦後重試

---

### Q2: docker run 權限不足？

**解決方案**：
```bash
sudo usermod -aG docker $USER
# 重新登錄
```

---

### Q3: 鏡像下載太慢？

**解決方案**：
1. 配置國內鏡像加速器
2. 使用代理

---

## 📚 相關資源

- 官方文檔：https://docs.docker.com
- Docker Hub：https://hub.docker.com

---

*本文檔由 AI 助手生成*
