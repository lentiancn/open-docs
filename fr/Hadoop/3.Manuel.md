# Manuel Hadoop

## Commandes HDFS

```bash
hdfs dfs -ls /
hdfs dfs -mkdir -p /user/data
hdfs dfs -put local.txt /user/data/
hdfs dfs -get /user/data/output.txt
```

## MapReduce

Exemple WordCount.

## YARN

```bash
hadoop jar wordcount.jar WordCount /input /output
yarn application -list
```

## Optimisation

1. Ajuster la taille des blocs
2. Compression des données
3. Optimiser Map/Reduce
