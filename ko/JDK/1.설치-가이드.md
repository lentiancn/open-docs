# JDK 설치 가이드

JDK(Java Development Kit)는 공식 Java 개발 키트입니다. 이 가이드에서는 다양한 환경에 대한 설치 방법을 설명합니다.

## 버전 정보

| 버전 | 상태 | 설명 |
|------|------|------|
| JDK 21 (LTS) | ✅ 현재 | 최신 LTS 버전 |
| JDK 17 (LTS) | ✅ 지원 | 이전 LTS |
| JDK 11 (LTS) | ✅ 지원 | 레거시 LTS |
| JDK 8 | ✅ 지원 | 레거시 버전 |
| JDK 22 | ✅ 최신 | 최신 기능 버전 |

### LTS와 기능 버전

- **LTS (Long Term Support)**: 2~3년 업데이트, 프로덕션 환경 권장
- **기능 버전**: 6개월마다 새로운 기능, 짧은 지원 기간

## 시스템 요구사항

### 하드웨어 요구사항

| 요구사항 | 최소 | 권장 |
|----------|------|------|
| CPU | 1코어 | 2+코어 |
| RAM | 2GB | 4+GB |
| 디스크 | 2GB | 10+GB |

### 소프트웨어 요구사항

| 소프트웨어 | 버전 |
|-----------|------|
| OS | Windows/macOS/Linux |
| RAM | 개발 시 2+GB |

## 설치 방법

### 방법 1: Oracle JDK

#### Windows

1. https://www.oracle.com/java/technologies/downloads/ 에서 다운로드
2. 설치 프로그램 실행
3. 마법사 따라하기

#### macOS

```bash
brew install openjdk@21
```

#### Linux

```bash
wget https://download.oracle.com/java/21/latest/jdk-21_linux-x64_bin.tar.gz
tar -xzf jdk-21_linux-x64_bin.tar.gz
sudo mv jdk-21 /opt/jdk-21
```

### 방법 2: Eclipse Temurin (Adoptium)

#### Windows

1. https://adoptium.net/temurin/releases/ 에서 다운로드
2. 설치 프로그램 실행

#### macOS

```bash
brew install --cask temurin
```

### 방법 3: SDKMAN

```bash
curl -s "https://get.sdkman.io" | bash
sdk install java 21.0.2-tem
```

### 방법 4: Docker

```dockerfile
FROM eclipse-temurin:21-jdk
WORKDIR /app
COPY . .
RUN javac Main.java
CMD ["java", "Main"]
```

### 방법 5: Windows 패키지 관리자

```powershell
choco install openjdk21
```

## 환경 변수 설정

### Windows

```powershell
[System.Environment]::SetEnvironmentVariable("JAVA_HOME", "C:\Program Files\Java\jdk-21", "User")
[System.Environment]::SetEnvironmentVariable("PATH", "$env:PATH;C:\Program Files\Java\jdk-21\bin", "User")
```

### macOS/Linux

```bash
export JAVA_HOME=/opt/jdk-21
export PATH=$JAVA_HOME/bin:$PATH
source ~/.bashrc
```

## 설치 확인

```bash
java -version
javac -version
echo $JAVA_HOME
```

## 여러 JDK 버전 사용

```bash
sdk install java 17.0.8-tem
sdk install java 21.0.2-tem
sdk use java 17.0.8-tem
```

## 일반적인 문제

### "java"를 찾을 수 없음?

PATH에 Java bin 디렉터리가 포함되어 있는지 확인.

### 잘못된 Java 버전?

```bash
which java
java -version
```

## 관련 링크

- [Oracle JDK](https://www.oracle.com/java/technologies/downloads/)
- [Eclipse Temurin](https://adoptium.net/temurin/)
- [SDKMAN](https://sdkman.io/)
