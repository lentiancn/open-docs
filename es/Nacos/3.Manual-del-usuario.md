# Manual del Usuario de Nacos

## Descripción General

Nacos proporciona dos funciones principales: descubrimiento de servicios y gestión de configuración. Este manual introducirá cómo usar estas funciones en detalle.

## Descubrimiento de Servicios

### Registro de Servicios

Los proveedores de servicios pueden registrar servicios en Nacos de las siguientes maneras:

#### Registrar Usando SDK Nativo

Registra servicios usando el SDK Java de Nacos:

```java
import com.alibaba.nacos.api.naming.NamingService;
import com.alibaba.nacos.api.naming.pojo.Instance;

NamingService namingService = NacosFactory.createNamingService("127.0.0.1:8848");

Instance instance = new Instance();
instance.setIp("192.168.1.100");
instance.setPort(8080);
instance.setEphemeral(true);

Map<String, String> metadata = new HashMap<>();
metadata.put("version", "1.0");
instance.setMetadata(metadata);

namingService.registerInstance("service-provider", instance);
```

#### Registrar Usando OpenAPI

```bash
curl -X POST 'http://127.0.0.1:8848/nacos/v3/client/ns/instance' \
  -d 'serviceName=service-provider' \
  -d 'ip=192.168.1.100' \
  -d 'port=8080'
```

### Descubrimiento de Servicios

Los consumidores de servicios pueden descubrir servicios de las siguientes maneras:

#### Usando SDK Nativo

```java
NamingService namingService = NacosFactory.createNamingService("127.0.0.1:8848");

// Obtener todas las instancias saludables
List<Instance> instances = namingService.selectInstances("service-provider", true);

// Usar balanceo de carga para seleccionar una instancia
Instance instance = namingService.selectOneHealthyInstance("service-provider");
```

#### Usando OpenAPI

```bash
# Obtener lista de servicios
curl -X GET 'http://127.0.0.1:8848/nacos/v3/client/ns/instance/list?serviceName=service-provider'
```

### Gestión de Metadatos de Servicios

Nacos soporta agregar y gestionar metadatos para servicios:

```java
Map<String, String> metadata = new HashMap<>();
metadata.put("version", "1.0");
metadata.put("env", "production");

namingService.registerInstance("service-name", instance);
```

## Gestión de Configuración

### Publicar Configuración

#### Usando OpenAPI

```bash
# Primero iniciar sesión para obtener token de acceso
curl -X POST 'http://127.0.0.1:8848/nacos/v3/auth/user/login' \
  -d 'username=nacos' \
  -d 'password=${your_password}'

# Usar token de acceso para crear configuración
curl -X POST 'http://127.0.0.1:8848/nacos/v3/admin/cs/config?dataId=example.config&groupName=DEFAULT_GROUP&content=HelloWorld' \
  -H "accessToken:${your_access_token}"
```

#### Usando SDK

```java
ConfigService configService = NacosFactory.createConfigService("127.0.0.1:8848");

boolean isPublished = configService.publishConfig(
    "example.config",
    "DEFAULT_GROUP",
    "Hello Nacos"
);
```

### Obtener Configuración

#### Usando OpenAPI

```bash
curl -X GET 'http://127.0.0.1:8848/nacos/v3/client/cs/config?dataId=example.config&groupName=DEFAULT_GROUP'
```

#### Usando SDK

```java
String config = configService.getConfig(
    "example.config",
    "DEFAULT_GROUP",
    5000
);
```

### Escucha de Configuración

#### Usando SDK

```java
configService.addListener(
    "example.config",
    "DEFAULT_GROUP",
    new Listener() {
        @Override
        public void receiveConfigInfo(String configInfo) {
            System.out.println("Configuración cambiada:" + configInfo);
        }

        @Override
        public Executor getExecutor() {
            return Executors.newSingleThreadExecutor();
        }
    }
);
```

## Integración de Nacos con Spring Cloud

### Prerrequisitos

Asegúrate de que Nacos Server esté instalado y iniciado.

### Agregar Dependencias

Agrega las siguientes dependencias en pom.xml de tu proyecto Spring Cloud:

```xml
<dependency>
    <groupId>com.alibaba.cloud</groupId>
    <artifactId>spring-cloud-starter-alibaba-nacos-config</artifactId>
    <version>2023.0.3.2</version>
</dependency>

<dependency>
    <groupId>com.alibaba.cloud</groupId>
    <artifactId>spring-cloud-starter-alibaba-nacos-discovery</artifactId>
    <version>2023.0.3.2</version>
</dependency>
```

### Gestión de Configuración

Configura la dirección del servidor Nacos en application.properties:

```properties
spring.application.name=springclouddemo
spring.config.import[0]=nacos:springclouddemo.properties?group=DEFAULT_GROUP
spring.cloud.nacos.config.server-addr=127.0.0.1:8848
spring.cloud.nacos.discovery.server-addr=127.0.0.1:8848
```

### Usando Anotaciones

#### Anotación @NacosConfig

Inyecta valores de configuración de Nacos directamente en propiedades de Spring Bean:

```java
@RestController
public class ConfigController {
    @NacosConfig(dataId = "routeconfig", group = "config", key = "rate")
    String rate;

    @RequestMapping("/rate")
    public String rate() {
        return rate;
    }
}
```

#### Anotación @NacosConfigListener

Recibe eventos de cambio de configuración de Nacos:

```java
@Service
public class MyRateConfigService {
    @NacosConfigListener(dataId = "routeconfig", group = "config")
    public void rate(String rateConfig) {
        System.out.println("Recibido cambio de configuración:" + rateConfig);
    }
}
```

### Registro y Descubrimiento de Servicios

Habilita el registro y descubrimiento de servicios en la aplicación Spring Boot:

```java
@SpringBootApplication
@EnableDiscoveryClient
public class NacosProviderApplication {
    public static void main(String[] args) {
        SpringApplication.run(NacosProviderApplication.class, args);
    }

    @RestController
    class EchoController {
        @RequestMapping(value = "/echo/{string}", method = RequestMethod.GET)
        public String echo(@PathVariable String string) {
            return "Hello Nacos Discovery " + string;
        }
    }
}
```

Configura en application.properties:

```properties
server.port=8070
spring.application.name=service-provider
spring.cloud.nacos.discovery.server-addr=127.0.0.1:8848
```

Configuración del consumidor de servicios:

```java
@SpringBootApplication
@EnableDiscoveryClient
public class NacosConsumerApplication {
    @LoadBalanced
    @Bean
    public RestTemplate restTemplate() {
        return new RestTemplate();
    }

    @RestController
    public class TestController {
        private final RestTemplate restTemplate;

        @Autowired
        public TestController(RestTemplate restTemplate) {
            this.restTemplate = restTemplate;
        }

        @RequestMapping(value = "/echo/{str}", method = RequestMethod.GET)
        public String echo(@PathVariable String str) {
            return restTemplate.getForObject("http://service-provider/echo/" + str, String.class);
        }
    }
}
```

## Gestión de Servicios MCP

Nacos 3.0 soporta el registro, descubrimiento y gestión de servicios MCP (Model Context Protocol).

### Auto-Registro de Servidor MCP

#### Usando Marco Spring AI Alibaba Nacos MCP

1. Agregar dependencias:

```xml
<dependency>
    <groupId>com.alibaba.cloud.ai</groupId>
    <artifactId>spring-ai-alibaba-starter-mcp-registry</artifactId>
    <version>1.0.0.3+</version>
</dependency>
<dependency>
    <groupId>org.springframework.ai</groupId>
    <artifactId>spring-ai-starter-mcp-server-webmvc</artifactId>
    <version>1.0.0+</version>
</dependency>
```

2. Configurar auto-registro:

```yaml
spring:
  application:
    name: mcp-nacos-registry-example
  ai:
    mcp:
      server:
        name: webmvc-mcp-server
        version: 1.0.0
        type: SYNC
        instructions: "This mcp server provides time information tools and resources"
  alibaba:
    mcp:
      nacos:
        server-addr: 127.0.0.1:8848
        namespace: public
        username: nacos
        password: nacos
        register:
          enabled: true
```

#### Usando Nacos MCP Wrapper Python

```python
pip install nacos-mcp-wrapper-python

from nacos_mcp_wrapper.server.nacos_mcp import NacosMCP
from nacos_mcp_wrapper.server.nacos_settings import NacosSettings

nacos_settings = NacosSettings()
nacos_settings.SERVER_ADDR = "127.0.0.1:8848"
nacos_settings.NAMESPACE = "public"
nacos_settings.USERNAME = "nacos"
nacos_settings.PASSWORD = "nacos"

mcp = NacosMCP("nacos-mcp-python", nacos_settings=nacos_settings, version="1.0.1", port=18001)

@mcp.tool()
def add(a: int, b: int) -> int:
    """Add two integers together"""
    return a + b

if __name__ == "__main__":
    mcp.run(transport="sse")
```

### Descubrimiento e Invocación de Servicios MCP

#### Usando Spring AI Alibaba para Descubrir Servidor MCP

```java
@Autowired
private List<LoadbalancedMcpSyncClient> loadbalancedMcpSyncClients;
```

O:

```java
@Qualifier("loadbalancedMcpToolCallbacks")
ToolCallbackProvider tools
```

Configurar application.yml:

```yaml
spring:
  alibaba:
    mcp:
      nacos:
        namespace: public
        server-addr: 127.0.0.1:8848
        username: nacos
        password: nacos
        client:
          enabled: true
```

## Uso de la Consola

Nacos proporciona una consola web para gestión visual:

- Dirección de la consola: http://127.0.0.1:8080/index.html
- Usuario por defecto: nacos
- Contraseña por defecto: nacos@demo (por favor cambia la contraseña en producción)

Funciones principales de la consola:

- Gestión de Servicios: Ver, editar y eliminar servicios registrados
- Gestión de Configuración: Crear, modificar, eliminar y consultar configuraciones
- Gestión de Espacios de Nombres: Crear espacios de nombres aislados
- Gestión de Usuarios: Gestionar usuarios de acceso y permisos
- Gestión de Clúster: Ver estado de nodos del clúster
