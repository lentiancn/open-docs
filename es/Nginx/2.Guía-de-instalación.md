# Guía de instalación de Nginx

## Instalación

```bash
# Ubuntu/Debian
sudo apt install nginx

# CentOS
sudo yum install nginx

# Docker
docker pull nginx
```

## Verificar
```bash
nginx -v
curl http://localhost
```
