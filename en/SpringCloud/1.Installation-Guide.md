# Spring Cloud Installation Guide

This guide covers how to install and set up Spring Cloud development environment.

## System Requirements

### Java and Spring Boot Version Compatibility

| Release Train | Spring Boot Version |
|---------------|-------------------|
| 2025.1.x (Oakwood) | 4.0.x |
| 2025.0.x (Northfields) | 3.5.x |
| 2024.0.x (Moorgate) | 3.4.x |
| 2023.0.x (Leyton) | 3.3.x, 3.2.x |
| 2022.0.x (Kilburn) | 3.0.x, 3.1.x |
| 2021.0.x (Jubilee) | 2.6.x, 2.7.x |

**Note:** Older versions (Dalston, Edgware, Finchley, Greenwich, Ilford) are no longer supported.

## Prerequisites

### Install Java

Spring Cloud requires Java 17 or later (for 2023.0+).

**Windows:**
```powershell
choco install openjdk21
```

**macOS:**
```bash
brew install openjdk@21
```

**Linux:**
```bash
sudo apt install openjdk-21-jdk
```

Verify:
```bash
java -version
```

### Install Maven or Gradle

**Maven:**
```bash
# macOS
brew install maven

# Linux
sudo apt install maven
```

**Gradle:**
```bash
# macOS
brew install gradle
```

## Create Spring Cloud Project

### Option 1: Spring Initializr

1. Visit [start.spring.io](https://start.spring.io/)
2. Select:
   - Project: Maven / Gradle
   - Language: Java
   - Spring Boot: 3.3.x (latest stable)
   - Group: com.example
   - Artifact: myapp
3. Add dependencies:
   - Spring Cloud Config Client
   - Spring Cloud Netflix Eureka
   - Spring Cloud Gateway
   - Spring Cloud OpenFeign
4. Click "Generate"

### Option 2: Command Line

```bash
curl https://start.spring.io/starter.zip \
  -d type=maven-project \
  -d language=java \
  -d bootVersion=3.3.0 \
  -d baseDir=myapp \
  -d dependencies=cloud-config-client,cloud-eureka,cloud-gateway,cloud-openfeign \
  -o myapp.zip
```

## Add Spring Cloud to Existing Project

### Maven Configuration

Add to `pom.xml`:

```xml
<properties>
    <spring-cloud.version>2023.0.3</spring-cloud.version>
</properties>

<dependencyManagement>
    <dependencies>
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-dependencies</artifactId>
            <version>${spring-cloud.version}</version>
            <type>pom</type>
            <scope>import</scope>
        </dependency>
    </dependencies>
</dependencyManagement>
```

### Gradle Configuration

Add to `build.gradle`:

```groovy
plugins {
    id 'java'
    id 'org.springframework.boot' version '3.3.0'
    id 'io.spring.dependency-management' version '1.1.7'
}

ext {
    set('springCloudVersion', "2023.0.3")
}

dependencyManagement {
    imports {
        mavenBom "org.springframework.cloud:spring-cloud-dependencies:${springCloudVersion}"
    }
}
```

### Add Dependencies

```xml
<!-- Config Client -->
<dependency>
    <groupId>org.springframework.cloud</groupId>
    <artifactId>spring-cloud-starter-config</artifactId>
</dependency>

<!-- Service Discovery (Eureka) -->
<dependency>
    <groupId>org.springframework.cloud</groupId>
    <artifactId>spring-cloud-starter-netflix-eureka-client</artifactId>
</dependency>

<!-- API Gateway -->
<dependency>
    <groupId>org.springframework.cloud</groupId>
    <artifactId>spring-cloud-starter-gateway</artifactId>
</dependency>

<!-- OpenFeign -->
<dependency>
    <groupId>org.springframework.cloud</groupId>
    <artifactId>spring-cloud-starter-openfeign</artifactId>
</dependency>
```

## Configure application.properties

```properties
# Application Name
spring.application.name=myapp

# Spring Cloud Config
spring.config.import=optional:configserver:http://localhost:8888

# Eureka Client
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.instance.prefer-ip-address=true

# Gateway (if using)
spring.cloud.gateway.routes[0].id=service1
spring.cloud.gateway.routes[0].uri=lb://service1
spring.cloud.gateway.routes[0].predicates[0]=Path=/service1/**
```

## Run the Application

```bash
./mvnw spring-boot:run
```

## Main Spring Cloud Projects

| Project | Description |
|--------|-------------|
| Spring Cloud Config | Centralized configuration management |
| Spring Cloud Gateway | Intelligent routing |
| Spring Cloud Netflix Eureka | Service discovery |
| Spring Cloud OpenFeign | REST client |
| Spring Cloud Stream | Event-driven messaging |
| Spring Cloud Task | Short-lived microservices |
| Spring Cloud Zookeeper | Service discovery with Zookeeper |
| Spring Cloud Contract | Consumer-driven contracts |

## Docker Support

```dockerfile
FROM eclipse-temurin:21-jdk-alpine AS build
WORKDIR /app
COPY pom.xml .
COPY src ./src
RUN apk add maven && mvn clean package -DskipTests

FROM eclipse-temurin:21-jre-alpine
WORKDIR /app
COPY --from=build /app/target/*.jar app.jar
EXPOSE 8080
ENTRYPOINT ["java", "-jar", "app.jar"]
```

## Related Links

- [Spring Cloud Official](https://spring.io/projects/spring-cloud)
- [Spring Cloud Reference](https://docs.spring.io/spring-cloud/docs/current/reference/)
- [Spring Cloud Releases](https://github.com/spring-cloud/spring-cloud-release/wiki)
- [Spring Initializr](https://start.spring.io/)
