# PostgreSQL Manual de Usuario

## Conectar

```bash
psql -U postgres
```

## Crear tabla

```sql
CREATE TABLE usuarios (
    id SERIAL PRIMARY KEY,
    nombre VARCHAR(50),
    email VARCHAR(100)
);
```

## Backup

```bash
pg_dump -U postgres miapp > backup.sql
```
