# Vue Guía de Instalación

Vue es un marco de JavaScript progresivo para construir interfaces de usuario.

## Versión

Vue tiene dos versiones principales en uso activo:

| Versión | Fecha de lanzamiento | Estado |
|---------|---------------------|--------|
| Vue 3 | Septiembre 2020 | Recomendado |
| Vue 2 | Octubre 2016 | Legado |

**Recomendación:** Use Vue 3 para todos los proyectos nuevos.

## Requisitos Previos

### Requisitos del Sistema

| Requisito | Vue 2 | Vue 3 |
|-----------|-------|-------|
| Node.js | 8.9+ | 16+ (18+ recomendado) |
| npm | 5.6+ | 8+ |

## Métodos de Instalación

### Método 1: Usando npm (Recomendado)

```bash
# Crear nuevo proyecto Vue
npm create vue@latest
```

### Método 2: Usando Vite (Recomendado para Vue 3)

```bash
# Crear proyecto Vue 3 con Vite
npm create vite@latest my-vue-app -- --template vue

cd my-vue-app
npm install
npm run dev
```

### Método 3: CDN

```html
<script src="https://unpkg.com/vue@3/dist/vue.global.js"></script>
```

## Configuración del Proyecto

### Estructura del Proyecto Vue 3 + Vite

```
my-vue-app/
├── public/
├── src/
│   ├── components/
│   ├── App.vue
│   └── main.js
├── index.html
├── package.json
└── vite.config.js
```

## Instalar Dependencias

```bash
npm install
npm install vue-router@4
npm install pinia
```

## Configurar Vue Router

```javascript
import { createRouter, createWebHistory } from 'vue-router'
import Home from './views/Home.vue'

const routes = [
  { path: '/', component: Home }
]

const router = createRouter({
  history: createWebHistory(),
  routes
})

export default router
```

## Pinia (Gestión de Estado)

```javascript
import { defineStore } from 'pinia'

export const useCounterStore = defineStore('counter', {
  state: () => ({
    count: 0
  }),
  actions: {
    increment() {
      this.count++
    }
  }
})
```

## Servidor de Desarrollo

```bash
# Iniciar servidor de desarrollo
npm run dev

# Construir para producción
npm run build
```

## Solución de Problemas

### npm install falla

```bash
# Limpiar caché npm
npm cache clean --force

# Eliminar node_modules y reinstalar
rm -rf node_modules
npm install
```

## Mejores Prácticas

1. **Usar Vue 3** - Para proyectos nuevos
2. **Usar Vite** - Desarrollo y construcción más rápidos
3. **Usar Composition API** - Mejor soporte TypeScript

## Enlaces Relacionados

- [Sitio Oficial](https://vuejs.org)
- [Documentación Vue 3](https://vuejs.org/guide/)
- [Vue Router](https://router.vuejs.org/)
- [Pinia](https://pinia.vuejs.org/)
