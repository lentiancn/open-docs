# MultiTalk 安装指南

MultiTalk 的安装过程需要配置 Python 环境和下载模型权重。以下是详细的安装步骤说明。

## 环境要求

### 硬件要求

MultiTalk 是一个大型视频生成模型，对硬件有较高要求。推荐使用 NVIDIA GPU 运行，显存需求根据输出分辨率不同而变化。生成 480P 视频需要至少 16GB 显存，生成 720P 视频则需要多 GPU 配置。项目也支持在低显存环境下运行，通过特定优化可在单张 RTX 4090 上生成 480P 视频。

### 软件要求

操作系统支持 Linux 系统。Python 版本需要 3.10 或更高版本。还需要 CUDA 12.1 或更高版本支持。项目依赖 PyTorch 2.4.1、xformers 等深度学习框架。

## 创建虚拟环境

首先创建一个新的 Python 虚拟环境来隔离项目依赖。打开终端，执行以下命令：

```bash
conda create -n multitalk python=3.10
conda activate multitalk
```

## 安装 PyTorch 和 xformers

在激活的环境中安装 PyTorch 相关包。执行以下命令安装 PyTorch 2.4.1：

```bash
pip install torch==2.4.1 torchvision==0.19.1 torchaudio==2.4.1 --index-url https://download.pytorch.org/whl/cu121
```

接下来安装 xformers：

```bash
pip install -U xformers==0.0.28 --index-url https://download.pytorch.org/whl/cu121
```

## 安装项目依赖

安装其他必要的依赖包：

```bash
pip install misaki[en]
pip install ninja
pip install psutil
pip install packaging
pip install flash_attn==2.7.4.post1
```

然后克隆项目仓库并安装项目依赖：

```bash
git clone https://github.com/MeiGen-AI/MultiTalk.git
cd MultiTalk
pip install -r requirements.txt
```

## 安装音频处理库

项目需要音频处理相关的库。执行以下命令安装：

```bash
conda install -c conda-forge librosa
conda install -c conda-forge ffmpeg
```

如果是 CentOS 或 RHEL 系统，使用以下命令：

```bash
sudo yum install ffmpeg ffmpeg-devel
```

## 下载模型权重

MultiTalk 需要下载多个模型文件才能正常运行。主要包括以下模型：

Wan2.1-I2V-14B-480P 是基础扩散模型，可从 Hugging Face 下载。chinese-wav2vec2-base 是音频编码器，用于处理输入音频。Kokoro-82M 是 TTS 权重，用于文本转语音功能。MeiGen-MultiTalk 是项目提供的音频条件权重。

使用 huggingface-cli 下载模型：

```bash
huggingface-cli download Wan-AI/Wan2.1-I2V-14B-480P --local-dir ./weights/Wan2.1-I2V-14B-480P
huggingface-cli download TencentGameMate/chinese-wav2vec2-base --local-dir ./weights/chinese-wav2vec2-base
huggingface-cli download hexgrad/Kokoro-82M --local-dir ./weights/Kokoro-82M
huggingface-cli download MeiGen-AI/MeiGen-MultiTalk --local-dir ./weights/MeiGen-MultiTalk
```

## 配置模型链接

模型下载完成后，需要进行链接配置。有两种方式可以选择。

方式一是使用符号链接。进入 weights 目录，执行以下命令：

```bash
cd weights
mv Wan2.1-I2V-14B-480P/diffusion_pytorch_model.safetensors.index.json Wan2.1-I2V-14B-480P/diffusion_pytorch_model.safetensors.index.json_old
ln -s /path/to/weights/MeiGen-MultiTalk/diffusion_pytorch_model.safetensors.index.json Wan2.1-I2V-14B-480P/
ln -s /path/to/weights/MeiGen-MultiTalk/multitalk.safetensors Wan2.1-I2V-14B-480P/
```

方式二是直接复制文件：

```bash
cd weights
mv Wan2.1-I2V-14B-480P/diffusion_pytorch_model.safetensors.index.json Wan2.1-I2V-14B-480P/diffusion_pytorch_model.safetensors.index.json_old
cp MeiGen-MultiTalk/diffusion_pytorch_model.safetensors_index.json Wan2.1-I2V-14B-480P/
cp MeiGen-MultiTalk/multitalk.safetensors Wan2.1-I2V-14B-480P/
```

## 验证安装

安装完成后，可以运行示例脚本验证安装是否成功。项目提供了多个示例文件，位于 examples 目录下。
