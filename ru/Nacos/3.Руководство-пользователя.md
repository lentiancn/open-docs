# Руководство пользователя Nacos

## Обзор

Nacos предоставляет две основные функции: обнаружение сервисов и управление конфигурацией. В этом руководстве подробно описано, как использовать эти функции.

## Обнаружение сервисов

### Регистрация сервисов

Провайдеры сервисов могут регистрировать сервисы в Nacos следующими способами:

#### Регистрация с использованием нативного SDK

Регистрация сервисов с использованием Java SDK Nacos:

```java
import com.alibaba.nacos.api.naming.NamingService;
import com.alibaba.nacos.api.naming.pojo.Instance;

NamingService namingService = NacosFactory.createNamingService("127.0.0.1:8848");

Instance instance = new Instance();
instance.setIp("192.168.1.100");
instance.setPort(8080);
instance.setEphemeral(true);

Map<String, String> metadata = new HashMap<>();
metadata.put("version", "1.0");
instance.setMetadata(metadata);

namingService.registerInstance("service-provider", instance);
```

#### Регистрация с использованием OpenAPI

```bash
curl -X POST 'http://127.0.0.1:8848/nacos/v3/client/ns/instance' \
  -d 'serviceName=service-provider' \
  -d 'ip=192.168.1.100' \
  -d 'port=8080'
```

### Обнаружение сервисов

Потребители сервисов могут обнаруживать сервисы следующими способами:

#### Использование нативного SDK

```java
NamingService namingService = NacosFactory.createNamingService("127.0.0.1:8848");

// Получить все работоспособные экземпляры
List<Instance> instances = namingService.selectInstances("service-provider", true);

// Использовать балансировку нагрузки для выбора экземпляра
Instance instance = namingService.selectOneHealthyInstance("service-provider");
```

#### Использование OpenAPI

```bash
# Получить список сервисов
curl -X GET 'http://127.0.0.1:8848/nacos/v3/client/ns/instance/list?serviceName=service-provider'
```

### Управление метаданными сервисов

Nacos поддерживает добавление и управление метаданными для сервисов:

```java
Map<String, String> metadata = new HashMap<>();
metadata.put("version", "1.0");
metadata.put("env", "production");

namingService.registerInstance("service-name", instance);
```

## Управление конфигурацией

### Публикация конфигурации

#### Использование OpenAPI

```bash
# Сначала войдите для получения токена доступа
curl -X POST 'http://127.0.0.1:8848/nacos/v3/auth/user/login' \
  -d 'username=nacos' \
  -d 'password=${your_password}'

# Используйте токен доступа для создания конфигурации
curl -X POST 'http://127.0.0.1:8848/nacos/v3/admin/cs/config?dataId=example.config&groupName=DEFAULT_GROUP&content=HelloWorld' \
  -H "accessToken:${your_access_token}"
```

#### Использование SDK

```java
ConfigService configService = NacosFactory.createConfigService("127.0.0.1:8848");

boolean isPublished = configService.publishConfig(
    "example.config",
    "DEFAULT_GROUP",
    "Hello Nacos"
);
```

### Получение конфигурации

#### Использование OpenAPI

```bash
curl -X GET 'http://127.0.0.1:8848/nacos/v3/client/cs/config?dataId=example.config&groupName=DEFAULT_GROUP'
```

#### Использование SDK

```java
String config = configService.getConfig(
    "example.config",
    "DEFAULT_GROUP",
    5000
);
```

### Прослушивание конфигурации

#### Использование SDK

```java
configService.addListener(
    "example.config",
    "DEFAULT_GROUP",
    new Listener() {
        @Override
        public void receiveConfigInfo(String configInfo) {
            System.out.println("Конфигурация изменена:" + configInfo);
        }

        @Override
        public Executor getExecutor() {
            return Executors.newSingleThreadExecutor();
        }
    }
);
```

## Интеграция Nacos со Spring Cloud

### Предварительные требования

Убедитесь, что Nacos Server установлен и запущен.

### Добавление зависимостей

Добавьте следующие зависимости в pom.xml вашего проекта Spring Cloud:

```xml
<dependency>
    <groupId>com.alibaba.cloud</groupId>
    <artifactId>spring-cloud-starter-alibaba-nacos-config</artifactId>
    <version>2023.0.3.2</version>
</dependency>

<dependency>
    <groupId>com.alibaba.cloud</groupId>
    <artifactId>spring-cloud-starter-alibaba-nacos-discovery</artifactId>
    <version>2023.0.3.2</version>
</dependency>
```

### Управление конфигурацией

Настройте адрес сервера Nacos в application.properties:

```properties
spring.application.name=springclouddemo
spring.config.import[0]=nacos:springclouddemo.properties?group=DEFAULT_GROUP
spring.cloud.nacos.config.server-addr=127.0.0.1:8848
spring.cloud.nacos.discovery.server-addr=127.0.0.1:8848
```

### Использование аннотаций

#### Аннотация @NacosConfig

Внедрение значений конфигурации из Nacos непосредственно в свойства Spring Bean:

```java
@RestController
public class ConfigController {
    @NacosConfig(dataId = "routeconfig", group = "config", key = "rate")
    String rate;

    @RequestMapping("/rate")
    public String rate() {
        return rate;
    }
}
```

#### Аннотация @NacosConfigListener

Получение событий изменения конфигурации от Nacos:

```java
@Service
public class MyRateConfigService {
    @NacosConfigListener(dataId = "routeconfig", group = "config")
    public void rate(String rateConfig) {
        System.out.println("Получено изменение конфигурации:" + rateConfig);
    }
}
```

### Регистрация и обнаружение сервисов

Включите регистрацию и обнаружение сервисов в приложении Spring Boot:

```java
@SpringBootApplication
@EnableDiscoveryClient
public class NacosProviderApplication {
    public static void main(String[] args) {
        SpringApplication.run(NacosProviderApplication.class, args);
    }

    @RestController
    class EchoController {
        @RequestMapping(value = "/echo/{string}", method = RequestMethod.GET)
        public String echo(@PathVariable String string) {
            return "Hello Nacos Discovery " + string;
        }
    }
}
```

Настройте в application.properties:

```properties
server.port=8070
spring.application.name=service-provider
spring.cloud.nacos.discovery.server-addr=127.0.0.1:8848
```

Настройка потребителя сервисов:

```java
@SpringBootApplication
@EnableDiscoveryClient
public class NacosConsumerApplication {
    @LoadBalanced
    @Bean
    public RestTemplate restTemplate() {
        return new RestTemplate();
    }

    @RestController
    public class TestController {
        private final RestTemplate restTemplate;

        @Autowired
        public TestController(RestTemplate restTemplate) {
            this.restTemplate = restTemplate;
        }

        @RequestMapping(value = "/echo/{str}", method = RequestMethod.GET)
        public String echo(@PathVariable String str) {
            return restTemplate.getForObject("http://service-provider/echo/" + str, String.class);
        }
    }
}
```

## Управление MCP сервисами

Nacos 3.0 поддерживает регистрацию, обнаружение и управление MCP сервисами (Model Context Protocol).

### Автоматическая регистрация MCP сервера

#### Использование фреймворка Spring AI Alibaba Nacos MCP

1. Добавьте зависимости:

```xml
<dependency>
    <groupId>com.alibaba.cloud.ai</groupId>
    <artifactId>spring-ai-alibaba-starter-mcp-registry</artifactId>
    <version>1.0.0.3+</version>
</dependency>
<dependency>
    <groupId>org.springframework.ai</groupId>
    <artifactId>spring-ai-starter-mcp-server-webmvc</artifactId>
    <version>1.0.0+</version>
</dependency>
```

2. Настройте автоматическую регистрацию:

```yaml
spring:
  application:
    name: mcp-nacos-registry-example
  ai:
    mcp:
      server:
        name: webmvc-mcp-server
        version: 1.0.0
        type: SYNC
        instructions: "This mcp server provides time information tools and resources"
  alibaba:
    mcp:
      nacos:
        server-addr: 127.0.0.1:8848
        namespace: public
        username: nacos
        password: nacos
        register:
          enabled: true
```

#### Использование Nacos MCP Wrapper Python

```python
pip install nacos-mcp-wrapper-python

from nacos_mcp_wrapper.server.nacos_mcp import NacosMCP
from nacos_mcp_wrapper.server.nacos_settings import NacosSettings

nacos_settings = NacosSettings()
nacos_settings.SERVER_ADDR = "127.0.0.1:8848"
nacos_settings.NAMESPACE = "public"
nacos_settings.USERNAME = "nacos"
nacos_settings.PASSWORD = "nacos"

mcp = NacosMCP("nacos-mcp-python", nacos_settings=nacos_settings, version="1.0.1", port=18001)

@mcp.tool()
def add(a: int, b: int) -> int:
    """Add two integers together"""
    return a + b

if __name__ == "__main__":
    mcp.run(transport="sse")
```

### Обнаружение и вызов MCP сервисов

#### Использование Spring AI Alibaba для обнаружения MCP сервера

```java
@Autowired
private List<LoadbalancedMcpSyncClient> loadbalancedMcpSyncClients;
```

Или:

```java
@Qualifier("loadbalancedMcpToolCallbacks")
ToolCallbackProvider tools
```

Настройте application.yml:

```yaml
spring:
  alibaba:
    mcp:
      nacos:
        namespace: public
        server-addr: 127.0.0.1:8848
        username: nacos
        password: nacos
        client:
          enabled: true
```

## Использование консоли

Nacos предоставляет веб-консоль для визуального управления:

- Адрес консоли: http://127.0.0.1:8080/index.html
- Имя пользователя по умолчанию: nacos
- Пароль по умолчанию: nacos@demo (пожалуйста, измените пароль в производственной среде)

Основные функции консоли:

- Управление сервисами: Просмотр, редактирование и удаление зарегистрированных сервисов
- Управление конфигурацией: Создание, изменение, удаление и запрос конфигураций
- Управление пространствами имен: Создание изолированных пространств имен
- Управление пользователями: Управление пользователями доступа и разрешениями
- Управление кластером: Просмотр состояния узлов кластера
