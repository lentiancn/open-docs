# Angular 安裝指南

> 🎯 本文檔詳細介紹 Angular 在各種作業系統環境下的安裝方法
> 
> 📅 更新時間：2026年2月
> 
> 📖 適用版本：Angular 17+ (目前最新版本)

---

## 目錄

1. [前置要求](#1-前置要求)
2. [在 Windows 上安裝](#2-在-windows-上安裝)
3. [在 macOS 上安裝](#3-在-macos-上安裝)
4. [在 Linux 上安裝](#4-在-linux-上安裝)
5. [驗證安裝](#5-驗證安裝)
6. [建立第一個專案](#6-建立第一個專案)
7. [解除安裝 Angular CLI](#7-解除安裝-angular-cli)
8. [常見問題](#8-常見問題)

---

## 1. 前置要求

### 1.1 Node.js 要求

Angular 需要 Node.js 環境。不同版本的 Angular 對 Node.js 版本有不同的要求：

| Angular 版本 | 最低 Node.js 版本 | 推薦 Node.js 版本 |
|-------------|------------------|------------------|
| Angular 17+ | Node.js 18.13.0+ | Node.js 20 LTS |
| Angular 16 | Node.js 16.14.0+ | Node.js 18 LTS |
| Angular 15 | Node.js 14.20.0+ | Node.js 16 LTS |
| Angular 14 | Node.js 14.20.0+ | Node.js 16 LTS |

> 💡 **提示**：建議使用 Node.js LTS（長期支援）版本以獲得最佳穩定性。

### 1.2 npm 要求

Angular CLI 透過 npm（Node Package Manager）分發。npm 隨 Node.js 一起安裝。

- 最低要求：npm 6.14.0+
- 推薦：npm 8+（隨 Node.js 20 一起提供）

### 1.3 可選：yarn 或 pnpm

如果你更喜歡使用 yarn 或 pnpm 作為套件管理器，也可以使用：

```bash
# 安裝 yarn（可選）
npm install -g yarn

# 安裝 pnpm（可選）
npm install -g pnpm
```

---

## 2. 在 Windows 上安裝

### 2.1 方法一：使用 npm（推薦）

這是最簡單、最常用的安裝方法。

#### 步驟 1：安裝 Node.js

1. 造訪 Node.js 官網：https://nodejs.org/
2. 下載 **LTS（長期支援）** 版本（推薦 Node.js 20 LTS）
3. 執行安裝程式
4. 在安裝精靈中：
   - 勾選「Add to PATH」（新增到環境變數）
   - 其他選項保持預設即可

#### 步驟 2：驗證 Node.js 和 npm

開啟 **命令提示字元**（cmd）或 **PowerShell**，執行以下命令：

```cmd
node --version
npm --version
```

如果顯示版本號，表示安裝成功。

#### 步驟 3：安裝 Angular CLI

在命令提示字元或 PowerShell 中執行：

```cmd
npm install -g @angular/cli
```

> ⚠️ **注意**：`-g` 表示全域安裝，這樣你可以在任何目錄下使用 `ng` 命令。

#### 步驟 4：驗證 Angular CLI

```cmd
ng version
```

如果安裝成功，會顯示 Angular CLI 的版本資訊。

---

### 2.2 方法二：使用系統管理員權限安裝

如果你遇到權限錯誤，請使用系統管理員權限：

1. 右鍵點擊「命令提示字元」或「PowerShell」
2. 選擇「以系統管理員身份執行」
3. 然後執行安裝命令：

```cmd
npm install -g @angular/cli
```

---

### 2.3 方法三：使用 Windows Package Manager (winget)

如果你使用 Windows 10/11，可以使用 winget：

```cmd
winget install OpenJS.NodeJS.LTS
winget install Angular.AngularCLI
```

---

### 2.4 方法四：使用 Chocolatey

如果你安裝了 Chocolatey 套件管理器：

```cmd
choco install nodejs-lts
choco install angular-cli
```

---

## 3. 在 macOS 上安裝

### 3.1 方法一：使用 npm（推薦）

#### 步驟 1：安裝 Node.js

**方式 A：透過 Homebrew（推薦）**

```bash
# 安裝 Homebrew（如果沒有的話）
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# 安裝 Node.js LTS
brew install node@20
```

**方式 B：直接下載**

1. 造訪 https://nodejs.org/
2. 下載 macOS Installer (.pkg)
3. 執行安裝程式

#### 步驟 2：驗證安裝

```bash
node --version
npm --version
```

#### 步驟 3：安裝 Angular CLI

```bash
sudo npm install -g @angular/cli
```

> 💡 **提示**：macOS 可能需要系統管理員權限（sudo）。

#### 步驟 4：驗證安裝

```bash
ng version
```

---

### 3.2 方法二：使用 nvm 管理多版本

如果你需要同時使用多個 Node.js 版本，推薦使用 nvm：

```bash
# 安裝 nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash

# 使用 nvm 安裝 Node.js
nvm install 20
nvm use 20

# 然後安裝 Angular CLI
npm install -g @angular/cli
```

---

### 3.3 方法三：使用 yarn

```bash
# 安裝 yarn
brew install yarn

# 使用 yarn 安裝 Angular CLI
yarn global add @angular/cli
```

---

## 4. 在 Linux 上安裝

### 4.1 Ubuntu / Debian

#### 步驟 1：安裝 Node.js

```bash
# 更新軟體來源
sudo apt update

# 安裝 Node.js 20 LTS
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs
```

#### 步驟 2：驗證安裝

```bash
node --version
npm --version
```

#### 步驟 3：安裝 Angular CLI

```bash
sudo npm install -g @angular/cli
```

---

### 4.2 CentOS / RHEL / Fedora

#### 步驟 1：安裝 Node.js

```bash
# CentOS/RHEL
curl -fsSL https://rpm.nodesource.com/setup_20.x | sudo bash -
sudo yum install nodejs

# Fedora
curl -fsSL https://rpm.nodesource.com/setup_20.x | sudo bash -
sudo dnf install nodejs
```

#### 步驟 2：安裝 Angular CLI

```bash
sudo npm install -g @angular/cli
```

---

### 4.3 Arch Linux

```bash
sudo pacman -S nodejs npm
sudo npm install -g @angular/cli
```

---

### 4.4 openSUSE

```bash
sudo zypper install nodejs npm
sudo npm install -g @angular/cli
```

---

### 4.5 使用 nvm（適用於所有 Linux 發行版）

```bash
# 安裝 nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash

# 重新載入 shell 設定
source ~/.bashrc

# 安裝並使用 Node.js 20
nvm install 20
nvm use 20

# 安裝 Angular CLI
npm install -g @angular/cli
```

---

## 5. 驗證安裝

無論使用哪種作業系統，安裝完成後都可以透過以下命令驗證：

### 5.1 查看 Angular CLI 版本

```bash
ng version
```

預期輸出類似：

```
     _                      _                 ____ _     ___
    / \   _ __   __ _ _   _| | __ _ _ __     / ___| |   |_ _|
   / △ \ | '_ \ / _` | | | | |/ _` | '__|   | |   | |    | |
  / ___ \| | | | (_| | |_| | | (_| | |      | |___| |___ | |
 /_/   \_\_| |_|\__,_|\__,_|_|\__,_|_|       \____|_____|___|
 

Angular CLI: 17.3.0
Node: 20.11.0
Package Manager: npm 10.2.4
OS: win32 x64
```

### 5.2 查看可用命令

```bash
ng help
```

---

## 6. 建立第一個專案

安裝完成後，你可以建立第一個 Angular 專案：

### 6.1 使用預設設定建立專案

```bash
ng new my-first-app
```

這會：
- 建立名為 `my-first-app` 的目錄
- 安裝專案依賴
- 設定 Angular 環境

### 6.2 跳過安裝（僅建立專案檔案）

```bash
ng new my-first-app --skip-install
```

### 6.3 使用特定選項建立專案

```bash
# 建立不使用 Git 的專案
ng new my-first-app --skip-git

# 建立不使用單元測試的專案
ng new my-first-app --skip-tests

# 使用 SCSS 替代 CSS
ng new my-first-app --style=scss

# 使用內嵌樣式和範本（學習時有用）
ng new my-first-app --inline-template --inline-style
```

---

## 7. 解除安裝 Angular CLI

如果需要解除安裝 Angular CLI：

### 7.1 解除安裝 Angular CLI

```bash
npm uninstall -g @angular/cli
```

### 7.2 清理快取

```bash
npm cache clean --force
```

### 7.3 驗證解除安裝

```bash
ng version
```

應該會顯示「command not found」或「ng 不是內部或外部命令」。

---

## 8. 常見問題

### Q1: 安裝時出現權限錯誤怎麼辦？

**Windows:**
- 以系統管理員身份執行命令提示字元或 PowerShell
- 檢查 Node.js 安裝目錄的權限

**macOS/Linux:**
- 使用 `sudo npm install -g @angular/cli`
- 或修改 npm 全域目錄的權限

---

### Q2: 如何更新 Angular CLI 到最新版本？

```bash
npm install -g @angular/cli@latest
```

---

### Q3: 如何查看目前使用的 Angular 版本？

在專案目錄下執行：

```bash
ng version
```

或在 `package.json` 中查看：

```json
{
  "dependencies": {
    "@angular/core": "^17.0.0"
  }
}
```

---

### Q4: 出現「ng 不是內部或外部命令」怎麼辦？

**Windows:**
1. 檢查 Node.js 是否正確安裝：`node --version`
2. 檢查 npm 全域模組路徑是否在 PATH 中
3. 手動新增路徑到系統環境變數

**macOS/Linux:**
1. 檢查 npm 全域模組路徑
2. 可能需要將 npm 全域 bin 目錄新增到 PATH：
   ```bash
   export PATH="$PATH:$(npm global bin)"
   ```

---

### Q5: 安裝很慢怎麼辦？

可以使用國內鏡像源：

```bash
# 設定 npm 鏡像
npm config set registry https://registry.npmmirror.com

# 安裝 Angular CLI
npm install -g @angular/cli
```

---

### Q6: 如何在不同專案中使用不同版本的 Angular CLI？

可以使用 nvm 管理多個 Node.js 版本，或在每個專案中本地安裝 Angular CLI：

```bash
# 在專案目錄中
npm install @angular/cli --save-dev
```

然後使用：

```bash
npx ng version
```

---

### Q7: Node.js 版本不相容怎麼辦？

使用 nvm（Node Version Manager）切換 Node.js 版本：

```bash
# 查看可用版本
nvm list

# 安裝特定版本
nvm install 18

# 使用特定版本
nvm use 18
```

---

### Q8: macOS M1/M2 晶片安裝問題

如果使用 Apple Silicon (M1/M2/M3) 晶片：

```bash
# 確保使用 arm64 版本的 Node.js
arch -arm64 node --version

# 如果遇到問題，可能需要安裝 x64 版本的 Node.js
nvm install 20 --arm64
```

---

### Q9: 如何查看 Angular CLI 安裝路徑？

```bash
npm list -g @angular/cli
```

---

### Q10: 企業網路需要代理怎麼辦？

```bash
# 設定代理
npm config set proxy http://proxy.company.com:8080
npm config set https-proxy http://proxy.company.com:8080

# 移除代理
npm config delete proxy
npm config delete https-proxy
```

---

## 📚 相關資源

- 官方文檔：https://angular.io/docs
- Angular CLI 命令參考：https://angular.io/cli
- Angular 官方 GitHub：https://github.com/angular/angular
- Stack Overflow Angular 標籤：https://stackoverflow.com/questions/tagged/angular

---

*本文檔由 AI 助手生成，歡迎提交回饋和改進建議。*
