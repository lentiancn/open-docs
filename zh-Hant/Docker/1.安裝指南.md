# Docker 安裝指南

> 🎯 詳細介紹 Docker 在各種作業系統環境下的安裝方法
> 
> 📅 更新時間：2026年2月
> 
> 📖 適用版本：Docker 27.x

---

## 1. Docker 簡介

### 1.1 什麼是 Docker？

Docker 是一個開源的容器化平台，用於開發、部署和運行應用程式。通過容器化技術，Docker 可以將應用程式及其依賴打包成輕量級、可移植的容器。

### 1.2 Docker 版本

| 版本 | 發布日期 |
|------|----------|
| Docker 27.x | 2024年 |
| Docker 26.x | 2024年 |
| Docker 25.x | 2023年 |

---

## 2. 前置要求

### 2.1 Windows 要求

- Windows 10 64位 專業版/企業版 或 Windows 11
- 4GB RAM
- 64GB SSD

### 2.2 macOS 要求

- macOS 10.15 (Catalina) 或更高
- 4GB RAM
- Intel 或 Apple Silicon (M1/M2/M3)

---

## 3. 在 Windows 上安裝

### 3.1 Docker Desktop（推薦）

1. 訪問 https://www.docker.com/products/docker-desktop
2. 下載 "Download for Windows"
3. 執行安裝程式
4. 驗證：
```powershell
docker --version
docker run hello-world
```

---

## 4. 在 macOS 上安裝

### 4.1 Docker Desktop（推薦）

1. 訪問 https://www.docker.com/products/docker-desktop
2. 下載 "Download for Mac"
3. 選擇晶片類型：Apple Silicon 或 Intel
4. 驗證：
```bash
docker --version
docker run hello-world
```

---

## 5. 在 Linux 上安裝

### 5.1 Ubuntu / Debian

```bash
# 安裝依賴
sudo apt install ca-certificates curl gnupg lsb-release

# 添加 Docker GPG 金鑰
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg

# 添加 Docker 倉庫
echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list

# 安裝 Docker
sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

# 啟動 Docker
sudo systemctl start docker
sudo systemctl enable docker
```

---

## 6. 驗證安裝

```bash
docker --version
docker run hello-world
```

---

## 7. 配置 Docker

### 7.1 配置鏡像加速器

中國地區用戶建議配置國內鏡像：

```json
{
  "registry-mirrors": [
    "https://docker.mirrors.ustc.edu.cn",
    "https://hub-mirror.c.163.com"
  ]
}
```

Linux 配置：
```bash
sudo mkdir -p /etc/docker
sudo tee /etc/docker/daemon.json <<EOF
{
  "registry-mirrors": [
    "https://docker.mirrors.ustc.edu.cn"
  ]
}
EOF
sudo systemctl daemon-reload
sudo systemctl restart docker
```

---

## 8. 常見問題

### Q: Docker Desktop 啟動失敗？

確保 BIOS 中已啟用虛擬化，並正確安裝 WSL 2。

### Q: 鏡像下載太慢？

配置國內鏡像加速器（見上文）。

### Q: 權限不足？

```bash
sudo usermod -aG docker $USER
# 重新登入
```

---

## 📚 相關資源

- 官方文檔：https://docs.docker.com
- Docker Hub：https://hub.docker.com

---

*本文檔由 AI 助手生成*
