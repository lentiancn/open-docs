# Bootstrap 安装指南

> 🎯 本文档详细介绍 Bootstrap 在各种操作系统和环境下的安装方法
> 
> 📅 更新时间：2026年2月
> 
> 📖 适用版本：Bootstrap 5.3.x（当前最新版本）

---

## 目录

1. [简介](#1-简介)
2. [前置要求](#2-前置要求)
3. [安装方式](#3-安装方式)
4. [使用包管理器安装](#4-使用包管理器安装)
5. [CDN 引入](#5-cdn-引入)
6. [下载源码](#6-下载源码)
7. [在框架中使用](#7-在框架中使用)
8. [验证安装](#8-验证安装)
9. [版本选择](#9-版本选择)
10. [常见问题](#10-常见问题)

---

## 1. 简介

### 1.1 什么是 Bootstrap？

Bootstrap 是全球最流行的开源 CSS 框架，由 Twitter 的设计师和工程师创建。它提供了响应式、移动设备优先的网页前端开发工具。

### 1.2 Bootstrap 特性

- 📱 **响应式设计** - 适配所有设备尺寸
- 🎨 **预制组件** - 按钮、导航、表单等
- 🎯 **栅格系统** - 12列响应式布局
- 🎭 **主题系统** - 自定义样式
- ⚡ **JavaScript 插件** - 交互组件
- 🌐 **浏览器兼容** - 支持所有现代浏览器

### 1.3 版本历史

| 版本 | 发布日期 | 重要特性 |
|------|----------|----------|
| Bootstrap 5.3 | 2023年 | CSS 变量、暗色模式、RFS |
| Bootstrap 5.2 | 2022年 | 新的实用类、容器 |
| Bootstrap 5.1 | 2021年 | CSS 自定义属性 |
| Bootstrap 5.0 | 2020年 | 全新设计、停止 jQuery |
| Bootstrap 4 | 2018年 | Flexbox 支持 |
| Bootstrap 3 | 2013年 | 移动优先设计 |

---

## 2. 前置要求

### 2.1 基本要求

- 现代浏览器（Chrome、Firefox、Safari、Edge）
- 文本编辑器（VS Code、Sublime Text 等）
- 基础的 HTML/CSS 知识

### 2.2 开发环境要求

| 环境 | 最低版本 |
|------|----------|
| Node.js | 12.x+ |
| npm | 6.x+ |

### 2.3 可选工具

```bash
# 安装 Sass 编译器（自定义 Bootstrap 主题需要）
npm install -g sass

# 安装 Bootstrap Icons
npm install bootstrap-icons
```

---

## 3. 安装方式

Bootstrap 提供多种安装方式：

| 方式 | 适用场景 | 优点 | 缺点 |
|------|----------|------|------|
| CDN | 快速原型 | 无需安装、立即可用 | 需要网络 |
| npm | 生产项目 | 版本管理、构建优化 | 需要配置 |
| 下载 | 离线使用 | 无需网络 | 需要手动更新 |
| 模板 | 新项目 | 开箱即用 | 可能有冗余代码 |

---

## 4. 使用包管理器安装

### 4.1 npm 安装

```bash
# 创建项目目录
mkdir my-bootstrap-project
cd my-bootstrap-project

# 初始化项目
npm init -y

# 安装 Bootstrap
npm install bootstrap

# 安装 Bootstrap 及其依赖（推荐）
npm install bootstrap @popperjs/core
```

#### package.json 依赖

```json
{
  "dependencies": {
    "bootstrap": "^5.3.3",
    "@popperjs/core": "^2.11.8"
  }
}
```

### 4.2 yarn 安装

```bash
# 使用 yarn 安装
yarn add bootstrap @popperjs/core
```

### 4.3 pnpm 安装

```bash
# 使用 pnpm 安装
pnpm add bootstrap @popperjs/core
```

### 4.4 Bower 安装（已废弃）

```bash
# 不推荐使用 Bower
bower install bootstrap
```

---

## 5. CDN 引入

### 5.1 CSS 引入

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bootstrap 项目</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <!-- 内容区域 -->
    <h1>Hello, Bootstrap!</h1>
    
    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
```

### 5.2 完整引入示例

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bootstrap 5 页面</title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Bootstrap Icons（可选） -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="#">我的网站</a>
        </div>
    </nav>
    
    <div class="container mt-5">
        <h1>欢迎使用 Bootstrap</h1>
        <p class="lead">这是一个响应式布局的页面。</p>
        
        <button class="btn btn-primary">主要按钮</button>
        <button class="btn btn-secondary">次要按钮</button>
    </div>
    
    <!-- jQuery（Bootstrap 5 不再需要） -->
    <!-- Bootstrap Bundle JS（包含 Popper） -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
```

### 5.3 CDN 提供商

| 提供商 | URL | 特点 |
|--------|-----|------|
| jsDelivr | cdn.jsdelivr.net | 推荐、免费 |
| cdnjs | cdnjs.cloudflare.com | 稳定、快速 |
| unpkg | unpkg.com | NPM 镜像 |

---

## 6. 下载源码

### 6.1 下载预编译版本

1. 访问 Bootstrap 官网：https://getbootstrap.com
2. 点击 "Download" 按钮
3. 选择 "Download CSS & JS"（预编译版本）
4. 解压下载的文件

#### 文件结构

```
bootstrap-5.3.3/
├── css/
│   ├── bootstrap.css
│   ├── bootstrap.css.map
│   ├── bootstrap.min.css
│   ├── bootstrap.min.css.map
│   ├── bootstrap-grid.css
│   ├── bootstrap-grid.min.css
│   ├── bootstrap-utilities.css
│   └── bootstrap-utilities.min.css
├── js/
│   ├── bootstrap.bundle.js
│   ├── bootstrap.bundle.min.js
│   ├── bootstrap.js
│   └── bootstrap.min.js
└── LICENSE
```

### 6.2 下载源代码版本

1. 访问 GitHub：https://github.com/twbs/bootstrap
2. 下载 Source code (zip)
3. 需要使用 Sass 编译器

#### 源码结构

```
bootstrap-5.3.3/
├── scss/
│   ├── bootstrap.scss
│   ├── _variables.scss
│   ├── _functions.scss
│   ├── _mixins.scss
│   └── ...
├── js/
│   ├── src/
│   └── dist/
├── dist/
│   ├── css/
│   └── js/
└── package.json
```

### 6.3 本地引用

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>本地 Bootstrap</title>
    <!-- 引用本地 CSS -->
    <link href="css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container">
        <h1>本地 Bootstrap</h1>
    </div>
    
    <!-- 引用本地 JS -->
    <script src="js/bootstrap.bundle.min.js"></script>
</body>
</html>
```

---

## 7. 在框架中使用

### 7.1 在 React 中使用 Bootstrap

```bash
# 安装 Bootstrap
npm install bootstrap
```

#### 在 App.js 中引入

```javascript
import 'bootstrap/dist/css/bootstrap.min.css';
import { Button } from 'react-bootstrap';

function App() {
  return (
    <div className="App">
      <Button variant="primary">主要按钮</Button>
    </div>
  );
}

export default App;
```

#### 使用 react-bootstrap

```bash
# 安装 react-bootstrap
npm install react-bootstrap bootstrap
```

```javascript
import { Button } from 'react-bootstrap';
import 'bootstrap/dist/css/bootstrap.min.css';

function App() {
  return <Button variant="primary">主要按钮</Button>;
}
```

### 7.2 在 Vue 中使用 Bootstrap

```bash
# 安装 Bootstrap
npm install bootstrap
```

#### 在 main.js 中引入

```javascript
import { createApp } from 'vue';
import App from './App.vue';
import 'bootstrap/dist/css/bootstrap.min.css';
import 'bootstrap/dist/js/bootstrap.bundle.min.js';

createApp(App).mount('#app');
```

#### 使用 bootstrap-vue-next

```bash
# 安装 bootstrap-vue-next
npm install bootstrap bootstrap-vue-next
```

### 7.3 在 Angular 中使用 Bootstrap

```bash
# 安装 Bootstrap
npm install bootstrap
```

#### 在 angular.json 中配置

```json
{
  "styles": [
    "src/styles.css",
    "node_modules/bootstrap/dist/css/bootstrap.min.css"
  ],
  "scripts": [
    "node_modules/bootstrap/dist/js/bootstrap.bundle.min.js"
  ]
}
```

### 7.4 在 jQuery 项目中使用

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <title>jQuery + Bootstrap</title>
    <link href="css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container">
        <button id="myButton" class="btn btn-primary">点击我</button>
        <div id="myAlert" class="alert alert-success mt-3" style="display:none;">
            按钮被点击了！
        </div>
    </div>
    
    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
    <script src="js/bootstrap.bundle.min.js"></script>
    <script>
        $(document).ready(function() {
            $('#myButton').click(function() {
                $('#myAlert').fadeIn();
            });
        });
    </script>
</body>
</html>
```

---

## 8. 验证安装

### 8.1 验证 CSS

在浏览器控制台执行：

```javascript
// 检查 Bootstrap 是否加载
typeof bootstrap !== 'undefined'
// 或者检查 CSS 变量
getComputedStyle(document.body).getPropertyValue('--bs-body-font-family')
```

### 8.2 验证 JS

```javascript
// 检查 Bootstrap JS
typeof bootstrap === 'object'
// 或者检查 Modal
typeof bootstrap.Modal === 'function'
```

### 8.3 验证响应式

打开浏览器开发者工具，切换不同设备尺寸，检查布局是否响应式变化。

---

## 9. 版本选择

### 9.1 生产环境推荐

| 场景 | 推荐版本 | 原因 |
|------|----------|------|
| 新项目 | Bootstrap 5.3.x | 最新特性、CSS 变量 |
| 维护项目 | 当前使用版本 | 避免破坏性更新 |
| 学习 | Bootstrap 5.3.x | 最新文档 |

### 9.2 版本 URL

```html
<!-- Bootstrap 5.3.3 -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

<!-- Bootstrap 5.2.3 -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">

<!-- Bootstrap 4.6 -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css" rel="stylesheet">
```

---

## 10. 常见问题

### Q1: Bootstrap 5 还需要 jQuery 吗？

**不需要**。Bootstrap 5 完全移除了 jQuery 依赖，使用原生 JavaScript。

---

### Q2: bootstrap.css 和 bootstrap.min.css 有什么区别？

- `bootstrap.css` - 未压缩，可读性好，用于开发
- `bootstrap.min.css` - 压缩版，文件更小，用于生产环境

---

### Q3: bootstrap.bundle.js 和 bootstrap.js 有什么区别？

- `bootstrap.bundle.js` - 包含 Popper.js（工具提示、下拉菜单等需要）
- `bootstrap.js` - 不包含 Popper.js

**推荐使用 bootstrap.bundle.js**

---

### Q4: 如何自定义 Bootstrap 样式？

#### 方法 1：覆盖 CSS 类

```css
.btn-primary {
    background-color: #your-color;
    border-color: #your-color;
}
```

#### 方法 2：使用 Sass 自定义

```scss
// 自定义变量
$primary: #your-color;
$secondary: #another-color;

// 引入 Bootstrap
@import "bootstrap";
```

---

### Q5: Bootstrap 图标如何添加？

```html
<!-- CDN 引入 -->
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">

<!-- 使用图标 -->
<i class="bi bi-bootstrap"></i>
<i class="bi bi-github"></i>
<button class="btn btn-primary">
    <i class="bi bi-plus-lg"></i> 新建
</button>
```

---

### Q6: 如何升级 Bootstrap 版本？

```bash
# npm 升级
npm install bootstrap@latest @popperjs/core@latest
```

CDN 方式只需修改版本号：

```html
<!-- 升级到 5.3.3 -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
```

---

### Q7: Bootstrap 兼容哪些浏览器？

Bootstrap 5 支持所有**现代浏览器**：

| 浏览器 | 最低版本 |
|--------|----------|
| Chrome | 90+ |
| Firefox | 90+ |
| Safari | 14+ |
| Edge | 90+ |
| Opera | 76+ |

不支持 Internet Explorer。

---

### Q8: CDN 加载失败怎么办？

```html
<!-- 备用 CDN -->
<link href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/5.3.3/css/bootstrap.min.css" rel="stylesheet">

<!-- 或者本地引用 -->
<link href="css/bootstrap.min.css" rel="stylesheet">
```

---

### Q9: 如何调试 Bootstrap 样式？

1. 使用浏览器开发者工具
2. 检查元素样式
3. 查看 CSS 源文件

```css
/* 在浏览器控制台查看 Bootstrap 变量 */
getComputedStyle(document.body).getPropertyValue('--bs-blue')
```

---

### Q10: Bootstrap 5 有什么新特性？

- ✅ CSS 自定义属性（变量）
- ✅ 暗色模式支持
- ✅ 改进的栅格系统
- ✅ 新的实用工具类
- ✅ 停止 jQuery 依赖
- ✅ 改进的表单组件

---

## 📚 相关资源

- 官方网站：https://getbootstrap.com
- 中文文档：https://bootstrap.doc.cn
- GitHub：https://github.com/twbs/bootstrap
- Bootstrap Icons：https://icons.getbootstrap.com

---

*本文档由 AI 助手生成，欢迎提交反馈和改进建议。*
