# Bootstrap 常见问题

> 🎯 解答 Bootstrap 开发中遇到的常见问题
> 
> 📅 更新时间：2026年2月
> 
> 📖 适用版本：Bootstrap 5.3.x

---

## 目录

1. [安装问题](#1-安装问题)
2. [布局问题](#2-布局问题)
3. [组件问题](#3-组件问题)
4. [自定义问题](#4-自定义问题)
5. [兼容性问题](#5-兼容性问题)

---

## 1. 安装问题

### Q1: Bootstrap 5 还需要 jQuery 吗？

**不需要**。Bootstrap 5 完全移除了 jQuery 依赖，使用原生 JavaScript。

---

### Q2: bootstrap.min.css 和 bootstrap.css 有什么区别？

- `bootstrap.css` - 未压缩，调试用
- `bootstrap.min.css` - 压缩版，生产用

---

### Q3: bootstrap.bundle.js 和 bootstrap.js 有什么区别？

- `bootstrap.bundle.js` - 包含 Popper.js（工具提示、下拉菜单需要）
- `bootstrap.js` - 不包含 Popper.js

---

### Q4: CDN 加载失败怎么办？

```html
<!-- 备用 CDN -->
<link href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/5.3.3/css/bootstrap.min.css" rel="stylesheet">

<!-- 或本地引用 -->
<link href="css/bootstrap.min.css" rel="stylesheet">
```

---

## 2. 布局问题

### Q5: 栅格系统不生效？

确保：
1. 行（row）在容器（container）内
2. 列（col）在行（row）内
3. 不要在列上直接添加 padding

```html
<!-- 正确 -->
<div class="container">
    <div class="row">
        <div class="col-md-6">内容</div>
    </div>
</div>
```

---

### Q6: 如何让列垂直居中？

```html
<div class="row align-items-center">
    <div class="col">居中内容</div>
</div>
```

---

### Q7: 列之间的间距太大？

检查是否有不必要的 padding：

```css
.row {
    --bs-gutter-x: 1.5rem; /* 可调整 */
}
```

---

## 3. 组件问题

### Q8: 模态框无法关闭？

确保：
1. 正确引入 bootstrap.bundle.js
2. 使用正确的关闭按钮

```html
<button type="button" class="btn-close" data-bs-dismiss="modal"></button>
```

---

### Q9: 工具提示不显示？

需要初始化：

```javascript
var tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="tooltip"]'))
var tooltipList = tooltipTriggerList.map(function (tooltipTriggerEl) {
    return new bootstrap.Tooltip(tooltipTriggerEl)
})
```

---

### Q10: 导航栏在手机上不折叠？

确保：
1. 引入 Bootstrap JS
2. 正确的折叠按钮结构

```html
<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
    <span class="navbar-toggler-icon"></span>
</button>
```

---

### Q11: 下拉菜单不工作？

```html
<!-- 确保有 data-bs-toggle -->
<div class="dropdown">
    <button class="btn btn-secondary dropdown-toggle" data-bs-toggle="dropdown">
        下拉菜单
    </button>
    <ul class="dropdown-menu">
        <li><a class="dropdown-item" href="#">选项</a></li>
    </ul>
</div>
```

---

## 4. 自定义问题

### Q12: 如何修改主题颜色？

```css
:root {
    --bs-primary: #your-color;
}
```

---

### Q13: 如何添加自定义样式？

```html
<!-- 在 Bootstrap CSS 之后添加 -->
<link href="bootstrap.min.css" rel="stylesheet">
<link href="custom.css" rel="stylesheet">
```

---

### Q14: 如何使用 Bootstrap Icons？

```html
<link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">

<i class="bi bi-github"></i>
```

---

## 5. 兼容性问题

### Q15: 兼容哪些浏览器？

Bootstrap 5 支持：
- Chrome 90+
- Firefox 90+
- Safari 14+
- Edge 90+

**不支持 Internet Explorer**

---

### Q16: 移动端布局错乱？

确保添加视口 meta 标签：

```html
<meta name="viewport" content="width=device-width, initial-scale=1.0">
```

---

### Q17: 响应式图片不工作？

```html
<img src="..." class="img-fluid" alt="...">
```

---

## 📚 相关资源

- 官方文档：https://getbootstrap.com/docs
- 中文文档：https://bootstrap.doc.cn

---

*本文档由 AI 助手生成*
