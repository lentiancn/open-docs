# H2 Benutzerhandbuch

## Verbindung

```java
// Eingebettet (In-Memory)
String url = "jdbc:h2:mem:testdb";
Connection conn = DriverManager.getConnection(url, "sa", "");

// Datei
String url = "jdbc:h2:file:./data/testdb";
Connection conn = DriverManager.getConnection(url, "sa", "password");
```

## Tabelle erstellen

```sql
CREATE TABLE users (
    id INT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(100) NOT NULL,
    email VARCHAR(100) UNIQUE
);
```

## Daten einfügen

```sql
INSERT INTO users (name, email) VALUES ('Max', 'max@beispiel.de');
```

## Daten abfragen

```sql
SELECT * FROM users;
```

## Vollständiges JDBC-Beispiel

```java
import java.sql.*;

Class.forName("org.h2.Driver");
Connection conn = DriverManager.getConnection("jdbc:h2:mem:testdb", "sa", "");

Statement stmt = conn.createStatement();
stmt.execute("CREATE TABLE users (id INT PRIMARY KEY AUTO_INCREMENT, name VARCHAR(100))");
stmt.execute("INSERT INTO users (name) VALUES ('Max')");

ResultSet rs = stmt.executeQuery("SELECT * FROM users");
while (rs.next()) {
    System.out.println(rs.getInt("id") + ": " + rs.getString("name"));
}

rs.close();
stmt.close();
conn.close();
```

## Erweiterte Funktionen

### Datenbankmodus
```java
jdbc:h2:mem:testdb;MODE=MySQL
jdbc:h2:mem:testdb;MODE=PostgreSQL
```

### Verschlüsselung
```java
jdbc:h2:file:./data/encrypted;CIPHER=AES
```

### TCP-Server starten
```java
import org.h2.tools.Server;
Server server = Server.createTcpServer().start();
```

## Leistungsoptimierung

### Batch-Operationen
```java
conn.setAutoCommit(false);
PreparedStatement pstmt = conn.prepareStatement("INSERT INTO users (name) VALUES (?)");
for (int i = 0; i < 1000; i++) {
    pstmt.setString(1, "User" + i);
    pstmt.addBatch();
}
pstmt.executeBatch();
conn.commit();
```

## Backup
```sql
SCRIPT TO 'backup.sql';
RUNSCRIPT FROM 'backup.sql';
```
