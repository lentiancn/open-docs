# Apache APISIX 설치 가이드

## 요구 사항

- **운영체제**: Linux (Ubuntu, CentOS, Debian), macOS
- **메모리**: 최소 2GB (프로덕션은 4GB+)
- **CPU**: 최소 2코어

## 설치 방법

### 방법 1: Docker Quickstart (권장)

```bash
curl -sL https://run.api7.ai/apisix/quickstart | sh
```

설치 후:
```
✔ APISIX is ready!
```

기본 포트:
- 9080: HTTP
- 9443: HTTPS
- 9180: Admin API

### 방법 2: Docker Compose

```yaml
version: '3'
services:
  apisix:
    image: apache/apisix:3.3.0
    ports:
      - "9080:9080"
      - "9443:9443"
      - "9180:9180"
  etcd:
    image: apache/apisix/etcdserve:v3.5.0
```

```bash
docker-compose up -d
```

### 방법 3: Helm (Kubernetes)

```bash
helm repo add apisix https://apache.github.io/apisix-helm-chart
helm install apisix apisix/apisix --namespace apisix
```

## 설치 확인

```bash
curl "http://127.0.0.1:9080" --head
# 응답: Server: APISIX/3.3.0
```

## 대시보드

```
http://127.0.0.1:9180/ui
```

## 제거

### Docker
```bash
docker stop apisix-quickstart etcd
docker rm apisix-quickstart etcd
```

### Kubernetes
```bash
helm uninstall apisix --namespace apisix
```
