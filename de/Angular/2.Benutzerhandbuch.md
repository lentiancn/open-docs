# Angular Benutzerhandbuch

> 🎯 Vollständige Anleitung zu Angular
> 
> 📅 Aktualisiert: Februar 2026
> 
> 📖 Version: Angular 17+

---

## 1. Einführung

Angular ist ein Open-Source-Web-Framework von Google.

### Hauptmerkmale
- Komponentenbasiert
- Bidirektionale Datenbindung
- Dependency Injection
- Routing
- Formulare
- HTTP Client
- Testing

---

## 2. Projektstruktur

```
mein-app/
├── src/app/
│   ├── app.component.ts
│   └── app.config.ts
├── angular.json
└── package.json
```

---

## 3. Komponenten

```bash
ng generate component mein-component
```

```typescript
@Component({
  selector: 'app-mein-component',
  standalone: true,
  template: `<h1>{{ titel }}</h1>`
})
export class MeinComponentComponent {
  titel = 'Hallo Angular';
}
```

---

## 4. Template-Syntax

```html
<!-- Interpolierung -->
<p>Hallo, {{ name }}!</p>

<!-- Property Binding -->
<img [src]="bildUrl">

<!-- Event Binding -->
<button (click)="klick()">Klick</button>

<!-- Two-way Binding -->
<input [(ngModel)]="name">
```

---

## 5. Services

```typescript
@Injectable({ providedIn: 'root' })
export class DatenService {
  constructor(private http: HttpClient) {}
}
```

---

## 6. Routing

```typescript
export const routes: Routes = [
  { path: '', component: HomeComponent },
  { path: 'about', component: AboutComponent }
];
```

---

## 7. HTTP

```typescript
this.http.get('/api/daten').subscribe(daten => console.log(daten));
```

---

## 8. Signal

```typescript
count = signal(0);

increment(): void {
  this.count.update(c => c + 1);
}
```

---

## 9. Build

```bash
ng build --configuration production
```

---

## 📚 Ressourcen

- https://angular.io/docs

---

*Erstellt von AI*
