# Element UI Guía de Instalación

> Element UI es una biblioteca de componentes UI de escritorio basada en Vue 2
> 
> Actualizado: Febrero 2026
> 
> Versión actual: 2.x (Estable)
> 
> Nota: Para proyectos Vue 3, use Element Plus

---

## 1. Introducción del Proyecto

Element UI es una biblioteca de componentes UI de escritorio basada en Vue 2.0, desarrollada y mantenida por el equipo de Ele.me. Proporciona un rico conjunto de componentes commonly used para PC, incluyendo formularios, tablas, diálogos, menús de navegación, botones, campos de entrada y otros elementos UI comunes.

Element UI tiene un estilo de diseño limpio y elegante con documentación detallada. Es una de las bibliotecas de componentes UI Vue 2 más populares en China. Aunque el mantenimiento oficial se ha detenido (se recomienda migrar a Element Plus para Vue 3), Element UI todavía tiene un alto valor de referencia y aprendizaje debido a la gran cantidad de proyectos existentes en uso.

### 1.1 Información de Versión

| Versión | Vue Versión | Estado | Descripción |
|---------|-------------|--------|-------------|
| Element UI 2.x | Vue 2 | Estable | Versión principal de documentación |
| Element UI 1.x | Vue 2 | Fin de vida | Versión temprana |

### 1.2 Diferencia con Element Plus

| Función | Element UI | Element Plus |
|---------|------------|--------------|
| Vue Versión | Vue 2 | Vue 3 |
| TypeScript | Soporte parcial | Soporte completo |
| Composition API | No soportado | Soportado |
| Estado de mantenimiento | Detenido | Mantenimiento activo |
| Años de lanzamiento | 2016-2021 | 2022-actual |

---

## 2. Requisitos del Entorno

### 2.1 Requisitos del Entorno de Desarrollo

| Entorno | Versión Mínima | Versión Recomendada |
|---------|-----------------|---------------------|
| Node.js | 8.x | 10.x o 12.x |
| npm | 5.x | 6.x |
| Vue | 2.6.0 | 2.6.14 |
| Herramienta de compilación | Webpack 4.x | Webpack 4.x o 5.x |

### 2.2 Compatibilidad con Navegadores

Element UI soporta las siguientes versiones de navegadores:

- Chrome >= 49
- Firefox >= 45
- Safari >= 10
- Edge >= 14
- IE 11 (Soporte limitado)

---

## 3. Pasos de Instalación

### 3.1 Usando npm (Recomendado)

```bash
# Entrar en el directorio del proyecto Vue 2
cd tu-proyecto-vue

# Instalar Element UI
npm install element-ui --save
```

### 3.2 Usando yarn

```bash
yarn add element-ui
```

---

## 4. Importar en Proyecto Vue 2

### 4.1 Importación Completa (Adecuada para Proyectos Pequeños)

Importe Element UI y sus estilos en el archivo de entrada (generalmente main.js):

```javascript
// main.js
import Vue from 'vue'
import App from './App.vue'

// Importar Element UI
import ElementUI from 'element-ui'
import 'element-ui/lib/theme-chalk/index.css'

Vue.use(ElementUI)

new Vue({
  render: h => h(App)
}).$mount('#app')
```

### 4.2 Importación Bajo Demanda (Recomendada para Producción)

Para reducir el tamaño del paquete, importe solo los componentes que necesita.

#### 4.2.1 Instalar babel-plugin-component

```bash
npm install babel-plugin-component -D
```

#### 4.2.2 Configurar babel

Agregue configuración en .babelrc:

```json
{
  "presets": [
    ["env", {
      "modules": false,
      "targets": {
        "browsers": ["> 1%", "last 2 versions", "not ie <= 8"]
      }
    }]
  ],
  "plugins": [
    ["component", {
      "libraryName": "element-ui",
      "styleLibraryName": "theme-chalk"
    }]
  ]
}
```

#### 4.2.3 Importar Componentes Bajo Demanda

```javascript
import { Button, Form, FormItem, Input, Table, TableColumn } from 'element-ui'

Vue.use(Button)
Vue.use(Form)
Vue.use(FormItem)
Vue.use(Input)
Vue.use(Table)
Vue.use(TableColumn)
```

---

## 5. Ejemplos de Inicio Rápido

### 5.1 Botón Básico

```html
<template>
  <div>
    <el-button>Predeterminado</el-button>
    <el-button type="primary">Primario</el-button>
    <el-button type="success">Éxito</el-button>
    <el-button type="info">Información</el-button>
    <el-button type="warning">Advertencia</el-button>
    <el-button type="danger">Peligro</el-button>
  </div>
</template>
```

### 5.2 Formulario Básico

```html
<template>
  <el-form :model="form" :rules="rules" ref="loginForm" label-width="80px">
    <el-form-item label="Usuario" prop="username">
      <el-input v-model="form.username" placeholder="Ingrese usuario"></el-input>
    </el-form-item>
    <el-form-item label="Contraseña" prop="password">
      <el-input v-model="form.password" type="password" placeholder="Ingrese contraseña"></el-input>
    </el-form-item>
    <el-form-item>
      <el-button type="primary" @click="submitForm">Iniciar sesión</el-button>
      <el-button @click="resetForm">Reiniciar</el-button>
    </el-form-item>
  </el-form>
</template>
```

### 5.3 Tabla Básica

```html
<template>
  <el-table :data="tableData" stripe border>
    <el-table-column prop="date" label="Fecha" width="180"></el-table-column>
    <el-table-column prop="name" label="Nombre" width="180"></el-table-column>
    <el-table-column prop="address" label="Dirección"></el-table-column>
    <el-table-column label="Acciones" width="180">
      <template slot-scope="scope">
        <el-button size="mini" @click="handleEdit(scope.$index, scope.row)">Editar</el-button>
        <el-button size="mini" type="danger" @click="handleDelete(scope.$index, scope.row)">Eliminar</el-button>
      </template>
    </el-table-column>
  </el-table>
</template>
```

### 5.4 Diálogo

```html
<template>
  <el-button type="text" @click="dialogVisible = true">Abrir diálogo</el-button>

  <el-dialog
    title="Editar información"
    :visible.sync="dialogVisible"
    width="30%"
    :before-close="handleClose">
    <span>Este es el contenido del diálogo</span>
    <span slot="footer" class="dialog-footer">
      <el-button @click="dialogVisible = false">Cancelar</el-button>
      <el-button type="primary" @click="dialogVisible = false">Aceptar</el-button>
    </span>
  </el-dialog>
</template>
```

---

## 6. Configuración de Internacionalización

### 6.1 Configuración Global en Chino

```javascript
import locale from 'element-ui/lib/locale/lang/zh-CN'

Vue.use(ElementUI, { locale })
```

---

## 7. Temas Personalizados

### 7.1 Usar Herramienta de Temas en Línea

Visite la página de personalización de temas: https://elementui.github.io/theme-chalk/

---

## 8. Problemas Comunes

### 8.1 Error de Instalación

**Problema**: npm install falla

**Soluciones**:

1. Limpiar caché npm: `npm cache clean --force`
2. Verificar red, puede usar mirrors nacionales

### 8.2 Estilos No Aplicados

**Problema**: Estilos de componentes se muestran anormalmente

**Soluciones**:

1. Asegurarse de importar archivo de estilos
2. Verificar configuración de webpack

---

## 9. Recursos Relacionados

- Documentación oficial: https://element.eleme.io/
- Repositorio GitHub: https://github.com/ElemeFE/element
- Paquete NPM: https://www.npmjs.com/package/element-ui

---

## 10. Notas Importantes

1. **Advertencia de seguridad**: Element UI mantenimiento detenido, hay vulnerabilidades conocidas
2. **Vue 2 EOL**: Vue 2 mantenimiento terminó en 2023
3. **Soporte a largo plazo**: Para proyectos nuevos, use Element Plus (Vue 3)
