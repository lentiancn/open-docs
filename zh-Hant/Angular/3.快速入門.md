# Angular 快速入門

> 🎯 本指南將幫助你在 5 分鐘內建立並執行第一個 Angular 應用
> 
> 📅 更新時間：2026年2月
> 
> 📖 適用版本：Angular 17+

---

## 目錄

1. [前置準備](#1-前置準備)
2. [建立專案](#2-建立專案)
3. [執行應用](#3-執行應用)
4. [理解專案結構](#4-理解專案結構)
5. [修改第一個元件](#5-修改第一個元件)
6. [新增互動功能](#6-新增互動功能)
7. [下一步](#7-下一步)

---

## 1. 前置準備

在開始之前，請確保已安裝：

### 1.1 Node.js

```bash
node --version
# 預期輸出: v18.x.x 或更高版本
```

### 1.2 npm

```bash
npm --version
# 預期輸出: 8.x.x 或更高版本
```

### 1.3 Angular CLI

```bash
ng version
# 預期輸出: Angular CLI 版本資訊
```

如果未安裝，請先閱讀 [安裝指南](./1.安裝指南.md)

---

## 2. 建立專案

開啟終端機，執行以下命令建立新專案：

```bash
ng new hello-angular
```

### 2.1 安裝選項選擇

安裝過程中會詢問以下問題：

| 選項 | 推薦選擇 | 說明 |
|------|----------|------|
| Would you like to add Angular routing? | Yes | 新增路由功能 |
| Which stylesheet format would you like to use? | CSS 或 SCSS | 推薦 SCSS |

等待安裝完成（約 2-5 分鐘）。

### 2.2 進入專案目錄

```bash
cd hello-angular
```

---

## 3. 執行應用

### 3.1 啟動開發伺服器

```bash
ng serve
```

### 3.2 在瀏覽器中開啟

開啟瀏覽器，造訪：http://localhost:4200

你應該能看到類似以下頁面：

![Angular 預設頁面](https://angular.io/generated/images/guide/setup-local/app-works.png)

---

## 4. 理解專案結構

### 4.1 目錄結構

```
hello-angular/
├── src/
│   ├── app/
│   │   ├── app.component.ts       # 根元件
│   │   ├── app.component.html    # 根元件範本
│   │   ├── app.component.css    # 根元件樣式
│   │   ├── app.config.ts        # 應用設定
│   │   └── app.routes.ts        # 路由設定
│   ├── assets/                   # 靜態資源
│   ├── main.ts                  # 應用入口
│   └── index.html               # HTML 入口
├── angular.json                  # CLI 設定
├── package.json                  # 依賴設定
└── tsconfig.json                 # TS 設定
```

### 4.2 根元件檔案

開啟 `src/app/app.component.ts`：

```typescript
import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';
import { RouterOutlet, RouterLink } from '@angular/router';

@Component({
  selector: 'app-root',
  standalone: true,
  imports: [CommonModule, RouterOutlet, RouterLink],
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css']
})
export class AppComponent {
  title = 'hello-angular';
}
```

---

## 5. 修改第一個元件

### 5.1 修改範本

開啟 `src/app/app.component.html`，替換為：

```html
<div class="container">
  <h1>歡迎來到 Angular！</h1>
  <p>🎉 你的第一個 Angular 應用正在執行。</p>
  
  <div class="card">
    <h2>{{ title }}</h2>
    <p>這是透過插值綁定顯示的變數。</p>
  </div>
</div>
```

### 5.2 修改樣式

開啟 `src/app/app.component.css`，新增：

```css
.container {
  max-width: 800px;
  margin: 0 auto;
  padding: 2rem;
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
}

h1 {
  color: #dd0031;
}

.card {
  background: #f5f5f5;
  padding: 1.5rem;
  border-radius: 8px;
  margin-top: 1rem;
}
```

### 5.3 查看效果

儲存檔案後，瀏覽器會自動重新整理，你應該能看到更新後的頁面。

---

## 6. 新增互動功能

### 6.1 建立計數器功能

在 `app.component.ts` 中新增：

```typescript
import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
  selector: 'app-root',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css']
})
export class AppComponent {
  title = 'hello-angular';
  count = 0;
  
  increment(): void {
    this.count++;
  }
  
  decrement(): void {
    this.count--;
  }
}
```

### 6.2 在範本中使用

更新 `app.component.html`：

```html
<div class="container">
  <h1>歡迎來到 Angular！</h1>
  
  <div class="card">
    <h2>計數器範例</h2>
    <p class="count">{{ count }}</p>
    <div class="buttons">
      <button (click)="decrement()">-</button>
      <button (click)="increment()">+</button>
    </div>
  </div>
</div>
```

### 6.3 新增按鈕樣式

更新 `app.component.css`：

```css
.count {
  font-size: 3rem;
  font-weight: bold;
  text-align: center;
  margin: 1rem 0;
}

.buttons {
  display: flex;
  gap: 1rem;
  justify-content: center;
}

button {
  padding: 0.5rem 1.5rem;
  font-size: 1.2rem;
  border: none;
  border-radius: 4px;
  cursor: pointer;
  background: #dd0031;
  color: white;
  transition: background 0.2s;
}

button:hover {
  background: #b3002d;
}
```

### 6.4 查看效果

儲存後，瀏覽器自動重新整理，你可以點擊按鈕測試互動功能。

---

## 7. 下一步

### 7.1 學習更多

- 📖 閱讀 [使用手冊](./2.使用手冊.md) 深入學習
- 🛠️ 查看 [安裝指南](./1.安裝指南.md) 了解環境設定

### 7.2 建立新元件

```bash
ng generate component my-component
```

### 7.3 建立新服務

```bash
ng generate service services/my-service
```

### 7.4 新增路由

```bash
ng generate component pages/home
ng generate component pages/about
```

然後在 `app.routes.ts` 中設定路由。

### 7.5 建置生產版本

```bash
ng build --configuration production
```

建置產物在 `dist/hello-angular` 目錄下。

---

## 🎉 恭喜！

你已經完成了 Angular 快速入門。Angular 還有更多強大的功能等待你去探索：

- 📦 元件化開發
- 🌐 路由系統
- 📝 表單處理
- 🔌 HTTP 通訊
- 📊 狀態管理
- 🧪 測試工具

祝你學習愉快！

---

*本文檔由 AI 助手生成*
