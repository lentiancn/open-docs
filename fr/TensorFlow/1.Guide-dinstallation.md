# TensorFlow Guide d'Installation

Ce guide vous aide à installer TensorFlow.

## Prérequis

- Python 3.7-3.11
- pip 19.0+

## Installation

### Version CPU

```bash
pip install tensorflow
```

### Version GPU

```bash
pip install tensorflow-gpu
```

### Vérifier l'Installation

```python
import tensorflow as tf
print(tf.__version__)
print("GPU Available:", tf.config.list_physical_devices('GPU'))
```

## Environnement Virtuel

### Using venv

```bash
python -m venv tf-env
source tf-env/bin/activate  # Linux/macOS
tf-env\Scripts\activate    # Windows
pip install tensorflow
```

### Using Conda

```bash
conda create -n tf-env python=3.10
conda activate tf-env
conda install tensorflow
```

## Configuration GPU

### Vérifier GPU

```python
import tensorflow as tf
gpus = tf.config.list_physical_devices('GPU')
print(gpus)
```

### Configurer Mémoire GPU

```python
gpus = tf.config.list_physical_devices('GPU')
if gpus:
    try:
        for gpu in gpus:
            tf.config.experimental.set_memory_growth(gpu, True)
    except RuntimeError as e:
        print(e)
```

## Docker

### Avec GPU

```bash
docker pull tensorflow/tensorflow:latest-gpu
docker run -it --gpus all -v $(pwd):/workspace tensorflow/tensorflow:latest-gpu
```

## Prochaines Étapes

- Lisez le [Guide d'Utilisation](./2.Guide-dutilisation.md) pour commencer
