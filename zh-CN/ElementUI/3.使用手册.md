# Element UI 使用手册

本手册将帮助你快速上手 Element UI，掌握日常使用中常用的组件和功能。

## 快速开始

在使用 Element UI 之前，确保已完成安装配置。

### 基础使用

在 Vue 组件中直接使用组件：

```vue
<template>
  <el-button>默认按钮</el-button>
  <el-button type="primary">主要按钮</el-button>
  <el-button type="success">成功按钮</el-button>
  <el-button type="warning">警告按钮</el-button>
  <el-button type="danger">危险按钮</el-button>
</template>
```

## 常用组件

### 按钮（Button）

基础用法：

```vue
<el-button>默认</el-button>
<el-button type="primary">主要</el-button>
<el-button type="success">成功</el-button>
<el-button type="warning">警告</el-button>
<el-button type="danger">危险</el-button>
```

不同尺寸：

```vue
<el-button size="medium">中型</el-button>
<el-button>默认</el-button>
<el-button size="small">小型</el-button>
<el-button size="mini">微型</el-button>
```

禁用状态：

```vue
<el-button disabled>禁用</el-button>
```

图标按钮：

```vue
<el-button icon="el-icon-search">搜索</el-button>
<el-button icon="el-icon-edit">编辑</el-button>
<el-button icon="el-icon-delete">删除</el-button>
```

### 输入框（Input）

基础用法：

```vue
<el-input v-model="input" placeholder="请输入内容" />
```

带图标：

```vue
<el-input v-model="input" prefix-icon="el-icon-search" />
<el-input v-model="input" suffix-icon="el-icon-date" />
```

带清空按钮：

```vue
<el-input v-model="input" clearable />
```

密码输入框：

```vue
<el-input v-model="input" type="password" show-password />
```

### 选择器（Select）

基础选择器：

```vue
<el-select v-model="value" placeholder="请选择">
  <el-option label="选项1" value="1" />
  <el-option label="选项2" value="2" />
</el-select>
```

多选：

```vue
<el-select v-model="value" multiple placeholder="请选择">
  <el-option label="选项1" value="1" />
  <el-option label="选项2" value="2" />
</el-select>
```

### 表格（Table）

基础表格：

```vue
<el-table :data="tableData" stripe border>
  <el-table-column prop="date" label="日期" width="180" />
  <el-table-column prop="name" label="姓名" width="180" />
  <el-table-column prop="address" label="地址" />
</el-table>
```

### 表单（Form）

基础表单：

```vue
<el-form :model="form" label-width="80px">
  <el-form-item label="活动名称">
    <el-input v-model="form.name" />
  </el-form-item>
  <el-form-item>
    <el-button type="primary" @click="onSubmit">提交</el-button>
  </el-form-item>
</el-form>
```

### 对话框（Dialog）

```vue
<el-dialog :visible.sync="dialogVisible" title="提示" width="30%">
  <span>内容</span>
</el-dialog>
```

### 消息提示

```javascript
import { Message } from 'element-ui'

Message.success('成功消息')
Message.warning('警告消息')
Message.error('错误消息')
```

### 菜单（Menu）

水平菜单：

```vue
<el-menu mode="horizontal">
  <el-menu-item index="1">处理中心</el-menu-item>
  <el-menu-item index="2">订单管理</el-menu-item>
</el-menu>
```

### 标签页（Tabs）

```vue
<el-tabs v-model="activeName">
  <el-tab-pane label="用户管理" name="first">用户管理</el-tab-pane>
  <el-tab-pane label="配置管理" name="second">配置管理</el-tab-pane>
</el-tabs>
```

### 分页（Pagination）

```vue
<el-pagination
  @current-change="handleCurrentChange"
  :current-page="currentPage"
  :page-size="100"
  :total="1000"
  layout="total, prev, pager, next"
/>
```

## 布局系统

### 栅格系统

Element UI 使用 24 栏栅格系统：

```vue
<el-row :gutter="20">
  <el-col :span="12"><div>12栏</div></el-col>
  <el-col :span="12"><div>12栏</div></el-col>
</el-row>
```

### 容器布局

```vue
<el-container>
  <el-header>Header</el-header>
  <el-main>Main</el-main>
  <el-footer>Footer</el-footer>
</el-container>
```

## 主题定制

### 使用 SCSS 变量

```scss
$--color-primary: #409eff;
```

### 在线主题生成

访问 https://elementui.github.io/theme-chalk-preview/ 在线生成主题。

## 最佳实践

### 响应式设计

```vue
<el-row>
  <el-col :xs="24" :sm="12" :md="8" :lg="6">响应式列</el-col>
</el-row>
```

### 表单验证

始终对用户输入进行验证，提供清晰的错误提示。

## 常见问题

### 组件样式不生效

确保已正确引入 CSS：

```javascript
import 'element-ui/lib/theme-chalk/index.css'
```

### 图标不显示

需要引入图标字体：

```javascript
import 'element-ui/lib/theme-chalk/fonts element-icons.ttf'
```
