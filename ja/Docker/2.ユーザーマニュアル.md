# Docker ユーザーマニュアル

> 🎯 Docker のコアコンセプト、基本的な使用法、開発ガイド
> 
> 📅 更新：2026年2月
> 
> 📖 バージョン：Docker 27.x

---

## 1. コアコンセプト

### 1.1 イメージ

イメージはコンテナを作成するための読み取り専用テンプレートです。

```bash
# イメージ一覧
docker images

# イメージをプル
docker pull ubuntu:22.04

# イメージを削除
docker rmi ubuntu:22.04

# イメージをビルド
docker build -t my-image:tag .
```

### 1.2 コンテナ

コンテナはイメージの実行インスタンスです。

```bash
# コンテナを実行
docker run -it ubuntu /bin/bash
docker run -d nginx:latest

# コンテナ一覧
docker ps
docker ps -a

# コンテナを停止/起動
docker stop container_id
docker start container_id

# コンテナを削除
docker rm container_id

# コンテナに入る
docker exec -it container_id /bin/bash
```

---

## 2. 一般的なコマンド

### 2.1 イメージ操作

```bash
docker pull nginx
docker images
docker rmi nginx
docker build -t my-app .
```

### 2.2 コンテナ操作

```bash
docker run -d -p 8080:80 nginx
docker stop container_id
docker rm container_id
docker logs container_id
docker exec -it container_id /bin/bash
```

---

## 3. Dockerfile

### 3.1 基本的な構文

```dockerfile
FROM ubuntu:22.04
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
EXPOSE 3000
CMD ["npm", "start"]
```

### 3.2 一般的な命令

| 命令 | 説明 |
|------|------|
| FROM | ベースイメージ |
| RUN | コマンド実行 |
| COPY | ファイルコピー |
| WORKDIR | 作業ディレクトリ |
| ENV | 環境変数 |
| EXPOSE | ポート公開 |
| CMD | デフォルトコマンド |

---

## 4. Docker Compose

### 4.1 docker-compose.yml

```yaml
version: '3.8'
services:
  web:
    build: .
    ports:
      - "3000:3000"
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
```

### 4.2 一般的なコマンド

```bash
docker-compose up -d
docker-compose down
docker-compose logs -f
```

---

## 5. ネットワーク管理

```bash
docker network create my-network
docker network ls
```

---

## 6. データ管理

### 6.1 ボリューム

```bash
docker volume create my-volume
docker volume ls
```

### 6.2 バインドマウント

```bash
docker run -v /host/path:/container/path my-app
```

---

## 7. 監視とデバッグ

```bash
docker logs container_id
docker logs -f container_id
docker stats
docker exec -it container_id /bin/bash
```

---

## 📚 リソース

- 官网：https://docs.docker.com

---

*AI生成*
