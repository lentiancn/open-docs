# React 使用指南

高效使用 React 的完整指南。

---

## JSX

### 基本语法

```jsx
function App() {
  return (
    <div>
      <h1>Hello World</h1>
      <p>这是 React</p>
    </div>
  );
}
```

---

## 组件

### 函数组件

```jsx
function Welcome() {
  return <h1>Hello World</h1>;
}
```

### 带 props 的组件

```jsx
function Welcome(props) {
  return <h1>Hello, {props.name}</h1>;
}

// 使用
<Welcome name="John" />
```

---

## 状态 (useState)

### 基本状态

```jsx
import { useState } from 'react';

function Counter() {
  const [count, setCount] = useState(0);

  return (
    <div>
      <p>计数: {count}</p>
      <button onClick={() => setCount(count + 1)}>增加</button>
    </div>
  );
}
```

---

## 副作用 (useEffect)

### 基本效果

```jsx
import { useState, useEffect } from 'react';

function DataFetcher() {
  const [data, setData] = useState(null);

  useEffect(() => {
    fetch('https://api.example.com/data')
      .then(res => res.json())
      .then(data => setData(data));
  }, []); // 空数组 = 挂载时运行一次

  return <div>{data}</div>;
}
```

---

## 条件渲染

### 三元运算符

```jsx
function Greeting({ isLoggedIn }) {
  return (
    <div>
      {isLoggedIn ? <h1>欢迎回来！</h1> : <h1>请登录</h1>}
    </div>
  );
}
```

### 逻辑 &&

```jsx
function Greeting({ show }) {
  return (
    <div>
      {show && <p>你好！</p>}
    </div>
  );
}
```

---

## 列表和 key

### 渲染列表

```jsx
function List() {
  const items = ['苹果', '香蕉', '橙子'];

  return (
    <ul>
      {items.map((item, index) => (
        <li key={index}>{item}</li>
      ))}
    </ul>
  );
}
```

---

## 表单

### 受控输入

```jsx
function Form() {
  const [name, setName] = useState('');

  const handleSubmit = (e) => {
    e.preventDefault();
    console.log(name);
  };

  return (
    <form onSubmit={handleSubmit}>
      <input
        type="text"
        value={name}
        onChange={(e) => setName(e.target.value)}
      />
      <button type="submit">提交</button>
    </form>
  );
}
```

---

## Context

### 创建 Context

```jsx
import { createContext, useContext } from 'react';

const ThemeContext = createContext('light');

function App() {
  return (
    <ThemeContext.Provider value="dark">
      <Toolbar />
    </ThemeContext.Provider>
  );
}

function Toolbar() {
  const theme = useContext(ThemeContext);
  return <div>主题: {theme}</div>;
}
```

---

## 事件处理

### 基本事件

```jsx
function Events() {
  const handleClick = () => {
    console.log('点击了！');
  };

  return (
    <div>
      <button onClick={handleClick}>点击我</button>
    </div>
  );
}
```

---

## 样式

### 内联样式

```jsx
const style = {
  color: 'blue',
  fontSize: '16px'
};

function Styled() {
  return <p style={style}>样式文本</p>;
}
```

---

## 最佳实践

1. **使用函数组件** - 优先使用带 hooks 的函数组件
2. **保持组件小** - 拆分大型组件
3. **使用有意义的名称** - 组件名称应描述其用途
4. **正确使用 key** - 为列表提供唯一的 key

---

## 下一步

- 探索 [React 文档](https://react.dev/)
