# Angular FAQ

Frequently asked questions and solutions.

## Installation Issues

### Q: Angular CLI installation failed?

**Solution**:
1. Check Node.js version
```bash
node --version  # Requires 18+ or 20+
```
2. Clean npm cache
```bash
npm cache clean --force
```
3. Use administrator privileges (Windows)
4. Check network connection

### Q: ng command not found?

**Solution**:
1. Confirm Angular CLI is installed
```bash
npm list -g @angular/cli
```
2. Check environment variables
3. Reinstall
```bash
npm uninstall -g @angular/cli
npm install -g @angular/cli@latest
```

### Q: Project creation failed?

**Solution**:
1. Check disk space
2. Check npm permissions
3. Use --skip-install to skip installation
```bash
ng new my-app --skip-install
cd my-app
npm install
```

## Development Issues

### Q: Component not displaying?

**Solution**:
1. Confirm component is properly imported
2. Confirm selector is correctly used
3. Check template syntax
4. Check console errors

### Q: Data not updating?

**Solution**:
1. For Signals, use set/update methods
```typescript
count.set(1);           // Set new value
count.update(c => c + 1); // Update based on old value
```
2. For RxJS, ensure proper subscription
3. Use ChangeDetectorRef for manual detection
```typescript
constructor(private cdr: ChangeDetectorRef) {}

refresh() {
  this.cdr.detectChanges();
}
```

### Q: Routing not working?

**Solution**:
1. Confirm route configuration is correct
2. Confirm RouterOutlet is placed
3. Check route order
```typescript
// Specific routes first, wildcard last
{ path: 'user/:id', ... },
{ path: '**', component: NotFoundComponent }
```

### Q: Form validation not working?

**Solution**:
1. Import ReactiveFormsModule or FormsModule
2. Confirm FormControl name is correct
3. Use validators properly
```typescript
this.form = this.fb.group({
  email: ['', [Validators.required, Validators.email]],
  password: ['', [Validators.required, Validators.minLength(6)]]
});
```

## Performance Issues

### Q: Slow initial load?

**Solution**:
1. Use lazy loading routes
```typescript
{
  path: 'admin',
  loadComponent: () => import('./admin/admin.component')
    .then(m => m.AdminComponent)
}
```
2. Enable production mode
```bash
ng build --configuration=production
```
3. AOT compilation is enabled by default

### Q: Memory leaks?

**Solution**:
1. Unsubscribe properly
```typescript
import { Subject } from 'rxjs';
import { takeUntil } from 'rxjs/operators';

@Component({...})
export class MyComponent implements OnDestroy {
  private destroy$ = new Subject<void>();

  ngOnInit() {
    this.http.get('/api').pipe(
      takeUntil(this.destroy$)
    ).subscribe();
  }

  ngOnDestroy() {
    this.destroy$.next();
    this.destroy$.complete();
  }
}
```
2. Use async pipe for automatic subscribe/unsubscribe

### Q: Bundle size too large?

**Solution**:
1. Check dependency sizes
```bash
npm run build -- --stats-json
npx webpack-bundle-analyzer dist/stats.json
```
2. Use lazy loading
3. Remove unused imports

## Testing Issues

### Q: How to write unit tests?

**Solution**:
```typescript
import { ComponentFixture, TestBed } from '@angular/core/testing';
import { UserCardComponent } from './user-card.component';

describe('UserCardComponent', () => {
  let component: UserCardComponent;
  let fixture: ComponentFixture<UserCardComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [UserCardComponent]
    }).compileComponents();

    fixture = TestBed.createComponent(UserCardComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });

  it('should display user name', () => {
    component.user = { name: 'John', email: 'test@example.com' };
    fixture.detectChanges();
    const compiled = fixture.nativeElement as HTMLElement;
    expect(compiled.querySelector('h3')?.textContent).toContain('John');
  });
});
```

### Q: Tests can't find modules?

**Solution**:
```typescript
TestBed.configureTestingModule({
  imports: [
    CommonModule,
    HttpClientModule,
    // Other needed modules
  ]
});
```

## Build Issues

### Q: Build failed?

**Solution**:
1. Check TypeScript errors
```bash
npx tsc --noEmit
```
2. Clean cache
```bash
rm -rf node_modules
npm install
```
3. Update Angular version
```bash
ng update @angular/core @angular/cli
```

### Q: Environment variables not working?

**Solution**:
1. Confirm environment file name is correct
2. Configure environment in angular.json
```json
"configurations": {
  "production": {
    "fileReplacements": [
      {
        "replace": "src/environments/environment.ts",
        "with": "src/environments/environment.prod.ts"
      }
    ]
  }
}
```

## Security Issues

### Q: How to prevent XSS attacks?

**Solution**:
1. Angular automatically escapes by default
2. Use innerHTML carefully
```typescript
import { DomSanitizer } from '@angular/platform-browser';

constructor(private sanitizer: DomSanitizer) {}

getHtml() {
  // Only use for trusted content
  return this.sanitizer.bypassSecurityTrustHtml(this.trustedContent);
}
```

### Q: CSRF protection?

**Solution**:
```typescript
import { HttpClientModule } from '@angular/common/http';

@NgModule({
  imports: [
    HttpClientModule,
    HttpClientXsrfModule.withOptions({
      cookieName: 'XSRF-TOKEN',
      headerName: 'X-XSRF-TOKEN'
    })
  ]
})
export class AppModule {}
```

## Upgrade Issues

### Q: How to upgrade Angular version?

**Solution**:
1. Use ng update
```bash
ng update @angular/core@17 @angular/cli@17
```
2. Check migration guide
3. Update dependency versions
4. Run tests to ensure compatibility

### Q: Code incompatible after upgrade?

**Solution**:
1. Check official upgrade guide
2. Use ng update for automatic migration
3. Migrate gradually, don't do major version upgrades all at once

## Learning Resources

### Q: Where to learn Angular?

- Official docs: https://angular.io
- Angular University: https://angular-university.io
- StackBlitz: Write Angular code online

### Q: How to get help?

- Stack Overflow: https://stackoverflow.com/questions/tagged/angular
- Discord: https://discord.com/invite/angular
- GitHub Discussions: https://github.com/angular/angular/discussions
