# Docker インストールガイド

> 🎯 Docker の詳細なインストール方法
> 
> 📅 更新：2026年2月
> 
> 📖 バージョン：Docker 27.x

---

## 1. Docker 简介

### 1.1 Docker とは？

Dockerは、アプリケーションを開発、デプロイ、実行するためのオープンソースのプラットフォームです。コンテナ化技術により、アプリケーションとその依存関係を軽量でポータブルなコンテナにパッケージ化できます。

### 1.2 Docker バージョン

| バージョン | リリース日 |
|-----------|----------|
| Docker 27.x | 2024年 |
| Docker 26.x | 2024年 |

---

## 2. 前提条件

### 2.1 Windows

- Windows 10 64ビット Pro/Enterprise または Windows 11
- 4GB RAM
- 64GB SSD

### 2.2 macOS

- macOS 10.15 (Catalina) 以降
- 4GB RAM
- Intel または Apple Silicon (M1/M2/M3)

---

## 3. Windows にインストール

### 3.1 Docker Desktop（推奨）

1. 官网にアクセス：https://www.docker.com/products/docker-desktop
2. 「Download for Windows」をクリック
3. インストールを実行
4. 確認：
```powershell
docker --version
docker run hello-world
```

---

## 4. macOS にインストール

### 4.1 Docker Desktop（推奨）

1. 官网にアクセス：https://www.docker.com/products/docker-desktop
2. 「Download for Mac」をクリック
3. チップタイプを選択：Apple Silicon または Intel
4. 確認：
```bash
docker --version
docker run hello-world
```

---

## 5. Linux にインストール

### 5.1 Ubuntu

```bash
# 依存関係をインストール
sudo apt install ca-certificates curl gnupg lsb-release

# Docker GPG キーを追加
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg

# Docker リポジトリを追加
echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list

# Docker をインストール
sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

# Docker を起動
sudo systemctl start docker
sudo systemctl enable docker
```

---

## 6. インストール確認

```bash
docker --version
docker run hello-world
```

---

## 7. 設定

### 7.1 レジストーミラー（中国）

```json
{
  "registry-mirrors": [
    "https://docker.mirrors.ustc.edu.cn"
  ]
}
```

Linux の設定：
```bash
sudo mkdir -p /etc/docker
sudo tee /etc/docker/daemon.json <<EOF
{
  "registry-mirrors": [
    "https://docker.mirrors.ustc.edu.cn"
  ]
}
EOF
sudo systemctl daemon-reload
sudo systemctl restart docker
```

---

## 8. よくある質問

### Q: Docker Desktop が起動しない？

BIOSで仮想化が有効になっていることを確認し、WSL 2を正しくインストールしてください。

### Q: イメージのダウンロードが遅い？

国内ミラーを設定してください（上記参照）。

### Q: 権限がない？

```bash
sudo usermod -aG docker $USER
# 再ログイン
```

---

## 📚 リソース

- 官网：https://docs.docker.com

---

*AI生成*
