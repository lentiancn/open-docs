# Docker 常见问题

> 🎯 解答 Docker 开发中遇到的常见问题
> 
> 📅 更新时间：2026年2月
> 
> 📖 适用版本：Docker 27.x

---

## 目录

1. [安装问题](#1-安装问题)
2. [镜像问题](#2-镜像问题)
3. [容器问题](#3-容器问题)
4. [网络问题](#4-网络问题)
5. [存储问题](#5-存储问题)
6. [性能问题](#6-性能问题)

---

## 1. 安装问题

### Q1: Docker Desktop 启动失败？

**解决方案**：
1. 确保 BIOS 中已启用虚拟化
2. 确保 WSL 2 已正确安装（Windows）
3. 重启电脑后重试

---

### Q2: docker 命令找不到？

确保 Docker 已正确安装并添加到 PATH。

```bash
# 重新加载 shell
source ~/.bashrc

# 或手动添加到 PATH
export PATH=$PATH:/usr/local/bin
```

---

### Q3: 权限不足？

```bash
# 添加用户到 docker 组
sudo usermod -aG docker $USER

# 重新登录
```

---

## 2. 镜像问题

### Q4: 镜像下载太慢？

配置国内镜像加速器：

```json
{
  "registry-mirrors": [
    "https://docker.mirrors.ustc.edu.cn",
    "https://hub-mirror.c.163.com"
  ]
}
```

---

### Q5: 如何删除未使用的镜像？

```bash
# 删除 dangling 镜像
docker image prune

# 删除所有未使用镜像
docker image prune -a
```

---

## 3. 容器问题

### Q6: 容器无法启动？

```bash
# 查看容器日志
docker logs container_id

# 查看详情
docker inspect container_id
```

---

### Q7: 容器自动退出？

检查启动命令是否正确：

```dockerfile
CMD ["tail", "-f", "/dev/null"]
```

---

### Q8: 如何进入运行中的容器？

```bash
docker exec -it container_id /bin/bash
```

---

## 4. 网络问题

### Q9: 容器无法访问外部网络？

```bash
# 检查 DNS
docker exec container_id cat /etc/resolv.conf

# 重启 Docker
sudo systemctl restart docker
```

---

### Q10: 端口冲突？

```bash
# 查看占用端口
netstat -tulpn | grep PORT

# 使用其他端口
docker run -p 8081:80 nginx
```

---

## 5. 存储问题

### Q11: Docker 占用太多空间？

```bash
# 查看空间使用
docker system df

# 清理所有未使用资源
docker system prune -a
```

---

### Q12: 数据如何持久化？

使用卷：

```bash
docker run -v /host/path:/container/path my-app
```

---

## 6. 性能问题

### Q13: 容器运行慢？

1. 增加资源限制：
```bash
docker run -m 2g --cpus=2 my-app
```

2. 使用轻量级镜像：
```dockerfile
FROM node:18-alpine
```

---

### Q14: 构建速度慢？

使用构建缓存：

```dockerfile
# 先复制依赖文件
COPY package*.json ./
RUN npm install
# 再复制代码
COPY . .
```

---

## 📚 相关资源

- 官方文档：https://docs.docker.com

---

*本文档由 AI 助手生成*
