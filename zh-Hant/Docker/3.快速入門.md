# Docker 快速入門

> 🎯 10 分鐘快速上手 Docker
> 
> 📅 更新時間：2026年2月
> 
> 📖 適用版本：Docker 27.x

---

## 什麼是 Docker？

Docker 是一個開源的容器化平台。容器是輕量級的，包含運行軟件所需的一切：代碼、運行時、系統工具和庫。

---

## 前置要求

| 操作系統 | 要求 |
|----------|------|
| Windows | Windows 10 Pro 或 Windows 11 |
| macOS | mac15+ |
|OS 10. Linux | Ubuntu 20.04+ 或類似版本 |

---

## 步驟 1：安裝 Docker

### Windows / macOS

下載並安裝 Docker Desktop：
https://www.docker.com/products/docker-desktop

### Linux (Ubuntu)

```bash
curl -fsSL https://get.docker.com | sh
sudo usermod -aG docker $USER
```

---

## 步驟 2：驗證安裝

```bash
docker --version
docker run hello-world
```

預期輸出：
```
Hello from Docker!
This message shows that your installation appears to be working correctly.
```

---

## 步驟 3：第一個容器

### 運行 Nginx Web 服務器

```bash
docker run -d -p 8080:80 nginx
```

在瀏覽器中訪問 http://localhost:8080

### 運行 Ubuntu

```bash
docker run -it ubuntu /bin/bash
```

在容器內：
```bash
echo "Hello from container!"
exit
```

---

## 步驟 4：基本命令

### 鏡像

```bash
# 拉取鏡像
docker pull nginx

# 查看鏡像列表
docker images

# 刪除鏡像
docker rmi nginx
```

### 容器

```bash
# 查看運行中的容器
docker ps

# 查看所有容器
docker ps -a

# 停止容器
docker stop container_id

# 刪除容器
docker rm container_id
```

---

## 步驟 5：創建自己的鏡像

### 創建項目

```bash
mkdir my-app
cd my-app
```

### 創建 Dockerfile

```dockerfile
FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
EXPOSE 3000
CMD ["node", "-e", "console.log('Hello from my app!')"]
```

### 構建和運行

```bash
docker build -t my-app .
docker run my-app
```

---

## 步驟 6：使用 Docker Compose

### 創建 docker-compose.yml

```yaml
version: '3.8'
services:
  web:
    image: nginx
    ports:
      - "8080:80"
  db:
    image: postgres:15
    environment:
      POSTGRES_PASSWORD: secret
```

### 啟動服務

```bash
docker-compose up -d
```

### 停止服務

```bash
docker-compose down
```

---

## 常見操作

### 端口映射

```bash
docker run -p 8080:80 nginx
# 主機端口 8080 -> 容器端口 80
```

### Volume 掛載

```bash
docker run -v /host/path:/container/path nginx
```

### 環境變量

```bash
docker run -e MY_VAR=value nginx
```

---

## 下一步

### 深入學習

- [安裝指南](./1.安裝指南.md) - 完整安裝教程
- [使用手冊](./2.使用手冊.md) - 完整 Docker 指南
- [常見問題](./4.常見問題.md) - 常見問題

---

## 資源

- 官方文檔：https://docs.docker.com
- Docker Hub：https://hub.docker.com

---

*本文檔由 AI 助手生成*
