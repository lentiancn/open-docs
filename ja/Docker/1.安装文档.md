# Docker インストールガイド

すべての主要プラットフォームへの Docker インストール方法を説明します。

---

## 目次

- [Docker Desktop（推奨）](#docker-desktop推奨)
- [Linux](#linux)
- [macOS](#macos)
- [Windows](#windows)
- [インストール確認](#インストール確認)

---

## Docker Desktop（推奨）

macOS、Windows、Linux に最适合です。

### 必要環境

**macOS:**
- Apple Silicon: macOS 11.0+, 4GB RAM 以上
- Intel: macOS 10.15+, 4GB RAM 以上

**Windows:**
- Windows 10/11 64-bit
- WSL2（推奨）または Hyper-V

**Linux:**
- Ubuntu、Debian、Fedora、Arch Linux
- 4GB RAM 以上

### ダウンロード

- [Docker Desktop for Mac](https://desktop.docker.com/mac/main/amd64/Docker.dmg)
- [Docker Desktop for Windows](https://desktop.docker.com/win/main/amd64/Docker%20Desktop%20Installer.exe)

---

## Linux

### Ubuntu

```bash
sudo apt update
sudo apt install -y ca-certificates curl gnupg lsb-release

sudo install -m 0755 -d /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
sudo chmod a+r /etc/apt/keyrings/docker.gpg

echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

sudo apt update
sudo apt install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

sudo systemctl start docker
sudo systemctl enable docker
sudo usermod -aG docker $USER
```

### CentOS/RHEL

```bash
sudo yum install -y yum-utils
sudo yum-config-manager --add-repo https://download.docker.com/linux/centos/docker-ce.repo
sudo yum install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
sudo systemctl start docker
sudo systemctl enable docker
```

### Fedora

```bash
sudo dnf -y install dnf-plugins-core
sudo dnf config-manager --add-repo https://download.docker.com/linux/fedora/docker-ce.repo
sudo dnf install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
sudo systemctl start docker
sudo systemctl enable docker
```

---

## macOS

```bash
brew install --cask docker
```

または [Docker Desktop for Mac](https://desktop.docker.com/mac/main/amd64/Docker.dmg) をダウンロード

---

## Windows

### WSL2（推奨）

```powershell
wsl --install
# 再起動後
sudo apt update
sudo apt install -y docker.io
```

### Docker Desktop

1. [Docker Desktop for Windows](https://desktop.docker.com/win/main/amd64/Docker%20Desktop%20Installer.exe) をダウンロード
2. インストール実行
3. WSL2 を選択（推奨）
4. Docker Desktop を起動

---

## インストール確認

```bash
docker --version
docker compose version
docker run hello-world
```

---

## 次のステップ

- [使用ガイド](./2.使用指南.md)
- [Docker 公式ドキュメント](https://docs.docker.com/)
