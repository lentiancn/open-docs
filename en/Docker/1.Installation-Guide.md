# Docker Installation Guide

> 🎯 This document provides detailed installation instructions for Docker on various operating systems
> 
> 📅 Last Updated: February 2026
> 
> 📖 Applicable Version: Docker 27.x

---

## Table of Contents

1. [Introduction](#1-introduction)
2. [Prerequisites](#2-prerequisites)
3. [Installing on Windows](#3-installing-on-windows)
4. [Installing on macOS](#4-installing-on-macos)
5. [Installing on Linux](#5-installing-on-linux)
6. [Verifying Installation](#6-verifying-installation)
7. [Docker Desktop vs Docker Engine](#7-docker-desktop-vs-docker-engine)
8. [Configuring Docker](#8-configuring-docker)
9. [Uninstalling Docker](#9-uninstalling-docker)
10. [FAQ](#10-faq)

---

## 1. Introduction

### 1.1 What is Docker?

Docker is an open-source platform for developing, deploying, and running applications. Through containerization technology, Docker can package applications and their dependencies into lightweight, portable containers.

### 1.2 Core Concepts

- **Image**: A read-only template for creating containers
- **Container**: A running instance of an image
- **Repository**: Where images are stored
- **Dockerfile**: Script that defines how to build an image

### 1.3 Docker Versions

| Version | Release Date | Notes |
|---------|--------------|-------|
| Docker 27.x | 2024 | Latest stable |
| Docker 26.x | 2024 | Stable |
| Docker 25.x | 2023 | Stable |

---

## 2. Prerequisites

### 2.1 Windows Requirements

| Requirement | Minimum Version |
|-------------|-----------------|
| OS | Windows 10 64-bit Pro/Enterprise or Windows 11 |
| Memory | 4GB RAM |
| Storage | 64GB SSD |
| Virtualization | Must be enabled in BIOS |

**Note**: Windows Home edition does not support Docker Desktop.

### 2.2 macOS Requirements

| Requirement | Minimum Version |
|-------------|-----------------|
| OS | macOS 10.15 (Catalina) or higher |
| Memory | 4GB RAM |
| Storage | 64GB SSD |
| Chip | Intel or Apple Silicon (M1/M2/M3) |

### 2.3 Linux Requirements

| Component | Requirement |
|-----------|-------------|
| Kernel | 3.10 or higher |
| Architecture | x86_64, ARM64, ARMv7 |
| Storage Driver | overlay2 |

---

## 3. Installing on Windows

### 3.1 Method One: Docker Desktop (Recommended)

#### Step 1: Download Docker Desktop

1. Visit Docker website: https://www.docker.com/products/docker-desktop
2. Click "Download for Windows"
3. Run the installer `Docker Desktop Installer.exe`

#### Step 2: Install Docker Desktop

1. Double-click the installer
2. Check "Use WSL 2 instead of Hyper-V" (recommended)
3. Click "Install"
4. After installation, click "Close"

#### Step 3: Start Docker Desktop

1. Find "Docker Desktop" in Start menu
2. Login to Docker Hub (optional)
3. Wait for Docker engine to start

#### Step 4: Verify Installation

Open PowerShell or command prompt:

```powershell
docker --version
docker-compose --version
docker run hello-world
```

### 3.2 Method Two: Winget

```powershell
winget install Docker.DockerDesktop
```

---

## 4. Installing on macOS

### 4.1 Method One: Docker Desktop (Recommended)

#### Step 1: Download Docker Desktop

1. Visit https://www.docker.com/products/docker-desktop
2. Click "Download for Mac"
3. Choose chip type:
   - Apple Silicon: "Apple Chip"
   - Intel: "Intel Chip"

#### Step 2: Install Docker Desktop

1. Double-click `Docker.dmg`
2. Drag Docker icon to Applications folder
3. Launch Docker Desktop

#### Step 4: Verify Installation

Open terminal:

```bash
docker --version
docker-compose --version
docker run hello-world
```

### 4.2 Method Two: Homebrew

```bash
# Install Homebrew if needed
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# Install Docker
brew install --cask docker
```

### 4.3 Apple Silicon Notes

On M1/M2/M3 Macs:

1. Ensure Apple Silicon version of Docker Desktop is installed
2. Most images already have arm64 version
3. To run x86 images:
```bash
docker run --platform linux/amd64 image-name
```

---

## 5. Installing on Linux

### 5.1 Ubuntu / Debian

#### Step 1: Uninstall old versions

```bash
sudo apt remove docker docker-engine docker.io containerd runc
```

#### Step 2: Install dependencies

```bash
sudo apt update
sudo apt install ca-certificates curl gnupg lsb-release
```

#### Step 3: Add Docker GPG key

```bash
sudo install -m 0755 -d /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
sudo chmod a+r /etc/apt/keyrings/docker.gpg
```

#### Step 4: Add Docker repository

```bash
echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
```

#### Step 5: Install Docker

```bash
sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

### 5.2 CentOS / RHEL / Fedora

#### Step 1: Install dependencies

```bash
sudo yum install -y yum-utils
```

#### Step 2: Add Docker repository

```bash
# CentOS/RHEL
sudo yum-config-manager --add-repo https://download.docker.com/linux/centos/docker-ce.repo

# Fedora
sudo dnf config-manager --add-repo https://download.docker.com/linux/fedora/docker-ce.repo
```

#### Step 3: Install Docker

```bash
sudo yum install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

### 5.3 Start Docker

```bash
# Start Docker
sudo systemctl start docker

# Enable on boot
sudo systemctl enable docker

# Verify
sudo docker run hello-world
```

---

## 6. Verifying Installation

### 6.1 Check Docker version

```bash
docker --version
# Output: Docker version 27.x.x, build xxx

docker-compose --version
# Output: Docker Compose version v2.x.x
```

### 6.2 Run test container

```bash
docker run hello-world
```

Success output:

```
Hello from Docker!
This message shows that your installation appears to be working correctly.
```

### 6.3 Check Docker status

```bash
docker info
```

---

## 7. Docker Desktop vs Docker Engine

### 7.1 Docker Desktop

For:
- Windows 10/11 Pro
- macOS

Features:
- GUI interface
- Built-in Kubernetes
- Auto-update
- Free for personal and small business

### 7.2 Docker Engine (Docker Server)

For:
- Linux
- Windows Server
- Cloud servers

Features:
- Command-line only
- Lighter weight
- Completely open source and free

---

## 8. Configuring Docker

### 8.1 Configure Registry Mirrors

For China region, configure domestic mirrors:

```json
{
  "registry-mirrors": [
    "https://docker.mirrors.ustc.edu.cn",
    "https://hub-mirror.c.163.com",
    "https://mirror.baidubce.com"
  ]
}
```

#### Windows/macOS

1. Open Docker Desktop settings
2. Find "Docker Engine"
3. Add the above configuration
4. Click "Apply & Restart"

#### Linux

```bash
sudo mkdir -p /etc/docker
sudo tee /etc/docker/daemon.json <<EOF
{
  "registry-mirrors": [
    "https://docker.mirrors.ustc.edu.cn",
    "https://hub-mirror.c.163.com"
  ]
}
EOF
sudo systemctl daemon-reload
sudo systemctl restart docker
```

### 8.2 Configure Resource Limits

**Docker Desktop**:
- Memory: 4GB+
- CPUs: 2+
- Disk: 64GB+

### 8.3 Configure non-root user

```bash
# Create docker group
sudo groupadd docker

# Add user to docker group
sudo usermod -aG docker $USER

# Log out and log back in
```

---

## 9. Uninstalling Docker

### 9.1 Windows

1. Open "Settings" > "Apps"
2. Find "Docker Desktop"
3. Click "Uninstall"

Or use command:
```powershell
winget uninstall Docker.DockerDesktop
```

### 9.2 macOS

1. Quit Docker Desktop
2. Delete Docker from Applications
3. Delete related files:
```bash
rm -rf ~/Library/Containers/com.docker.docker
rm -rf ~/Library/Application\ Support/Docker\ Desktop
rm -rf ~/.docker
```

### 9.3 Linux

```bash
# Uninstall Docker
sudo apt remove docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

# Delete images, containers, etc.
sudo rm -rf /var/lib/docker
sudo rm -rf /var/lib/containerd
```

---

## 10. FAQ

### Q1: Docker Desktop fails to start on Windows?

**Solutions**:
1. Ensure virtualization is enabled in BIOS
2. Ensure WSL 2 is properly installed
3. Restart computer and try again
4. Check for conflicting VM software (VirtualBox, VMware)

---

### Q2: Permission denied for docker command?

**Solutions**:
```bash
sudo usermod -aG docker $USER
# Log out and log back in
```

---

### Q3: Image download too slow?

**Solutions**:
1. Configure domestic registry mirrors (see section 8.1)
2. Use proxy

---

### Q4: Container out of memory?

**Solutions**:
1. Increase Docker Desktop memory limit
2. Use `-m` parameter to limit container memory:
```bash
docker run -m 512m image-name
```

---

### Q5: How to view Docker logs?

**Linux**:
```bash
sudo journalctl -u docker -f
```

**Windows/macOS**:
Open Docker Desktop, click "Troubleshoot" > "View Logs"

---

### Q6: Docker vs Containers?

- **Docker** is the container platform
- **Container** is the running instance of an image

---

### Q7: How to clean up unused Docker resources?

```bash
# Clean unused images
docker image prune -a

# Clean unused containers
docker container prune

# Clean unused volumes
docker volume prune

# Clean build cache
docker builder prune

# Clean all unused resources
docker system prune -a
```

---

## 📚 Related Resources

- Official Documentation: https://docs.docker.com
- Docker Hub: https://hub.docker.com
- Official Tutorial: https://docker-curriculum.com

---

*This document is generated by AI Assistant*
