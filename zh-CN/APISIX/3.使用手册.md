# APISIX 使用手册

## 创建路由

```bash
curl -X POST http://127.0.0.1:9180/apisix/admin/routes/1 \
  -H 'X-API-KEY: api-key' \
  -d '{
    "uri": "/test",
    "upstream": {
      "type": "roundrobin",
      "nodes": {
        "127.0.0.1:1980": 1
      }
    }
  }'
```

## 插件配置

启用限流插件等
