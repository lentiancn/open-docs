# Nginx ユーザーマニュアル

## 基本操作
```bash
nginx              # 起動
nginx -s reload   # 設定再読み込み
nginx -t          # 設定テスト
```

## リバースプロキシ
```nginx
location / {
    proxy_pass http://backend;
}
```

## ロードバランシング
```nginx
upstream backend {
    server backend1;
    server backend2;
}
```
