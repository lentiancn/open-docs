# Element Plus 使用手冊

Element Plus 核心功能和使用技巧。

## 快速開始

### 第一個示例

```vue
<template>
  <el-button type="primary" @click="handleClick">
    點擊我
  </el-button>
</template>

<script setup>
import { ref } from 'vue'

const handleClick = () => {
  ElMessage.success('你好 Element Plus!')
}
</script>
```

## 元件使用

### Button 按鈕

```vue
<template>
  <el-button>默認</el-button>
  <el-button type="primary">主要</el-button>
  <el-button type="success">成功</el-button>
  <el-button type="warning">警告</el-button>
  <el-button type="danger">危險</el-button>
  <el-button type="info">信息</el-button>
  
  <el-button plain>樸素</el-button>
  <el-button round>圓角</el-button>
  <el-button circle icon="Search"></el-button>
  
  <el-button disabled>禁用</el-button>
  <el-button loading>加載中</el-button>
</template>
```

### Input 輸入框

```vue
<template>
  <el-input v-model="value" placeholder="請輸入內容" />
  <el-input v-model="value" disabled placeholder="禁用狀態" />
  <el-input v-model="value" clearable placeholder="可清空" />
  <el-input v-model="value" show-password placeholder="密碼輸入" />
  
  <el-input
    v-model="value"
    type="textarea"
    :rows="3"
    placeholder="多行文本"
  />
</template>

<script setup>
import { ref } from 'vue'
const value = ref('')
</script>
```

### Table 表格

```vue
<template>
  <el-table :data="tableData" stripe border>
    <el-table-column prop="date" label="日期" width="180" />
    <el-table-column prop="name" label="姓名" width="180" />
    <el-table-column prop="address" label="地址" />
    
    <el-table-column label="操作">
      <template #default="{ row }">
        <el-button type="primary" link>編輯</el-button>
        <el-button type="danger" link>刪除</el-button>
      </template>
    </el-table-column>
  </el-table>
</template>

<script setup>
import { ref } from 'vue'

const tableData = ref([
  { date: '2024-01-01', name: '王小明', address: '台北市信義區' },
  { date: '2024-01-02', name: '李小華', address: '高雄市前鎮區' },
])
</script>
```

### Form 表單

```vue
<template>
  <el-form :model="form" :rules="rules" ref="formRef" label-width="100px">
    <el-form-item label="用戶名" prop="username">
      <el-input v-model="form.username" />
    </el-form-item>
    
    <el-form-item label="郵箱" prop="email">
      <el-input v-model="form.email" />
    </el-form-item>
    
    <el-form-item label="角色" prop="role">
      <el-select v-model="form.role" placeholder="請選擇角色">
        <el-option label="管理員" value="admin" />
        <el-option label="普通用戶" value="user" />
      </el-select>
    </el-form-item>
    
    <el-form-item>
      <el-button type="primary" @click="submitForm">提交</el-button>
      <el-button @click="resetForm">重置</el-button>
    </el-form-item>
  </el-form>
</template>

<script setup>
import { ref, reactive } from 'vue'

const formRef = ref()
const form = reactive({
  username: '',
  email: '',
  role: '',
})

const rules = {
  username: [{ required: true, message: '請輸入用戶名', trigger: 'blur' }],
  email: [
    { required: true, message: '請輸入郵箱', trigger: 'blur' },
    { type: 'email', message: '請輸入正確的郵箱格式', trigger: 'blur' }
  ],
  role: [{ required: true, message: '請選擇角色', trigger: 'change' }],
}

const submitForm = async () => {
  const valid = await formRef.value.validate()
  if (valid) {
    console.log('提交成功', form)
  }
}

const resetForm = () => {
  formRef.value.resetFields()
}
</script>
```

### Dialog 對話框

```vue
<template>
  <el-button type="primary" @click="visible = true">
    打開對話框
  </el-button>
  
  <el-dialog
    v-model="visible"
    title="提示"
    width="500"
    :before-close="handleClose"
  >
    <span>這是一段內容</span>
    <template #footer>
      <el-button @click="visible = false">取消</el-button>
      <el-button type="primary" @click="visible = false">確定</el-button>
    </template>
  </el-dialog>
</template>

<script setup>
import { ref } from 'vue'

const visible = ref(false)

const handleClose = (done) => {
  done()
}
</script>
```

### Drawer 抽屜

```vue
<template>
  <el-button type="primary" @click="visible = true">
    打開抽屜
  </el-button>
  
  <el-drawer v-model="visible" title="抽屜標題" direction="rtl" size="50%">
    <p>抽屜內容</p>
  </el-drawer>
</template>

<script setup>
import { ref } from 'vue'
const visible = ref(false)
</script>
```

### Select 選擇器

```vue
<template>
  <el-select v-model="value" placeholder="請選擇">
    <el-option
      v-for="item in options"
      :key="item.value"
      :label="item.label"
      :value="item.value"
    />
  </el-select>
  
  <el-select v-model="value" multiple placeholder="多選">
    <el-option
      v-for="item in options"
      :key="item.value"
      :label="item.label"
      :value="item.value"
    />
  </el-select>
</template>

<script setup>
import { ref } from 'vue'

const value = ref('')
const options = [
  { value: '選項1', label: '黃金糕' },
  { value: '選項2', label: '雙皮奶' },
  { value: '選項3', label: '蚵仔煎' },
]
</script>
```

### Message 消息提示

```typescript
import { ElMessage } from 'element-plus'

// 成功消息
ElMessage.success('操作成功')

// 警告消息
ElMessage.warning('警告信息')

// 錯誤消息
ElMessage.error('操作失敗')

// 普通消息
ElMessage.info('普通信息')

// 長時間顯示
ElMessage({
  message: '自定義時長',
  duration: 10000,
})
```

### Notification 通知

```typescript
import { ElNotification } from 'element-plus'

ElNotification({
  title: '標題',
  message: '這是通知內容',
  type: 'success',
})
```

### Upload 上傳

```vue
<template>
  <el-upload
    action="/api/upload"
    :on-success="handleSuccess"
    :on-error="handleError"
    :limit="3"
    accept=".jpg,.png"
  >
    <el-button type="primary">點擊上傳</el-button>
    <template #tip>
      <div class="el-upload__tip">支持 jpg/png 格式，不超過 2MB</div>
    </template>
  </el-upload>
</template>

<script setup>
const handleSuccess = (response) => {
  ElMessage.success('上傳成功')
}

const handleError = () => {
  ElMessage.error('上傳失敗')
}
</script>
```

## 佈局使用

### Layout 佈局

```vue
<template>
  <el-row :gutter="20">
    <el-col :span="12"><div class="grid-content">12</div></el-col>
    <el-col :span="12"><div class="grid-content">12</div></el-col>
  </el-row>
  
  <el-row :gutter="20">
    <el-col :span="8"><div class="grid-content">8</div></el-col>
    <el-col :span="8"><div class="grid-content">8</div></el-col>
    <el-col :span="8"><div class="grid-content">8</div></el-col>
  </el-row>
</template>

<style>
.grid-content {
  background: #d3dce6;
  padding: 20px;
  text-align: center;
}
</style>
```

### Container 佈局容器

```vue
<template>
  <el-container>
    <el-header>頂部導航</el-header>
    <el-container>
      <el-aside width="200px">側邊欄</el-aside>
      <el-main>主要內容</el-main>
    </el-container>
    <el-footer>底部</el-footer>
  </el-container>
</template>
```

## 最佳實踐

### 表單驗證

```typescript
const rules = {
  name: [
    { required: true, message: '請輸入名稱', trigger: 'blur' },
    { min: 2, max: 20, message: '長度在 2 到 20 個字符', trigger: 'blur' }
  ],
  email: [
    { required: true, message: '請輸入郵箱', trigger: 'blur' },
    { type: 'email', message: '請輸入正確的郵箱格式', trigger: 'blur' }
  ],
  phone: [
    { pattern: /^1[3-9]\d{9}$/, message: '請輸入正確的手機號', trigger: 'blur' }
  ]
}
```

### 表格分頁

```vue
<template>
  <el-table :data="paginatedData">
    <el-table-column prop="name" label="名稱" />
  </el-table>
  
  <el-pagination
    v-model:current-page="currentPage"
    v-model:page-size="pageSize"
    :total="total"
    :page-sizes="[10, 20, 50, 100]"
    layout="total, sizes, prev, pager, next, jumper"
    @size-change="handleSizeChange"
    @current-change="handleCurrentChange"
  />
</template>

<script setup>
import { ref, computed } from 'vue'

const currentPage = ref(1)
const pageSize = ref(10)
const tableData = ref([/* 大量數據 */])
const total = computed(() => tableData.value.length)

const paginatedData = computed(() => {
  const start = (currentPage.value - 1) * pageSize.value
  return tableData.value.slice(start, start + pageSize.value)
})
</script>
```

### 國際化

```typescript
import { ref } from 'vue'
import zhTw from 'element-plus/es/locale/lang/zh-tw'
import en from 'element-plus/es/locale/lang/en'

const locale = ref(zhTw)

const switchLanguage = (lang) => {
  locale.value = lang === 'zh' ? zhTw : en
}
```

## 調試技巧

### 檢查元件問題

```bash
# 查看 Element Plus 版本
npm list element-plus
```

### 常見錯誤

**元件未註冊**：確保已正確引入或配置自動導入

**樣式不生效**：檢查是否有 CSS 衝突，嘗試使用 `:deep()`

**類型錯誤**：安裝 `element-plus/global` 類型
