# Docker 安装文档

在所有主流平台上安装 Docker 的完整指南�?
---

## 目录

- [Linux](#linux)
  - [Ubuntu/Debian](#ubuntudebian)
  - [CentOS/RHEL](#centosrhel)
  - [Fedora](#fedora)
  - [Arch Linux](#arch-linux)
- [macOS](#macos)
  - [Intel Mac](#intel-mac)
  - [Apple Silicon (M1/M2/M3)](#apple-silicon-m1m2m3)
- [Windows](#windows)
  - [WSL2（推荐）](#wsl2推荐)
  - [Docker Desktop](#docker-desktop)
- [验证安装](#验证安装)
- [安装后配置](#安装后配�?

---

## Linux

### Ubuntu / Debian

```bash
# 更新软件包索�?sudo apt update

# 安装依赖
sudo apt install -y ca-certificates curl gnupg lsb-release

# 添加 Docker 官方 GPG 密钥
sudo install -m 0755 -d /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
sudo chmod a+r /etc/apt/keyrings/docker.gpg

# 添加 Docker 仓库
echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

# 安装 Docker
sudo apt update
sudo apt install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

# 启动并开机自�?sudo systemctl start docker
sudo systemctl enable docker

# 添加当前用户�?docker �?sudo usermod -aG docker $USER
```

### CentOS / RHEL

```bash
# 安装依赖
sudo yum install -y yum-utils

# 添加 Docker 仓库
sudo yum-config-manager --add-repo https://download.docker.com/linux/centos/docker-ce.repo

# 安装 Docker
sudo yum install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

# 启动并开机自�?sudo systemctl start docker
sudo systemctl enable docker
```

### Fedora

```bash
# 安装依赖
sudo dnf -y install dnf-plugins-core

# 添加 Docker 仓库
sudo dnf config-manager --add-repo https://download.docker.com/linux/fedora/docker-ce.repo

# 安装 Docker
sudo dnf install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

# 启动并开机自�?sudo systemctl start docker
sudo systemctl enable docker
```

### Arch Linux

```bash
# 安装 Docker
sudo pacman -S docker

# 启动并开机自�?sudo systemctl start docker
sudo systemctl enable docker
```

---

## macOS

### Intel Mac

```bash
# 方式一：Homebrew
brew install --cask docker

# 方式二：手动下载
# �?https://www.docker.com/products/docker-desktop 下载
```

### Apple Silicon (M1/M2/M3)

```bash
# Homebrew（自动安�?ARM 版本�?brew install --cask docker

# 或从 Docker 官网下载
# https://www.docker.com/products/docker-desktop
```

**注意�?* Apple Silicon 版本�?Docker Desktop 包含 Rosetta 2，用于兼�?x86_64 镜像�?
---

## Windows

### WSL2（推荐）

```powershell
# 1. 启用 WSL2
wsl --install

# 2. 重启电脑

# 3. �?WSL2 中安�?Docker
sudo apt update
sudo apt install -y docker.io
sudo service docker start
```

### Docker Desktop

1. **下载**�?[Docker Desktop for Windows](https://www.docker.com/products/docker-desktop)
2. **安装**：运行安装程�?3. **启用 WSL2**：当提示时启用（推荐�?4. **启动**：打开 Docker Desktop

**系统要求�?*
- Windows 10/11 64 �?- 启用 WSL2
- 启用 Hyper-V（Windows 10�?
---

## 验证安装

```bash
# 检�?Docker 版本
docker --version

# 检�?Docker Compose 版本
docker-compose --version

# 运行测试容器
docker run hello-world
```

---

## 安装后配�?
### Linux：非 root 用户运行 Docker

```bash
# 创建 docker �?sudo groupadd docker

# 将用户添加到 docker �?sudo usermod -aG docker $USER

# 重新登录使更改生�?```

### Docker Compose（独立版本）

```bash
# Linux/macOS
sudo curl -L "https://github.com/docker/compose/releases/download/v2.24.0/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose
```

---

## 卸载 Docker

### Ubuntu/Debian

```bash
sudo apt purge -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
sudo rm -rf /var/lib/docker
```

### macOS

```bash
# Homebrew 方式
brew uninstall --cask docker
```

### Windows

打开 Docker Desktop �?设置 �?故障排除 �?清除/删除数据

---

## 下一�?
- [使用指南](./2.使用指南.md)
- 阅读 [官方 Docker 文档](https://docs.docker.com/)

