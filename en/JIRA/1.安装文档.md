# JIRA 安装指南

JIRA 是 Atlassian 公司开发的问题跟踪和项目管理工具。本指南涵盖在各种环境中的安装方法。

## 版本信息

| 版本 | 状态 | 说明 |
|------|------|------|
| JIRA 9.x | ✅ 当前 | 最新版本 |
| JIRA 8.x | ✅ 支持 | 稳定版本 |

## 系统要求

### 硬件要求

| 要求 | 最低配置 | 推荐配置 |
|------|----------|----------|
| CPU | 4 核 | 8+ 核 |
| 内存 | 4 GB | 16+ GB |
| 磁盘 | 10 GB | 50+ GB |

### 软件要求

| 软件 | 版本 |
|------|------|
| Java | JDK 11 或 17 |
| 操作系统 | Linux/Windows/macOS |
| 数据库 | PostgreSQL/MySQL/Oracle |

## 安装方法

### 方法一：Windows 安装

#### 1. 下载安装程序

1. 访问 https://www.atlassian.com/software/jira/download
2. 下载 JIRA Software
3. 运行安装程序

#### 2. 运行安装

按照安装向导完成安装：
- 选择安装类型
- 设置端口
- 配置数据库
- 创建管理员账户

### 方法二：Linux 安装

#### 1. 下载安装包

```bash
# 下载 JIRA
wget https://product-downloads.atlassian.com/software/jira/downloads/atlassian-jira-software-X.X.X-x64.bin
```

#### 2. 安装

```bash
# 添加执行权限
chmod +x atlassian-jira-software-X.X.X-x64.bin

# 运行安装
sudo ./atlassian-jira-software-X.X.X-x64.bin
```

#### 3. 配置

```bash
# 启动 JIRA
sudo /opt/atlassian/jira/bin/start-jira.sh

# 停止 JIRA
sudo /opt/atlassian/jira/bin/stop-jira.sh
```

### 方法三：Docker 安装

```bash
# 拉取镜像
docker pull atlassian/jira-software

# 运行容器
docker run -v jira-data:/var/atlassian/application-data/jira --name="jira" -d -p 8080:8080 atlassian/jira-software
```

### 方法四：使用 AWS

1. 在 AWS Marketplace 搜索 JIRA
2. 选择实例类型
3. 配置数据库
4. 启动实例

## 配置

### 数据库配置

```properties
# dbconfig.xml
<?xml version="1.0" encoding="UTF-8"?>
<jira-database-config>
  <name>defaultDS</name>
  <connection-type>default</connection-type>
  <url>jdbc:postgresql://localhost:5432/jira</url>
  <username>jira</username>
  <password>password</password>
  <pool-size>20</pool-size>
</jira-database-config>
```

### 反向代理配置

#### Nginx

```nginx
server {
    listen 80;
    server_name jira.example.com;
    
    location / {
        proxy_pass http://localhost:8080;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

## 常见问题

### 启动失败？

检查 Java 版本：

```bash
java -version
```

### 数据库连接失败？

检查数据库配置和连接信息。

### 内存不足？

修改内存设置：

```bash
# setenv.sh
export JVM_MINIMUM_MEMORY="2048m"
export JVM_MAXIMUM_MEMORY="4096m"
```

## 相关链接

- [JIRA 官方网站](https://www.atlassian.com/software/jira)
- [JIRA 文档](https://docs.atlassian.com/jira)
