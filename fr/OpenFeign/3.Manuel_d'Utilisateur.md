# OpenFeign Manuel d'Utilisateur

## Équilibrage de charge

### Intégration Spring Cloud LoadBalancer

OpenFeign s'intègre par défaut avec Spring Cloud LoadBalancer, fournissant une fonctionnalité d'équilibrage de charge côté client.

```java
@FeignClient(name = "user-service")
public interface UserService {
    @GetMapping("/users/{id}")
    User getUser(@PathVariable("id") Long id);
}
```

Configurez les instances de service dans `application.yml` :

```yaml
spring:
  cloud:
    discovery:
      client:
        simple:
          instances:
            user-service:
              - uri: http://localhost:8081
              - uri: http://localhost:8082
              - uri: http://localhost:8083
```

### Stratégie d'équilibrage de charge personnalisée

```java
@Configuration
public class LoadBalancerConfig {
    
    @Bean
    public ServiceInstanceListSupplier serviceInstanceListSupplier(
            ConfigurableApplicationContext context) {
        return ServiceInstanceListSupplier.builder()
                .withDiscoveryClient()
                .withCaching()
                .build(context);
    }
}
```

### Transformer la requête HTTP équilibrée en charge

```java
@Bean
public LoadBalancerFeignRequestTransformer transformer() {
    return new LoadBalancerFeignRequestTransformer() {
        @Override
        public Request transformRequest(Request request, ServiceInstance instance) {
            Map<String, Collection<String>> headers = new HashMap<>(request.headers());
            headers.put("X-ServiceId", Collections.singletonList(instance.getServiceId()));
            headers.put("X-InstanceId", Collections.singletonList(instance.getInstanceId()));
            return Request.create(request.httpMethod(), request.url(), headers, 
                request.body(), request.charset(), request.requestTemplate());
        }
    };
}
```

## Prise en charge du disjoncteur

### Intégration Spring Cloud CircuitBreaker

Activez la prise en charge du disjoncteur :

```yaml
spring:
  cloud:
    openfeign:
      circuitbreaker:
        enabled: true
```

### Fallback du disjoncteur

```java
@FeignClient(name = "user-service", fallback = UserServiceFallback.class)
public interface UserService {
    @GetMapping("/users/{id}")
    User getUser(@PathVariable("id") Long id);
}

@Component
public class UserServiceFallback implements UserService {
    @Override
    public User getUser(Long id) {
        // Retourner un utilisateur par défaut ou lever une exception spécifique
        return new User(id, "Default User");
    }
}
```

### Fabrique de fallback avec informations d'exception

```java
@FeignClient(name = "user-service", fallbackFactory = UserServiceFallbackFactory.class)
public interface UserService {
    @GetMapping("/users/{id}")
    User getUser(@PathVariable("id") Long id);
}

@Component
public class UserServiceFallbackFactory implements FallbackFactory<UserService> {
    @Override
    public UserService create(Throwable cause) {
        return new UserService() {
            @Override
            public User getUser(Long id) {
                // Peut accéder à l'exception qui a déclenché le fallback
                log.error("UserService fallback triggered", cause);
                return new User(id, "Fallback User");
            }
        };
    }
}
```

### Nom de disjoncteur personnalisé

```java
@Configuration
public class CircuitBreakerConfig {
    @Bean
    public CircuitBreakerNameResolver circuitBreakerNameResolver() {
        return (String feignClientName, Target<?> target, Method method) -> 
            feignClientName + "_" + method.getName();
    }
}
```

## Journalisation

### Configurer le niveau de journalisation

```java
@Configuration
public class FeignLoggingConfig {
    
    @Bean
    Logger.Level feignLoggerLevel() {
        return Logger.Level.FULL;
    }
}
```

Ou via le fichier de configuration :

```yaml
logging:
  level:
    com.example.clients.UserService: DEBUG

spring:
  cloud:
    openfeign:
      client:
        config:
          user-service:
            logger-level: full
```

### Filtrage de journalisation personnalisé

```java
public class CustomLogger extends Logger {
    
    @Override
    protected void logRequest(String configKey, Level logLevel, Request request) {
        // Filtrer les informations sensibles
        if (!shouldLogRequestHeader(request)) {
            // Logique de journalisation personnalisée
        }
    }
    
    @Override
    protected Response logAndRebufferResponse(String configKey, Level logLevel, 
            Response response, long elapsedTime) throws IOException {
        // Filtrer les informations sensibles de réponse
        if (!shouldLogResponseHeader(response)) {
            // Logique de journalisation personnalisée
        }
        return super.logAndRebufferResponse(configKey, logLevel, response, elapsedTime);
    }
    
    private boolean shouldLogRequestHeader(Request request) {
        // Filtrer l'en-tête Authorization et autres informations sensibles
        return !request.headers().containsKey("Authorization");
    }
    
    private boolean shouldLogResponseHeader(Response response) {
        return true; // Ou filtrer selon les besoins
    }
}
```

## Compression requête/réponse

### Activer la compression

```yaml
spring:
  cloud:
    openfeign:
      compression:
        request:
          enabled: true
          mime-types: text/xml,application/xml,application/json
          min-request-size: 2048
        response:
          enabled: true
```

### Notes importantes

- Lors de l'utilisation d'OkHttpClient, la compression est désactivée si les en-têtes `content-encoding` ou `accept-encoding` sont présents
- La compression s'applique uniquement aux types MIME spécifiés
- La taille de la requête doit dépasser `min-request-size` pour être compressée

## Prise en charge OAuth2

### Activer OAuth2

```yaml
spring:
  cloud:
    openfeign:
      oauth2:
        enabled: true
        client-registration-id: my-client
```

### Configuration des dépendances

```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-oauth2-client</artifactId>
</dependency>
```

### Configuration du client OAuth2

```yaml
spring:
  security:
    oauth2:
      client:
        registration:
          my-client:
            client-id: your-client-id
            client-secret: your-client-secret
            authorization-grant-type: client_credentials
            scope: read,write
        provider:
          my-provider:
            token-uri: https://auth-server.com/oauth/token
```

## Prise en charge de la mise en cache

### Activer la mise en cache

```java
@SpringBootApplication
@EnableCaching
@EnableFeignClients
public class Application {
    // ...
}
```

### Utiliser la mise en cache dans le client Feign

```java
@FeignClient(name = "data-service")
public interface DataService {
    
    @GetMapping("/data/{key}")
    @Cacheable(cacheNames = "data-cache", key = "#key")
    Data getData(@PathVariable String key);
    
    @PostMapping("/data")
    @CachePut(cacheNames = "data-cache", key = "#data.key")
    Data updateData(@RequestBody Data data);
    
    @DeleteMapping("/data/{key}")
    @CacheEvict(cacheNames = "data-cache", key = "#key")
    void deleteData(@PathVariable String key);
}
```

### Désactiver la mise en cache

```yaml
spring:
  cloud:
    openfeign:
      cache:
        enabled: false
```

## Prise en charge asynchrone

### Prise en charge CompletableFuture

```java
@FeignClient(name = "async-service")
public interface AsyncService {
    
    @GetMapping("/data/{id}")
    CompletableFuture<Data> getDataAsync(@PathVariable String id);
}
```

### Configurer le client asynchrone

```java
@Configuration
public class AsyncFeignConfig {
    
    @Bean
    public AsyncClient asyncClient() {
        return new AsyncClient.Default();
    }
}
```

### Exemple d'utilisation

```java
@Service
public class AsyncProcessingService {
    
    private final AsyncService asyncService;
    
    public AsyncProcessingService(AsyncService asyncService) {
        this.asyncService = asyncService;
    }
    
    public void processData() {
        CompletableFuture<Data> future1 = asyncService.getDataAsync("1");
        CompletableFuture<Data> future2 = asyncService.getDataAsync("2");
        
        // Traiter en parallèle
        CompletableFuture.allOf(future1, future2).thenRun(() -> {
            try {
                Data data1 = future1.get();
                Data data2 = future2.get();
                // Traiter les données
            } catch (Exception e) {
                log.error("Error processing async data", e);
            }
        });
    }
}
```

## Prise en charge de l'héritage

### Définition de l'interface de base

```java
public interface BaseService<T, ID> {
    @GetMapping("/{id}")
    T findById(@PathVariable ID id);
    
    @GetMapping
    List<T> findAll();
    
    @PostMapping
    T save(@RequestBody T entity);
    
    @DeleteMapping("/{id}")
    void deleteById(@PathVariable ID id);
}
```

### Implémentation concrète du service

```java
@FeignClient(name = "user-service")
public interface UserService extends BaseService<User, Long> {
    @GetMapping("/users/by-email/{email}")
    User findByEmail(@PathVariable String email);
}

@FeignClient(name = "order-service")
public interface OrderService extends BaseService<Order, Long> {
    @GetMapping("/orders/by-user/{userId}")
    List<Order> findByUserId(@PathVariable Long userId);
}
```

## Mappage des paramètres de requête

### Utilisation de @SpringQueryMap

```java
public class SearchCriteria {
    private String name;
    private String status;
    private Integer page;
    private Integer size;
    
    // getters and setters
}

@FeignClient(name = "search-service")
public interface SearchService {
    @GetMapping("/search")
    SearchResult search(@SpringQueryMap SearchCriteria criteria);
}
```

### QueryMapEncoder personnalisé

```java
@Configuration
public class FeignConfig {
    
    @Bean
    public QueryMapEncoder queryMapEncoder() {
        return new BeanQueryMapEncoder(); // Utiliser les propriétés Java Bean
        // Ou return new CustomQueryMapEncoder(); // Implémentation personnalisée
    }
}
```

## Prise en charge HATEOAS

### Activer HATEOAS

Ajouter la dépendance :

```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-hateoas</artifactId>
</dependency>
```

### Utiliser les modèles HATEOAS

```java
@FeignClient(name = "hateoas-service")
public interface HateoasService {
    
    @GetMapping("/resources")
    CollectionModel<Resource> getResources();
    
    @GetMapping("/resources/{id}")
    EntityModel<Resource> getResource(@PathVariable String id);
}
```

## Prise en charge de la portée de rafraîchissement

### Activer la prise en charge du rafraîchissement

```yaml
spring:
  cloud:
    openfeign:
      client:
        refresh-enabled: true
```

### Rafraîchir la configuration

Rafraîchir via le point de terminaison Actuator :

```bash
POST /actuator/refresh
```

### Notes importantes

- N'annotez pas l'interface `@FeignClient` avec `@RefreshScope`
- La fonctionnalité de rafraîchissement est désactivée par défaut, elle nécessite une activation explicite
- Prend en charge le rafraîchissement du délai de connexion, du délai de lecture et de la configuration de l'URL

## Prise en charge AOT et image native

### Exigences de configuration

```yaml
spring:
  cloud:
    refresh:
      enabled: false
    openfeign:
      client:
        refresh-enabled: false
      lazy-attributes-resolution: false
```

### Construire l'image native

Assurez-vous d'avoir la configuration correcte pendant la construction, et remplacez l'URL au moment de l'exécution via les propriétés système :

```bash
-Dspring.cloud.openfeign.client.config.[clientId].url=[url]
```

Ces fonctionnalités avancées font d'OpenFeign non seulement un simple client HTTP, mais une solution complète de communication de microservices capable de répondre à diverses exigences d'applications d'entreprise.