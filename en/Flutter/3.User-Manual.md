# Flutter User Manual

Flutter core concepts and common development examples.

## Project Creation

### Create new project

```bash
# Create new project
flutter create my_app
cd my_app

# Run project
flutter run
```

### Project structure

```
my_app/
├── lib/
│   └── main.dart        # Entry point
├── test/                 # Test files
├── pubspec.yaml         # Configuration file
├── android/            # Android configuration
├── ios/                 # iOS configuration
├── web/                 # Web configuration
└── README.md
```

## Basic Widgets

### Text Component

```dart
import 'package:flutter/material.dart';

Text(
  'Hello Flutter',
  style: TextStyle(
    fontSize: 24,
    color: Colors.blue,
  ),
)
```

### Buttons

```dart
// Regular button
ElevatedButton(
  onPressed: () {},
  child: Text('Click'),
)

// Icon button
IconButton(
  icon: Icon(Icons.add),
  onPressed: () {},
)

// Text button
TextButton(
  onPressed: () {},
  child: Text('Text Button'),
)
```

### Images

```dart
// Local image
Image.asset('assets/image.png')

// Network image
Image.network('https://example.com/image.png')

// Icon
Icon(Icons.home, size: 48)
```

## Layout Components

### Linear Layout

```dart
// Horizontal layout
Row(
  children: [
    Text('A'),
    Text('B'),
    Text('C'),
  ],
)

// Vertical layout
Column(
  children: [
    Text('A'),
    Text('B'),
    Text('C'),
  ],
)
```

### Flexible Layout

```dart
// Flexible space allocation
Row(
  children: [
    Expanded(
      flex: 1,
      child: Container(color: Colors.red),
    ),
    Expanded(
      flex: 2,
      child: Container(color: Colors.blue),
    ),
  ],
)
```

### Stack Layout

```dart
Stack(
  children: [
    Container(color: Colors.red),
    Positioned(
      left: 10,
      top: 10,
      child: Text('Overlay'),
    ),
  ],
)
```

## State Management

### StatefulWidget

```dart
class Counter extends StatefulWidget {
  @override
  _CounterState createState() => _CounterState();
}

class _CounterState extends State<Counter> {
  int _count = 0;

  void _increment() {
    setState(() {
      _count++;
    });
  }

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text('Count: $_count'),
        ElevatedButton(
          onPressed: _increment,
          child: Text('Increment'),
        ),
      ],
    );
  }
}
```

### Provider State Management

```dart
// Create Provider
class Counter extends ChangeNotifier {
  int _count = 0;
  int get count => _count;

  void increment() {
    _count++;
    notifyListeners();
  }
}

// Use Provider
Consumer<Counter>(
  builder: (context, counter, child) {
    return Text('${counter.count}');
  },
)
```

## Navigation

### Basic Navigation

```dart
// Navigate to new page
Navigator.push(
  context,
  MaterialPageRoute(builder: (context) => SecondScreen()),
);

// Return
Navigator.pop(context);
```

### Route Configuration

```dart
// Define routes
MaterialApp(
  routes: {
    '/': (context) => HomePage(),
    '/detail': (context) => DetailPage(),
  },
)

// Navigate
Navigator.pushNamed(context, '/detail');
```

## Network Requests

### HTTP Request

```dart
import 'package:http/http.dart' as http;

Future<void> fetchData() async {
  final response = await http.get(Uri.parse('https://api.example.com/data'));
  if (response.statusCode == 200) {
    print(response.body);
  }
}
```

### Dio Request

```dart
import 'package:dio/dio.dart';

final dio = Dio();
Response response = await dio.get('https://api.example.com/data');
```

## Local Storage

### SharedPreferences

```dart
import 'package:shared_preferences/shared_preferences.dart';

// Save
Future<void> saveData() async {
  final prefs = await SharedPreferences.getInstance();
  await prefs.setString('name', 'Flutter');
}

// Load
Future<void> loadData() async {
  final prefs = await SharedPreferences.getInstance();
  String name = prefs.getString('name') ?? '';
}
```

## Common Widgets

### List

```dart
ListView.builder(
  itemCount: items.length,
  itemBuilder: (context, index) {
    return ListTile(
      title: Text(items[index].title),
    );
  },
)
```

### Form

```dart
TextFormField(
  decoration: InputDecoration(
    labelText: 'Username',
    hintText: 'Enter username',
  ),
  validator: (value) {
    if (value.isEmpty) {
      return 'Please enter username';
    }
    return null;
  },
)
```

### Dialog

```dart
showDialog(
  context: context,
  builder: (context) => AlertDialog(
    title: Text('Confirm'),
    content: Text('Are you sure you want to delete?'),
    actions: [
      TextButton(
        onPressed: () => Navigator.pop(context),
        child: Text('Cancel'),
      ),
      TextButton(
        onPressed:(context),
        child () => Navigator.pop: Text('Confirm'),
      ),
    ],
  ),
);
```

## Build and Release

### Android APK

```bash
# Debug version
flutter build apk --debug

# Release version
flutter build apk --release
```

### iOS

```bash
# Simulator
flutter build ios --simulator --no-codesign

# Device
flutter build ios --release
```

### Web

```bash
flutter build web
```

## Best Practices

1. **Use const**: Reduce unnecessary rebuilds
2. **Extract Widgets**: Reuse UI code
3. **Use appropriate layouts**: Choose suitable layout components
4. **State management**: Choose appropriate state management solution
5. **Error handling**: Do proper exception handling

## Further Learning

- FAQ: See 4.FAQ.md
