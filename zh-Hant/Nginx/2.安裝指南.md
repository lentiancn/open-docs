# Nginx 安裝指南

## 安裝方式

### Ubuntu/Debian
```bash
sudo apt update
sudo apt install nginx
```

### CentOS
```bash
sudo yum install epel-release
sudo yum install nginx
```

### Docker
```bash
docker pull nginx
```

## 驗證
```bash
nginx -v
curl http://localhost
```
