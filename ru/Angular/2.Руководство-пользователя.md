# Angular Руководство пользователя

> 🎯 Полное руководство по Angular
> 
> 📅 Обновлено: Февраль 2026
> 
> 📖 Версия: Angular 17+

---

## 1. Введение

Angular — открытый веб-фреймворк от Google.

### Основные функции
- Компоненты
- Двустороннее связывание
- Внедрение зависимостей
- Маршрутизация
- Формы
- HTTP клиент
- Тестирование

---

## 2. Структура проекта

```
moe-prilozhenie/
├── src/app/
├── angular.json
└── package.json
```

---

## 3. Компоненты

```bash
ng generate component moi-komponent
```

```typescript
@Component({
  selector: 'app-moi-komponent',
  standalone: true,
  template: `<h1>{{ zagolovok }}</h1>`
})
export class MoiKomponentComponent {
  zagolovok = 'Привет Angular';
}
```

---

## 4. Шаблоны

```html
<!-- Интерполяция -->
<p>Привет, {{ imya }}!</p>

<!-- Связывание свойств -->
<img [src]="urlKartin">

<!-- Связывание событий -->
<button (click)="klick()">Клик</button>

<!-- Двустороннее связывание -->
<input [(ngModel)]="imya">
```

---

## 5. Сервисы

```typescript
@Injectable({ providedIn: 'root' })
export class DannyService {
  constructor(private http: HttpClient) {}
}
```

---

## 6. Маршрутизация

```typescript
export const routes: Routes = [
  { path: '', component: HomeComponent },
  { path: 'about', component: AboutComponent }
];
```

---

## 7. HTTP

```this.http.get('/api/dannye').subscribe(d => console.log(d));>

---

## 8. Signal

```typescript
schet = signal(0);

increment(): void {
  this.schet.update(z => z + 1);
}
```

---

## 9. Сборка

```bash
ng build --configuration production
```

---

## 📚 Ресурсы

- https://angular.io/docs

---

*Создано AI*
