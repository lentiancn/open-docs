# Flutter - Руководство пользователя

Основные концепции Flutter и примеры разработки.

## Создание проекта

### Создать новый проект

```bash
# Создать новый проект
flutter create my_app
cd my_app

# Запустить проект
flutter run
```

### Структура проекта

```
my_app/
├── lib/
│   └── main.dart        # Точка входа
├── test/                 # Тестовые файлы
├── pubspec.yaml         # Файл конфигурации
├── android/            # Конфигурация Android
├── ios/                 # Конфигурация iOS
├── web/                 # Конфигурация веба
└── README.md
```

## Базовые виджеты

### Текстовый компонент

```dart
import 'package:flutter/material.dart';

Text(
  'Привет, Flutter',
  style: TextStyle(
    fontSize: 24,
    color: Colors.blue,
  ),
)
```

### Кнопки

```dart
// Обычная кнопка
ElevatedButton(
  onPressed: () {},
  child: Text('Нажать'),
)

// Кнопка с иконкой
IconButton(
  icon: Icon(Icons.add),
  onPressed: () {},
)

// Текстовая кнопка
TextButton(
  onPressed: () {},
  child: Text('Текстовая кнопка'),
)
```

### Изображения

```dart
// Локальное изображение
Image.asset('assets/image.png')

// Изображение из сети
Image.network('https://example.com/image.png')

// Иконка
Icon(Icons.home, size: 48)
```

## Компоненты макета

### Линейный макет

```dart
// Горизонтальный макет
Row(
  children: [
    Text('A'),
    Text('B'),
    Text('C'),
  ],
)

// Вертикальный макет
Column(
  children: [
    Text('A'),
    Text('B'),
    Text('C'),
  ],
)
```

### Гибкий макет

```dart
// Гибкое распределение пространства
Row(
  children: [
    Expanded(
      flex: 1,
      child: Container(color: Colors.red),
    ),
    Expanded(
      flex: 2,
      child: Container(color: Colors.blue),
    ),
  ],
)
```

### Макет стека

```dart
Stack(
  children: [
    Container(color: Colors.red),
    Positioned(
      left: 10,
      top: 10,
      child: Text('Наложение'),
    ),
  ],
)
```

## Управление состоянием

### StatefulWidget

```dart
class Counter extends StatefulWidget {
  @override
  _CounterState createState() => _CounterState();
}

class _CounterState extends State<Counter> {
  int _count = 0;

  void _increment() {
    setState(() {
      _count++;
    });
  }

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text('Счёт: $_count'),
        ElevatedButton(
          onPressed: _increment,
          child: Text('Увеличить'),
        ),
      ],
    );
  }
}
```

### Provider

```dart
// Создать Provider
class Counter extends ChangeNotifier {
  int _count = 0;
  int get count => _count;

  void increment() {
    _count++;
    notifyListeners();
  }
}

// Использовать Provider
Consumer<Counter>(
  builder: (context, counter, child) {
    return Text('${counter.count}');
  },
)
```

## Навигация

### Базовая навигация

```dart
// Переход на новую страницу
Navigator.push(
  context,
  MaterialPageRoute(builder: (context) => SecondScreen()),
);

// Назад
Navigator.pop(context);
```

### Конфигурация маршрутов

```dart
// Определить маршруты
MaterialApp(
  routes: {
    '/': (context) => HomePage(),
    '/detail': (context) => DetailPage(),
  },
)

// Переход
Navigator.pushNamed(context, '/detail');
```

## Сетевые запросы

### HTTP-запрос

```dart
import 'package:http/http.dart' as http;

Future<void> fetchData() async {
  final response = await http.get(Uri.parse('https://api.example.com/data'));
  if (response.statusCode == 200) {
    print(response.body);
  }
}
```

### Dio-запрос

```dart
import 'package:dio/dio.dart';

final dio = Dio();
Response response = await dio.get('https://api.example.com/data');
```

## Локальное хранилище

### SharedPreferences

```dart
import 'package:shared_preferences/shared_preferences.dart';

// Сохранить
Future<void> saveData() async {
  final prefs = await SharedPreferences.getInstance();
  await prefs.setString('name', 'Flutter');
}

// Загрузить
Future<void> loadData() async {
  final prefs = await SharedPreferences.getInstance();
  String name = prefs.getString('name') ?? '';
}
```

## Часто используемые виджеты

### Список

```dart
ListView.builder(
  itemCount: items.length,
  itemBuilder: (context, index) {
    return ListTile(
      title: Text(items[index].title),
    );
  },
)
```

### Форма

```dart
TextFormField(
  decoration: InputDecoration(
    labelText: 'Имя пользователя',
    hintText: 'Введите имя',
  ),
  validator: (value) {
    if (value.isEmpty) {
      return 'Пожалуйста, введите имя';
    }
    return null;
  },
)
```

### Диалог

```dart
showDialog(
  context: context,
  builder: (context) => AlertDialog(
    title: Text('Подтверждение'),
    content: Text('Вы уверены, что хотите удалить?'),
    actions: [
      TextButton(
        onPressed: () => Navigator.pop(context),
        child: Text('Отмена'),
      ),
      TextButton(
        onPressed: () => Navigator.pop(context),
        child: Text('Подтвердить'),
      ),
    ],
  ),
);
```

## Сборка и публикация

### Android APK

```bash
# Отладочная версия
flutter build apk --debug

# Релиз-версия
flutter build apk --release
```

### iOS

```bash
# Симулятор
flutter build ios --simulator --no-codesign

# Устройство
flutter build ios --release
```

### Веб

```bash
flutter build web
```

## Рекомендации

1. **Использовать const**: Уменьшить ненужные перестроения
2. **Извлекать виджеты**: Переиспользовать код UI
3. **Использовать подходящие макеты**: Выбирать подходящие компоненты макета
4. **Управление состоянием**: Выбрать подходящее решение
5. **Обработка ошибок**: Правильно обрабатывать исключения

## Следующие шаги

- FAQ: см. 4.Часто-задаваемые-вопросы.md
