# MongoDB 安装指南

## 版本选择与平台要求

### 版本选择

MongoDB 当前有两个主要版本系列：

- **MongoDB 8.2**：最新稳定版本，推荐新项目使用
- **MongoDB 7.0**：长期支持版本，适合需要稳定性的生产环境

从 MongoDB 8.2 开始，次要版本可用于特定使用案例的本地部署。

### 平台要求

- MongoDB 不再支持 32 位平台，请确保您的操作系统是 64 位
- MongoDB 6.0 已于 2025年7月31日停止支持

### 支持的平台

MongoDB 社区版支持以下平台和架构：

**Ubuntu**

- Ubuntu 24.04 (x86_64, arm64)
- Ubuntu 22.04 (x86_64, arm64)
- Ubuntu 20.04 (x86_64, arm64)

**Debian**

- Debian 12 (x86_64)
- Debian 11 (x86_64)

**RHEL/CentOS/Rocky Linux**

- RHEL/Rocky/Alma/Oracle Linux 9.0+ (x86_64, arm64)
- RHEL/Rocky/Alma/Oracle Linux 8.0+ (x86_64, arm64, ppc64le, s390x)
- RHEL/Oracle Linux 7.0+ (x86_64)

**macOS**

- macOS 14 (x86_64, arm64)
- macOS 13 (x86_64, arm64)
- macOS 12 (x86_64, arm64)
- macOS 11 (x86_64, arm64)

**Windows**

- Windows 11 (x86_64)
- Windows Server 2022 (x86_64)
- Windows Server 2019 (x86_64)

**其他 Linux**

- Amazon Linux 2023 (x86_64, arm64)
- Amazon Linux 2 (x86_64, arm64)
- SLES 15 (x86_64)
- SLES 12 (x86_64)

## 在 Ubuntu 上安装 MongoDB 社区版

### 步骤一：导入包管理器的公密钥

```bash
curl -fsSL https://www.mongodb.org/static/pgp/server-8.0.asc | sudo gpg -o /usr/share/keyrings/mongodb-server-8.0.gpg --dearmor
```

### 步骤二：创建 MongoDB 源列表文件

根据您的 Ubuntu 版本，选择合适的配置：

**Ubuntu 24.04 (Noble)**：

```bash
echo "deb [ signed-by=/usr/share/keyrings/mongodb-server-8.0.gpg ] http://repo.mongodb.org/apt/debian noble/mongodb-org/8.0 main" | sudo tee /etc/apt/sources.list.d/mongodb-org-8.0.list
```

**Ubuntu 22.04 (Jammy)**：

```bash
echo "deb [ signed-by=/usr/share/keyrings/mongodb-server-8.0.gpg ] http://repo.mongodb.org/apt/debian jammy/mongodb-org/8.0 main" | sudo tee /etc/apt/sources.list.d/mongodb-org-8.0.list
```

**Ubuntu 20.04 (Focal)**：

```bash
echo "deb [ signed-by=/usr/share/keyrings/mongodb-server-8.0.gpg ] http://repo.mongodb.org/apt/debian focal/mongodb-org/8.0 main" | sudo tee /etc/apt/sources.list.d/mongodb-org-8.0.list
```

### 步骤三：更新软件包列表

```bash
sudo apt update
```

### 步骤四：安装 MongoDB

安装最新版本：

```bash
sudo apt install -y mongodb-org
```

或者安装特定版本：

```bash
sudo apt install -y mongodb-org=8.0.0 mongodb-org-server=8.0.0 mongodb-org-shell=8.0.0 mongodb-org-mongos=8.0.0 mongodb-org-tools=8.0.0
```

### 步骤五：启动 MongoDB

启动 mongod 服务：

```bash
sudo systemctl start mongod
```

设置为开机自启：

```bash
sudo systemctl enable mongod
```

## 在 Debian 上安装 MongoDB 社区版

### 步骤一：导入公密钥

```bash
curl -fsSL https://www.mongodb.org/static/pgp/server-8.0.asc | sudo gpg -o /usr/share/keyrings/mongodb-server-8.0.gpg --dearmor
```

### 步骤二：创建源列表

**Debian 12 (Bookworm)**：

```bash
echo "deb [ signed-by=/usr/share/keyrings/mongodb-server-8.0.gpg ] http://repo.mongodb.org/apt/debian bookworm/mongodb-org/8.0 main" | sudo tee /etc/apt/sources.list.d/mongodb-org-8.0.list
```

**Debian 11 (Bullseye)**：

```bash
echo "deb [ signed-by=/usr/share/keyrings/mongodb-server-8.0.gpg ] http://repo.mongodb.org/apt/debian bullseye/mongodb-org/8.0 main" | sudo tee /etc/apt/sources.list.d/mongodb-org-8.0.list
```

### 步骤三：更新并安装

```bash
sudo apt update
sudo apt install -y mongodb-org
```

## 在 RHEL/CentOS/Rocky Linux 上安装

### 使用 YUM/DNF 安装

#### 步骤一：创建仓库文件

创建 `/etc/yum.repos.d/mongodb-org.repo` 文件：

```bash
[mongodb-org-8.0]
name=MongoDB Repository
baseurl=https://repo.mongodb.org/yum/redhat/$releasever/mongodb-org/8.0/x86_64/
gpgcheck=1
enabled=1
gpgkey=https://www.mongodb.org/static/pgp/server-8.0.asc
```

#### 步骤二：安装 MongoDB

使用 DNF（推荐）：

```bash
sudo dnf install -y mongodb-org
```

或使用 YUM：

```bash
sudo yum install -y mongodb-org
```

#### 步骤三：启动 MongoDB

```bash
sudo systemctl start mongod
sudo systemctl enable mongod
```

## 在 macOS 上安装

### 使用 Homebrew 安装

#### 步骤一：更新 Homebrew

```bash
brew update
```

#### 步骤二：安装 MongoDB

```bash
brew install mongodb-community
```

#### 步骤三：启动 MongoDB

作为后台服务启动（推荐）：

```bash
brew services start mongodb-community
```

或者手动运行：

```bash
mongod --config /usr/local/etc/mongod.conf
```

#### 步骤四：停止 MongoDB

```bash
brew services stop mongodb-community
```

### 手动安装

#### 步骤一：下载 MongoDB

```bash
curl -O https://fastdl.mongodb.org/osx/mongodb-macos-x86_64-8.0.0.tgz
```

ARM64 架构：

```bash
curl -O https://fastdl.mongodb.org/osx/mongodb-macos-arm64-8.0.0.tgz
```

#### 步骤二：解压

```bash
tar -zxvf mongodb-macos-*.tgz
```

#### 步骤三：移动二进制文件

```bash
sudo mkdir -p /usr/local/bin
sudo cp mongodb-macos-*/bin/* /usr/local/bin/
```

#### 步骤四：创建数据目录

```bash
sudo mkdir -p /data/db
sudo chown -R $(whoami) /data/db
```

#### 步骤五：启动 MongoDB

```bash
mongod --dbpath /data/db
```

## 在 Windows 上安装

### 使用 MSI 安装程序

1. 从 MongoDB 下载中心下载 Windows 版本的 MSI 安装程序
2. 运行安装程序，按照向导步骤操作
3. 选择"Complete"安装类型以安装所有组件
4. 完成安装后，MongoDB 会自动安装为 Windows 服务

### 使用 ZIP 包

#### 步骤一：下载 MongoDB

从 MongoDB 下载中心下载 Windows ZIP 包。

#### 步骤二：解压

解压文件到目标目录，例如 `C:\Program Files\MongoDB`。

#### 步骤三：创建数据目录

```cmd
mkdir C:\data\db
```

#### 步骤四：启动 MongoDB

```cmd
"C:\Program Files\MongoDB\Server\8.0\bin\mongod.exe" --dbpath C:\data\db
```

## 使用 Docker 安装

### 安装 MongoDB 社区版

#### 步骤一：拉取镜像

```bash
docker pull mongodb/mongodb-community-server
```

#### 步骤二：运行容器

```bash
docker run --name mongodb -d -p 27017:27017 mongodb/mongodb-community-server
```

#### 数据持久化

如果需要持久化数据，使用卷挂载：

```bash
docker run --name mongodb -d -p 27017:27017 -v mongodb_data:/data/db mongodb/mongodb-community-server
```

### 使用 Docker Compose

创建 `docker-compose.yml` 文件：

```yaml
version: '3.8'
services:
  mongodb:
    image: mongodb/mongodb-community-server
    container_name: mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    environment:
      - MONGO_INITDB_ROOT_USERNAME=admin
      - MONGO_INITDB_ROOT_PASSWORD=yourpassword

volumes:
  mongodb_data:
```

启动：

```bash
docker-compose up -d
```

## 验证安装

### 连接到 MongoDB

安装完成后，使用 mongosh（MongoDB Shell）连接：

```bash
mongosh
```

如果连接成功，您会看到 MongoDB 的交互式 shell 提示符。

### 检查版本

在 mongosh 中检查版本：

```bash
db.version()
```

### 基本命令

```bash
# 查看数据库
show dbs

# 切换数据库
use test

# 查看当前数据库
db

# 查看帮助
db.help()
```

## 后续配置

### 创建管理员用户

在 mongosh 中执行：

```bash
use admin
db.createUser({
  user: "admin",
  pwd: "your_secure_password",
  roles: [
    { role: "userAdminAnyDatabase", db: "admin" },
    { role: "readWriteAnyDatabase", db: "admin" }
  ]
})
```

### 启用身份认证

1. 停止 MongoDB 服务
2. 修改 `/etc/mongod.conf` 启用身份认证：

```yaml
security:
  authorization: enabled
```

3. 重启 MongoDB 服务：

```bash
sudo systemctl restart mongod
```

### 配置远程连接

1. 修改 `/etc/mongod.conf` 中的网络配置：

```yaml
net:
  port: 27017
  bindIp: 0.0.0.0
```

2. 重启 MongoDB 服务

3. 在防火墙中打开端口：

```bash
sudo ufw allow 27017
```

## 卸载 MongoDB

### Ubuntu/Debian

```bash
sudo apt remove --purge mongodb-org
sudo rm -rf /var/lib/mongodb
sudo rm -rf /var/log/mongodb
sudo rm -rf /etc/mongod.conf
```

### macOS (Homebrew)

```bash
brew uninstall mongodb-community
brew services stop mongodb-community
rm -rf /usr/local/etc/mongod.conf
rm -rf /data/db
```

### RHEL/CentOS

```bash
sudo yum remove -y mongodb-org
sudo rm -rf /var/lib/mongo
sudo rm -rf /var/log/mongodb
```

### Windows

通过控制面板的"程序和功能"卸载 MongoDB，然后手动删除数据目录。

## 常见问题

### MongoDB 默认端口是多少？

默认端口是 27017。也可以通过 `--port` 参数指定其他端口。

### MongoDB 数据存储在哪里？

默认情况下：
- Linux/macOS：`/data/db`
- Windows：`C:\data\db`

可以通过修改配置文件中的 `storage.dbPath` 来更改。

### 如何检查 MongoDB 运行状态？

```bash
sudo systemctl status mongod
```

或者在 mongosh 中：

```bash
db.adminCommand({ ping: 1 })
```
