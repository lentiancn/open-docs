# Nginx 安裝文檔

在所有主流平台上安裝 Nginx 的完整指南。

---

## 目錄

- [Linux](#linux)
  - [Ubuntu/Debian](#ubuntudebian)
  - [CentOS/RHEL](#centosrhel)
  - [Fedora](#fedora)
  - [Arch Linux](#arch-linux)
- [macOS](#macos)
  - [Homebrew](#homebrew)
- [Windows](#windows)
  - [二進制包](#二進制包)
  - [Docker](#docker)
- [驗證安裝](#驗證安裝)
- [配置](#配置)
- [解除安裝](#解除安裝)

---

## Linux

### Ubuntu / Debian

```bash
# 更新軟體包索引
sudo apt update

# 安裝 Nginx
sudo apt install nginx

# 啟動 Nginx
sudo systemctl start nginx

# 開機自啟
sudo systemctl enable nginx
```

### CentOS / RHEL

```bash
# 安裝 EPEL 倉庫
sudo yum install epel-release

# 安裝 Nginx
sudo yum install nginx

# 啟動 Nginx
sudo systemctl start nginx
```

### Fedora

```bash
sudo dnf install nginx
sudo systemctl start nginx
```

### Arch Linux

```bash
sudo pacman -S nginx
sudo systemctl start nginx
```

---

## macOS

### Homebrew

```bash
brew install nginx
brew services start nginx
```

---

## Windows

### 二進制包

1. 從 [nginx.org](http://nginx.org/en/download.html) 下載
2. 解壓到 `C:\nginx`

```powershell
cd C:\nginx
start nginx
```

### Docker

```bash
docker run -d -p 80:80 --name nginx nginx:latest
```

---

## 驗證安裝

```bash
nginx -v
nginx -t
curl http://localhost
```

---

## 配置

### 主要配置文件位置

| 作業系統 | 路徑 |
|---------|------|
| Ubuntu/Debian | `/etc/nginx/nginx.conf` |
| CentOS/RHEL | `/etc/nginx/nginx.conf` |
| macOS | `/usr/local/etc/nginx/nginx.conf` |

### 常用命令

```bash
nginx -t
sudo systemctl reload nginx
sudo systemctl restart nginx
```

---

## 解除安裝

### Ubuntu/Debian

```bash
sudo apt remove nginx
```

### CentOS/RHEL

```bash
sudo yum remove nginx
```

### macOS

```bash
brew uninstall nginx
```

---

## 下一步

- [使用指南](./2.使用指南.md)
