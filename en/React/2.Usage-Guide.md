# React Usage Guide

Comprehensive guide for using React effectively.

---

## JSX

### Basic Syntax

```jsx
function App() {
  return (
    <div>
      <h1>Hello World</h1>
      <p>This is React</p>
    </div>
  );
}
```

### Expressions in JSX

```jsx
const name = 'John';
function App() {
  return <h1>Hello, {name}</h1>;
}
```

---

## Components

### Function Component

```jsx
function Welcome() {
  return <h1>Hello World</h1>;
}
```

### Arrow Function Component

```jsx
const Welcome = () => {
  return <h1>Hello World</h1>;
};
```

### Component with Props

```jsx
function Welcome(props) {
  return <h1>Hello, {props.name}</h1>;
}

// Usage
<Welcome name="John" />
```

---

## State (useState)

### Basic State

```jsx
import { useState } from 'react';

function Counter() {
  const [count, setCount] = useState(0);

  return (
    <div>
      <p>Count: {count}</p>
      <button onClick={() => setCount(count + 1)}>Increment</button>
    </div>
  );
}
```

### Multiple State Variables

```jsx
const [name, setName] = useState('');
const [age, setAge] = useState(0);
const [isActive, setIsActive] = useState(false);
```

---

## Effects (useEffect)

### Basic Effect

```jsx
import { useState, useEffect } from 'react';

function DataFetcher() {
  const [data, setData] = useState(null);

  useEffect(() => {
    fetch('https://api.example.com/data')
      .then(res => res.json())
      .then(data => setData(data));
  }, []); // Empty array = run once on mount

  return <div>{data}</div>;
}
```

### Cleanup Function

```jsx
useEffect(() => {
  const subscription = props.source.subscribe();
  return () => subscription.unsubscribe();
}, [props.source]);
```

---

## Conditional Rendering

### If Statement

```jsx
function Greeting({ isLoggedIn }) {
  if (isLoggedIn) {
    return <h1>Welcome back!</h1>;
  }
  return <h1>Please sign in</h1>;
}
```

### Ternary Operator

```jsx
function Greeting({ isLoggedIn }) {
  return (
    <div>
      {isLoggedIn ? <h1>Welcome back!</h1> : <h1>Please sign in</h1>}
    </div>
  );
}
```

### Logical &&

```jsx
function Greeting({ show }) {
  return (
    <div>
      {show && <p>Hello!</p>}
    </div>
  );
}
```

---

## Lists and Keys

### Rendering Lists

```jsx
function List() {
  const items = ['Apple', 'Banana', 'Orange'];

  return (
    <ul>
      {items.map((item, index) => (
        <li key={index}>{item}</li>
      ))}
    </ul>
  );
}
```

---

## Forms

### Controlled Input

```jsx
function Form() {
  const [name, setName] = useState('');

  const handleSubmit = (e) => {
    e.preventDefault();
    console.log(name);
  };

  return (
    <form onSubmit={handleSubmit}>
      <input
        type="text"
        value={name}
        onChange={(e) => setName(e.target.value)}
      />
      <button type="submit">Submit</button>
    </form>
  );
}
```

---

## Context

### Create Context

```jsx
import { createContext, useContext } from 'react';

const ThemeContext = createContext('light');

function App() {
  return (
    <ThemeContext.Provider value="dark">
      <Toolbar />
    </ThemeContext.Provider>
  );
}

function Toolbar() {
  const theme = useContext(ThemeContext);
  return <div>Theme: {theme}</div>;
}
```

---

## Hooks

### useRef

```jsx
import { useRef } from 'react';

function TextInput() {
  const inputRef = useRef(null);

  const handleClick = () => {
    inputRef.current.focus();
  };

  return (
    <div>
      <input ref={inputRef} type="text" />
      <button onClick={handleClick}>Focus</button>
    </div>
  );
}
```

### useMemo

```jsx
const memoizedValue = useMemo(() => computeExpensiveValue(a, b), [a, b]);
```

### useCallback

```jsx
const memoizedCallback = useCallback(() => {
  doSomething(a, b);
}, [a, b]);
```

---

## Event Handling

### Basic Events

```jsx
function Events() {
  const handleClick = () => {
    console.log('Clicked!');
  };

  const handleChange = (e) => {
    console.log(e.target.value);
  };

  return (
    <div>
      <button onClick={handleClick}>Click me</button>
      <input onChange={handleChange} />
    </div>
  );
}
```

---

## Styling

### Inline Styles

```jsx
const style = {
  color: 'blue',
  fontSize: '16px'
};

function Styled() {
  return <p style={style}>Styled text</p>;
}
```

### CSS Modules

```jsx
import styles from './Button.module.css';

function Button() {
  return <button className={styles.button}>Click me</button>;
}
```

---

## Best Practices

1. **Use functional components** - Prefer function components with hooks
2. **Keep components small** - Break down large components
3. **Use meaningful names** - Component names should describe their purpose
4. **Avoid direct DOM manipulation** - Let React handle the DOM
5. **Use keys properly** - Always provide unique keys for lists
6. **Lift state up** - Share state by moving it to the closest common ancestor

---

## Next Steps

- Explore [React Documentation](https://react.dev/)
- Learn about [React Router](https://reactrouter.com/)
- Explore [Redux](https://redux.js.org/)
