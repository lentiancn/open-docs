# Element Plus 安裝指南

Element Plus 是為開發者、設計師和產品經理打造的 Vue 3 組件庫。本指南涵蓋在所有環境中的安裝方法。

## 版本資訊

| 版本 | 狀態 | 說明 |
|------|------|------|
| Element Plus 2.x | ✅ 當前 | Vue 3 最新穩定版 |
| Element Plus 1.x | ⚠️ 維護 | 仅安全更新 |
| Element UI | ❌ 舊版 | 仅支持 Vue 2 |

> **重要提示**：Element Plus 仅支持 Vue 3。如果使用 Vue 2，請使用 Element UI。

## 前提條件

### 系統要求

| 要求 | 版本 |
|------|------|
| Vue | 3.x |
| Node.js | 16.x+ |
| npm / yarn / pnpm | 最新版 |

### 瀏覽器支持

| 瀏覽器 | 版本 |
|--------|------|
| Chrome | 80+ |
| Firefox | 80+ |
| Safari | 14+ |
| Edge | 80+ |

## 安裝方法

### 方法一：npm（推薦）

```bash
# 使用 npm
npm install element-plus

# 使用 yarn
yarn add element-plus

# 使用 pnpm
pnpm add element-plus
```

### 方法二：CDN

```html
<!-- 引入 Vue 3 -->
<script src="https://unpkg.com/vue@3/dist/vue.global.js"></script>

<!-- 引入 Element Plus -->
<link rel="stylesheet" href="https://unpkg.com/element-plus/dist/index.css" />
<script src="https://unpkg.com/element-plus/dist/index.full.js"></script>

<!-- 引入圖標（可選） -->
<script src="https://unpkg.com/@element-plus/icons-vue"></script>
```

#### 完整 CDN 模板

```html
<!DOCTYPE html>
<html lang="zh-TW">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Element Plus CDN</title>
    <link rel="stylesheet" href="https://unpkg.com/element-plus/dist/index.css">
</head>
<body>
    <div id="app">
        <el-button>按鈕</el-button>
        <el-date-picker></el-date-picker>
    </div>

    <script src="https://unpkg.com/vue@3/dist/vue.global.js"></script>
    <script src="https://unpkg.com/element-plus/dist/index.full.js"></script>
    <script src="https://unpkg.com/@element-plus/icons-vue"></script>
    
    <script>
        const { createApp, ref } = Vue
        const app = createApp({
            setup() {
                return {}
            }
        })
        
        for (const [key, component] of Object.entries(ElementPlus)) {
            app.component(key, component)
        }
        
        for (const [key, component] of Object.entries(ElementPlusIconsVue)) {
            app.component(key, component)
        }
        
        app.use(ElementPlus)
        app.mount('#app')
    </script>
</body>
</html>
```

### 方法三：使用 Vite

```bash
# 創建 Vite 項目
npm create vite@latest my-app -- --template vue

# 進入項目目錄
cd my-app

# 安裝 Element Plus
npm install element-plus

# 安裝圖標
npm install @element-plus/icons-vue
```

#### 在 main.js 中配置

```javascript
import { createApp } from 'vue'
import App from './App.vue'

// 引入 Element Plus 樣式
import 'element-plus/dist/index.css'

// 引入 Element Plus
import ElementPlus from 'element-plus'

// 引入圖標
import * as ElementPlusIconsVue from '@element-plus/icons-vue'

const app = createApp(App)

// 全域註冊所有圖標
for (const [key, component] of Object.entries(ElementPlusIconsVue)) {
    app.component(key, component)
}

app.use(ElementPlus)
app.mount('#app')
```

### 方法四：使用 Vue CLI

```bash
# 安裝 Vue CLI
npm install -g @vue/cli

# 創建項目
vue create my-project

# 添加 Element Plus
cd my-project
npm install element-plus
npm install @element-plus/icons-vue
```

#### 在 main.js 中配置

```javascript
import { createApp } from 'vue'
import App from './App.vue'

import 'element-plus/dist/index.css'
import ElementPlus from 'element-plus'
import * as icons from '@element-plus/icons-vue'

const app = createApp(App)

// 全域註冊圖標
for (const [key, component] of Object.entries(icons)) {
    app.component(key, component)
}

app.use(ElementPlus)
app.mount('#app')
```

### 方法五：按需導入（推薦用於生產環境）

#### 使用 unplugin-vue-components

```bash
# 安裝依賴
npm install element-plus unplugin-vue-components unplugin-auto-import
```

#### Vite 配置

```javascript
// vite.config.js
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import AutoImport from 'unplugin-auto-import/vite'
import Components from 'unplugin-vue-components/vite'
import { ElementPlusResolver } from 'unplugin-vue-components/resolvers'

export default defineConfig({
  plugins: [
    vue(),
    AutoImport({
      resolvers: [ElementPlusResolver()],
    }),
    Components({
      resolvers: [ElementPlusResolver()],
    }),
  ],
})
```

#### 使用方法

```vue
<template>
  <!-- 無需導入組件即可使用 -->
  <el-button type="primary">按鈕</el-button>
  <el-input v-model="input" placeholder="請輸入" />
</template>

<script setup>
import { ref } from 'vue'
const input = ref('')
</script>
```

### 方法六：使用 Nuxt 3

```bash
# 創建 Nuxt 項目
npx nuxi@latest init my-app
cd my-app

# 安裝 Element Plus
npm install element-plus @element-plus/nuxt
```

#### 配置 nuxt.config.ts

```typescript
export default defineNuxtConfig({
  modules: ['@element-plus/nuxt'],
  elementPlus: {
    /** Options */
  }
})
```

## 圖標配置

### 全域註冊

```javascript
import * as icons from '@element-plus/icons-vue'

const app = createApp(App)

for (const [key, icon] of Object.entries(icons)) {
    app.component(key, icon)
}
```

### 在模板中使用

```html
<el-icon><Plus /></el-icon>
<el-icon><Search /></el-icon>
<el-icon><Edit /></el-icon>
```

## 配置

### 語言配置

```javascript
import ElementPlus from 'element-plus'
import zhTw from 'element-plus/es/locale/lang/zh-tw'

app.use(ElementPlus, {
  locale: zhTw,
})
```

### 支持的語言

- en（英語）
- zhCn（簡體中文）
- zhTw（繁體中文）
- ja（日語）
- es（西班牙語）
- de（德語）
- fr（法語）
- ru（俄語）
- ko（韓語）

### 主題定制

#### 使用 CSS 變數

```css
:root {
    --el-color-primary: #409eff;
    --el-color-success: #67c23a;
    --el-color-warning: #e6a23c;
    --el-color-danger: #f56c6c;
    --el-font-size-base: 14px;
}
```

#### 使用 SCSS

```scss
$--color-primary: #409eff;

@import "element-plus/theme-chalk/src/common/var.scss";
@import "element-plus/packages/theme-chalk/src/index.scss";
```

## 項目結構示例

```
my-element-app/
├── src/
│   ├── main.js
│   ├── App.vue
│   └── components/
│       └── HelloWorld.vue
├── index.html
├── package.json
└── vite.config.js
```

## 下一步

安裝完成後，請查看 [使用指南](./2.使用指南.md) 了解如何使用 Element Plus 組件。

## 常見問題

### 圖標不顯示？

確保已全域註冊圖標：

```javascript
import * as icons from '@element-plus/icons-vue'
for (const [key, icon] of Object.entries(icons)) {
    app.component(key, icon)
}
```

### 樣式未生效？

確保已引入 Element Plus 樣式：

```javascript
import 'element-plus/dist/index.css'
```

### Vue 版本不匹配？

Element Plus 需要 Vue 3。檢查 Vue 版本：

```bash
npm list vue
```

## 相關連結

- [官方網站](https://element-plus.org/)
- [GitHub](https://github.com/element-plus/element-plus)
- [組件文檔](https://element-plus.org/en-US/component/button.html)
