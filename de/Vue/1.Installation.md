# Vue Installationsanleitung

Vue ist ein progressives JavaScript-Framework zum Erstellen von Benutzeroberflächen.

## Versionen

Vue hat zwei Hauptversionen:

| Version | Veröffentlichung | Status |
|---------|------------------|--------|
| Vue 3 | September 2020 | Aktuell - Empfohlen |
| Vue 2 | Oktober 2016 | Veraltet (Ende 2023) |

**Empfehlung:** Verwenden Sie Vue 3 für alle neuen Projekte.

### Unterschiede: Vue 2 vs Vue 3

| Feature | Vue 2 | Vue 3 |
|---------|-------|-------|
| Architektur | Options API | Composition API |
| Reaktivität | Object.defineProperty | Proxy |
| TypeScript | Teilweise | Vollständig |
| Leistung | Gut | Besser |
| Größe | ~20KB gzip | ~10KB gzip |

## Voraussetzungen

### Systemanforderungen

| Anforderung | Vue 2 | Vue 3 |
|-------------|-------|-------|
| Node.js | 8.9+ | 16+ (18+ empfohlen) |
| npm | 5.6+ | 8+ |

## Installationsmethoden

### Methode 1: npm (empfohlen)

```bash
# Vue 3 (empfohlen)
npm create vue@latest

# Oder Vue CLI
npm install -g @vue/cli
vue create my-vue-app
```

### Methode 2: Vite

```bash
npm create vite@latest my-vue-app -- --template vue
cd my-vue-app
npm install
npm run dev
```

### Methode 3: CDN

```html
<script src="https://unpkg.com/vue@3/dist/vue.global.js"></script>
```

## Projektstruktur

```
my-vue-app/
├── src/
│   ├── components/
│   ├── App.vue
│   └── main.js
├── package.json
└── vite.config.js
```

## Abhängigkeiten

```bash
npm install vue vue-router pinia axios
```

## Vue Router

```bash
npm install vue-router@4
```

```javascript
import { createRouter, createWebHistory } from 'vue-router'

const routes = [
  { path: '/', component: Home },
  { path: '/about', component: About }
]

const router = createRouter({
  history: createWebHistory(),
  routes
})
```

## Pinia

```bash
npm install pinia
```

```javascript
import { createPinia } from 'pinia'
const pinia = createPinia()
app.use(pinia)
```

## Entwicklungsserver

```bash
npm run dev   # Starten
npm run build # Bauen
```

## Vue 3 Composition API

```javascript
import { ref, computed } from 'vue'

export default {
  setup() {
    const count = ref(0)
    const double = computed(() => count.value * 2)
    return { count, double }
  }
}
```

## Best Practices

1. Vue 3 verwenden
2. Vite für schnellere Entwicklung
3. Composition API für bessere TypeScript-Unterstützung

## Links

- [Offizielle Website](https://vuejs.org)
- [Dokumentation](https://vuejs.org/guide/)
- [Vue Router](https://router.vuejs.org/)
- [Pinia](https://pinia.vuejs.org/)
