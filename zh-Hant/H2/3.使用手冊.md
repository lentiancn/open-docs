# H2 資料庫使用手冊

## 基礎操作

### 建立資料庫

嵌入式模式（記憶體資料庫）：
```java
String url = "jdbc:h2:mem:testdb";
Connection conn = DriverManager.getConnection(url, "sa", "");
```

檔案資料庫：
```java
String url = "jdbc:h2:file:./data/testdb";
Connection conn = DriverManager.getConnection(url, "sa", "password");
```

### 建立資料表

```sql
CREATE TABLE users (
    id INT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(100) NOT NULL,
    email VARCHAR(100) UNIQUE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 插入資料

```sql
INSERT INTO users (name, email) VALUES ('王小明', 'wang@example.com');
INSERT INTO users (name, email) VALUES ('李小華', 'li@example.com');
```

### 查詢資料

```sql
SELECT * FROM users;
SELECT * FROM users WHERE name = '王小明';
SELECT COUNT(*) FROM users;
```

### 更新資料

```sql
UPDATE users SET email = 'newemail@example.com' WHERE id = 1;
```

### 刪除資料

```sql
DELETE FROM users WHERE id = 2;
```

## Java 代碼示例

### 完整的 JDBC 示例

```java
import java.sql.*;

public class H2Example {
    public static void main(String[] args) throws Exception {
        // 載入驅動
        Class.forName("org.h2.Driver");
        
        // 連線資料庫
        String url = "jdbc:h2:mem:testdb";
        Connection conn = DriverManager.getConnection(url, "sa", "");
        
        // 建立資料表
        Statement stmt = conn.createStatement();
        stmt.execute("CREATE TABLE users (id INT PRIMARY KEY AUTO_INCREMENT, name VARCHAR(100))");
        
        // 插入資料
        stmt.execute("INSERT INTO users (name) VALUES ('王小明')");
        stmt.execute("INSERT INTO users (name) VALUES ('李小華')");
        
        // 查詢資料
        ResultSet rs = stmt.executeQuery("SELECT * FROM users");
        while (rs.next()) {
            System.out.println(rs.getInt("id") + ": " + rs.getString("name"));
        }
        
        // 關閉連線
        rs.close();
        stmt.close();
        conn.close();
    }
}
```

### 使用 PreparedStatement

```java
PreparedStatement pstmt = conn.prepareStatement("INSERT INTO users (name, email) VALUES (?, ?)");
pstmt.setString(1, "王小明");
pstmt.setString(2, "wang@example.com");
pstmt.executeUpdate();
pstmt.close();
```

### 使用交易

```java
conn.setAutoCommit(false);
try {
    Statement stmt = conn.createStatement();
    stmt.execute("INSERT INTO users (name) VALUES ('王小明')");
    stmt.execute("INSERT INTO users (name) VALUES ('李小華')");
    conn.commit();
} catch (Exception e) {
    conn.rollback();
} finally {
    conn.setAutoCommit(true);
}
```

## 進階特性

### 資料庫模式切換

H2 可以模擬其他資料庫的 SQL 語法：

```java
// 模擬 MySQL 模式
String url = "jdbc:h2:mem:testdb;MODE=MySQL";

// 模擬 PostgreSQL 模式
String url = "jdbc:h2:mem:testdb;MODE=PostgreSQL";

// 模擬 Oracle 模式
String url = "jdbc:h2:mem:testdb;MODE=Oracle";
```

### 資料庫加密

```java
// 使用加密資料庫
String url = "jdbc:h2:file:./data/encrypted;CIPHER=AES";
Connection conn = DriverManager.getConnection(url, "sa", "password");
```

### 追蹤功能

```java
// 啟用 SQL 追蹤
String url = "jdbc:h2:mem:testdb;TRACE_LEVEL_FILE=3";
```

### 啟動 TCP 伺服器

```java
import org.h2.tools.Server;

Server server = Server.createTcpServer().start();
System.out.println("H2 TCP Server started: " + server.getURL());
```

### 使用 H2 控制台

啟動 H2 Web 控制台：

```bash
java -jar h2.jar -web
```

然後在瀏覽器中開啟：http://localhost:8082

## 常用 SQL 語句

### 資料類型

H2 支援多種資料類型：
- 整數類型：INT、INTEGER，BIGINT、SMALLINT、TINYINT
- 浮點類型：FLOAT、REAL、DOUBLE、DECIMAL
- 字串類型：VARCHAR、CHAR、CLOB
- 日期時間類型：DATE、TIME、TIMESTAMP
- 布林類型：BOOLEAN
- 二進制類型：BLOB、BINARY

### 索引操作

建立索引：
```sql
CREATE INDEX idx_name ON users(name);
```

刪除索引：
```sql
DROP INDEX idx_name;
```

### 約束操作

新增主鍵：
```sql
ALTER TABLE users ADD PRIMARY KEY (id);
```

新增外鍵：
```sql
ALTER TABLE orders ADD FOREIGN KEY (user_id) REFERENCES users(id);
```

### 查看資料表結構

```sql
DESCRIBE users;
-- 或
SHOW COLUMNS FROM users;
```

### 查看所有資料表

```sql
SHOW TABLES;
```

## 效能優化

### 批量操作

使用批量操作提高效能：

```java
conn.setAutoCommit(false);
PreparedStatement pstmt = conn.prepareStatement("INSERT INTO users (name) VALUES (?)");
for (int i = 0; i < 1000; i++) {
    pstmt.setString(1, "User" + i);
    pstmt.addBatch();
}
pstmt.executeBatch();
conn.commit();
```

### 連線池

在生產環境中，建議使用連線池，如 HikariCP：

```java
HikariConfig config = new HikariConfig();
config.setJdbcUrl("jdbc:h2:file:./data/testdb");
config.setUsername("sa");
config.setPassword("");
config.setMaximumPoolSize(10);

HikariDataSource ds = new HikariDataSource(config);
```

## 備份與復原

### 備份資料庫

```sql
SCRIPT TO 'backup.sql';
```

### 復原資料庫

```sql
RUNSCRIPT FROM 'backup.sql';
```

## 常見錯誤處理

### 資料庫檔案被鎖定
確保沒有其他連線在使用資料庫檔案。H2 支援並發讀取，但寫入需要獨佔訪問。

### 記憶體資料庫資料丟失
記憶體資料庫在連線關閉後資料會丟失。如需持久化，請使用檔案資料庫模式。

### 類找不到
確保 org.h2.Driver 類在 classpath 中。
