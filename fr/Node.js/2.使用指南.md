# Guide utilisateur Node.js

Ce guide montre comment utiliser Node.js pour créer des applications backend et des outils en ligne de commande.

## Démarrage rapide

### Créer votre première application

```javascript
// hello.js
console.log('Hello, Node.js!');

// Exécuter
node hello.js
```

### Créer un serveur HTTP

```javascript
// server.js
const http = require('http');

const server = http.createServer((req, res) => {
  res.writeHead(200, { 'Content-Type': 'text/plain' });
  res.end('Hello World!');
});

server.listen(3000, () => {
  console.log('Serveur actif sur http://localhost:3000/');
});
```

Exécuter :

```bash
node server.js
```

## Modules principaux

### fs - Système de fichiers

```javascript
const fs = require('fs');

// Lire un fichier
fs.readFile('file.txt', 'utf8', (err, data) => {
  if (err) throw err;
  console.log(data);
});

// Écrire dans un fichier
fs.writeFile('file.txt', 'Hello!', (err) => {
  if (err) throw err;
  console.log('Fichier enregistré');
});

// Lecture synchrone
const data = fs.readFileSync('file.txt', 'utf8');
```

### path - Chemins

```javascript
const path = require('path');

path.join('/foo', 'bar', 'baz'); // /foo/bar/baz
path.resolve(__dirname, 'file.txt'); // Chemin absolu
path.extname('file.txt'); // .txt
path.basename('/foo/bar.txt'); // bar.txt
```

### os - Système d'exploitation

```javascript
const os = require('os');

os.platform(); // 'win32', 'linux', 'darwin'
os.arch(); // 'x64', 'arm64'
os.cpus(); // Informations CPU
os.freemem(); // Mémoire libre
os.totalmem(); // Mémoire totale
```

### events - Événements

```javascript
const EventEmitter = require('events');

class MyEmitter extends EventEmitter {}

const myEmitter = new MyEmitter();

myEmitter.on('event', () => {
  console.log('Événement déclenché !');
});

myEmitter.emit('event');
```

### crypto - Cryptographie

```javascript
const crypto = require('crypto');

const hash = crypto.createHash('sha256');
hash.update('password');
const password = hash.digest('hex');
```

## Utilisation de NPM

### package.json

```json
{
  "name": "my-app",
  "version": "1.0.0",
  "description": "Mon application",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "jest"
  },
  "keywords": ["nodejs", "express"],
  "author": "Votre nom",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}
```

### Installer les dépendances

```bash
# Installer toutes les dépendances
npm install

# Installer les dépendances de production
npm install express

# Installer les dépendances de développement
npm install nodemon --save-dev

# Installer globalement
npm install -g nodemon
```

## Framework Express

### Installation

```bash
npm install express
```

### Créer une application

```javascript
const express = require('express');
const app = express();

app.get('/', (req, res) => {
  res.send('Hello World!');
});

app.listen(3000, () => {
  console.log('Application active sur http://localhost:3000');
});
```

### Routes

```javascript
// Route GET
app.get('/api/users', (req, res) => {
  res.json({ users: [] });
});

// Route POST
app.post('/api/users', (req, res) => {
  const user = req.body;
  res.json({ success: true, user });
});

// Route avec paramètres
app.get('/api/users/:id', (req, res) => {
  const { id } = req.params;
  res.json({ id });
});
```

### Middleware

```javascript
// Parser JSON
app.use(express.json());

// Parser les données URL-encodées
app.use(express.urlencoded({ extended: true }));

// Middleware de journalisation
app.use((req, res, next) => {
  console.log(`${req.method} ${req.url}`);
  next();
});

// Gestion des erreurs
app.use((err, req, res, next) => {
  console.error(err.stack);
  res.status(500).send('Something broke!');
});
```

## Programmation asynchrone

### Callback

```javascript
fs.readFile('file.txt', 'utf8', (err, data) => {
  if (err) throw err;
  console.log(data);
});
```

### Promise

```javascript
const fs = require('fs').promises;

async function read() {
  try {
    const data = await fs.readFile('file.txt', 'utf8');
    console.log(data);
  } catch (err) {
    console.error(err);
  }
}

read();
```

### Async/Await

```javascript
const axios = require('axios');

async function fetchData() {
  try {
    const response = await axios.get('https://api.example.com/data');
    console.log(response.data);
  } catch (error) {
    console.error(error);
  }
}

fetchData();
```

## Système de modules

### Exporter des modules

```javascript
// math.js
module.exports = {
  add: (a, b) => a + b,
  subtract: (a, b) => a - b,
};

// Ou utiliser ES6
export const add = (a, b) => a + b;
export default { add };
```

### Importer des modules

```javascript
// CommonJS
const math = require('./math');
console.log(math.add(2, 3));

// ES6
import { add } from './math';
console.log(add(2, 3));
```

## Outils en ligne de commande

### Créer une application CLI

```javascript
#!/usr/bin/env node

console.log('Hello CLI!');

// Utiliser commander
const { program } = require('commander');

program
  .name('my-cli')
  .description('CLI to do something')
  .version('1.0.0');

program.parse();
```

### Rendre le script exécutable

```bash
chmod +x myscript.js
./myscript.js
```

## Débogage

### Utiliser nodemon

```bash
# Installer
npm install -g nodemon

# Exécuter
nodemon server.js

# Surveiller des fichiers spécifiques
nodemon --watch src --ext js server.js
```

### Déboguer avec VS Code

```json
// .vscode/launch.json
{
  "type": "node",
  "request": "launch",
  "name": "Lancer le programme",
  "program": "${workspaceFolder}/index.js"
}
```

## Bonnes pratiques

### 1. Utiliser async/await

```javascript
// Éviter
getData((err, data) => {
  if (err) handleError(err);
  processData(data, (err, result) => {
    if (err) handleError(err);
    saveResult(result);
  });
});

// Recommandé
async function main() {
  try {
    const data = await getData();
    const result = await processData(data);
    await saveResult(result);
  } catch (err) {
    handleError(err);
  }
}
```

### 2. Gestion des erreurs

```javascript
// Toujours capturer les erreurs asynchrones
async function fetchData() {
  try {
    const response = await fetch(url);
    return await response.json();
  } catch (error) {
    console.error('Échec de la récupération des données :', error);
    throw error;
  }
}
```

### 3. Utiliser les variables d'environnement

```javascript
require('dotenv').config();

const port = process.env.PORT || 3000;
const dbUrl = process.env.DATABASE_URL;
```

## Ressources connexes

- [Documentation officielle Node.js](https://nodejs.org/docs/)
- [Documentation Express](https://expressjs.com/)
- [Documentation officielle npm](https://docs.npmjs.com/)
