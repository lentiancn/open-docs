# Nacos Benutzerhandbuch

## Übersicht

Nacos bietet zwei Hauptfunktionen: Service-Entdeckung und Konfigurationsmanagement. Dieses Handbuch beschreibt, wie Sie diese Funktionen im Detail nutzen.

## Service-Entdeckung

### Service-Registrierung

Service-Anbieter können Dienste auf folgende Weise bei Nacos registrieren:

#### Registrierung mit nativem SDK

Dienste mit dem Nacos Java SDK registrieren:

```java
import com.alibaba.nacos.api.naming.NamingService;
import com.alibaba.nacos.api.naming.pojo.Instance;

NamingService namingService = NacosFactory.createNamingService("127.0.0.1:8848");

Instance instance = new Instance();
instance.setIp("192.168.1.100");
instance.setPort(8080);
instance.setEphemeral(true);

Map<String, String> metadata = new HashMap<>();
metadata.put("version", "1.0");
instance.setMetadata(metadata);

namingService.registerInstance("service-provider", instance);
```

#### Registrierung mit OpenAPI

```bash
curl -X POST 'http://127.0.0.1:8848/nacos/v3/client/ns/instance' \
  -d 'serviceName=service-provider' \
  -d 'ip=192.168.1.100' \
  -d 'port=8080'
```

### Service-Entdeckung

Service-Konsumenten können Dienste auf folgende Weise entdecken:

#### Mit nativem SDK

```java
NamingService namingService = NacosFactory.createNamingService("127.0.0.1:8848");

// Alle gesunden Instanzen abrufen
List<Instance> instances = namingService.selectInstances("service-provider", true);

// Load-Balancing verwenden, um eine Instanz auszuwählen
Instance instance = namingService.selectOneHealthyInstance("service-provider");
```

#### Mit OpenAPI

```bash
# Service-Liste abrufen
curl -X GET 'http://127.0.0.1:8848/nacos/v3/client/ns/instance/list?serviceName=service-provider'
```

### Service-Metadatenverwaltung

Nacos unterstützt das Hinzufügen und Verwalten von Metadaten für Dienste:

```java
Map<String, String> metadata = new HashMap<>();
metadata.put("version", "1.0");
metadata.put("env", "production");

namingService.registerInstance("service-name", instance);
```

## Konfigurationsmanagement

### Konfiguration veröffentlichen

#### Mit OpenAPI

```bash
# Zuerst anmelden, um Access-Token zu erhalten
curl -X POST 'http://127.0.0.1:8848/nacos/v3/auth/user/login' \
  -d 'username=nacos' \
  -d 'password=${your_password}'

# Access-Token verwenden, um Konfiguration zu erstellen
curl -X POST 'http://127.0.0.1:8848/nacos/v3/admin/cs/config?dataId=example.config&groupName=DEFAULT_GROUP&content=HelloWorld' \
  -H "accessToken:${your_access_token}"
```

#### Mit SDK

```java
ConfigService configService = NacosFactory.createConfigService("127.0.0.1:8848");

boolean isPublished = configService.publishConfig(
    "example.config",
    "DEFAULT_GROUP",
    "Hello Nacos"
);
```

### Konfiguration abrufen

#### Mit OpenAPI

```bash
curl -X GET 'http://127.0.0.1:8848/nacos/v3/client/cs/config?dataId=example.config&groupName=DEFAULT_GROUP'
```

#### Mit SDK

```java
String config = configService.getConfig(
    "example.config",
    "DEFAULT_GROUP",
    5000
);
```

### Konfigurationsüberwachung

#### Mit SDK

```java
configService.addListener(
    "example.config",
    "DEFAULT_GROUP",
    new Listener() {
        @Override
        public void receiveConfigInfo(String configInfo) {
            System.out.println("Konfiguration geändert:" + configInfo);
        }

        @Override
        public Executor getExecutor() {
            return Executors.newSingleThreadExecutor();
        }
    }
);
```

## Nacos-Integration mit Spring Cloud

### Voraussetzungen

Stellen Sie sicher, dass Nacos Server installiert und gestartet ist.

### Abhängigkeiten hinzufügen

Fügen Sie die folgenden Abhängigkeiten in pom.xml Ihres Spring Cloud-Projekts hinzu:

```xml
<dependency>
    <groupId>com.alibaba.cloud</groupId>
    <artifactId>spring-cloud-starter-alibaba-nacos-config</artifactId>
    <version>2023.0.3.2</version>
</dependency>

<dependency>
    <groupId>com.alibaba.cloud</groupId>
    <artifactId>spring-cloud-starter-alibaba-nacos-discovery</artifactId>
    <version>2023.0.3.2</version>
</dependency>
```

### Konfigurationsmanagement

Konfigurieren Sie die Nacos-Server-Adresse in application.properties:

```properties
spring.application.name=springclouddemo
spring.config.import[0]=nacos:springclouddemo.properties?group=DEFAULT_GROUP
spring.cloud.nacos.config.server-addr=127.0.0.1:8848
spring.cloud.nacos.discovery.server-addr=127.0.0.1:8848
```

### Annotationen verwenden

#### @NacosConfig-Annotation

Konfigurationswerte aus Nacos direkt in Spring Bean-Eigenschaften injizieren:

```java
@RestController
public class ConfigController {
    @NacosConfig(dataId = "routeconfig", group = "config", key = "rate")
    String rate;

    @RequestMapping("/rate")
    public String rate() {
        return rate;
    }
}
```

#### @NacosConfigListener-Annotation

Konfigurationsänderungsereignisse von Nacos empfangen:

```java
@Service
public class MyRateConfigService {
    @NacosConfigListener(dataId = "routeconfig", group = "config")
    public void rate(String rateConfig) {
        System.out.println("Konfigurationsänderung erhalten:" + rateConfig);
    }
}
```

### Service-Registrierung und -Entdeckung

Service-Registrierung und -Entdeckung in der Spring Boot-Anwendung aktivieren:

```java
@SpringBootApplication
@EnableDiscoveryClient
public class NacosProviderApplication {
    public static void main(String[] args) {
        SpringApplication.run(NacosProviderApplication.class, args);
    }

    @RestController
    class EchoController {
        @RequestMapping(value = "/echo/{string}", method = RequestMethod.GET)
        public String echo(@PathVariable String string) {
            return "Hello Nacos Discovery " + string;
        }
    }
}
```

Konfigurieren in application.properties:

```properties
server.port=8070
spring.application.name=service-provider
spring.cloud.nacos.discovery.server-addr=127.0.0.1:8848
```

Service-Konsumenten-Konfiguration:

```java
@SpringBootApplication
@EnableDiscoveryClient
public class NacosConsumerApplication {
    @LoadBalanced
    @Bean
    public RestTemplate restTemplate() {
        return new RestTemplate();
    }

    @RestController
    public class TestController {
        private final RestTemplate restTemplate;

        @Autowired
        public TestController(RestTemplate restTemplate) {
            this.restTemplate = restTemplate;
        }

        @RequestMapping(value = "/echo/{str}", method = RequestMethod.GET)
        public String echo(@PathVariable String str) {
            return restTemplate.getForObject("http://service-provider/echo/" + str, String.class);
        }
    }
}
```

## MCP-Service-Management

Nacos 3.0 unterstützt die Registrierung, Entdeckung und Verwaltung von MCP-Services (Model Context Protocol).

### MCP-Server-Auto-Registrierung

#### Verwendung des Spring AI Alibaba Nacos MCP-Frameworks

1. Abhängigkeiten hinzufügen:

```xml
<dependency>
    <groupId>com.alibaba.cloud.ai</groupId>
    <artifactId>spring-ai-alibaba-starter-mcp-registry</artifactId>
    <version>1.0.0.3+</version>
</dependency>
<dependency>
    <groupId>org.springframework.ai</groupId>
    <artifactId>spring-ai-starter-mcp-server-webmvc</artifactId>
    <version>1.0.0+</version>
</dependency>
```

2. Auto-Registrierung konfigurieren:

```yaml
spring:
  application:
    name: mcp-nacos-registry-example
  ai:
    mcp:
      server:
        name: webmvc-mcp-server
        version: 1.0.0
        type: SYNC
        instructions: "This mcp server provides time information tools and resources"
  alibaba:
    mcp:
      nacos:
        server-addr: 127.0.0.1:8848
        namespace: public
        username: nacos
        password: nacos
        register:
          enabled: true
```

#### Verwendung von Nacos MCP Wrapper Python

```python
pip install nacos-mcp-wrapper-python

from nacos_mcp_wrapper.server.nacos_mcp import NacosMCP
from nacos_mcp_wrapper.server.nacos_settings import NacosSettings

nacos_settings = NacosSettings()
nacos_settings.SERVER_ADDR = "127.0.0.1:8848"
nacos_settings.NAMESPACE = "public"
nacos_settings.USERNAME = "nacos"
nacos_settings.PASSWORD = "nacos"

mcp = NacosMCP("nacos-mcp-python", nacos_settings=nacos_settings, version="1.0.1", port=18001)

@mcp.tool()
def add(a: int, b: int) -> int:
    """Add two integers together"""
    return a + b

if __name__ == "__main__":
    mcp.run(transport="sse")
```

### MCP-Service-Entdeckung und -Aufruf

#### Spring AI Alibaba zur MCP-Server-Entdeckung verwenden

```java
@Autowired
private List<LoadbalancedMcpSyncClient> loadbalancedMcpSyncClients;
```

Oder:

```java
@Qualifier("loadbalancedMcpToolCallbacks")
ToolCallbackProvider tools
```

application.yml konfigurieren:

```yaml
spring:
  alibaba:
    mcp:
      nacos:
        namespace: public
        server-addr: 127.0.0.1:8848
        username: nacos
        password: nacos
        client:
          enabled: true
```

## Konsole

Nacos bietet eine Webkonsole für visuelle Verwaltung:

- Konsole-Adresse: http://127.0.0.1:8080/index.html
- Standard-Benutzername: nacos
- Standard-Passwort: nacos@demo (bitte in Produktion ändern)

Hauptfunktionen der Konsole:

- Service-Management: Registrierte Dienste anzeigen, bearbeiten, löschen
- Konfigurationsmanagement: Konfigurationen erstellen, bearbeiten, löschen, abfragen
- Namespace-Management: Isolierte Namespaces erstellen
- Benutzerverwaltung: Zugriffsbenutzer und Berechtigungen verwalten
- Cluster-Management: Cluster-Knotenstatus anzeigen
