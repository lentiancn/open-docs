# Element Plus 설치 가이드

이 가이드에서는 다양한 프로젝트에서 Element Plus를 설치하고 사용하는 방법에 대해詳細히 설명합니다.

## 환경 요구사항

Element Plus에는 다음이 필요합니다:

- Vue 3.0.0 이상
- Node.js 14.0.0 이상
- 최신 브라우저 (Chrome, Firefox, Safari, Edge)

## 설치 방법

### 패키지 관리자 사용

npm, yarn 또는 pnpm을 사용하여 설치하는 것이 좋습니다:

npm의 경우:
npm install element-plus

yarn의 경우:
yarn add element-plus

pnpm의 경우:
pnpm add element-plus

## 전체 임포트

번들 크기가 문제가되지 않으면 전체 임포트가 더 편리합니다.

main.ts에서:

import { createApp } from 'vue'
import ElementPlus from 'element-plus'
import 'element-plus/dist/index.css'
import App from './App.vue'

const app = createApp(App)

app.use(ElementPlus)
app.mount('#app')

### Volar 지원

Volar를 사용하는 경우 tsconfig.json의 compilerOptions.types에 전역 컴포넌트 타입 정의를 추가하세요:

{
  "compilerOptions": {
    "types": ["element-plus/global"]
  }
}

## 온디맨드 임포트

온디맨드 임포트는 프로젝트 크기를 크게 줄일 수 있습니다. 사용하려는 컴포넌트를 임포트하려면 추가 플러그인을 사용해야 합니다.

### 자동 임포트 (권장)

먼저 플러그인을 설치합니다:

npm의 경우:
npm install -D unplugin-vue-components unplugin-auto-import

yarn의 경우:
yarn add -D unplugin-vue-components unplugin-auto-import

pnpm의 경우:
pnpm add -D unplugin-vue-components unplugin-auto-import

#### Vite 구성

vite.config.ts에서:

import { defineConfig } from 'vite'
import AutoImport from 'unplugin-auto-import/vite'
import Components from 'unplugin-vue-components/vite'
import { ElementPlusResolver } from 'unplugin-vue-components/resolvers'

export default defineConfig({
  plugins: [
    AutoImport({ resolvers: [ElementPlusResolver()] }),
    Components({ resolvers: [ElementPlusResolver()] }),
  ],
})

#### Webpack 구성

webpack.config.js에서:

const AutoImport = require('unplugin-auto-import/webpack')
const Components = require('unplugin-vue-components/webpack')
const { ElementPlusResolver } = require('unplugin-vue-components/resolvers')

module.exports = {
  plugins: [
    AutoImport({ resolvers: [ElementPlusResolver()] }),
    Components({ resolvers: [ElementPlusResolver()] }),
  ],
}

### Nuxt 사용

Nuxt 사용자의 경우 @element-plus/nuxt만 설치하면 됩니다:

npm install -D @element-plus/nuxt

然后nuxt.config.ts에 구성을 추가하세요:

export default defineNuxtConfig({
  modules: ['@element-plus/nuxt'],
})

### 수동 임포트

Element Plus는 ES 모듈 기반의 즉시 사용 가능한 Tree Shaking 기능을 제공합니다.

스타일을 임포트하려면 unplugin-element-plus를 설치해야 합니다:

npm install -D unplugin-element-plus

컴포넌트에서:

<template>
  <el-button>버튼</el-button>
</template>

<script>
import { ElButton } from 'element-plus'

export default {
  components: { ElButton },
}
</script>

Vite 구성:

import { defineConfig } from 'vite'
import ElementPlus from 'unplugin-element-plus/vite'

export default defineConfig({
  plugins: [ElementPlus()],
})

## 시작 템플릿

공식 프로젝트 템플릿을 제공합니다:

- Vite 템플릿: https://github.com/element-plus/element-plus-vite-starter
- Nuxt 템플릿: https://github.com/element-plus/element-plus-nuxt-starter
- Laravel 템플릿: https://github.com/element-plus/element-plus-in-laravel-starter

## 전역 구성

Element Plus를 등록 할 때 size 및 zIndex가있는 전역 구성 객체를 전달할 수 있습니다:

import { createApp } from 'vue'
import ElementPlus from 'element-plus'
import App from './App.vue'

const app = createApp(App)
app.use(ElementPlus, { size: 'small', zIndex: 3000 })

구성 옵션:

- size: 폼 컴포넌트의 기본 크기 설정. 가능한 값: large, default, small
- zIndex: 팝업 컴포넌트의 z-index 설정. 기본값: 2000

### 온디맨드 임포트 구성

el-config-provider 컴포넌트를 사용하세요:

<template>
  <el-config-provider :size="size" :z-index="zIndex">
    <app />
  </el-config-provider>
</template>

<script>
import { defineComponent } from 'vue'
import { ElConfigProvider } from 'element-plus'

export default defineComponent({
  components: { ElConfigProvider },
  setup() {
    return { zIndex: 3000, size: 'small' }
  },
})
</script>

자세한 내용은 Element Plus 공식 웹사이트를 참조하세요.
