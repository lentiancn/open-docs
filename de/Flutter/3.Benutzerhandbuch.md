# Flutter - Benutzerhandbuch

Flutter-Konzepte und häufige Entwicklungsbeispiele.

## Projekterstellung

### Neues Projekt erstellen

```bash
# Neues Projekt erstellen
flutter create my_app
cd my_app

# Projekt ausführen
flutter run
```

### Projektstruktur

```
my_app/
├── lib/
│   └── main.dart        # Einstiegspunkt
├── test/                 # Testdateien
├── pubspec.yaml         # Konfigurationsdatei
├── android/            # Android-Konfiguration
├── ios/                 # iOS-Konfiguration
├── web/                 # Web-Konfiguration
└── README.md
```

## Grundlegende Widgets

### Text-Komponente

```dart
import 'package:flutter/material.dart';

Text(
  'Hello Flutter',
  style: TextStyle(
    fontSize: 24,
    color: Colors.blue,
  ),
)
```

### Buttons

```dart
// Normaler Button
ElevatedButton(
  onPressed: () {},
  child: Text('Klicken'),
)

// Icon-Button
IconButton(
  icon: Icon(Icons.add),
  onPressed: () {},
)

// Text-Button
TextButton(
  onPressed: () {},
  child: Text('Text-Button'),
)
```

### Bilder

```dart
// Lokales Bild
Image.asset('assets/image.png')

// Netzwerk-Bild
Image.network('https://example.com/image.png')

// Icon
Icon(Icons.home, size: 48)
```

## Layout-Komponenten

### Lineares Layout

```dart
// Horizontales Layout
Row(
  children: [
    Text('A'),
    Text('B'),
    Text('C'),
  ],
)

// Vertikales Layout
Column(
  children: [
    Text('A'),
    Text('B'),
    Text('C'),
  ],
)
```

### Flexibles Layout

```dart
// Flexible Raumverteilung
Row(
  children: [
    Expanded(
      flex: 1,
      child: Container(color: Colors.red),
    ),
    Expanded(
      flex: 2,
      child: Container(color: Colors.blue),
    ),
  ],
)
```

### Stack-Layout

```dart
Stack(
  children: [
    Container(color: Colors.red),
    Positioned(
      left: 10,
      top: 10,
      child: Text('Überlagerung'),
    ),
  ],
)
```

## Zustandsverwaltung

### StatefulWidget

```dart
class Counter extends StatefulWidget {
  @override
  _CounterState createState() => _CounterState();
}

class _CounterState extends State<Counter> {
  int _count = 0;

  void _increment() {
    setState(() {
      _count++;
    });
  }

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text('Zähler: $_count'),
        ElevatedButton(
          onPressed: _increment,
          child: Text('Erhöhen'),
        ),
      ],
    );
  }
}
```

### Provider-Zustandsverwaltung

```dart
// Provider erstellen
class Counter extends ChangeNotifier {
  int _count = 0;
  int get count => _count;

  void increment() {
    _count++;
    notifyListeners();
  }
}

// Provider verwenden
Consumer<Counter>(
  builder: (context, counter, child) {
    return Text('${counter.count}');
  },
)
```

## Navigation

### Grundlegende Navigation

```dart
// Zu neuer Seite navigieren
Navigator.push(
  context,
  MaterialPageRoute(builder: (context) => SecondScreen()),
);

// Zurück
Navigator.pop(context);
```

### Routen-Konfiguration

```dart
// Routen definieren
MaterialApp(
  routes: {
    '/': (context) => HomePage(),
    '/detail': (context) => DetailPage(),
  },
)

// Navigieren
Navigator.pushNamed(context, '/detail');
```

## Netzwerkanfragen

### HTTP-Anfrage

```dart
import 'package:http/http.dart' as http;

Future<void> fetchData() async {
  final response = await http.get(Uri.parse('https://api.example.com/data'));
  if (response.statusCode == 200) {
    print(response.body);
  }
}
```

### Dio-Anfrage

```dart
import 'package:dio/dio.dart';

final dio = Dio();
Response response = await dio.get('https://api.example.com/data');
```

## Lokale Speicherung

### SharedPreferences

```dart
import 'package:shared_preferences/shared_preferences.dart';

// Speichern
Future<void> saveData() async {
  final prefs = await SharedPreferences.getInstance();
  await prefs.setString('name', 'Flutter');
}

// Laden
Future<void> loadData() async {
  final prefs = await SharedPreferences.getInstance();
  String name = prefs.getString('name') ?? '';
}
```

## Häufige Komponenten

### Liste

```dart
ListView.builder(
  itemCount: items.length,
  itemBuilder: (context, index) {
    return ListTile(
      title: Text(items[index].title),
    );
  },
)
```

### Formular

```dart
TextFormField(
  decoration: InputDecoration(
    labelText: 'Benutzername',
    hintText: 'Benutzername eingeben',
  ),
  validator: (value) {
    if (value.isEmpty) {
      return 'Bitte Benutzername eingeben';
    }
    return null;
  },
)
```

### Dialog

```dart
showDialog(
  context: context,
  builder: (context) => AlertDialog(
    title: Text('Hinweis'),
    content: Text('Möchten Sie löschen?'),
    actions: [
      TextButton(
        onPressed: () => Navigator.pop(context),
        child: Text('Abbrechen'),
      ),
      TextButton(
        onPressed: () => Navigator.pop(context),
        child: Text('Bestätigen'),
      ),
    ],
  ),
);
```

## Build und Veröffentlichung

### Android APK

```bash
# Debug-Version
flutter build apk --debug

# Release-Version
flutter build apk --release
```

### iOS

```bash
# Simulator
flutter build ios --simulator --no-codesign

# Gerät
flutter build ios --release
```

### Web

```bash
flutter build web
```

## Best Practices

1. **const verwenden**: Unnötige Neuerstellungen vermeiden
2. **Widget extrahieren**: UI-Code wiederverwenden
3. **Geeignetes Layout**: Passende Layout-Komponenten verwenden
4. **Zustandsverwaltung**: Geeignetes Zustandsverwaltungsschema wählen
5. **Fehlerbehandlung**: Ausnahmen abfangen

## Weitere Lernschritte

- Häufige Fragen: Siehe 4.FAQ.md
