# Element Plus 使用手册

> 详细说明 Element Plus 组件库的使用方法和开发指南
> 
> 更新日期：2026年2月

---

## 一、组件使用基础

### 1.1 基础按钮

按钮是最常用的组件之一，Element Plus 提供了多种类型的按钮。

```vue
<template>
  <!-- 主要按钮 -->
  <el-button type="primary">主要按钮</el-button>
  
  <!-- 成功按钮 -->
  <el-button type="success">成功按钮</el-button>
  
  <!-- 信息按钮 -->
  <el-button type="info">信息按钮</el-button>
  
  <!-- 警告按钮 -->
  <el-button type="warning">警告按钮</el-button>
  
  <!-- 危险按钮 -->
  <el-button type="danger">危险按钮</el-button>
  
  <!-- 文字按钮 -->
  <el-button type="text">文字按钮</el-button>
  
  <!-- 朴素按钮 -->
  <el-button plain>朴素按钮</el-button>
  
  <!-- 圆角按钮 -->
  <el-button round>圆角按钮</el-button>
  
  <!-- 图标按钮 -->
  <el-button icon="el-icon-search">搜索</el-button>
</template>
```

### 1.2 按钮属性

| 属性 | 说明 | 类型 | 可选值 | 默认值 |
|------|------|------|--------|--------|
| type | 按钮类型 | string | primary / success / warning / danger / info / text | — |
| plain | 是否朴素按钮 | boolean | — | false |
| round | 是否圆角按钮 | boolean | — | false |
| circle | 是否圆形按钮 | boolean | — | false |
| loading | 是否加载中 | boolean | — | false |
| disabled | 是否禁用 | boolean | — | false |
| icon | 图标类名 | string | — | — |
| size | 尺寸 | string | large / default / small | — |

### 1.3 按钮事件

```vue
<template>
  <el-button @click="handleClick">点击事件</el-button>
</template>

<script setup>
const handleClick = () => {
  console.log('按钮被点击了')
}
</script>
```

---

## 二、表单组件

### 2.1 基础表单

```vue
<template>
  <el-form :model="form" :rules="rules" ref="formRef" label-width="100px">
    <el-form-item label="用户名" prop="username">
      <el-input v-model="form.username" placeholder="请输入用户名" />
    </el-form-item>
    
    <el-form-item label="密码" prop="password">
      <el-input v-model="form.password" type="password" placeholder="请输入密码" show-password />
    </el-form-item>
    
    <el-form-item label="邮箱" prop="email">
      <el-input v-model="form.email" placeholder="请输入邮箱" />
    </el-form-item>
    
    <el-form-item label="性别" prop="gender">
      <el-radio-group v-model="form.gender">
        <el-radio label="male">男</el-radio>
        <el-radio label="female">女</el-radio>
      </el-radio-group>
    </el-form-item>
    
    <el-form-item label="爱好" prop="hobby">
      <el-checkbox-group v-model="form.hobby">
        <el-checkbox label="篮球" />
        <el-checkbox label="足球" />
        <el-checkbox label="游泳" />
      </el-checkbox-group>
    </el-form-item>
    
    <el-form-item label="简介" prop="bio">
      <el-input v-model="form.bio" type="textarea" :rows="3" placeholder="请输入个人简介" />
    </el-form-item>
    
    <el-form-item>
      <el-button type="primary" @click="submitForm">提交</el-button>
      <el-button @click="resetForm">重置</el-button>
    </el-form-item>
  </el-form>
</template>

<script setup>
import { reactive, ref } from 'vue'

const formRef = ref(null)

const form = reactive({
  username: '',
  password: '',
  email: '',
  gender: '',
  hobby: [],
  bio: ''
})

const rules = {
  username: [
    { required: true, message: '请输入用户名', trigger: 'blur' },
    { min: 3, max: 20, message: '用户名长度在 3 到 20 个字符', trigger: 'blur' }
  ],
  password: [
    { required: true, message: '请输入密码', trigger: 'blur' },
    { min: 6, message: '密码长度至少 6 位', trigger: 'blur' }
  ],
  email: [
    { required: true, message: '请输入邮箱', trigger: 'blur' },
    { type: 'email', message: '请输入正确的邮箱格式', trigger: 'blur' }
  ],
  gender: [
    { required: true, message: '请选择性别', trigger: 'change' }
  ]
}

const submitForm = async () => {
  if (!formRef.value) return
  
  await formRef.value.validate((valid) => {
    if (valid) {
      console.log('表单数据：', form)
      ElMessage.success('提交成功')
    } else {
      ElMessage.error('请检查表单填写')
    }
  })
}

const resetForm = () => {
  formRef.value?.resetFields()
}
</script>
```

### 2.2 表单验证规则

| 规则类型 | 说明 | 示例 |
|----------|------|------|
| required | 必填项 | { required: true, message: '必填', trigger: 'blur' } |
| type | 数据类型 | type: 'email', type: 'number', type: 'date' |
| min/max | 长度限制 | { min: 3, max: 20, message: '长度不对', trigger: 'blur' } |
| pattern | 正则表达式 | { pattern: /^1[3-9]\d{9}$/, message: '手机号格式不对' } |
| validator | 自定义验证 | { validator: validatePass, trigger: 'blur' } |

---

## 三、表格组件

### 3.1 基础表格

```vue
<template>
  <el-table :data="tableData" stripe border>
    <el-table-column prop="date" label="日期" width="180" />
    <el-table-column prop="name" label="姓名" width="180" />
    <el-table-column prop="address" label="地址" />
    <el-table-column label="操作" width="200">
      <template #default="{ row }">
        <el-button type="primary" size="small" @click="handleEdit(row)">编辑</el-button>
        <el-button type="danger" size="small" @click="handleDelete(row)">删除</el-button>
      </template>
    </el-table-column>
  </el-table>
</template>

<script setup>
import { ref } from 'vue'

const tableData = ref([
  { date: '2024-01-01', name: '张三', address: '北京市朝阳区' },
  { date: '2024-01-02', name: '李四', address: '上海市浦东新区' },
  { date: '2024-01-03', name: '王五', address: '广州市天河区' }
])

const handleEdit = (row) => {
  console.log('编辑：', row)
}

const handleDelete = (row) => {
  console.log('删除：', row)
}
</script>
```

### 3.2 带分页的表格

```vue
<template>
  <div>
    <el-table :data="tableData" v-loading="loading">
      <el-table-column prop="id" label="ID" width="80" />
      <el-table-column prop="title" label="标题" />
      <el-table-column prop="author" label="作者" width="120" />
      <el-table-column prop="date" label="发布日期" width="180" />
    </el-table>
    
    <el-pagination
      v-model:current-page="currentPage"
      v-model:page-size="pageSize"
      :total="total"
      :page-sizes="[10, 20, 50, 100]"
      layout="total, sizes, prev, pager, next, jumper"
      @size-change="handleSizeChange"
      @current-change="handleCurrentChange"
    />
  </div>
</template>

<script setup>
import { ref, onMounted } from 'vue'

const tableData = ref([])
const loading = ref(false)
const currentPage = ref(1)
const pageSize = ref(10)
const total = ref(0)

const loadData = async () => {
  loading.value = true
  // 模拟接口调用
  setTimeout(() => {
    tableData.value = [
      { id: 1, title: '文章标题1', author: '作者1', date: '2024-01-01' },
      { id: 2, title: '文章标题2', author: '作者2', date: '2024-01-02' }
    ]
    total.value = 100
    loading.value = false
  }, 500)
}

const handleSizeChange = (val) => {
  pageSize.value = val
  loadData()
}

const handleCurrentChange = (val) => {
  currentPage.value = val
  loadData()
}

onMounted(() => {
  loadData()
})
</script>
```

---

## 四、对话框和抽屉

### 4.1 对话框

```vue
<template>
  <el-button type="primary" @click="dialogVisible = true">打开对话框</el-button>
  
  <el-dialog
    v-model="dialogVisible"
    title="编辑信息"
    width="500px"
    :close-on-click-modal="false"
    @close="handleClose"
  >
    <el-form :model="form" label-width="80px">
      <el-form-item label="名称">
        <el-input v-model="form.name" />
      </el-form-item>
      <el-form-item label="描述">
        <el-input v-model="form.desc" type="textarea" :rows="3" />
      </el-form-item>
    </el-form>
    
    <template #footer>
      <el-button @click="dialogVisible = false">取消</el-button>
      <el-button type="primary" @click="handleConfirm">确定</el-button>
    </template>
  </el-dialog>
</template>

<script setup>
import { ref, reactive } from 'vue'

const dialogVisible = ref(false)

const form = reactive({
  name: '',
  desc: ''
})

const handleClose = () => {
  form.name = ''
  form.desc = ''
}

const handleConfirm = () => {
  console.log('提交：', form)
  dialogVisible.value = false
  ElMessage.success('操作成功')
}
</script>
```

### 4.2 抽屉

```vue
<template>
  <el-button type="primary" @click="drawerVisible = true">打开抽屉</el-button>
  
  <el-drawer
    v-model="drawerVisible"
    title="我是抽屉标题"
    direction="rtl"
    size="50%"
  >
    <div>抽屉内容区域</div>
  </el-drawer>
</template>

<script setup>
import { ref } from 'vue'

const drawerVisible = ref(false)
</script>
```

---

## 五、消息提示

### 5.1 全局消息

```vue
<script setup>
import { ElMessage } from 'element-plus'

// 成功提示
const showSuccess = () => {
  ElMessage.success('操作成功')
}

// 警告提示
const showWarning = () => {
  ElMessage.warning('警告信息')
}

// 错误提示
const showError = () => {
  ElMessage.error('操作失败')
}

// 信息提示
const showInfo = () => {
  ElMessage.info('提示信息')
}

// 可关闭的消息
const showClosable = () => {
  ElMessage({
    message: '这是一条可关闭的消息',
    showClose: true,
  })
}

// 长时间显示
const showDuration = () => {
  ElMessage({
    message: '这条消息会显示 10 秒',
    duration: 10000,
  })
}
</script>
```

### 5.2 通知提醒

```vue
<script setup>
import { ElNotification } from 'element-plus'

// 成功通知
const notifySuccess = () => {
  ElNotification({
    title: '成功',
    message: '操作已成功完成',
    type: 'success',
  })
}

// 警告通知
const notifyWarning = () => {
  ElNotification({
    title: '警告',
    message: '请注意当前操作',
    type: 'warning',
  })
}

// 错误通知
const notifyError = () => {
  ElNotification({
    title: '错误',
    message: '操作执行失败',
    type: 'error',
  })
}

// 带位置偏移
const notifyWithOffset = () => {
  ElNotification({
    title: '偏移消息',
    message: '这条消息会偏移显示',
    offset: 100,
  })
}
</script>
```

---

## 六、导航菜单

### 6.1 顶部导航菜单

```vue
<template>
  <el-menu
    :default-active="activeIndex"
    class="el-menu-horizontal"
    mode="horizontal"
    @select="handleSelect"
  >
    <el-menu-item index="1">首页</el-menu-item>
    <el-sub-menu index="2">
      <template #title>产品</template>
      <el-menu-item index="2-1">产品一</el-menu-item>
      <el-menu-item index="2-2">产品二</el-menu-item>
      <el-menu-item index="2-3">产品三</el-menu-item>
    </el-sub-menu>
    <el-menu-item index="3">解决方案</el-menu-item>
    <el-menu-item index="4">关于</el-menu-item>
  </el-menu>
</template>

<script setup>
import { ref } from 'vue'

const activeIndex = ref('1')

const handleSelect = (key) => {
  console.log('选择：', key)
}
</script>
```

### 6.2 侧边导航菜单

```vue
<template>
  <el-menu
    :default-active="$route.path"
    class="el-menu-vertical"
    background-color="#545c64"
    text-color="#fff"
    active-text-color="#ffd04b"
  >
    <el-menu-item index="/home">
      <el-icon><House /></el-icon>
      <span>首页</span>
    </el-menu-item>
    <el-menu-item index="/user">
      <el-icon><User /></el-icon>
      <span>用户</span>
    </el-menu-item>
    <el-sub-menu index="/system">
      <template #title>
        <el-icon><Setting /></el-icon>
        <span>系统</span>
      </template>
      <el-menu-item index="/system/role">角色管理</el-menu-item>
      <el-menu-item index="/system/menu">菜单管理</el-menu-item>
    </el-sub-menu>
  </el-menu>
</template>

<script setup>
import { House, User, Setting } from '@element-plus/icons-vue'
</script>
```

---

## 七、常用组件组合示例

### 7.1 用户列表页面

```vue
<template>
  <div class="user-list">
    <!-- 搜索区域 -->
    <div class="search-bar">
      <el-input
        v-model="searchKeyword"
        placeholder="搜索用户名"
        clearable
        @clear="loadData"
      >
        <template #append>
          <el-button icon="Search" @click="loadData" />
        </template>
      </el-input>
      <el-button type="primary" @click="handleAdd">新增用户</el-button>
    </div>
    
    <!-- 表格区域 -->
    <el-table :data="tableData" v-loading="loading" stripe>
      <el-table-column type="selection" width="55" />
      <el-table-column prop="id" label="ID" width="80" />
      <el-table-column prop="name" label="姓名" width="120" />
      <el-table-column prop="email" label="邮箱" />
      <el-table-column prop="status" label="状态" width="100">
        <template #default="{ row }">
          <el-tag :type="row.status === 'active' ? 'success' : 'info'">
            {{ row.status === 'active' ? '启用' : '禁用' }}
          </el-tag>
        </template>
      </el-table-column>
      <el-table-column prop="createTime" label="创建时间" width="180" />
      <el-table-column label="操作" width="180" fixed="right">
        <template #default="{ row }">
          <el-button type="primary" link @click="handleEdit(row)">编辑</el-button>
          <el-button type="danger" link @click="handleDelete(row)">删除</el-button>
        </template>
      </el-table-column>
    </el-table>
    
    <!-- 分页区域 -->
    <div class="pagination">
      <el-pagination
        v-model:current-page="pagination.page"
        v-model:page-size="pagination.size"
        :total="pagination.total"
        :page-sizes="[10, 20, 50]"
        layout="total, sizes, prev, pager, next"
        @change="loadData"
      />
    </div>
  </div>
</template>

<script setup>
import { ref, reactive, onMounted } from 'vue'

const loading = ref(false)
const searchKeyword = ref('')
const tableData = ref([])
const pagination = reactive({
  page: 1,
  size: 10,
  total: 0
})

const loadData = async () => {
  loading.value = true
  // 模拟接口
  setTimeout(() => {
    tableData.value = [
      { id: 1, name: '张三', email: 'zhangsan@example.com', status: 'active', createTime: '2024-01-01 10:00:00' },
      { id: 2, name: '李四', email: 'lisi@example.com', status: 'inactive', createTime: '2024-01-02 11:00:00' }
    ]
    pagination.total = 100
    loading.value = false
  }, 300)
}

const handleAdd = () => {
  // 新增用户逻辑
}

const handleEdit = (row) => {
  // 编辑用户逻辑
}

const handleDelete = (row) => {
  // 删除用户逻辑
}

onMounted(() => {
  loadData()
})
</script>

<style scoped>
.user-list {
  padding: 20px;
}

.search-bar {
  display: flex;
  gap: 10px;
  margin-bottom: 20px;
}

.search-bar .el-input {
  width: 300px;
}

.pagination {
  margin-top: 20px;
  display: flex;
  justify-content: flex-end;
}
</style>
```

---

## 八、组件事件处理

### 8.1 事件修饰符

```vue
<template>
  <!-- 阻止事件冒泡 -->
  <el-button @click.stop="handleClick">点击</el-button>
  
  <!-- 阻止默认行为 -->
  <el-form @submit.prevent="handleSubmit">
    <el-button native-type="submit">提交</el-button>
  </el-form>
  
  <!-- 仅触发一次 -->
  <el-button @click.once="handleOnce">仅触发一次</el-button>
</template>
```

### 8.2 表单验证事件

```vue
<script setup>
import { ref } from 'vue'

const formRef = ref(null)

// 验证单个字段
const validateField = async () => {
  await formRef.value?.validateField('username')
}

// 清除验证
const clearValidate = () => {
  formRef.value?.clearValidate()
}

// 获取验证状态
const getFields = () => {
  console.log(formRef.value?.fields)
}
</script>
```

---

## 九、实用技巧

### 9.1 响应式显示隐藏

```vue
<template>
  <!-- 使用 v-if 控制显示 -->
  <el-button v-if="hasPermission">需要权限的按钮</el-button>
  
  <!-- 使用 v-show 控制显示 -->
  <el-button v-show="isVisible">显示/隐藏</el-button>
</template>
```

### 9.2 加载状态

```vue
<template>
  <!-- 按钮加载状态 -->
  <el-button :loading="btnLoading" @click="handleClick">
    {{ btnLoading ? '加载中...' : '提交' }}
  </el-button>
  
  <!-- 区域加载状态 -->
  <div v-loading="contentLoading">
    <!-- 内容区域 -->
  </div>
</template>
```

### 9.3 空状态

```vue
<template>
  <el-empty
    v-if="dataList.length === 0"
    description="暂无数据"
    :image-size="100"
  >
    <el-button type="primary" @click="handleRefresh">刷新</el-button>
  </el-empty>
</template>
```

---

## 十、性能优化建议

### 10.1 按需加载

使用自动导入插件，只引入需要的组件，减少打包体积。

### 10.2 虚拟滚动

对于大数据量表格，使用 el-table-v2 虚拟滚动组件。

### 10.3 避免不必要的响应式

使用 shallowRef 或 markRaw 处理不需要响应式的对象：

```typescript
import { shallowRef, markRaw } from 'vue'

// 大数据对象使用 shallowRef
const largeData = shallowRef([])

// 非响应式对象使用 markRaw
const complexObj = markRaw({ /* ... */ })
```

---

## 十一、相关资源

- 组件演示：https://element-plus.run/
- 在线 Playground：https://element-plus.run/
- GitHub：https://github.com/element-plus/element-plus
