# JDK インストールガイド

JDK（Java Development Kit）は公式のJava開発キットです。このガイドでは、さまざまな環境でのインストール方法を説明します。

## バージョン情報

| バージョン | 状態 | 説明 |
|---------|--------|-------------|
| JDK 21 (LTS) | ✅ 現行 | 最新のLTSバージョン |
| JDK 17 (LTS) | ✅ サポート中 | 以前のLTSバージョン |
| JDK 11 (LTS) | ✅ サポート中 | 旧LTSバージョン |
| JDK 8 | ✅ サポート中 | 旧バージョン |
| JDK 22 | ✅ 最新 | 最新のフィーチャーバージョン |

### LTSとフィーチャーバージョンの違い

- **LTS（長期サポート）**：2〜3年のアップデートを提供、本番環境におすすめ
- **フィーチャーバージョン**：6ヶ月ごとに新機能を追加、サポート期間が短い

## システム要件

### ハードウェア要件

| 要件 | 最小構成 | 推奨構成 |
|-------------|---------|-------------|
| CPU | 1コア | 2+コア |
| メモリ | 2 GB | 4+ GB |
| ディスク | 2 GB | 10+ GB |

### ソフトウェア要件

| ソフトウェア | バージョン |
|----------|---------|
| OS | Windows/macOS/Linux |
| メモリ | 開発環境で2+ GB |

## インストール方法

### 方法1：Oracle JDK

#### Windows

1. ダウンロード先：https://www.oracle.com/java/technologies/downloads/
2. インストーラー（.exe）を実行
3. ウィザードに従う

#### macOS

```bash
# OracleウェブサイトからDMGをダウンロード
# またはHomebrewを使用
brew install openjdk@21
```

#### Linux

```bash
# tar.gzをダウンロード
wget https://download.oracle.com/java/21/latest/jdk-21_linux-x64_bin.tar.gz
tar -xzf jdk-21_linux-x64_bin.tar.gz
sudo mv jdk-21 /opt/jdk-21
```

### 方法2：Eclipse Temurin（Adoptium）

#### Windows

1. ダウンロード先：https://adoptium.net/temurin/releases/
2. インストーラーを実行

#### macOS

```bash
# Homebrewを使用
brew install --cask temurin

# またはウェブサイトからpkgをダウンロード
```

#### Linux

```bash
# Ubuntu/Debian
wget https://github.com/adoptium/temurin21-binaries/releases/download/jdk-21.0.2%2B13/OpenJDK21U-jdk_x64_linux_hotspot_21.0.2_13.tar.gz
tar -xzf OpenJDK21U-jdk_x64_linux_hotspot_21.0.2_13.tar.gz

# 環境変数の設定
export JAVA_HOME=/path/to/jdk-21
export PATH=$JAVA_HOME/bin:$PATH
```

### 方法3：SDKMAN

```bash
# SDKMANをインストール
curl -s "https://get.sdkman.io" | bash

# JDKをインストール
sdk install java 21.0.2-tem

# 利用可能なバージョンを表示
sdk list java
```

### 方法4：Docker

```dockerfile
FROM eclipse-temurin:21-jdk
WORKDIR /app
COPY . .
RUN javac Main.java
CMD ["java", "Main"]
```

### 方法5：Windowsパッケージマネージャー

#### Chocolatey

```powershell
choco install openjdk21
```

#### Scoop

```powershell
scoop install openjdk
```

## 環境設定

### Windows

```powershell
# JAVA_HOMEを設定
[System.Environment]::SetEnvironmentVariable("JAVA_HOME", "C:\Program Files\Java\jdk-21", "User")

# PATHに追加
[System.Environment]::SetEnvironmentVariable("PATH", "$env:PATH;C:\Program Files\Java\jdk-21\bin", "User")
```

### macOS/Linux

```bash
# ~/.bashrcまたは~/.zshrcに追加
export JAVA_HOME=/opt/jdk-21
export PATH=$JAVA_HOME/bin:$PATH

# 適用
source ~/.bashrc
```

## インストール確認

```bash
# Javaバージョンを確認
java -version

# Javacバージョンを確認
javac -version

# JAVA_HOMEを確認
echo $JAVA_HOME
```

## 複数JDKバージョンの管理

### SDKMANを使用

```bash
# 複数バージョンをインストール
sdk install java 17.0.8-tem
sdk install java 21.0.2-tem

# バージョンを切り替え
sdk use java 17.0.8-tem
```

## よくある問題

### 「java」が見つからない？

PATHにJavaのbinディレクトリが含まれているか確認してください。

### Javaのバージョンが違う？

使用されているjavaを確認：

```bash
which java
java -version
```

### JAVA_HOMEが設定されていない？

JAVA_HOMEをJDKのインストールディレクトリに設定してください。

## 関連リンク

- [Oracle JDK](https://www.oracle.com/java/technologies/downloads/)
- [Eclipse Temurin](https://adoptium.net/temurin/)
- [SDKMAN](https://sdkman.io/)
